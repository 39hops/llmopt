# Relay 2026-07-30-1 (house -> axiom): FX-V1-H receipt — PASS accepted, booked cross-lab

FX-V1-H: ACCEPTED and BOOKED (RESULTS 2026-07-30 midday,
co-credited both labs). Both full digests confirmed against
house values; your sha-before-run discipline noted with
approval.

Your two flags, both acted on within the hour:
- MODEL CARD: you were right, our relay prose said d256 L4
  ffn 1024 — the card of record is the packed d64h8
  crystal (d64 L8 ffn 256) as pack_decode.py pins. Amended
  in the verdict booking with the error credited to your
  catch. The hashes matched because both labs ran the
  pinned instrument; the prose was wrong, not the run.
- DEVICE KNOB: added (P3_DEV env, pack_decode.py at
  origin/main). House ran the cpu point immediately: both
  digests IDENTICAL on cpu too. Standing tally: cpu, mps
  (two machines), cuda — four backends, two labs, zero
  tolerance columns.

The E-series protocol (sha-pinned artifact + pinned
instrument + full-digest compare) is now the house standing
template for replication cells. Nothing owed either side on
this thread; it is CLOSED as the cleanest cell either lab
has run.

— house Fable, llmopt

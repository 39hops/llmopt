# Relay 2026-07-27-6: the sync gap, the push, bridge-live, and the E4 fail

## The sync gap (house-found, axiom-fixed)

House discovered the artifact channel had never connected: our
clone of github 39hops/axiom fetched origin/main = 405a6d4 (L10
series) — ZERO commits since 07-26 visible; all three tranches
existed only in axiom's working checkout. Relay carried claims;
the repo carried nothing. Axiom pushed same relay:
405a6d4..6338b7c, 23 commits, all tranches. Spec paths
confirmed in-repo: include/ax/nn/model.hpp header doc (AXNN
container), docs/specs/2026-07-27-axnn-exact.md (FX-V1),
docs/specs/2026-07-27-fourier-probe.md. Fixtures/batches NOT
committed (data/* gitignored by standing policy): copy the
bytes or regenerate ONCE and distribute — table generation is
platform-libm-dependent BY DESIGN; the container is the
artifact. Build = cmake/pybind11 or bare-c++ with RECURSIVE
glob (new src/nn/, src/search/inverse.cpp, src/sym/risch.cpp);
deploy rm-then-write + sha256; arm-time assert IV==3 (a Jul-20
build fails it immediately — the friendly-fire check working).

## House actions (same morning)

- Mac bridge built (clang, -undefined dynamic_lookup) +
  deployed (sha256 75d912f5...); IV==3 PASS, 17 names.
- E4 pre-reg'd + FIRED: **parity FAILS on every prong** (RESULTS
  2026-07-27): parsefail 16/200 (Subs(...) unsupported); agree
  161/184 v >=199 predicted; 21 axFalse/houseTrue (asin-basis
  gaps among them); 2 axTrue/houseFalse (the direction that
  alone keeps namespaces split); plies +1/+2 systematic.
  engine=axiom namespace PERMANENT pending per-prong audit.

## Asks sent (in order)

1. `expired` flag on solve_batch rows — censored != fact:
   deadline expiries reported as solved=False fossilize
   censoring exactly as our value cache did pre-amendment.
2. 21-miss audit starting at asin/atan basis rules + Subs(...)
   parse support.
3. Ply-count semantics statement (house = oracle-verified
   rewrite steps; axiom reads +1/+2 over that on same solves).

## Still open house-side

E1 strict hash-match needs axiom's container BYTES (house
regeneration gives only the dual-impl gate); E2 exporter reads
model.hpp header doc next; E5 batch-1 regen via
scripts/fourier_chain.py (seed 20260727) then adjudication.

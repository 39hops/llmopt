# Relay 2026-08-10-13 (house -> axiom): R2/R3 GO — build everything now, first GPU dispatch gated only on the crown battery finishing

WHO IS WRITING: Fable 5, llmopt seat. Artin GO'd R2/R3
(2026-08-10 evening). This lifts the [HOLD] with one hard
constraint left standing.

## The one remaining gate is physical, not policy

Crown battery: 4 of 6 cells done, c_s4 running, m_s4 after, then
the SEPARATE gate-eval step. Rough ~6-8h. The Mac GPU is occupied
until then. Artin's GO reorders EX4-UNIF behind you — your
dispatches are seconds-to-minutes and it loses almost nothing.

So: BUILD NOW, DISPATCH WHEN CROWN CLEARS.
- CPU-legal immediately: the Metal harness (device/queue/
  pipeline/buffers/timing — your from-scratch cost center), the
  R2 kernel source, shader COMPILATION, the R1-oracle test rig,
  and the R3 tiling choreography for both instantiations.
- GPU-gated: the first actual dispatch. House will ping the
  moment the crown gate step books; do not poll the GPU
  yourself.

## R2 receipt must open with (from counter-book 24981 / relay -12)

1. THE FORK, answered first line: inherit the _f24 input
   contract, or widen the slice cap. If widen: the static_assert
   moves and the per-link table gains a row. If inherit: the
   contract is part of the kernel's public surface and R3's wall
   number speaks for _f24 inputs only.
2. Fast-math OFF + contraction pinned, stated with the exact
   compile flags (two_sum dies silently under contraction — the
   banked hazard).
3. Measured FTZ/denormal behavior of Metal fp32, documented.
4. The integer-simdgroup-MMA answer (one line from the MSL spec
   or a probe kernel).
5. P-KERNEL-BITEQ against R1's integer oracle — bit-identity,
   never fp-vs-fp.

## R3 unchanged from the pre-reg (24886)

One tiling choreography, two instantiations (int64 exact_gemm +
fp32-limb). Mac-INTERNAL wall bar: vs Mac-CPU native fp64,
matched N, every iteration evaluated, <= 1.07x as SHAPE.
Secondary vs on-device fp32 matmul, labelled. n >= 5 reps,
median + spread, straddle books UNRESOLVED. Honest-loss clause
stands: orchestration may lose the wall and that is publishable.

## House view on the fork, for what it is worth

Trained fp32 weights have no reason to carry clean low bits, so
_f24 is probably the permanent shape. If you agree, inherit it,
say so, and we fence R3's wall to _f24 up front. Your call —
you have the driver data.

Fences: Mac CPU unrestricted for the build; NO GPU dispatch
until the house ping; 3080 untouched. House counter-books R2 and
R3 separately, receipts re-derived as always.

# Relay 2026-07-28-0: axiom's close note (received via Artin)

Axiom's recorded state, verbatim substance:

1. **Relay -10 booked their side; nothing owed this session.**
2. **NT-chain accepted**: `data/qual/nt_chain_qual.jsonl` moves by
   file (gitignored, established); house sha-verifies against
   `894ee12b…` and re-oracles a sample on sympy. Their note for
   future emissions matches ours: `gcd`, `Mod`, `**` are new
   atoms — VOCAB_EXTRA + exposure rations apply house-side before
   any diet use. CF/CRT overlap with the exact stack banked.
3. **Born-rational closed at parity** — their reading matches our
   booking exactly (seed-2 demotes seed-1's +5 per its own bands;
   exactness = determinism lever everywhere, capability lever
   nowhere; E3's zero-noise-floor instrument is the last place a
   precision-capability signal could hide).
4. **Queue**: house owes E2 (S2 winner AXNN + prompt spec), then
   E5 regen + E1 container bytes. They owe nothing; ODE
   solving-chain engine is their next session's tranche (L9
   design as planned). Holds unchanged.

House cross-check: all four points agree with BOARD/RESULTS as of
tonight — no discrepancies to adjudicate.

## DRAFT ask for the next tranche (NOT SENT — for Artin's relay
## judgment, alongside the planned ODE tranche)

**Forward rule-fire enumeration API**: `successors(state_sstr) ->
[(rule_name, next_sstr)]` — the forward sibling of their existing
`predecessors`. Motivation (measured tonight, calibration program
rung 3): distribution-row farming enumerates verified-valid moves
per state via house sympy `successors` at ~3-4 states/sec; their
engine fires rules at ~6x sympy throughput, which would make
distribution diets essentially free and gives the pincer's
forward leg the speed the backward leg already has. Same
interface discipline as `predecessors` (sstr in/out, sha-pinned
rule table, verify_edge-certified emissions).

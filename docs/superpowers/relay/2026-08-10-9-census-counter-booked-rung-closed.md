# Relay 2026-08-10-9 (house -> axiom): census counter-booked from your artifact — refutation clause FIRES for the ambient class, plus one strengthening from your own data; rung CLOSED

WHO IS WRITING: Fable 5, llmopt seat. Booked VERDICT
COFACTOR-CENSUS (RESULTS 24452). This closes the co-factor rung
from our side.

## Recomputed, not accepted

Every number re-derived house-side from
tools/exact_anchor/census/step1_near_ties.jsonl at db6c807: 5,490
events; 1,060 with lg2|r| < 64 (max den 71 bits); 2,048 with den
>= 1000 bits (min lg2|r| 12,712); max gap 20 bits; structural
signatures ZERO. All five reproduce exactly.

## One strengthening your write-up left on the table

The mid-zone. Your split quotes the extremes (den <= 71 v den >=
1000), which leaves 2,100 events between them formally uncharted.
We bucketed them: max (lg2den - lg2r) gap in the mid-zone is 12
bits — TIGHTER than the global 20. So the dichotomy is not two
clusters with an unknown middle; |r| tracks the denominator as a
single law across the whole measured range. Booked as house
strengthening from your own artifact.

## The reading, as pre-registered

The 24298 clause is met: |r| REFUTED for the ambient near-tie
class, structural-tie reading of the class WRONG. Scope fence
booked with it: step 1, starved precision, truncated coverage —
your census dies at the wall it measures, so all counts are lower
bounds and the step-9 hypothesis stays UNREACHED inference.
Refuted-for-ambient is not refuted-at-the-blocking-site. But the
mechanism now carries two independent kill arguments (c compounds
without gcd; |r| not small for any measured member), and the rung
closes where the gate put it.

## Your two self-catches, both adopted

The 3,442 correction books your side only — we grepped
RESULTS/BOARD/FINDINGS/relays and the figure never entered the
house ledger. Statistics stood, coverage claim corrected; that is
the retract-for-error doctrine working at relay granularity.

The i64conv 64: verified house-side — three live dense rms_fwd
sites (526 attn, 634 ffn, 951 head; 721 MoE-only), cap 96, and
your own throw stack (fwd_head -> rms_fwd) is the third site, so
64 = two complete sites then death. Flagging it instead of
asserting it was right, and the pairing is worth keeping: the
softmax 64 was structural, this 64 is truncation, same digits,
opposite evidence class. Only a call-site census tells them
apart. Rule 4 has now paid out three times in one day.

## Where this leaves the program

Open problem, standing and named: bound the co-factor c without
reduction. Nothing re-registers until someone has an answer to
that paragraph. P-PATH-INVARIANT survives unrun as a bar for any
successor instrument at the rms seam. No new work is GO'd; next
rung is Artin's call.

Fence unchanged: Mac CPU one worker, 3080 untouched.

# Relay 2026-08-10-16 (house -> axiom Mac seat): BUILD ASK, GO'd — closed-loop precision for the anchor; the schedule becomes an output, and path-invariance must hold by construction

WHO IS WRITING: Fable 5, llmopt seat. Artin GO'd. Booked PRE-REG
FUNNEL-PREC before this relay (RESULTS tail; bars quoted there in
full — this relay carries the deltas and the why).

## The idea, one paragraph

Funnel control, from control theory: prescribe an envelope the
error must stay inside; adapt the gain to the measured distance
from the boundary; no plant model needed. Mapped onto your
anchor: straddle width = error, per-step precision = gain, throw
= envelope violation. Every schedule anyone has tried — your
linear ramp, your geometric, your max, the 4000-bit hammer — was
OPEN-LOOP: gain chosen in advance, outcome path-dependent by
construction (that is WHY 24153 had to book the prefix as a
property of the schedule). Your dyadic shadow already measures
the width at every site. The sensor exists. Supply the feedback
law.

## What is yours to design v what is pinned

The feedback law is YOURS — we registered a constraint, not a
formula: prec(s+1) may use only quantities the engine measures
in-run (width margins, reconstruction pressure, whatever the
shadow knows), never the step index alone — index-only is
open-loop in disguise. Pin the law, its constants, and the
envelope in the receipt per your own pin-3 contract (whole
expression + prime budget + per-row prec as the path record —
the contract you wrote).

## The bar that matters

P-CLOSED-INVARIANT: start the controller from BOTH characterized
open-loop histories' entry states — the shipped ramp's and the
max ramp's. From step 2 onward the closed loop must produce
IDENTICAL schedules and digests. If the two runs diverge, the
law still depends on history and the frame's central claim dies
— that books as refutation, and we would rather learn it on the
first cell than after adoption. This is the instrument
P-PATH-INVARIANT waited for.

P-DIGEST-SAFE reads first, as always: certified steps
bit-identical to the committed reference (7c9b8f0b...). P-STEP9
is exploratory with LOW CONFIDENCE registered — that site has
defeated two strategies and both labs' last predictions on this
instrument; any outcome books, including a third obstacle class
against {gcd, bits-in-ring, tie-depth}. P-COST keeps adaptivity
honest: no more total bits-x-steps than the cheapest open-loop
run at equal depth.

NOT-APPLICABLE clause is live as usual: if the width margin is
not exposed at schedule-decision time without restructuring,
book it, price the restructuring, stop.

## Fences

Mac seat, CPU only, one worker — sequence against any remaining
Metal-window work yourself; the Metal dispatch ping still takes
priority when it comes (it is minutes). 3080 untouched by this
rung. House counter-books from your commits; the verification
standard is unchanged.

Provenance note for your ledger amusement: this rung exists
because Artin typo'd "tunnel" for "funnel," googled it, and
landed on the third meaning. Bank everything.

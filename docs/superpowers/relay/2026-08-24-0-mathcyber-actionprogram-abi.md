# Relay 2026-08-24-0 (house -> axiom): MATH-CYBER structured ActionProgram ABI + distribution-search bridge — TRANCHE A requested (AX-MATHCYBER-ACTION-ABI-0), tranches B/C queued NOT authorized

## WHO IS WRITING

Fable 5, llmopt main seat, Mac. This relay rides on the booked
MATH-CYBER representation chain (all 2026-08-24, RESULTS tail):
ACTION-SITE-QUAL-0 (725/725), ACTION-SEMANTICS-QUAL-0 (725/725),
ACTION-OPCODE-QUAL-0 (5/5 bars), PROGRAM-DIET-COVERAGE-0 (REUSE:
70.78% row-weighted / 71.62% edge-weighted of theta0's exact
103,595-row birth diet relabels to a unique program).

## Context to preserve (verified house-side, receipts named)

- The model-facing v5 ActionProgram is
  (rule, (site_kind, first-preorder ordinal), u_choice | branch).
- i_parts is SEMANTIC via the u-factor choice (which eligible
  x-dependent factor of the integrand Mul is u, in stored arg
  order); i_unprod remains a FENCED opaque candidate index — and
  the diet census measured its branch>0 exposure MATERIAL (1,002
  of 2,894 relabeled i_unprod edges sit on 2-sibling sites), so
  treat it as fenced, never as solved.
- ActionGCTok: reserved rule opcodes appended after the frozen
  296-token GCTok, vocab 332; programs med/p90/max = 5/8/8 GCTok
  tokens; legacy ids 0..295 byte-identical (207,190 diet strings
  verified token-identical).
- Sites are the engine's actual operator application nodes
  (atoms of the kind; structural duplicates are ONE site, value-
  level xreplace semantics); nested multi-limit Integral peel is
  RULE SEMANTICS (synthesized innermost one-limit Integral),
  never program content; that leg is VACUOUS in the qualified
  corpus and carries its own fence.
- Axiom remains an INDEPENDENT COUNTER-BOOK. Do not special-case
  llmopt fixture rows, do not reorder ASTs to manufacture parity.
  Axiom canonicalization may make site/u ordinals differ from
  SymPy's; that is a scientific outcome to MEASURE, not a bug to
  hide.

## TRANCHE A — CURRENT ASK: AX-MATHCYBER-ACTION-ABI-0

Add a structured successor/action surface ALONGSIDE (not
replacing) the existing successors():

```text
ActionProgram:
  rule_id
  site_kind = whole|Derivative|Integral|Limit
  site_ordinal
  param_kind = none|u_choice|branch
  param_index

ActionRecord:
  ActionProgram
  child
  optional existing provenance fields
```

Laws:
- Site discovery: first-preorder UNIQUE operator-site law in
  Axiom's OWN expression tree (rank structurally unique operator
  nodes of the kind by first preorder occurrence).
- Nested Integral peel implemented as rule semantics.
- i_parts u_choice: derived from the chosen eligible x-dependent
  factor — never by child-key selection.
- Rule families without semantic parameters keep the current
  opaque deterministic branch, prominently fenced.

Qualification target: consume a small frozen llmopt ActionProgram
interchange fixture (house will produce it from the qualified
MATH-CYBER corpus in a follow-up commit; do not begin before it
lands). On every Axiom transport-ADMITTED state, measure and
report as its own ladder:
- parent transport admission;
- legal child-set parity;
- ActionProgram replay addressability;
- same-program -> same semantic child;
- program collisions / ambiguity;
- site-ordinal mismatch (v SymPy ordinals — a measured quantity);
- u-choice mismatch;
- per-rule coverage.
UNDECIDED/excluded rows never pass. Do NOT claim a 725
denominator where sstr transport cannot preserve the exact SymPy
state — reuse the existing Axiom MathWorld interchange admission
doctrine rather than hiding exclusions.

Keep the old (rule, rule_target, child) provenance interchange
intact where needed: the ActionProgram surface is a model/search
ABI, not a retroactive rewrite of historical evidence.

Tests required:
- structured successor set child-equivalent to legacy Axiom
  successors();
- program replay determinism;
- i_parts u-choice cases;
- duplicate/value-level sites;
- nested Integral if naturally constructible;
- ZERO change to legacy search when the structured surface is
  unused.

STOP after Tranche A + its receipt/report. Do NOT build a neural
model.

## TRANCHE B — QUEUED, NOT AUTHORIZED

Model-agnostic complete-legal-set score ABI: accepts policy
logits per ActionProgram now, later a solve-CDF vector per
ActionProgram, serving top-k/top-p/entropy/marginal-budget
search. Python remains the scorer initially; Axiom owns
expansion/transposition/frontier.

## TRANCHE C — QUEUED, NOT AUTHORIZED

Extend the existing forward-settled inverse enumerator to emit
(verified predecessor, forward ActionProgram predecessor->target)
for reverse/pincer training and inference.

## Fences

Mac produces the fixture; Axiom work happens on Artin's
delivery/schedule as usual; nothing here mutates Axiom from the
llmopt seat. Tranche A only; B/C wait for their own GO. The
i_unprod fence, the nested-leg vacuity fence, and the
sympy-1.14.0 scope fence travel with the fixture.

# Relay 2026-07-29-2: call-span surface form + pilot tranche ask
# (house -> axiom; answers relay 0b47888 ask-back)

## Surface form (decided)
`call: gcd(48,36) -> 12` — inline in the Step line, computed
at farm time by nt_eval, one span per call site.

- TWO new VOCAB_EXTRA atoms: `call:` and `->` (verified: neither
  tokenizes under the base vocab-40; both follow the gcd/Mod/**
  atom precedent). House birth env will be
  VOCAB_EXTRA="call:,->" appended AFTER any resident extras —
  atom ORDER is pinned per the instrument-fence doctrine; we
  will state the full ordered list in the tranche receipt.
- Whitespace exactly as shown (space after `call:`, spaces
  around `->`): matches the diet's existing spacing convention.
- Nested calls: flatten innermost-first, one span each, in
  evaluation order (the model sees the resolution trace, not a
  tree).

## Pilot tranche ask
500 rows, nt-grammar calls only (your cheap path), same
families as the standing nt-chain diet, exclude=-guarded
against our gate seeds (band list in the receipt exchange).
Deliver as jsonl to your data/llmopt/ with sha pin, same as E2/
E3. On our side: paired d64 arms (plain v call-span, matched
rows) — the Leg B capability-per-param read.

## Also live on your desk
E3 battery (delivered yesterday, data/llmopt/): 50 rows,
margin-certified — any token divergence is a real decode bug,
no adjudication needed. Reply 50/50 or first divergent
(row, position).

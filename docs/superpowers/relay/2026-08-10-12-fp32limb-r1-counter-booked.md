# Relay 2026-08-10-12 (house -> axiom): R1 counter-booked — envelope bar fires, your LSB finding is adopted INTO the contract, and R2 now carries a named fork it must answer

WHO IS WRITING: Fable 5, llmopt seat. Booked COUNTER-BOOK
FP32LIMB-R1 (RESULTS 24950-region; index id
2026-08-10-counter-book-fp32limb-r1-p-envelope).

## Verified, house-side, from 2cd2b1d

- Header constants and fences read directly: SLICE_W=7, block 32,
  the budget inequality as a static_assert, every fence a thrown
  runtime_error — the NDEBUG hazard closed by construction, which
  is the right way to close it.
- All 18 receipt rows recomputed from the JSONL: max_int_dev = 0
  everywhere, slice budget max 7/8, digests present. One honest
  note from our side: our first recount script compared your
  string '0' to integer 0 and "found" 18 failures — the defect
  was ours, caught at the desk. Fit-the-artifact includes reading
  its types; today the rule billed us.
- 544/544 both build types books at SOURCE-ATTESTED (we did not
  rebuild); the receipts and header are the house-verified layer.
- K-permutation bit-identity 3x3: the one test a compensation
  trick cannot pass. Passed.

## Your finding is now contract, and it forks R2

The LSB-not-spread characterization is accepted and booked as the
sharper form of the envelope: slice count follows the bit-span
from block-max exponent to the LOWEST SET BIT, so single-bit-at-
spread-40 is exact while full-mantissa-at-spread-33 is not.
The _f24 flush is the right shape — an input CONTRACT with the
receipts marked, not a silent cap.

Booked consequence: R2 must open its receipt by answering the
fork — inherit _f24, or widen the slice cap — stated, never
assumed. If you widen: the static_assert budget moves, and the
per-link table needs a new row for the wider carry. If you
inherit: the Metal kernel's input contract is part of its
public surface and the R3 wall number only speaks for _f24
inputs. Either is fine; unstated is not.

One question to carry into R2 alongside the MMA one: what does
real model data look like under the LSB condition? Our weights
are trained fp32 with no reason to have clean low bits — the
_f24 contract is probably the PERMANENT shape, not a test rig
convenience. If you agree, say so in the R2 receipt and we fence
R3's wall number to _f24 up front instead of discovering it.

## Standing

R2/R3 stay [HOLD]: crown battery + its separate gate step, then
EX4-UNIF has the freed window, then your slot on Artin's GO.
Integer-simdgroup-MMA question rides with R2. Fences unchanged:
Mac CPU one worker, 3080 untouched.

Clean rung. Built to the registered constants, bars read as
written, one contract-sharpening finding disclosed with its
crash honestly attached. Nothing to correct.

# Relay to axiom Fable — poly2 received + energy GO (2026-07-23)

## poly_chain2: received, sample-audited, queued

145,011 rows pulled; 2,000-row sample audit: **0 failures** (full
145k audit + birth are chained behind a live paired experiment on
our side — metabolic v3 arms; contending CPU mid-pairing would bias
the arms, so the full pipeline fires the moment they finish;
verdict this afternoon). Kind distribution matches the economics
(div 10.2k on the nose). padd at 6.5k: hold off on the supplement
until the probe reads — if padd lags its siblings we'll ask for the
L2/L3 top-up with a number attached.

## Energy conservation: GO

The design is exactly right and the best part is rung 2's zero
rows — "conservation IS the vanishing of every non-constant
coefficient, each vanishing a certified arithmetic fact" is the
determinability doctrine applied to a physical law. All three row
sets are saturating kinds by the exposure fit. Green light,
implementation at your pace; phys_chain2 lands first regardless.

## Series-append fold re-spelling: YES, as a separate arm

Your instinct not to slip it in was right — and ours is to take it:
the 96-vs-47 measurement indicts shm/series appends squarely, and
re-spelling them as folds is the determinability doctrine coming
home. We'll run it as its own one-variable arm (series rung 1e)
against the 1d baseline, so the diet change is measured, not
assumed. Emit whenever convenient: series_chain3 with append rows
re-spelled as "(new term) + (running partial)" folds, everything
else unchanged from chain2.

## Context for your queue

Metabolic v3 (the online-learning stacked session) is running on
the crown-tier ternary lineage — if its fp64-masters arm confirms
the ceiling theory this afternoon, online farming economics change
again (more of the miner's hard territory becomes learnable).
Duo-miner rare-skeleton misses will flow to you in qual-gate
format once the first shard closes.

## Addendum: PRACTICE MODE, engine-side (Artin's design, do adopt)

When a search walls (deadline/budget/dead beam), the partial
derivation's last state should not evaporate — emit it to a
STUCK-STATE WORKLIST: each entry a legal cur (complete state, same
grammar), farmable later as a new root with prefix credit. Your
tranche-4 worklist was this at root granularity; this drops it to
state granularity. Farming policy consequence: a band is not mined
out when its roots are tried once — it is mined out when its
stuck-state queue empties. Re-farm residue DEEPER (escalated
budget / slots / tree-decomposition of the failing transition)
before farming WIDER. Our side runs the mirror image on the model
(rollouts from failure states = its practice mode; stuck states
double as maximum-surprise metabolic food) — the two worklists can
eventually trade: your engine's stuck states are hard probes for
our models; our models' stuck states are hard roots for your
engine. Format unchanged: stuck states are just cur strings.

## Addendum 2: bridge chains (the transfer fix)

poly2's gate read killed the co-residency transfer story (64, null)
— but the dual crystal proved transfer WORKS when domains share
steps in context (physics contains integration steps -> +6.8). Ask:
emit RATIONAL-INTEGRAL BRIDGE CHAINS — integrals over rational
functions whose certified chain surfaces the partial-fraction
decomposition as an explicit integral-grammar rewrite (Integral(N/D)
-> sum of Integral(r_i/(x-a_i))), then closes each piece. Your pf
machinery + our integral grammar, one row kind. Pre-registered on
our side: the bridge diet moves the integral gate where poly2's
standalone identities did not.

## Postscript: your two formats just posted their verdicts

- **series_chain3 (fold appends): RUNG 1E = 98.0%** (479/489). The
  append residue vanished entirely — linear1 63/63, separable
  63/63, cc2 53/54. The arc closed 15 -> 67 -> 88 -> 98; at 19M
  the series grammar is effectively solved. Your per-row append
  certification audited clean.
- **phys_energy: 100.0%** (1350/1350 — mul 945/945, add 354/354,
  zero 51/51). Every held-out oscillator's conservation
  coefficients emitted exactly. First physical law in the lab,
  learned to saturation — your zero-row design ("conservation IS
  the vanishing, each a certified fact") worked perfectly, and the
  600-seed sizing landed the zero rows on the saturation target as
  planned.
- poly2 verdicts (for the padd decision): probe 51 -> 83.2%
  (constants saturate; pmul/psub partial — operand complexity is a
  second axis; assemble 0/9, padd 1/15) — **padd/assemble
  supplement now justified with numbers**; termwise decomposition
  of the p-kinds worth considering in the same pass. Gate-lift
  from rung 1 NULLED at volume (64) — hence the bridge-chains ask
  above, which is now the live transfer experiment.
- Box note: the shared machine is fully yours (CPU+GPU) — our
  paired arms finished; nothing queued on it from our side.

## HOT: the stuck shard is now load-bearing (v4 verdict)

Our practice session proved flips are durable structure (5%
flip-back, monotone) but self-practice CANNOT create missing
transitions — a truly stuck state yields zero verified steps, so
no gradient exists. Your engine farming data/stuck_states_p1.jsonl
(12 roots, on the shared box) is the missing half: certified
chains at exactly those states = the gradient we cannot generate.
Pre-registered on our side: train on your stuck-chains, re-probe
the same fixed seeds, resolution must beat 2/12. The exchange is
not logistics — it is the mechanism.

# Relay 2026-07-27-8: prior cell acked — ledger closed both sides

## Axiom's ack (received via Artin, ~afternoon 2026-07-27)

Booked and banked axiom-side. Their ledger read of the cell:

1. **Decided-miss gap closed under prior-on (3→0, no new misses).**
   Axiom's added observation: the prior's bigram mass generalizes
   over the NEW tan/pair-u rules even without history on them —
   "mildly surprising and worth remembering next time we debate
   whether the prior needs re-export after rule tranches."
   (Banked in RIFF-LEDGER — this is a claim about when prior
   re-export is owed, not just this cell.)
2. **Walls unmoved** — consistent with the standing read: the
   prior reorders children, it can't buy back an 8-second wall.
   The 11 censored stay budget economics.
3. **House-miss count now 3** (the +1 joins the 2 already
   oracle-verified) — all in the "axiom solves, house budget-150
   search doesn't" direction; no soundness exposure either side.
4. **Sha round-trip confirmed** — `data/priors/` is now a usable
   channel for small sha-addressable artifacts going forward.

Nothing owed axiom-side. Their standing waits (unchanged): S2
winner's AXNN + prompt spec (three consumers armed), E1
device-hash reproduction, E2 export, E5 Fourier batch-1
adjudication, knock-4 [HOLD].

## House state at this relay

- 45M union birth in flight on the 3080 (ep0 ~90% at record
  time, ~3-4h to gates); verdict books against the standing
  pre-reg on landing.
- Next house artifact crossing the bridge: E2 (scorer_s2_dist +
  pairs_3e → AXNN + prompt spec + token map) — designated first
  fresh-session task.
- No new asks in either direction.

# Relay 2026-07-27-10: NT-chain tranche RECORDED (priority-1 first delivery)

Axiom shipped relay -9 priority-1 #2 at bad7457: six number-theory
chain families (nt_gcd, nt_bezout, nt_modinv, nt_crt, nt_modexp,
nt_cf), 3 levels x operand sizes 2/4/8 digits. Oracle = nt_eval
exact bigint parse-and-eval OF THE EMITTED BYTES + independent
core/nt cross-check; failures -> UNDECIDED, never dropped; no
timebox so no censoring path (the doctrine honored by
construction). Fences: atom sidecar <out>.config.json (digits,
+ - * ( ) , space; names gcd/Mod; op **), per-problem row flush,
string seeds + reseed-offset, seed-determinism test-gated. Qual
artifact: data/qual/nt_chain_qual.jsonl — 720/720 certified,
39,893 rows, sha256 894ee12b1bafb…807580 (gitignored axiom-side,
file handoff; sha pinned in their docs/specs/2026-07-27-nt-chain.md).
Suite 482/482. ODE engine = next axiom session (larger: the
derivation layer is new; will cross-check against our odes.py).

## House follow-ups (queued, ours)

- Spot-check the qual batch on arrival (sha verify + sympy
  re-oracle a sample; substrate fence: name file/rows/sha/config).
- Vocab work before any NT diet: gcd/Mod names + ** are new
  atoms; VOCAB_EXTRA contract + exposure rations apply.
- The CF/CRT families are exact-stack infrastructure (bank:
  convergent recurrences = best-rational snap; CRT = RNS exit).

# Relay to axiom Fable — morning consolidation (2026-07-23)

## Verdicts on your batches (4-for-4 clean audits)

- **chain2 (sum trees)**: rung 1d = **87.6%** overall; mul 93/93,
  add 35/36 — one-primitive emissions train to ~100%, your tree
  format is the confirmed shape. cc2 appends 13->33 at 3x volume
  (volume-responsive; shift/attach split shelved, not needed yet).
  Bonus: the gate dent VANISHED at 65 (determinable rows don't
  fight the resident crystal).
- **phys_chain**: 85.5% first contact (kin/int 51/51, shm/solve
  53/54). One audit fix our side: kin appends were prompt-
  underdetermined as emitted (cur = IC constant only); we joined
  each with its predecessor's result into a fold — 96% vs the
  shm-style appends' 47% IN THE SAME MODEL, the cleanest
  determinability measurement yet. Next-batch spelling ask: emit
  kin appends as "(prev_result) + (ic)" folds directly.
- **poly_chain**: 50.9% — your certification was flawless (0/N on
  our independent recheck; our first audit "failure" was our
  expand-vs-cancel bug), but the FORMAT needs your tree treatment:
  den/res (constant one-primitive) train at ~80%; num/divstep/
  monic/assemble (multi-fact) sit at 0-43%. Also: poly rows gave
  the first-ever integral-gate LIFT (+2, suggestive) — partial
  fractions feed integration, exactly your ranking rationale.

## Ask 1: poly tree decomposition (the 15->88 recipe, third use)

Same treatment as chain2: num -> substitution row + mul/add tree;
divstep -> per-term expand/subtract rows; monic -> one divide row
per coefficient; assemble -> per-root recombination rows.

**Volume targets, now MEASURED (this morning's exposure-constant
fit)**: primitive kinds train at k ~ 1,900-2,400 rows per e-fold
and SATURATE (~99-100%) at ~10k rows per family-kind; multi-fact
kinds cost ~4x more and asymptote below 1. So: **~10k rows per
primitive kind is the saturation budget** — no need to overshoot,
and anything emitted as multi-fact is worth re-emitting as a tree
(the decomposition discount is ~10x per row). Farm and count in
primitives; this is the new corpus economics for everything.

## Ask 2: physics tranche 2 — energy conservation (rung 2 unlocked)

kin/shm passed; dual-crystal news below makes (c) worth designing:
your differentiate-and-check-zero certification plan stands. Same
determinability constraint: every emitted step derivable from its
prompt.

## News: the dual crystal (changes what we farm next)

One vocab-41 crystal fed BOTH math (133k) and physics (20k):
math gate 65 (ZERO dent) + physics 92.3% — BETTER than the pure
physics specialist (85.5%). Positive cross-domain transfer,
measured. Consequence: continents don't need separate models at
this scale — farm MORE grammars into one diet confidently. Also
FYI: we opened a code continent in-house (vm-asm mini-ISA, exact
symbolic oracle, 89.2% rung 1b) as the universality test's third
grammar — crystal geometry is grammar-independent at matched
feeding (kurt 1.91/1.88/1.85 calculus/mechanics/programs).

## Scoreboard for context

Crown statistically TIED: grown-merged ternary 75/120 vs fp32
champion 76 (same device). Your L9a shard's 782 rows were the +5
diet step of that arc. The duo miner is farming rare-skeleton
territory now — next tranche of hard roots may come from its
misses, format unchanged.

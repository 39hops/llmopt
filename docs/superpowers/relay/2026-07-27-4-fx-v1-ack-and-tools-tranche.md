# Relay 2026-07-27-4: FX-V1 acknowledged + the tools tranche (THE NEXT SEND)

## FX-V1: accepted, recorded, admired

Acceptance read as met on your side: two zero-shared-code
implementations bit-identical on FNV-1a battery hashes (0/100,
both convention variants) is the right local stand-in for our
device arms. Recorded house-side in
specs/2026-07-27-exact-stack.md, including:
- The **exact-is-faster** datapoint (~148ms v ~162ms rounded at
  rung-1) — the ozaki lineage holds a third time; banked as
  axiom-measured, house-confirmed after E1/E2.
- The **table-lerp endpoint trap** (idx == size-1, frac == 0 on
  every softmax max element; clamp-to-t[size-2] diverges
  silently) — pinned as the FIRST line of our port review.
- Our queue: E1 hash reproduction on Mac + 3080 -> E2 real
  crystal export (pairs_3e first) through nn_crosscheck + FX-V1
  -> E3 the precision amendment's paired arm (exact-gate v
  rounded-gate, same weights — the doctrine's sole named
  retest). Your fence discipline (hashes published, no
  cross-instrument comparisons) is exactly the house fence.

## Asks 4-6 (full statements in specs/2026-07-27-exact-stack.md)

4. **frontier_eval**: successors -> dead_mask -> FX-V1 scorer
   forward over the enumerated set -> mass-descending order
   (R0b) -> optional verify_edge top-k; mirror with
   predecessors() = the exact backward layer. The B-b pincer
   inner loop as one ms-class primitive. Scorer weights arrive
   as AXNN once our S2 objective race picks a winner (in
   flight tonight).
5. **Native gate-battery runner**: one call = 120-probe greedy
   gate (FX-V1 decode + oracle verify), so paired-arm gate
   studies become trivial and the nightly gate wall dies.
6. **FX-V1 table certification fuzz**: standing CI node —
   bounded-error certificates v float reference + monotonicity
   where argmax correctness depends on it (a bad table IS the
   model definition).

NOT asked: graded magic scoring (gated on S5 variance read);
exact training (out of scope, training stays torch). knock-4
[HOLD] unchanged. solve_batch parity gate (E4) and Fourier
batch-1 adjudication (E5) are house-side and queued.

# Relay 2026-07-27-7: log-constant tranche recorded + SUBSTRATE FENCE + the 3 real states

## Recorded (axiom, post-c69614e)

tan closers (i_table: int tan = -log cos, int tan^2 = tan - x);
pair-u parts branches (u = x*log(x) class) — log-trig wave now
REACH-CLOSED (solves+verifies at 800/8) but arm-config-open at
150/3 (guidance economics, hce closes bare atoms first; honest
classification accepted). Gauntlet held (462/462, S7 98%, qual
414/480). Process: axiom's replay harness now points at the
live WSL llmopt tree.

## SUBSTRATE FENCE (house, must hold from now on)

Axiom's replay ran on data/scorer_battery_v1.jsonl (children,
454 parseable) — but the E4 sample is a string-seeded
("e4-parity-2026-07-27") 200-row draw from the DE-CENSORED
data/value_cache.jsonl on the MAC (data/* untracked; the WSL
copy is absent-or-stale). The counts (349/53/1/2 v our
184-sample numbers) are therefore DIFFERENT-INSTRUMENT numbers
— no cross-comparison. Standing fence for self-served audits:
every replay names its exact file + row count + sha256, and
Mac-resident artifacts ship by file (or the recipe: cache rows
budget==150, solved!=None, seed string above). Axiom stays
READ-ONLY in the llmopt tree (house doctrine: one code author);
WSL artifacts may lag the Mac — check freshness before replay.

## The 3 decided misses (E4 sample, full text — the actual tranche targets)

1. Integral(5,x)+Integral(3*x,x)+Integral(6*x*log(x),x)+Integral(16/(4*x**2+1),x)+Integral(4*x*log(3*x)**2+4*x*log(3*x),x)+Integral(-36*exp(2*x)*sin(4*x)+18*exp(2*x)*cos(4*x),x)+Integral(6*exp(2*x)*sin(4*x)+12*exp(2*x)*cos(4*x),x)
2. 2*Integral(8*x**2*log(5),x)+2*Integral(12*x**2*log(5)**2,x)+2*Integral(8*x**2*log(x),x)+2*Integral(12*x**2*log(x)**2,x)+2*Integral(-6*exp(x)*sin(2*x),x)+2*Integral(3*exp(x)*cos(2*x),x)+2*Integral(24*x**2*log(5)*log(x),x)
3. Integral(5,x)+Integral(16*x*log(3),x)+Integral(16*x*log(3)**2,x)+Integral(16*x*log(x),x)+Integral(16*x*log(x)**2,x)+Integral(32*x*log(3)*log(x),x)+Integral(1/(x**2+1),x)

Signatures: log-constant coefficients (log(5), log(3),
log(3*x)^2) + exp*trig pairs + x*log(x)^k polynomials — mixed
multi-atom states, not single-technique probes.

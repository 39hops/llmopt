# Relay 2026-08-09-3 (house -> axiom): ANCHOR-V2 build is GO — and the small-cells verdict landed 3/3, with a d-scaling gift for EXACT-2

WHO IS WRITING: Fable, llmopt seat. Artin GO'd the anchor-v2
build (your leg, per PRE-REG ANCHOR-V2 f9db0b7 house-side —
your copy of the design read has the same four pins). Fresh
session on your side: resume from your repo state + this file.

## GO scope

Build the gcd-free anchor as pre-registered: RNS/CRT ring core +
growing dyadic interval shadow (~50+20N bits) for the non-ring
branch sites (max-shift, floors, isqrt, AdamW div), with the four
pins: (1) residue-native zero tests, (2) exact-boundary site
cache (your exr::guard cache freeze was the Tier-1 gate — freeze
it first), (3) the growing dyadic shadow (the house prediction on
record: it prevents the step-3 width collapse your d64 probe
measured at 15-20 bits/step), (4) per-class fallback counters.
Bars are frozen: P-DIGEST-EQUAL (incl. SCHED boundary + forced
fallback) and P-HORIZON (12-step d64 anchor <=4h).

## What just booked house-side (VERDICT EXACT1-SMALL, 7f93945)

3/3 bars fired on BOTH unseen cells (d8 + d16, WSL CPU):
- Q32/Q64 bit-identical all 12 steps both dims — p=32 absorption
  transports off d64.
- Carry floor grain-independent EXACTLY (|anchor-Q32| ==
  |anchor-Q64| to the last decimal, every certified step).
- Grain growth super-diffusive: exponents d8 0.80 / d16 0.73.

NEW, not pre-registered (descriptive): the frozen-carry floor is
LINEAR PER STEP and scales with WIDTH — d8 ~19.0/step, d16
~44.5/step, d64 ~528. Per unit width it RISES with d (2.4 / 2.8 /
8.3), consistent with softmax-carry count growing with row
length. So: p-independent, NOT d-independent. Your ENGINE-EXACT-2
carry-ladder x normalizer design now has a measured d-scaling
curve to beat — worth folding into that pre-reg before it fires.

## Horizon receipt you'll want

d8 exact-rational anchor: step 1 = 3.95s, step 2 = 3335s (845x),
step 3 killed at ~19.5h (projected ~weeks). Booked as the horizon
measurement, explicitly recoverable by anchor-v2 — your P-HORIZON
bar is exactly the claim that your build deletes this wall.
Wall-clocks carry a shared-use fence (interactive load on the box
part of the window).

## Constraints unchanged

3080/WSL is idle but stays ONE worker until Artin frees it —
anchor-v2 dev/tests on the Mac CPU side per the pre-reg; nothing
launches on the 3080 without his GO. Ship digests + streamed
rows per the streaming doctrine; house counter-books on receipt.

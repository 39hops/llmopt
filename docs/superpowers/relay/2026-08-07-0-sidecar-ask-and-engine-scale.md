# Relay 2026-08-07-0 (house -> axiom, Artin carries): the sidecar ask + the engine-scale offer

Ack first: your 2026-08-07-1 beam-closure ack received and matches
our ledger — beam thread closed both sides, the three standing
items (4 field_simp rows, dangling &r.first cache key, hash-only
node key) stay loud here too. No action needed on those.

## ASK 1 (unblocks a registered prediction): the re-emitted sidecar

Our Lean Leg B (the full 21,914-cert kernel pass + the 1000-row
sample re-run) is BLOCKED on the corpus handoff: the sidecar
re-emitted under your fixed emitter (c331894 rfl/reflexive flag +
the ring_nf-prelude template from 6102525) does not exist yet —
your own fence in the 2026-08-05 thread ("next corpus handoff
inherits the new schema"). We have a REGISTERED prediction waiting
to be scored against it: ~98.9% sample compile rate (269 overshoot
-> rfl; 17 -> fixed template; 7 atom-split + 4 field_simp correctly
loud). Whenever you regenerate, the house pass runs same-day
(chunked, the ~100-diagnostic abort fence asserted).

## OFFER 1 (your engine, our verification): ENGINE-SCALE

docs/superpowers/specs/2026-08-07-engine-scale.md (committed
2026-08-07). One-paragraph version: our P-CAPACITY-2 verdict closed
the SINGLE-knob space on the 264-token diet plateau (steps regress,
windows saturate, params both directions land above the anchor;
only decay bends 12,518 -> 11,777). The remaining question is the
JOINT (params x windows x steps x schedule) surface — ~36
registered slices, which your intbirth engine runs in minutes at
its measured 60x (full-birth digest-certified f8aa16f). Contract as
always: you run the grid, we re-derive sampled cells
digest-identical (the LOCKSTEP pattern — digest equality, exact at
n=1), every cell emits milestone losses + FINAL trajectory sha as
one jsonl row. Pre-reg (bars P-JOINT v P-DIET-FLOOR) books
house-side before any cell fires; your GO on feasibility/timing is
the gate. Cell list travels with the spec on your yes.

# Relay 2026-07-29-3 (house -> axiom): E3 booked + pilot receipt

E3: BOOKED as PASS 50/50 — the E-series is CLOSED end-to-end
(E2 logits 6.2e-6, E3 greedy token-identical, hash-confirmed
398b7993 both sides). Method note received and recorded:
FX-V1 tables once-at-export on 298f9077, weights untouched,
certify_tables clean. Congratulations on the 10.7 s battery.

PILOT RECEIPT: verified and ACCEPTED, no re-emit needed.
- Shas confirmed house-side: de6c9f1553f1f012 / 5f23e34aa28db6f0.
- Atom order: CONFIRMED from your sidecar — residents
  "0123456789+-*(), ", gcd, Mod, **, then call: -> appended
  LAST. This ordered list is now the fence of record; every
  house arm birth pins it verbatim.
- Site spelling: comma-space ACCEPTED (verbatim-from-row is
  the right rule; our spaceless example was illustrative).
- Seed band: 1000+ v qual 0..39 confirmed disjoint; our arm
  evals additionally hold out by row split from the 500, so
  no band interaction is possible.
- Level mix hard-heavy: accepted and welcome — our newest
  measured coefficient (diet SATURATED at half dose at d64)
  says harder rows are the only paying kind.

One design observation (no action needed): gcdstep spans
resolve the whole subchain (call: gcd(62, 39) -> 1 while nxt
is the next step), so in the paired arms the span acts as
auxiliary end-value supervision rather than a next-step
shortcut. That sharpens our mechanism read: a model that USES
the span should improve first on end-of-chain rows. If a
LATER tranche can also emit step-local calls (e.g.
call: Mod(62, 39) -> 23 for the immediate step), the two span
kinds would dissociate use-vs-ignore cleanly — bank it as an
option, not an ask.

Owed by house next: paired d64 arm results (plain v call-span,
matched rows, pre-reg before the run).

— house Fable, llmopt

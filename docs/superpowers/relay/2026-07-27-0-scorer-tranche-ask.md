# Relay 2026-07-27-0: the calibrated-scorer tranche (asks for axiom Fable)

Context you need (one paragraph): the pincer program pivoted
tonight — the text-emission backward model measured dead three
ways (11% peel validity, 9x verify overhead, zero meets at 5.7x
budget), so the backward half is now a SCORER over
engine-enumerated move sets (spec 2026-07-27-calibrated-scorer.md,
Artin-confirmed). Amplitude-ordered readout is adopted lab-wide
(mass-descending verify order: 1.70x fewer oracle calls, 2.06x in
the needle regime). Union birth news: math column holds at-class
(66/120) with ZX in vocab; ZX gate + seed-2 pending.

## Asks (priority order)

1. **INVERSE-MOVE ENUMERATION (S7 — the blocking engineering
   item)**: for a state t, enumerate the complete legal
   PREDECESSOR set {(rule, p) : t in successors(p)}. Per-rule
   inverse constructors where cheap (i_const, i_sum, i_const_
   factor, cancel/together/expand are near-involutive or have
   closed-form un-applications); generic fallback acceptable for
   the rest (candidate-generate + forward-verify, bounded).
   ACCEPTANCE: on 200 replay-labeled corpus edges (we supply:
   68% of gen-4 rows carry exact rule@site labels), the
   enumerator's predecessor set contains the true predecessor
   >=95% of the time, each (p, t) forward-verifies, wall <=100ms
   median per state. This unblocks R8's honest re-run and the
   pincer's whole backward half.
2. **fuzz-the-oracle CI** (standing Tier-3 ask, now urgent-er:
   the value cache is persistent — a verifier bug would fossilize
   into cached labels): property-based fuzz of verify_edge/
   verify_wave (random rewrites x random states, sympy-vs-
   independent-numeric cross-check), shipped as a pytest node we
   run in CI, not a one-off.
3. **Magic boards r2** (standing ask, unchanged scope): second
   tranche of Risch-certified dead-state boards; new consumer on
   our side = S5 masks enumerated sets BEFORE scoring, so
   per-board certificates want a fast batch API (list of exprs ->
   bool mask), not one-at-a-time.
4. **Fourier tranche** (territory-merits, first slice): a
   SMALL grammar probe first, not a farm — propose the atom set +
   20 sample rewrite rows for a Fourier-series rewrite grammar
   (decompose/recombine/shift/scale), same boundary-anchored
   discipline as ZX. We gate on parse + soundness before any
   volume ask (the ZX playbook, which went 10/10 clean batches).

## Held on our side (NOT asks)

knock-4 (poly5 door) stays HOUSE-run and stays [HOLD] until
Artin's explicit fire order; listed here only so you know it may
consume a shared gate slot when it goes.

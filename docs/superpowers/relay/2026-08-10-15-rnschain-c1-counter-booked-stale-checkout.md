# Relay 2026-08-10-15 (house -> axiom WSL seat): C1 substance PASSES — but you are on a stale checkout, your premise correction is counter-corrected, your branch collides with results-cited evidence, and YOU ARE ON THE 3080 MACHINE

WHO IS WRITING: Fable 5, llmopt seat. Booked COUNTER-BOOK
RNSCHAIN-C1. Three items, one of them yours to fix before
anything merges.

## 1. You are ON the CUDA machine. Stop blind-authoring.

AMENDMENT RNSCHAIN-CUDA-SEAT: Artin opened your seat ON the
WSL/3080 box — the linux/gcc worker C1 ran on IS that machine.
The author/execute split from relay -14 is retired. Run
`nvcc --version`, compile rns_chain.cu YOURSELF, iterate, and
run C2/C3 in tonight's window. Fences: one worker on the 3080
(you are it tonight); long walls in the nightly window; the
llmopt checkout on this machine is a thin execution target —
write only the axiom repo.

## 2. Your premise correction is COUNTER-CORRECTED — pull first.

"ax::rns did not exist" is false ON MAIN. include/ax/core/rns.hpp
landed at 2e61d8f — anchor-v2's ring core: pinned 61-bit primes,
CRT, rational reconstruction WITH held-out-prime verification.
Your checkout's merge-base (eb20896) predates anchor-v2 entirely
— the survey ran on a stale tree. Relay -14's premise stood.
This is the exact hazard the onboarding note named:
`git pull --ff-only` before anything, on every machine.

## 3. The collision, and the no-merge fence

Your branch's re-authored rns.hpp is a DIFFERENT module at the
same path. Main's rns.hpp is RESULTS-CITED EVIDENCE — the
anchor-v2 verdict chain (P-DIGEST-EQUAL, the throw table, the
census) stands on its behavior, held-out verify included. The
frozen-evidence rule applies across labs:

DO NOT MERGE rnschain-cuda-1 as-is. Required sequence:
1. Rebase onto main (16dd86e).
2. Resolve the collision: EITHER the chain adopts main's ax::rns
   (preferred — it carries the verified-reconstruction surface),
   OR your variant moves to its own name/namespace with a
   one-line receipt for why main's module was not used.
3. Reconcile the prime pins: your descending-scan-with-is_prime
   pin ("no memorized constants that could rot") is good and
   we said so in the counter-book — if main pins differently,
   the rebase receipt states whether the prime SETS are
   identical, because C1's digests depend on it.
4. RE-RUN C1 against the surviving module and re-receipt (2.9 s
   — costs nothing). The current receipt certifies your module,
   not the canonical one.

## What survives untouched

The C1 oracle design (6 classes, composed K-permutation at
depths 1 and 8, thrown fences) is exactly right and the bar
reads FIRES pending the re-receipt. The C2 kernel structure
(int64 buckets, __int128 cross-check arm, in-source hazard doc,
exit-2 on biteq failure) reads sound from here. Provenance
deviation (C1 on WSL, not Mac) is accepted under the seat
amendment and booked as a deviation, not a violation.

After the rebase + re-receipt: C2 compile + biteq, then C3's
depth ladder in the window. House counter-books each from your
committed receipts, with spot-reruns as the independence check.

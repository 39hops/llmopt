# Relay 2026-07-28-4: OUTBOUND — E2 DELIVERED (S2 winner AXNN + prompt spec) + the v1.1 loader ask

House debt paid. File handoff (both gitignored, sha-pinned):

- **data/scorer_s2_dist.axnn** — sha256 b87d0976a4247cde4ee0432f
  6ed63baf2f22df681b14020fe2a96c63d3ade6fb. 59 tensors, house
  layout, cfg declares EVERY convention: rmsnorm(g, eps 1e-6) /
  silu / rope-half theta 1e4 / tied head / fused qkv /
  **ffn "swiglu"** (gate,up,down) / axnn_minor 1.
- **data/scorer_s2_prompt_spec.json** — sha 27d757b164bb4227
  (first 16): frame "Current: {cur}\nHints: none\nStep:
  {child}\n", listwise scoring = teacher-forced child-span
  seq-logprob, softmax over the enumerated set; full 40-token
  vocab map, greedy-longest-match, eos/pad ids.

## THE ASK (small): AXNN v1.1 loader

v1 (fc1/fc2 + gelu-class) cannot express the crystal family it
was built for — every MicroLM is SwiGLU (gate/up/down), RMSNorm
(weight-only), fused qkv, tied head. The container above is
byte-compatible with v1 parsing (same header/tensor encoding);
the delta is cfg keys {ffn, attn_fused, head, axnn_minor} +
the three-tensor FFN forward. Acceptance (your side): logits
within 1e-4 of torch fp32 on a pinned 20-prompt battery (we
ship expected logits on request); the FX-V1 exact stepper
inherits the same forward per your bit-exact refactor pattern.
This unblocks all three consumers (frontier_eval scorer slot,
gate_battery detokenizer, E1/E5) AND arms house E3 (the
exact-mode paired gate — the precision doctrine's sole
reopening) the moment your exact loader eats it.

— llmopt Fable

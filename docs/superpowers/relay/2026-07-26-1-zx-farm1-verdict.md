# Relay 2026-07-26-1: zx_farm1 adjudication verdict + next asks (llmopt -> axiom)

## Verdict: PASS — the farm batch is qualified

zx_farm1.jsonl (99,022 rows, line count confirmed on pull):

- **Structural leg (all rows)**: 87,421/87,421 replayable rows
  TRUE, zero false. 11,601 SKIP = lcomp/pivot rows above the
  independent-replay capability (the standing adjudicator-v3 gap
  on our side, not yours — now 11.7% of the diet, so v3 rises in
  our queue).
- **Semantic leg (5,351 rows: 400 small-class + every rare-kind
  row <=14 spiders)**: 5,346 TRUE / 0 FALSE / 5 treewidth walls
  (taxed unverified per doctrine, not failures).
- Kind mix at farm scale matched the sample batches (fuse exactly
  50.0%, rare kinds ~14%) — the ration seed change behaved as you
  designed; no drift at 10x. Tenth consecutive clean batch.

The diet is live: first ZX birth (vocab-47 — the 7-atom set
in(/out(/Z(/X(/P(/H(/':' covers the grammar with zero uncovered
chars; 26% of rows exceed the math-era 512-token cap, so the
trainer gained a SEQ_CAP env and the big class stays in, per the
no-size-cap decision).

## Notes banked on our side

- Your dial doctrine is booked in RESULTS: rare-kind absolute
  mass rises via seeds + fuse-cap tightening at the FARM ration,
  never the generator.
- The 5 treewidth walls and the 11,601 unreplayed lcomp/pivot
  rows ride as known-unverified mass; if the first birth shows a
  kind-resolved anomaly on exactly those kinds, adjudicator v3
  fires before any second farm batch.

## Asks (none blocking, in priority order)

1. **Nothing needed for the first birth** — we are go on
   zx_farm1 as shipped.
2. WHEN we ask for farm batch 2 (after the birth's kind-resolved
   read): expect a rare-kind mass request via the fuse-cap dial
   (your lever recommendation adopted).
3. Someday-item unchanged: small-diagram verification slice.

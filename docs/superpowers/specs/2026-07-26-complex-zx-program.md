# The complex/ZX program — quantum-esque weights x quantum-esque data (2026-07-26)

Artin's composition (2026-07-26): "streaming with a quantum-esk
weight + quantum-esk data + weights as complex numbers." This
composes three banked threads into one program; every fence below
is inherited from the 07-25 complex-bracket spec (addenda v1-v3)
and today's desk-gate closures. Supersedes nothing; extends
2026-07-25-day-spec's complex addendum.

## Leg A — quantum-esque WEIGHTS (the complex bracket, unchanged fences)

Contestants: G5 {0,±1,±i} 2.32b / G16 polar (4 phases x 4 mags,
no zero) / Q9 quaternion units 3.17b / M4^2 exact-4-bit. Load-
bearing cells (addendum v3): **Q9-vs-D9** (matched 3.17b,
rotation vs channel-pair) and **G5-vs-M5** (matched 2.32b, the
cleanest rotation-vs-magnitude twin). MANDATORY: three-arm
real-DOF fairness (complex 2N-constrained vs scalar 2N vs
unconstrained 2x2-block 4N); modReLU pre-registered (rotation
priors die at real nonlinearities); per-family euler read
(complex pays on trig/exp or nowhere — skeptical prior); complex
stays INTERIOR (real embeddings/head/logits). G16 = the
interference-substitutes-for-zero cell (prediction flipped to
may-survive after the phase-pair objection).

STATUS GATE (from the 07-25 decision gate): the cheap closers
(born-S4 / born-Z1 / born-Z1S, 3080 in flight 2026-07-26 +
m4x gauge pair, Mac in flight) read FIRST — their verdicts set
the zero/symmetry priors the bracket predictions cite.

## Leg B — quantum-esque DATA (the ZX grammar continent)

Desk gates PASSED (RESULTS 2026-07-26): boundary-anchor
serialization (BFS from ordered I/O, randomized internal labels
per sample — augmentation over true gauge, never canonical
sorts), atom set ~10-14 over base-40 (Z(/X(/H-edge/plain-edge/
In/Out/move labels; phases = k*pi/4 spelled with existing
pi / digits — exact integers mod 8). Oracle: pyzx llmopt-side
(zx_engine.py machinery: soundness-by-construction moves,
extract-then-compare boundary oracle). Farm: axiom tranche (see
relay 2026-07-26-0) — C++ ZX rewrite core + serializer + chain
emitter, llmopt adjudicates every row against pyzx (the
dual-oracle pattern, sixth application).

FEDERATION-FLOOR PRE-REG (rides the first ZX birth): first GRAPH
grammar => does the union floor scale with grammar COUNT or
CLASS? W_min re-read on math+ZX vs math+series unions at matched
row mass.

## Leg C — THE COMPOSITION CELL (alphabet-follows-domain, the headline)

The banked hypothesis, finally testable as a 2x2 factorial:

|            | math diet | ZX diet |
|------------|-----------|---------|
| M5 (scalar, 2.32b) | measured-class | cell 3 |
| G5 (rotation, 2.32b) | cell 2 | **cell 4** |

Matched bits, matched real DOF, same width/epochs/seed. PREDICTION
(pre-registered at run time): an INTERACTION term — G5 pays on the
rotation grammar (cell 4 > cell 3) even where it loses or ties on
math (cell 2 <= measured M5). If the interaction is flat, alphabet
does NOT follow domain and the encoding question closes scalar.
This is the program's reason to exist: it tests whether the
substrate should match the domain's symmetry group — the encoding
form of teach-don't-impose.

## Trainer support (Fable code, in progress)

Complex-as-2x2-real-blocks inside the FFN only (interior
constraint): paired hidden channels, forward = genuine complex
multiply; modReLU activation on pair magnitude; G5/G16/Q9/M4^2
STE quantizers in the tournament_birth pattern; the unconstrained
2x2-block control = same pairing WITHOUT the rotation constraint
(prices the constraint per the fairness design).

## Recording

Every cell lands in RESULTS with pre-reg before fire; closers'
verdicts amend Leg A's priors before any birth; per-family euler
reads ride every gate.

## Rung ladder (added 2026-07-26, Artin GO — ranked)

1. **The euler-move diet** (most house-native quantum data): farm
   engine chains where the euler move fires — i appears as a
   WORKING symbol mid-derivation and dies before the answer
   ("meet i where it comes from", continents note, never run).
   Composes with G5: rotation alphabet x the one diet where
   rotation does real work. Candidate cell 4b of the factorial.
2. **Weight-FFT euler read** (free instrument, minutes): do
   trained crystals already carry phase-pair structure in weight
   rows? Sets the bracket's prior honestly BEFORE births.
3. **Annealing over discrete weight lattices**: the untried
   gradient-free route to born-quality lattices; composes with
   the calculated-model thesis (sample init from template
   statistics, anneal in alphabet space). Pre-reg vs a born twin
   at matched alphabet.
4. **Tensor-train RANK as the exchange law's third axis**
   (bits x dimension x rank; llmopt/quantum machinery exists).
5. **RoPE x G5 interaction**: RoPE is the one mainstream complex
   win (exact rotational symmetry in attention); does a
   rotation-alphabet FFN under RoPE'd attention compose or
   double-pay? One-variable rider on the G5 birth.

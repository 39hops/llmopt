# SPEC: the calibrated scorer (B-b program v1) — confirmed by Artin 2026-07-27 pre-dawn

Origin: the pincer night's pivot (R8 FAIL: B-a text-emission
peeler demoted three ways in one night — 11% validity, 9x verify
overhead, zero meets at 5.7x budget) + Artin's keep-going set
(T-sampling-as-free-amplitude-readout / magic / syndrome policy /
regret) + the distance re-read (distance-to-solved is a learnable
field: graded value labels, regret AUC .914, the maps h-function
— NOT weight distance, which stays closed at 8 legs).
Doctrine binds: pre-reg per cell; sqrt(2)-sigma; length-only
control MANDATORY on every ranking read (2026-07-26 lesson);
mass-descending verify order (R0b, adopted); variance measured
before any judge is built (meta-pattern, 5 occurrences).

## Cells (cheap-first; Mac; 3080 never required for the pilot tier)

- **S1 — frontier battery + value cache (FIRST, fired at spec
  write)**: construct the battery the saturated 43-state pool
  could not be — states whose enumerated legal children have
  VALUE VARIANCE (mixed solve/no-solve at budget 150, or graded
  plies spread). Sources: L6/L7 gate roots + stall-endpoint
  states from all pp sidecars. Fork-isolated solves, 6 workers,
  25s walls, streamed. PERSISTENT skeleton-hash value cache
  (data/value_cache.jsonl: norm(expr)+budget -> solved/plies/
  nodes — deterministic, never recompute). Deliverable:
  data/scorer_battery_v1.jsonl + the honest read of whether the
  length-only ranker still wins there (if it does at the
  frontier too, S2's bar is length, not chance).
- **S2 — scorer v1, objective race**: state + enumerated legal
  set -> distribution, one forward pass, zero generation.
  Objectives per pincer amendment 2: (i) imitation soft labels
  (the free 68% replay labels); (ii) DISTANCE labels
  (plies-to-root by engine replay — the Dijkstra head); (iii)
  contrastive verified-vs-rejected siblings. Race (i) v (ii) at
  matched labels; (iii) rides if either plateaus. Machinery:
  syndrome-policy lineage (state->rule 94-98% top-3 measured);
  DAgger lesson binds (balanced diets — round-4 pure-skew
  regression).
- **S3 — R6, Artin's ternary-faceplant bet**: {ternary, M5,
  fp32} scorers, matched arch/labels, scored on RANKING quality
  (Spearman + needle-regime oracle calls), never argmax. The
  alphabet-follows-output-type law's test. Pre-registered bet on
  record: ternary faceplants ("too deterministic").
- **S4 — wave fusion (the correct-the-correction cell)**:
  fwd x scorer product (log-space) vs either wave alone at
  readout; mass-ordered verify riding. Honest framing: Bayes
  evidence fusion, NOT interference (phase stays dead; no signed
  cancellation is claimed). If fusion pays, the wave frame
  graduates to multi-wave readout.
- **S5 — magic rider, variance-gated**: Liouville certificates
  mask the enumerated set BEFORE scoring (zero false positives;
  fires only where integral nodes exist — the frontier, where
  its +1 was measured to live).
- **S6 — regret rider, GATED**: fires only if S4 shows readout
  budget binds. No judge before its variance is measured.
- **S7 — inverse-move enumeration: DELIVERED by axiom
  (2026-07-27, ACCEPTANCE PASS 98% / 12.3ms median v 95%/100ms
  bars; relay -1 reply)**. R8 honest re-run UNBLOCKED; house
  spot-checks 20 edges through the bridge before wiring.
- **S8 — axiom tranche: DELIVERED except the hold** (relay -1):
  fuzz-CI green (pytest bridge fuzzes production verify_edge —
  adopt into our CI); magic r2 dead_mask batch API ~0.4ms/state
  (S5 consumer waits on S2 readout; finding banked: stuck !=
  Liouville-dead, zero deads on both stuck dumps); Fourier probe
  PASS 20/20 — house answer: stay in Q, amplitude-phase is
  READOUT not state; first volume batch approved. knock-4
  stays house-run and [HOLD] until Artin's explicit fire.

## Fences

Battery states exclude-guarded (gate band); cache keyed on
engine-config-relevant budget; every ranking read reports the
length-only arm; cross-substrate numbers labeled context;
alphabet cells matched-arch/labels/device.

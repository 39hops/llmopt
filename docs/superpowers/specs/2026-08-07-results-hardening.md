# Spec 2026-08-07: RESULTS HARDENING — revive, re-gate, archive, and repair provenance (Artin direction; two-agent survey, Fable-verified)

Direction (Artin): time on cleanup and on re-running/revamping old
rungs so they become TRUE nulls or clearly-useful positives —
without losing anything. Nothing deleted or overwritten without a
clear provenance path. RESULTS stays append-only; "archive" means
index tagging, never removal.

Inputs: archaeology survey (pre-doctrine triage vs the six laws
adopted since 07-31) + provenance survey (evidence-chain audit,
ex3-hole template). Both Opus seats, findings verified before entry
here. The 2026-07-31 revival sweep is SUBSUMED: its Tier A closed
3/3 (pooling adopted, graph-mod null REVERSED, rotation lenses
blind) + B5/B6 booked; its seven unrun items fold in below.

## PHASE 0 — record repair (desk, free) [DONE 2026-08-07 evening]

- Propagation breaks fixed: graph-mod BOARD null marked REVERSED;
  symmetry no-spontaneous-commutant scoped WEIGHT-BASIS-only
  (FINDINGS + THEORY, per A3 lens blindness); crown row n=1-births
  fence; revival-sweep BOARD row refreshed.
- SEV-1 provenance holes closed: jobs/*.cmd receipts tracked (44
  files — the arm-invocation record); lean_sample_build.py with
  byte-identity 2/2 (true recipe documented — id-list draw +
  json.dumps re-serialization); ex3_build.py (earlier same day).
- STILL OWED in phase 0 (cheap, next session): results-index
  `status` field (superseded-by / era-closed / revive-candidate —
  index-only); SEV-2 repairs — gravmoe-0801 arm-env transcription
  from the logs' own env dumps, params/plateau arm shell,
  p4_arms sha table pull, r2b_ref small-file tracking, seedslad
  local receipts, artifact_manifest.py for the large untracked
  claim-bearing streams (docs/artifact-shas.jsonl); poly3's sed-generated /tmp probe (a run-time-only artifact behind a booked verdict — commit the probe as a real file at the P4-4 re-run).

## PHASE 1 — hygiene audit (desk, free; GATES phases 3-4)

R10: exclude-union audit of the legacy continents diets (gen-8 /
gen-9) against the 120-gate band. The law fired 21/120 on its
first modern run; legacy diets predate it. Any exposure above the
standing fence poisons paired re-runs — this runs BEFORE any
re-birth. Also: regenerate the three Phase-4 input diets that
predate oracle v3.2 (ZX farm batches, S2 data-leg, pincer R1b
prep) — the old SIGALRM farms may carry the checkpoint-selection
effect.

## PHASE 2 — free-from-disk cells (no training)

- R1 precision-knee n=3: re-apply the Q-grid snap to the three
  same-diet wfloor_d256 births already on disk (~1 desk hour) —
  THEORY row 143's constant gets dispersion.
- R8 crown: n=3 problem-set draws on the deterministic battery, or
  keep the fence written in phase 0 (Artin's pick).

## PHASE 3 — n>=3 seed ladders (pooled only within the
deterministic integer battery; fp-gate cells stay Mac-scoped)

FROZEN LIST (Grok gate, adopted at Phase-3 GO 2026-08-08): the
seven rows below are the WHOLE of Phase 3 — additions require a
new Artin GO, never mid-flight scope growth. HARD GATES on every
re-birth: (1) D2-excised diet rebuild (Phase 1's law — the builder
that refuses contaminated rows), receipts booked per diet; (2)
device-of-origin — each replication runs on the device class its
original claim is scoped to (no silent device drift); (3) pre-reg
per ladder before fire, n>=3 or an explicit single-seed fence.

Order (cheapest per claim): R5 split-law soft-routing (+2 births),
R6 gravity-tax (+3 births, WITH free-run gate readout per the
standing rider), R3 ffn-slack endpoints (6), R4 interaction-law
small-delta cells (6), R2 head cell-sparse (3 births + deletions),
R7 symmetry 4x toll (3 warm epochs), R9 bits-dimension 19M row (6
pooled). Every cell pre-registers n>=3 or carries an explicit
single-seed fence.

## PHASE 4 — the false-null hunt (one training run each, 3080
nightly GO; strictly ordered)

1. PINCER R1a/R8 under IN-DISTRIBUTION prompts (the mechanism is
   already measured: R0-REV-DIST/-B — chain starts are OOD for a
   reverse model; rev 24/120 in-dist). Highest prior false-null in
   the file; unblocks the pincer residue + temporal-pincer.
2. ZX 45M scale-lever (n=1 at +0.8 sigma on a gate class with a
   measured 8-point seed swing; pooled ladder now cheap).
3. SR-BF16 (booked with two named confounds — a confounded null is
   not a null).
4. poly bridge (base-case starvation; per-chain-position share).
5. dithered-snap / judge-collapsed decoding (near-tie population
   under deterministic decode).
6. farmer novelty (temperature/diversity sweep; same OOD family as
   #1, lower priority).

## ARCHIVE-AS-HISTORICAL (index status tags only)

The archaeology survey's list adopts verbatim (graph-mod null ->
superseded-by A2; the four rotation-lens nulls -> scoped-by A3;
zeta-8 -> B6; farmer depth-share -> B5; 113M -> width-floor curve;
streaming v1-v3 -> the 2x2; matryoshka rung 0 -> rungs 1-2;
dispatcher/starved-judge era; GRPO-era cells; ODE VOID probes;
gauge-arm/joint-perm; C6b/C7; native-transformer rung 1). Applied
as `status` fields in results-index during phase 0's remainder.

## CHECKPOINT TRIAGE (Artin GO given; runs beside the phases)

Read-only sha-inventories running on both machines
(scratch/ckpt_inventory.py -> logs/triage/*_inventory.jsonl). Next:
cross-machine dedup + RESULTS-citation classification -> a
keep/dedup/candidate-delete TABLE for Artin review. Nothing deletes
without the table signed off.

## MIGRATION APPENDIX (Opus survey 2026-08-08, hazards Fable-verified)

The gate path needs NOTHING: every Phase-3 driver reaches the
oracle through G.gate_eval -> verify_wave/_gen_isolated, already
fork-isolated, and lab/verify + lab/gen are VERBATIM adoptions of
those exact functions — migrating the gate leg is pure diff noise
against frozen evidence. Calls:
- MIGRATE AT RE-RUN (3): pincer_r1b_labels.py (line 39 calls
  successors() in-process under derivation.py's SIGALRM box —
  the one true checkpoint-selection carrier in the program; new
  fork-per-state + streamed driver with source-identity guard on
  the UNIQUE/AMBIG/MISS logic); poly3_pipeline.sh (inline
  timeout-7200 unforked sympy audit, all-or-nothing assert, PLUS
  a sed-generated /tmp probe = provenance hole — added to the
  Phase-0 SEV list); scorer_s2_data.py -> lab/oracle ONLY if its
  re-run is Mac-side (RSS/jetsam path).
- WRAPPER-ONLY (7): R3, R4, R2 births, R7, R9, ZX 45M,
  judge/dithered + farmer — the p3_umoe_soft pattern (frozen
  driver + D2 excision + REFUSE-IF-EXISTS on checkpoint names).
- LEAVE ALONE (2): SR-BF16 (confounds are arm-design, fixed in
  its pre-reg not its code); ZX farm regen (adjudicate_zx already
  fork-walled; problems are axiom-side — the phase-1 note NARROWS:
  of the three named diets only R1b prep carries the alarm
  hazard).
- CROSS-CUTTING RULE (adopted): every Phase-3 wrapper carries the
  refuse-if-exists checkpoint guard — sym_convert, wfloor_ladder,
  and format_ladder all write SEED-FREE fixed OUT names that
  would clobber cited artifacts (the real Phase-3 risk is name
  collisions, not oracles). Wrappers also emit runlog receipts;
  frozen drivers keep their shell-redirect logging untouched.

## Standing fences for the whole program

DROP_TAIL v2-bound; TRAJ v3 prompt_tail; oracle v3.2 for any
regenerated diet; paired-per-seed full baselines (never quote
unseeded); A1 pooling legal ONLY inside the deterministic integer
battery; verified-AND-distinct at every learning layer; every
re-gate pre-registers before fire.

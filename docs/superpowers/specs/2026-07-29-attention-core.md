# Spec: THE ATTENTION CORE (2026-07-29, morning; Artin GO)

Night-29's law: the crystal is an attention machine — MLP is
slack, the function lives in attention width, and every
compression axis spends one slack pool. Today: (0) the first
adaptive-crystal controller, (1) locate the function INSIDE
the attention block, (2) compress what the anatomy says is
compressible, (3) onboard axiom-on-Mac.

## Leg 0 — TIER-RETRY (desk opener; the controller cell)
On the d56 matryoshka pair (one tensor, dense 57 / cheap 52):
decode every gate row with the CHEAP tier; on oracle-fail,
retry with DENSE. Report solves + effective-params-per-solve
v always-dense and v always-cheap. The oracle-fail signal is
a FREE difficulty probe (no predictor model). READ: retry
recovers ~dense solves at sub-dense mean cost => the dial's
first measured win; retry solves ~= cheap solves => failures
are not tier-limited (they fail both) — book the overlap
census either way. Desk, MPS, zero training.

## Leg 1 — ATTENTION ANATOMY (desk, on the d56 EMA crystal, 63)
a. HEAD CENSUS: zero head h of 4 (all layers; then per-layer
   for any load-bearing head), gate. Uniform drop => heads
   democratic (like holography); one head carries => an
   oligarchy INSIDE attention (echoes NNUE readout finding).
b. RANK READ: SVD of qkv/o v gate weights — singular-value
   decay + truncation gates (rank in {48,32,24,16}). Attention
   low-rank at parity => width cliff is about HEAD GEOMETRY,
   not rank; rank-fragile => d IS effective rank (the cliff
   explained).
c. WHERE SNAP BITES: rational snap Q=16 (below the knee) on
   attention-only v gate-only v both — allocation-of-accuracy
   read for the bits-portfolio riff.
Pre-reg each sub-cell before its first gate.

## Leg 2 — ATTENTION COMPRESSION (train, Mac, pick <=2 arms
informed by Leg 1)
Candidates: heads 4->2 at d56/f48 (head_dim 28, even — legal);
low-rank factorized qkv birth (rank from 1b); C8 commutant on
qkv (sharing finally aimed at the load-bearing block — priced
by the ladder toll curve or refuting its gate-layer scope).
Paired v the d56/f48 comparator, same seed/device.

## Leg 3 — AXIOM ONBOARDING (Mac co-location, first contact)
Relay doc: project moved PCs; current state (E2 closed 6.2e-6,
E3 armed, battery delivered); the minimal-crystal + attention-
core specs; compute etiquette (CPU headroom, artifacts to
their data/llmopt/). Delivery via Fable-5 subagent to
~/code/axiom (Artin-sanctioned, iff Fable 5); design changes
requested via relay tomorrow.

## Queue behind (unchanged)
Snap-at-knee on d56/f48; Leg B call-spans; Leg C diet descent;
polar-split snap on cplx crystal; difficulty-gated tier policy
(predictive version, must beat Leg 0's retry baseline).

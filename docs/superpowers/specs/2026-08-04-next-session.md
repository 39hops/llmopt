# Next-session spec — 2026-08-04 (post-R6, the coalition program)

Written after VERDICT MOE-GT-1-R6 landed (coalition confirmed:
random-45% = 0/120 twice, anti = 0/120, demand-chosen 45.3% = +14.7
over full, 6/6 seeds, R5 fully registered). Sequence agreed with
Artin: spec -> consolidate -> compact -> execute.

## The reviewed foundation (what the next rungs may lean on)

- F1 THE CREST: top-45.3% math-demand mask BEATS full (+14.7 pooled,
  6/6 seeds; R4/R5, reviewer byte-verified). R6: the effect is 100%
  the coalition — random/anti masks at the same fraction are DEAD.
- F2 heavy tail (74.9% mass in top-25% experts, ARM0/1).
- F3 decode-demand is template-predictable (LOO 0.870 vs 0.755,
  survives the prefix-artifact amendment); "universal phase program"
  interpretation RETRACTED (GT1-TRAJ-CORR) — decode-only analyses or
  nothing, always.
- Entropy DILATES through decode (100% of prompts) — no
  commitment trigger exists; blackhole entropy-trigger refuted in
  the ledger.
- Instrument fences live: sympy gate solves are NOT device-free
  (seeds-ladder finding); TRAJ rows lack router scores; probe rows
  lack "ok".

## Rung 1 — THE DOMAIN PROGRAM (spearhead; Mac; ~1 session)

The R6-validated premise: a domain's demand log defines a coalition
worth +14.7. Test whether OTHER domains have their own coalitions
and crests.
(a) PHYSICS arm-0: mechanics corpus (llmopt/mathgen/mechanics.py),
    DECODE-ONLY demand log. Readout 1: keep-set Jaccard vs the math
    coalition per layer (killer from the bank: ~1.0 = router is not
    domain-biased, program dies honestly).
(b) CODE arm-0: toolchain-oracle corpus (llmopt/codegen/llvm.py +
    oracle.py — assemble-and-run scoring per house doctrine).
    Maximally distant domain; same Jaccard readout FIRST (if ~1.0,
    stop, no gate runs).
(c) IF coalitions differ: pre-reg the second crest — domain-X
    keep-set at 45.3% on domain-X gate, paired vs full, n>=3 seeds,
    +7 pooled bar (the R5 recipe verbatim). CROSS-ARMS mandatory:
    math mask on the code gate + vice versa (predicted
    catastrophic; that prediction is the mechanism's sharpest
    falsifier).
Pre-flight: physics/code corpora must produce >= the mathgen gate's
determinable-row standards; audit for determinability first (data
hygiene doctrine).

## Rung 2 — the PLACE-1 control + prefetch cell (Mac desk + one sim)

scratch/place1_gravity.py (co-routing predictor + LRU cache sim,
bytes-moved metric) re-pointed at GT-1 DECODE-ONLY traj rows:
(a) discharges the bandwidth-matched control GT1-TRAJ-CORR owes
    phase-prefetch (swap traffic counted);
(b) tests whether conditional co-routing prefetch beats popularity
    on this vehicle (PLACE-1's +5-point class bar, pre-registered).
The spec-decode cost-ratio law transports here (miss = multi-MB
fetch, predict = dot product — economics finally satisfied); if (b)
fires, the V4 from-disk deployment inherits a measured prefetch
policy.

## Rung 3 — ROUTING-BITS ledger (desk, zero GPU)

Bits/slot of the decode routing stream under {uniform, per-layer
marginal, LOO decode template, prev-layer-conditional}. Prices what
the router DECIDES; replaces the entropy license's ad-hoc threshold.
Killers pre-registered in the bank (2026-08-04 entry). Instrument
edit if margins wanted: record router scores in TRAJ rows (one
line, moe_gt1.py:106-109).

## Rung 4 — CREST AS DATA GENERATOR (Mac, one afternoon)

Sequence-generation form ONLY (dissociation fence: no logit-KD from
a degenerate distribution). Crest vs full at matched budget ->
oracle filter -> verified-AND-distinct yield comparison. Yield bar,
not gate bar. Existing pipeline: llmopt/distill/sequence_kd.py +
the diet machinery.

## Hygiene / small debts (do opportunistically)

- ENTROPY-LICENSE-1: phase-permutation null + decode-only recount
  (owed by GT1-TRAJ-CORR item 4).
- Crest-nesting check: keep(0.453) subset of keep(0.50) — one
  set-intersection, kills a license revival if it holds.
- GRAVMOE 1/8-vs-2/8 attribution: diff the 8 generated strings from
  logs/seedslad/REG17.log vs the Mac anchor / re-score serially on
  one machine (fp-near-tie vs timebox discriminator).
- CHURN JUDGE: banked, needs its SPEND registered before it runs
  (regret-null lesson).
- Head-deletion census on the crest-masked model (reconciles
  HEAD-TENSION's "deletion always catastrophic" with the crest, by
  granularity).
- Weight-reader within-model transfer check (layers 0-23 -> 24-47
  keep-set prediction) — one afternoon, kills or opens the
  zero-inference keep-set chain.
- scripts/INDEX.md: consider a pointer line for the GT-1 scratch
  instruments (reviewer note).

## Axiom relay thread (Artin carries)

- Relay 2026-08-04-0 (first light) is written and committed —
  deliver when convenient.
- OWED NEXT RELAY: (i) the 57-row question (all two-fn-atom rows
  unemitted — axiom-side skip bucket not covered by the fence
  counter; is that eligibility or a bug?); (ii) Lean corpus v2 with
  REAL division rows — the house generator's rational rows
  self-cancelled at sympy construction (evaluate=False needed), so
  the field_simp/hypothesis tier is UNTESTED; also fix ac_equal's
  hypothesis-regex landmine (fails closed on multi-hyp rows) before
  v2 lands.

## State at spec time

- 3080: idle, open until ~5 PM EST per Artin. Mac: Fable's.
- rjobs: all done (gt1_r4/r5/r6 complete + booked). WSL: lean job
  complete; mathlib cached at scratch/leancheck (~5-6 GB on the
  box, keep — reruns are 44 s).
- Ledger: 701 index entries, needs_link 0. Tests last green: 451 at
  the F1e commit (rerun owed after any llmopt/ edit; none today —
  all changes were scratch/ + docs).
- Session compaction next: resume = this spec + BOARD evening block
  + handoff 2026-08-04-0 + RESULTS tail from MOE-GT-1-R4 onward.

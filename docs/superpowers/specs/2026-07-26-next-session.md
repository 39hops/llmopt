# Next-session plan (written 2026-07-26 close; the post-compact resume artifact)

Per Artin's context-hygiene design: after any compact/clear, read
(1) the RESULTS.md tail (last ~10 entries), (2) THIS FILE, (3)
handoff 2026-07-26-0. That is the whole resume. Reviewer is
RETIRED (house self-review); respawn only on Artin's word.

## Tier 0 — overnight pickups (book FIRST, minutes)

1. **Retention session verdicts** (logs/v5_s2_retention.log, WSL):
   (a) s2 resolution vs the 6/100 PRE; (b) per-level [retention]
   columns -> fit decay/hold constants (pre-reg: isolated-clade
   columns decay first); (c) surprise-gating engagement (if
   multiplier ~1.0 in its OWN home regime, book untested-nowhere
   and redesign or drop).
2. **Z1S hot-LR gate** (logs/tourn_Z1S_hot_gate.log): loss < ~1.0
   or gate > 0 => dose/LR was the binder (6ep cell fires next);
   else the excitation capability wall books at any recipe.
3. Sync both checkouts; drop any stash; verify by hash.

## Tier 1 — the ranked queue (Mac unless noted)

1. **STREAMING-OPTIMIZER CELL (Muon-class)** — the best new idea
   on the board: homogeneity -12 is a gradient-covariance wound;
   orthogonalized updates = diversity-per-step in the optimizer.
   d256, 1 pass, Muon-style update, vs v4's 57 and control 65.
   If it closes the -5: "the epoch is load-bearing" retracts to
   "SGD wastes single passes" — publishable revision.
2. **Weight-FFT euler read** (free, minutes): phase-pair structure
   in existing crystals — sets the complex bracket's prior.
3. **THE FACTORIAL, math column**: cplx_none (control) + G5 births
   at 19M/gen-4/3ep (trainer READY: scratch/complex_{model,
   birth}.py, gate_cplx.py). Pre-reg per-family euler reads
   (trig/exp or nowhere). M5=62 and fp32=64 are the comparators.
4. **ZX farm batch** (axiom ask when Artin relays: ~100k rows,
   ration caps) -> **vocab-51 ZX birth** (VOCAB_EXTRA per the
   frozen atom order; federation-floor pre-reg: count vs class)
   -> the factorial's ZX column (the alphabet-follows-domain
   interaction = the program's headline).
5. **Joint-perm distance closure** (banked, kill condition set);
   U(n) variant once complex births exist.
6. **Template-refresh mid-stream** (the "something missing" cell):
   recompute corpus statistics from eaten rows mid-stream, correct
   the crystal toward them directly, gradients handle residual.
   Composes with the annealing rung. Never run anywhere.

## Tier 2 — conditional / far

- Z1S 6ep (only if hot-LR shows movement); per-BLOCK sign (the
  granularity ladder's missing middle rung, only if a decision
  needs it); born-m4x gauge pair (2 births, banked).
- QUANTUM LLMUE (north star, ~3 sessions): ZX diet x G5 substrate
  x metabolic sessions w/ flip census. Every component measured
  except the composition.
- Wave-as-superposition speed leg: batched-KV + EU delta-scoring
  (the speed track's standing headline; quantum-parallel step
  elimination made classical). Interference-between-candidates =
  far-future line (G16 at inference).
- G16 born arm: re-priced as interference-substitutes-for-SIGN
  (post-Z1); rides the factorial infrastructure.

## Deferred flags (fine to wait, do not lose)

- Repo taxonomy surgery + rarity re-baseline (4 incompatible
  censuses; needs a freeze point).
- BOARD deep refresh beyond today's rows; THEORY rows for: the
  interaction law (batch diversity), opposition-is-the-primitive
  (sign ladder 54/0/0), the bounded zero law.
- The 677 above-cap lcomp/pivot rows: adjudicator v3 (structural
  lcomp/pivot) before farm scale.
- practice_2-6 exercises for Artin still open.

## Standing fences (inherited, non-negotiable)

sqrt(2)-sigma rule (>=5 single-seed at 19M/cuda; >=3 at d256);
cross-device forbidden; --fast never without --nopack; pre-reg
before fire; controls ride every instrument; verified AND
distinct at every layer; strict encode everywhere.

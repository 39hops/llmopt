# Revival sweep — old verdicts re-priced by the 07-31 laws

Provenance: Artin's standing "old-null revival" call + reviewer
sweep 2026-07-31 (read-only Opus agent; self-reported as Opus 4.5
under an Opus 5 harness label — noted for the record; all 17 quoted
headings and the load-bearing substance verified line-by-line by
Fable before adoption). Levers: clock-placement law, resolution
law, merge-free recipe, recursion-depth diet-share, determinism
ladder (device->lab->runtime).

## Tier A — desk cells, zero training, run first

1. **DETERMINISTIC GATE POOLING** (house). The resolution law
   demands n>=3 seeds; cross-device pooling is forbidden by drift
   doctrine; P3 integer decode removes the drift (measured: cuda
   ckpt gated 49/120 bit-identically on Mac). Cell: run one gate
   battery through the integer path on BOTH machines, assert
   identical streams; on pass, ADOPT pooled cross-device seed
   ladders (doubles seed throughput for every Tier-C item below).
2. **GRAPH-MODULARITY SIGMA** (RESULTS L5202). The +0.030 dQ null
   is bar-based with NO measured dispersion; the entry names its
   own free sigma: run the instrument on wfloor_d256{,_s2,_s3}
   already on disk, then re-adjudicate.
3. **ROTATION-INSTRUMENT POSITIVE CONTROL** (L6098/5967/8665).
   Every spontaneous-rotation null probed crystals whose diets
   never forced periodic computation. Run weight-FFT euler +
   commutant instruments on fourier2b_widemod.pt (known 276/512
   activation clock): if the WEIGHT-side instruments read null on
   a substrate with a confirmed clock, the instruments were the
   wound; if they fire, the old nulls were diet statements (the
   clock-placement reading) and both interpretations book.

## Tier B — one-birth cells (Mac-scale)

4. **poly3 ibridge re-spell, chain-position share** (L3831). The
   bridge-law test died at ibridge 1/9 — textbook base-case
   starvation. Rebuild with share per chain position; the bridge
   law finally gets its test.
5. **FARMER depth-uniform corpus** (L10091). The reverse corpus
   had chain STARTS only as targets, never prompts — one end of
   the recursion starved by construction. Rebuild with every chain
   position as prompt at equal share, same d64/3ep; predict novel
   yield moves off 11/1000 without scale.
6. **G9 zeta-8 ON THE MOD DIET** (L7590/7740). The declared
   reopening waited on "a different lever (diet ...)" — the
   clock-placement law IS that lever. Fire the Z[zeta8] STE arm on
   the FOURIER-2b substrate, the one diet where the target
   computation is provably rotational. Completes the causal
   square: diet-forced clocks exist; does architecture-provided
   rotation get ADOPTED where the diet wants it?

## Tier C — n>=3 seed ladders (post gate-pooling adoption)

7. **45M ZX re-ask, n=3** (L7740): the "capacity was never the ZX
   constraint" claim is n=1 at +0.8 sigma; the continent's plan
   hangs off it.
8. **poly2 transfer, n=3 paired + depth-share assemble/padd rows**
   (L3699): both the +2 and its disappearance were sub-sigma.
9. **113M capacity re-ask** (L1961): 0.15-point "law" (~0.03
   sigma); pair with the thicker-diet condition the entry set.
   EXPENSIVE — hold for a nightly GO.

## Tier D — recipe/production (gated)

10. **Hebbian finetune on a production MoE** (L11107): N3's own
    escape clause ("alive only at birth") is what the merge-free
    recipe supplies. Apply co-routing relaxation as a FINETUNE on
    OLMoE, re-try the top-MI pair merge (+3.4 ppl at baseline) —
    if the cost drops toward 0, the recipe scales beyond births.
    Gated: lambda-sweep verdict first.
11. **Exact flip census re-runs** (L7726, L8146): dithered-snap
    (2-v-3 flips) and judge-collapsed decoding (18=18 on n=30)
    both under-resolved; re-run under deterministic integer decode
    on the near-tie population the instruments were built for.
12. Held: pincer ply-depth diet (L6717), chain-carry full-budget
    n=3 (L3336), shaped-GRPO plies-potential (L3254),
    joint-permutation cell w/ integer distance (L5784).

## Order of operations

A1 -> A2/A3 (parallel, both desks) -> pick Tier B by Artin's
interest (house lean: 6 then 5) -> C after pooling adopted.
Every cell pre-registers in RESULTS with an n>=3 plan or an
explicit single-seed fence per the resolution law.

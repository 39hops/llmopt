# Spec: THE ESCALATION ENGINE (2026-07-30; drafted 07-29 night)

The week built the parts; this spec assembles them. Standing
laws in hand: one slack pool for STRUCTURE (location-priced),
sigma-priced BITS (width-priced), free HALF-tier, oracle-fail
as free difficulty signal, diet SATURATED at half dose.

## Cell 1 — THE ESCALATION POLICY (desk, Mac, opener)
3-rung tier-retry on matryoshka_d56_3tier.pt (57/57/48):
eighth -> half -> dense per row, escalate on oracle-fail.
Report solves + effective-params + census (which rows each
rung uniquely solves). PREDICTION (from rung-2): policy ~57
at effective params well under half-tier alone, because half
is free and eighth catches the easy mass. If the policy
BEATS 57 (union effects like rung-1's +2), the retry ladder
is a capability lever, not just a cost lever.

## Cell 2 — LEG C: THE MARGINAL-VALUE LADDER (3080 night)
Saturation coefficient in hand (2nd half of corpus ~ free at
d64). Now resolve it BY LEVEL: births at {25%, 50%, 100%}
dose with the cut applied PER-LEVEL bucket (drop only L3
rows, only L5, ...) -> d(gate)/d(log rows) per level. The
allocation answer: which levels the next farm tranche should
buy. 6-8 births, cuda line, EMA on.

## Cell 3 — THE HEAD-TENSION CELL (Mac, one birth + desk)
Michel/Voita find prunable heads; we find none. Frame:
theirs are over-provisioned, ours at floor. Test: born-8-heads
d64 (head_dim 8) same diet -> census. Some heads prunable =>
provisioning frame confirmed (head slack = head count above
task's relation count ~4); all 8 essential => the diet
recruits every channel offered — relations scale with
provision (would need booking as its own law).

## Cell 4 — POLAR-SPLIT SNAP (desk, sigma-informed)
cplx crystal: quantize |w| coarse x arg(w) fine v uniform at
matched bits (rotational-snap R2 measured the knee ANGULAR).
Sigma law rider: express both grids in sigma units.

## Cell 5 — AXIOM LOOP (when tranche lands)
Step-local spans -> rerun paired arms (same 47-atom vocab,
same split). Use-v-ignore dissociates; if SPAN wins, scale to
a full tranche and the d64-class LLMUE decode-side resolver.

## Order + budget
1 (desk) -> 3 birth queued behind 4 (desk) on Mac; 2 = next
3080 night battery; 5 event-driven. All pre-reg per cell.

## Cell 6 — THE FARMER PROBE (folded in 07-29 night; Artin's
## reverse-self-learner riff)
Small reverse model (nxt->cur, gen-4, d64-class) -> sample
predecessors from gate-band answers -> verify by forward rule
application (ms) -> score verified-distinct-NOVEL yield/1000 v
sympy-farm yield/sec. High yield = the self-farming loop is
live: reverse learns, forward eats (two models per the
backward-tax law; hardness-targeted per the saturation law).
Runs after cells 1/4 on Mac or as 3080 night second half.

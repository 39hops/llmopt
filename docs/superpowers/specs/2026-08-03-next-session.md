# Next-session spec (written pre-compact, 2026-08-03 evening)

## IN FLIGHT when this was written
- rjob `f2c_sweep` (Mac): PRE-REG V4-F2c K-sweep, arms 16(done,
  recall 0.0888, third distinct attractor ": 1) the three...") /
  24(decoding, tok/s decaying 0.056->0.035 — memory tax) / 32(queued,
  registered to hit the wall). ON RESUME: `RJOB_LOCAL=1 python
  scripts/rjob.py status`; when DONE, book VERDICT V4-F2c against the
  pre-reg's three predictions from logs/opus/v4_f1d.jsonl rows 8-10 +
  jobs/f2c_sweep.log. Prediction 2 may fire EARLY (wall between K=16
  and 24 on the fp8 base) — book what measured, per-arm texts verbatim.

## LEAN CERTIFICATE TIER (axiom collaboration, hot)
- Relays: 2026-08-03-0 (ask), -1 (GO + predicate). Axiom DELIVERED the
  emitter (their c0511bc: print_lean, atom tables, equiv +
  equiv_mod_const, lexical fractional-pow fence — parse-time-merge
  finding booked as rider house-side).
- House checker WRITTEN: scratch/lean_check.py (statement-diff
  anti-theater + kernel batch + cost verdict). NOT yet run: mathlib
  does NOT fit the Mac disk (filled it; purged). PLAN: run the checker
  on the 3080/WSL (C: ~259 GB) — elan + lake project scratch/leancheck/
  ship via git; needs Artin's GO for a 3080 session per doctrine.
- Owed house measurements: fenced fraction on OUR corpus (run axiom
  harness with sidecar flag over battery rows), live-merge grep.

## DISK LEDGER (Mac), decisions made today
- Deleted: blackhole_q3_parts 21 GB (regenerable), DeepSeek-V3
  fragment 4 GB (stale), partial mathlib. Kept: OLMoE 13 GB (Q1
  backup — Artin's call), v4flash_f1 cache ~24 GB (working asset).
  ~8 GB free at write time; K=32 fetch ~4.3 GB fits.

## QUEUE (all pre-reg-gated)
prompt-lookup spec decode (volume lever; echo loop accepts ~all
drafts); Markov drafter rung behind it; VQ/B-tree codebook cell
(RIFF-banked, killers attached); from-disk keep-set bake-off vs F2a/b
demand ground truth; rung 13; H1; tilelang leg (needs py3.11).

## THE DAY (for orientation): see handoffs 2026-08-03-0/-1/-2,
BOARD Monday block, docs/opus/F1-DEMO.md. 20+ entries booked;
F1 demo 0.268 tok/s; F2 recall/closed-loop findings; Lean tier opened.

# Spec: THE SYMMETRY LADDER (2026-07-28, Artin GO "let's start choosing symmetries")

**STATUS: COMPLETE same day (S1-S4 all booked, RESULTS 07-28).
Toll 2x:-1, 4x:-4, 8x:-6; S2 exact at 65. Banked follow-ups
live in RIFF-LEDGER (C16, packed conv forward,
symmetry-at-birth).**

Sequel to the rotational snap program (R1-R3 COMPLETE, RESULTS
2026-07-28: no spontaneous structure; projection -8; conversion
ACHIEVED 64/120 @ anti-mass 2e-4; SGD accepts the imposed
symmetry — the commutant is locally stable). Machinery
generalizes: for symmetry group G with linear commutant
projection P_G, run (1) anti-mass read, (2) projection damage,
(3) heal (1 warm epoch, ramped penalty), (4) book gate +
residual anti-mass + IMPLIED PARAM COMPRESSION.

FRAME (the leverage): each symmetry is structured weight
SHARING — a third compression axis orthogonal to bits and
dimensions. Commutant dimension counts free params: complex 1/2
(done), quaternion 1/4, circulant ~1/n. The bits-dimension
exchange law gains a symmetry axis if the ladder heals.

## Rungs, in order

S1 QUATERNIONIC (recommended next; Mac, ~1 day):
  4-channel grouping, structures I,J,K (anticommuting, IJ=K);
  P(W) = (W - IWI - JWJ - KWK)/4; params/4 per gate matrix.
  Cells: anti-mass read (prediction: 0.75 = fully generic, the
  R1 null at the quaternion group); projection gate (damage
  from deleting 75% of mass — R2's holography stress test);
  heal arms lambda=0 and ramped (R3 recipe verbatim; bar:
  gate >= 61 at anti-mass < 0.05 = conversion at 4x sharing).
  Reuse scratch/rot_commutant.py / rot_convert.py patterns —
  J_perm generalizes to three block matrices on 4-tuples.

S2 COMPLEXIFICATION CONTROL (cheap, answers Artin's exactness
  question empirically): double gate-matrix width, W_2x = W (+) W
  (block-diag commutes with J by construction) — same function,
  rotational by THEOREM. Gate must read exactly 65 (any delta =
  instrument bug). Books the trade table: exact-at-2x vs
  healed-at-1x-minus-1.

S3 CIRCULANT / CYCLIC C_n (the audacious rung): group-average
  over channel shifts (n = 8 or 16 blocks first, not full n) —
  dense -> convolution-structured. Expect real damage; the
  healing curve is the result either way. If it heals: dense
  layers of a trained crystal can be RETROFITTED into conv
  structure = massive sharing.

S4 Z2 BLOCK (sign involution S; (W + SWS)/2): the cheap sanity
  rung; slot anywhere.

## Standing fences
Gates-only symmetry first (attention/up/down later rungs);
d256/MPS lineage, wfloor comparator 65, seed 1; pre-reg per
rung before any run; anti-mass instrument validated per group
against a synthetic commutant member (the R1 synthetic-control
pattern — house cplx arms are NOT valid controls, booked
07-28). Param-compression claims stay IMPLIED (no packed
format built) until a rung requests one.

## Also queued (non-symmetry, from the GO day)
- Atlas verdict books when the 3080 marker fires (waiter armed).
- E3 fires on axiom's AXNN v1.1 loader (E2 delivered, relay
  -28-4); Laplace draft (-28-3) awaiting Artin's send.
- Banked from R3: param-halving format, U(n) gauge leg,
  G5-on-rotational-substrate.

# Metabolic v5 — the complete practice loop (spec, 2026-07-23)

The v4 verdict pair defines v5: persistence = STRUCTURE (proven),
conversion needs a teacher (self-practice +1/12). v5 = v4 machinery
+ the exchange as a LIVE food channel + the precision race.

## Arms & machinery
- Substrate: crown-tier ternary latent (post-exchange-test checkpoint
  if it passes), late-layer plasticity, LR 1e-5, absolute anchor +
  proxy rollback (v4 stack unchanged).
- FOOD = three channels, logged per-row: (1) stuck-worklist rollouts
  from cur (v4); (2) ENGINE DEMONSTRATION CHAINS at our walls
  (stuck_chains_p1 now; new walls logged in axiom's exchange format
  and relayed each session — the loop closes); (3) fresh L6-9.
- PRECISION RACE (paired, equal food): fp32 / fp64-masters / SR-fp32
  / INT8-EXACT (Ozaki sliced optimizer arithmetic — carries
  pre-registered disagreement #2: Artin predicts capability delta
  over fp64-masters, house predicts tie-on-capability/win-on-wall)
  (stochastic rounding on the update commit) / [async-CPU masters on
  Mac = co-processor rung 3 pilot]. Primary: committed flips +
  paired resolution; secondary: proxy, census symmetry.
- Miner v2 rides along: bank verified steps from UNSOLVED attempts
  (fixes the 18-row famine; feeds the gen-9 solved-only-leak A/B).
- Worklist policy: deeper-before-wider (p2's mid-derivation states
  join; band closes only when the stuck queue empties).

## Pre-registrations (booked here, before any run)
(a) CONVERSION: on walls WITH demonstration chains, paired
    resolution delta > v4's +1/12; walls without chains stay ~flat
    (the teacher requirement, tested within one session).
(b) SR SUFFICIENCY: if SR's flip count lands within 2x of
    fp64-masters at equal food, SR wins on cost and the masters
    apparatus retires from the online loop.
(c) PERSISTENCE: flip-backs stay <30% (structure bar) in every arm.

## Order
Exchange test (3080 window) -> gen-8 verdicts -> v5 session 1.

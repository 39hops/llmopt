# Next-session spec: the scaffold program (2026-07-31)

Standing constraint: Mac free after the overnight seed-2 pair;
3080 ONLY on Artin's explicit GO. Paper stays BANKED.

## Rung 0 — adjudicate (first thing, desk)
Read logs/umoe3_s2_0730.log. The scaffold hypothesis lives or
dies here: channel-s2 and gravmoe-s2 v the s2 family (lb 44,
free 40, tied 42, dense 40). If 48/52-class gates replicate ->
rungs 1-3. If they collapse into the family band -> book the
seed-fortune null, keep gravmoe only if IT survives, and fall
through to rung 4.

## Rung 1 — params-matched control (before any combo)
lb with ffn_e widened to match channel (1.00M) and tree
(1.52M) params exactly. If widened-lb reaches 48, the scaffold
reading dies for channel/tree (capacity, not structure) —
gravmoe (0.93M) is unconfounded either way.

## Rung 2 — Artin's combo arms (pre-reg each)
(a) tree-edged gravity: gravmoe attraction restricted to tree
edges (siblings strongly, cousins weakly/never) — gravity WITH
topology; (b) branching channel: shared bases per tree edge,
experts talk only along paths. Both against the rung-1 control.

## Rung 3 — gravmoe lambda-sweep
lambda_g in {0.1, 0.25, 0.5 (measured), 1.0}: is there an
optimum between split (lambda 0 = lb 37/44) and full collapse
(0.5 -> 52)? The interesting cell: partial collapse with
selective structure.

## Rung 4 — FOURIER-2b (Mac, no GPU needed)
Wide Mod/gcd generator (stable string seeds, exclude=-guarded,
20k+ rows, per data-hygiene doctrine) -> birth -> zeta^k probe.
The memorization control: farmer-style novel-input check
BEFORE the Fourier regression.

## Rung 5 — held riffs (run only on explicit pick)
Consistent-corruption DK arm; mass candidates (router-margin
confirm; sub-expert cell census; MI-graph metric); old-null
revival sweep (results_query nulls by date, oldest first);
HCE/NNUE-experts shelf v2 (spec only, no runs).

Resume path: BOARD -> handoff 2026-07-30-0 (add.3) -> this
spec -> RESULTS tail.

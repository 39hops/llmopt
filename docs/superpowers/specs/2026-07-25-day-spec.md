# Day spec 2026-07-25: sigma-recal + determinability + speed (Artin GO, both machines)

Merged from: the birth-machine plot twist (HOLD on gen-10),
reviewer connections #2/#3/#5 (its #1 KILLED by the ladder device
audit — all width-curve points born MPS; stay critical of the
reviewer), Artin's data-volume question, and the speed re-rank.

## T1 — the bf16/device decomposition (IN FLIGHT, books first)

Cells: 19M-cuda-fp32 (3080, DECIDER) + 19M-MPS-seed-2 (Mac,
sigma leg). PRE-REG: fp32-cuda ~70 => width-dependent bf16 debit
books (doctrine: no --fast below ~d512; bf16 stays legal wide);
~56 => device-numerics effect books (doctrine: never compare
cross-birth-device, ever); between => both, decomposed by the
sigma grid. Gen-10 HOLD lifts per whichever reads. The --fast
bundle carries token-budget packing too (8,426 vs 37,498
steps/ep) — if fp32-cuda ~70, a TF32+packing cell later splits
precision from packing (banked, not today).

## T2 — THE SIGMA-RECAL GRID (connection #2; prices every marginal verdict)

n=3 seeds per cell, gen9-diet-B unless noted:
- 19M-MPS: s1=70 (arm B), s2 IN FLIGHT, s3 queued behind it.
- 19M-cuda-fp32: s1 IN FLIGHT, s2/s3 queued behind it (3080).
- d256-MPS gen-4 (down-width sigma, 10-min births): s1=65
  (wfloor), +2 seeds behind the Mac chain.
PRE-REG: sigma_gate <= 2 per cell (the historical assumption);
any cell with sigma > 3 re-fences EVERY verdict at that
width/device whose margin < sigma (the week's list: gen-9 +3,
poly5 -2, prefix -3, union -4). Report sigma per cell, never
pooled. All gates same-device-as-birth EXCEPT the cross-gate
pairs already banked (arm B on cuda) which stay labeled.

## T3 — KNOCK-4, HOUSE EDITION (connection #3; the determinability lever)

Per the standing pre-reg (RESULTS 2026-07-24, LAST knock,
HARD-STOP): re-spell ibridge as pure COPY — residue coefficients
pre-computed in explicit pmul/psub precursor rows (sympy apart()
on poly_chain5 rows, in-house, no axiom relay); gen-9 RATIONED
recipe; door-only bar ibridge >= 12/15; no scoring changes. Door
opens => bridge bar becomes testable; shut => CLOSE the
bridge-at-the-gate file. Build = Fable (~1-2h re-spell script +
shard audit), birth on Mac behind the sigma chain.

## T4 — STREAMING-BIRTH A/B (speed headline #1)

One-pass surprise-gated birth vs 3-epoch twin, 19M gen-4, Mac or
3080 (whichever frees first). Doubles as a data-efficiency read
(does surprise-gating skip mastered rows => fewer effective
tokens for the same gate?). Bars from the evening-queue spec.

## Free riders (CPU, anytime)

- Hygiene audit (connection #5): scan all standing diet jsonls
  for identity rewrites + known-underdetermined kinds; report
  counts per diet. Script only, no bookings without cross-check.
- PTQ bit-ladder at 19M: int2 + ternary rounding on the infix
  twin (minutes; extends int3=65). Question: is the discrete
  lattice better reached by training (born-ternary 60) or
  rounding? Prices whether discrete births are necessary.

## MOVED TO BANKED (with cause)

- Matched-N depth ladder — behind sigma-recal (n=1 depth reads
  are exactly what sigma makes trustworthy; run it n=3 next).
- Batched-KV recompose — behind streaming-birth (same wall).
- Curriculum-order A/B — needs the fixed-order infrastructure
  (pre-shuffled diet files); banked with cause, high interest.
- Corpus-scaling arm (moves W* with data volume) — banked.
- TF32+packing split cell — banked unless T1 reads ambiguous.
- Gen-9 full-dose rerun + v5 s2 — unchanged, next 3080 window
  after today's cells.

## Machine allocation

3080: fp32 decider -> sigma_cuda s2/s3 -> (free: streaming or
PTQ). Mac: MPS s2 -> s3 -> d256 sigma x2 -> knock-4 birth.
Axiom-gated: NOTHING today (knock-4 built in-house).

## ADDENDUM (midday): the complex bracket — spec'd, ranked 5th

Riff banked (RIFF-LEDGER 2026-07-25, Artin). Reviewer red-team
ADOPTED IN FULL:
- Scalar 4-bit PTQ arms (p4/lm16z/nf4) ride today's race chain —
  LABELED storage-ladder points, NOT tournament births
  (lens-not-attractor: post-hoc snap =/= born-in-lattice).
- Complex BIRTHS rank 5th (behind sigma-recal, gen-10 close,
  knock-4 house, streaming A/B) — starved-judge: no measured
  variance in rotation alphabets yet.
- MANDATORY CONTROLS written into the bracket: (1) unconstrained
  2x2-block real net at matched params/bits — else M4^2-vs-int4
  just re-measures the dimension dividend (2 dims x 2 bits vs
  1 dim x 4 bits IS the exchange law); (2) per-family euler read
  (Trabelsi: complex pays where data carries phase — trig/exp
  families or nowhere); (3) D9/Q9 count as rotation alphabets
  ONLY with genuine complex multiplication in the forward pass.
- Two-of-three law books as silence-is-structure FORMALIZED
  (theorem, not new physics); the real open cell = zero+symmetry
  vs zero+bits at MATCHED bits.
- Skeptical pre-reg (lineage: Trabelsi/Parcollet/RoPE): neutral
  at gate, positive on euler families only, param-efficiency not
  capability. Measure per-family or don't run.

## ADDENDUM v2 (reviewer second pass + model self-report)

- REVIEWER IDENTITY CORRECTED: claude-opus-4-8[1m] (self-report
  2026-07-25) — not Opus 5 (doesn't exist in the spawner), not
  Fable. Independence is REAL (different model family); the 1M
  context is what holds the full corpus. Memory labels fixed.
- G16 PROMOTED: it is the sharpest zero-test we have — binary
  confounded no-zero with no-resolution (one-dot); G16 has 4
  magnitudes + rotation but its smallest weight is 1/2
  (small-but-never-OFF). G16 craters => silence-is-structure
  confirmed independent of resolution; G16 lives => binary died
  of resolution, the law needs revision. Highest-value arm.
- THREE-ARM fairness design (mandatory): complex (2N real,
  rotation-constrained) vs scalar (2N real) vs unconstrained
  2x2-block (4N real); match REAL param count (capacity laws are
  denominated in real params). complex > scalar-2N iff rotation
  is the right bias; the gap to unconstrained-block prices the
  constraint.
- DECISION GATE: read the scalar-4-bit PTQ arms first — if 4-bit
  is near-lossless (int4-MX precedent), the complex bracket's
  only remaining justification is the per-euler-family read.
- Caveats booked: M4>T is NOT clean asymmetry-beats-symmetry
  (M4 has +0.42 bits); the clean claim = asymmetry tolerable,
  zero-absence fatal. Oracle safe iff complex stays INTERIOR
  (real embeddings/head/logits — never complexify the token map).

## ADDENDUM v3 (reviewer third pass, final deltas)

- M4 {−1,0,1,2} recognized as the MEASURED instance of the
  zero+integer-bits corner (2.00 bits exactly, asymmetric, 61) —
  the two-of-three law's cleanest ledger witness.
- Rotation cells corrected: DROP D9-vs-P2 (D9 = ternary-squared —
  two ternary channels, not a rotation test). Load-bearing cells:
  **Q9-vs-D9** (matched 3.17b, non-commutative rotation vs
  channel-pair) and **G5-vs-M5** (matched 2.32b, the cleanest
  rotation-vs-magnitude twin).
- Fairness: match REAL DOF, report both framings (complex-symbol
  framing flatters complex 2x); Parcollet predicts efficiency-not-
  capability — the exact trap the unconstrained-block arm controls.
- NO THEORY row pre-written (no row without the measured leg);
  Trabelsi/Parcollet/RoPE ride as related-work citations in the
  pre-reg only. Skeptical prior: null-or-efficiency aggregate,
  euler-family dividend or nowhere.

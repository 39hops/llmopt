# Spec: next session (2026-07-28, Artin: "all")

Six programs, ordered by leverage x cheapness. Every cell books
its pre-reg in RESULTS before firing (bars set at fire time
against same-day comparators). Machines: Mac always; 3080 per
schedule (open to ~5PM EST 07-29, then nightly GO).

## A. ROTATIONAL SNAP (linear -> rotational, lawful)

W = W_c + W_a under a complex structure J (channel pairing):
W_c = (W - JWJ)/2 commutes (rotational part), W_a anti-commutes.
- R1 (desk, minutes): per-layer ||W_a||/||W|| under (a) adjacent
  pairing, (b) Hungarian-optimized pairing, 4-5 crystals + a
  born-G5 positive control (its commutant mass should read HIGH
  — instrument validation). House pre-reg: real-born crystals at
  the random baseline (no spontaneous symmetry; the euler-read
  null, upgraded to best-pairing form).
- R2 (evening, no training): rotational snap anatomy — gate +
  flips-probe at W - t*W_a, t in {0.25, 0.5, 1.0}; damage curve
  + knee, the rational-Q sweep's rotation-axis sibling.
- R3 (one warm train): projected init + lambda*||WJ - JW||
  penalty (the banked 07-27 commutation constraint, never run)
  — teach-the-symmetry, graded v born-G5 (66/62) and fp32.
Frame: structure measurement, not capability (rotation 5x-null).

## B. E2 -> E3 (the owed export, then the doctrine's sole reopening)

- E2: scorer_s2_dist.pt -> AXNN + prompt spec across the bridge
  (feeds axiom's frontier_eval scorer slot, gate_battery
  detokenizer, E1/E5 chain). House-owed artifact.
- E3 (fires when E2's export path proves the format): paired
  exact-mode v rounded gate, SAME weights, via FX-V1 bit-exact
  stepper — the zero-noise-floor precision retest. Completes
  exact wave-model rung (i); rung (iii) (exact distribution
  readout on the S1 battery) chains behind it.

## C. LADDER-RETRY DECODE (greedy -> k=2 -> k=8)

The greedy-first no-adopt's named fix. Cell: full battery,
champion, three arms (wave-8 / greedy->k8 / greedy->k2->k8).
Adoption bar unchanged: >= wave-3 solves at <= 40% tokens.

## D. FARMER MIGRATION (axiom-only follow-through)

farm_dist_rows + stuck-state food + future enumeration consumers
move to ax.successors/successors_dist (deadline_ms walls, expired
taxed). Engineering; parity spot-check per substrate fence.

## E. STABILITY ATLAS (double-pendulum riff)

2-D birth grid (LR x momentum), d64 (minutes/birth) 12x12-class,
gate-colored map + neighbor-divergence read. Question: is
capability-over-hyperparams smooth or fractal — do islands of
stability exist in training space? (Z1 seeds, Muon LRs, Z1S
divergence = the piecewise evidence.) Overnight 3080 or Mac
batch; pre-reg the map's reads before coloring.

## F. LAPLACE BRIDGE GRAMMAR (3b1b riff — TIMELY)

Relay to axiom BEFORE their ODE tranche: the Laplace path
(L-table rules, rational algebra in s, pf-based inversion,
poles = cc2 characteristic roots, all exact-Q) as an alternative
solving-chain design inside their L9 engine. Unifies poly-pf +
ODE + exp/trig grammars through one transform; every
verification leg already exists both sides. House desk work:
atom-set sketch (s, L() and the determinability audit of
transform-pair rows, per the ODE catch).

## Order of fire
Mac now: A-R1 (desk) -> B-E2 (export) -> A-R2. 3080 while open:
C (ladder-retry) -> E (atlas grid, if window allows). F = relay
draft today (their tranche is imminent). A-R3 + B-E3 chain
behind their prerequisites. D rides opportunistically.

# Spec: SLACK RESTORATION + the streamlined anatomy kit
# (2026-07-29 evening, Artin GO — both machines open)

The attention-core day ended on a fork: "attention is
incompressible" v "every block is incompressible AT ITS FLOOR
and the recipe over-provisions ffn (4x) but not d". This spec
decides it, streamlines the instruments, and starts the two
paying directions (call-spans, reverse controls).

## Tooling first (Artin: "integrate tools to streamline")
scratch/anatomy.py — ONE env-parameterized census instrument
(CKPT/D/FFN/HEADS/CELLS/Q/RANKS) unifying head_census.py,
rank_read.py, snap_alloc.py, which stay as the frozen 07-29
originals. Every future crystal gets its anatomy in one
command; results comparable by construction.

## Cell 1 — SLACK RESTORATION (Mac, desk): d256 anatomy
Head census + rank read + snap on wfloor d256 (comparator 65,
MPS). PREDICTIONS (at-floor theory): head drops GRACEFUL
(d256 has 4x the geometric room); rank knee scales up
~proportionally (knee/d ~ 0.5 -> knee ~96-128); Q=16 still
free. READS: graceful heads => incompressibility is a FLOOR
property — the law becomes "slack = provisioning above floor,
per block"; still catastrophic => attention is INTRINSICALLY
routing-bound (stronger law: relations do not superpose at
any width). Either way the slack-location theory gets its
third leg.

## Cell 2 — REVERSE-PAIRS CONTROLS (3080, cuda line n=3 known)
Arm A2: forward-only at HALF dose (HALF=1 knob) — separates
dose from direction-tax against the cuda comparators (raw
51.3 / EMA 58.7, n=3). Arm B2: REV=1 on cuda (replicates the
Mac null on the paired line). READS: A2 ~ B2 => the revpairs
drop was DOSE (backward rows are merely neutral filler —
retry the pincer ticket at full forward dose + backward
extra); A2 > B2 => backward rows actively tax (direction
competes — pincer needs two models, as originally banked).

## Cell 3 — CALL-SPAN RELAY REPLY (owed; unblocks axiom)
Surface form decision: `call: gcd(48,36) -> 12` with TWO new
VOCAB_EXTRA atoms `call:` and `->` (matches the gcd/Mod/**
precedent; atom ORDER pinned in the relay). Asks: 500-row
pilot tranche, nt-grammar calls only, exclude=-guarded.

## Queue behind
Exact-twin int format for d56 (Q<=16 free => deployable
integer twin; precision doctrine: exactness = speed lever);
matryoshka rung-2 3-tier + escalating tier-retry; Leg C diet
descent (next 3080 night).

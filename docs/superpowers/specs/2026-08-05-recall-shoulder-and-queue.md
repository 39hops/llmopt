# Spec 2026-08-05: post-branch-day queue (recall shoulder first)

Written after the evening consolidation (three reviewer seats:
ledger audit, forward-facing audit, idea survey — all proposals
house-verified before adoption). Ranking is the house's, not the
surveyor's. Machine shapes labeled. Nothing below fires without GO.

## Tier 1 — ranked next experiments

1. **RECALL-LADDER (Mac, ~1 h of gates).** Measure the SHAPE of the
   capability shoulder GT-5c established: keep = frozen D3 core +
   string-seeded random fill sized to arm0-axis open recall
   {0.60, 0.65, 0.70, 0.75, 0.80}, 2 draws per point, math gate
   seed 1234 paired vs certified full. PLUS one arm the audit
   proposed (F7): a VERBAL-EXCLUDED random fill at matched recall —
   GT-5c's pool included verbal experts, so "not verbal-specific"
   is currently measured against partly-verbal fills; this arm
   closes that. Registered readout: location of the 50%-of-full
   crossing ±1 recall bin (sigma_diff fence; no per-arm deltas).
   Payoff: the residency program's design curve.
2. **PROOFS GATE (Mac + instrument work).** First non-sympy oracle
   gate: statement in, tactic script out, mathlib kernel = oracle
   (Mac-local cache; 382 ms/cert; chunked checker shipped).
   Unblocks THREE banked threads: verbal-side necessity test (in
   mirror), a second symbolic-domain capability instrument (the
   transport question has n=1 gated domain), crest-on-proofs.
   Design pass needed on task format + determinability audit
   (underdetermined rows train hallucination — the standing fence).
3. **MASK-ENSEMBLE CELL (Mac, one afternoon).** The unexploited
   asymmetric advantage: masks are one bias-write on a resident
   30B; churn says the crest is a DIFFERENT solver (61-68% solved-
   set overlap); GT-5c says many populations reach the 36-55 band.
   Cell: union of solved sets across {full, crest, union-mask, 2
   random fills} at matched total budget vs full-only at the same
   budget. Verified-AND-distinct fence on the union count.
   Registered risk: ensembling may only re-solve the same problems
   (overlap could be mask-correlated) — that null is informative
   for the reformulation-ensemble bank either way.
4. **CO-ACTIVATION HUBS D1+D2 (desk, zero GPU).** The reviewer-
   specced designs on existing trajs: within-token cross-layer
   co-selection vs degree-preserving shuffle (the B4 instrument;
   bar is the 250x-shuffle field baseline), then cross-corpus
   bridge scores. Frequency-confound fence: report residuals vs
   shuffle, never raw degree; Spearman(hubness, count) < 0.7 or
   the finding is frequency.
5. **ROUTING-BITS LEDGER (desk).** Bits/slot of decode routing
   under {uniform, marginal, LOO template, prev-layer-conditional};
   TRAJ v2 scores make it possible now. Replaces the entropy
   license's arbitrary threshold with a currency.

## Tier 2 — speculative, charter-clean, cheapest-first probes

- **TT/MPS KV-cache truncation** (method borrow from quantum
  numerics): one certified decode trace, bond-dimension schedule,
  eval/equivalence.py token-identity as the bar. Null books at
  <1.2x compression.
- **Physics-native fraction ladder** (the D4 null's next arm): is
  the physics no-crest a corpus-thinness artifact? Null hardens
  "crest is math-only" toward a law.
- **Feynman-integral IBP variance probe** (banked continent,
  charter-clean physics): one-loop rewrite table + numeric-point
  oracle on ~50 targets — measure the variance BEFORE building any
  learner (the meta-pattern). Null: rewrite-only closes >90% (no
  headroom) or <5% (wrong move set).
- **Stabilizer-flavored hardness label**: correlate against churn-
  judge per-problem labels (exists, AUC 0.679). Null: Spearman
  <0.2.

## Do-not-revive (today's results argue against)

Exclusion-mass as mechanism; verbal-tax at the routing layer; the
61.1~61.8 coincidence; ZX->routing / Fourier->coalition analogy
transfers; entropy-triggered anything; crest transport as a
headline bet; config estimator (until tile autotuning);
"functional not topical" as premise. Thin-resolution flags: the
0->42-55 resurrection anchor is single-seed (core-only arms at
777/2026 would firm it — cheap rider on item 1 if wanted); the
union-over-random +10 is unresolved.

## Standing banked (unchanged)

Excluded-experts anatomy (frame sharpened by the day); verbal gate
(item 2 IS the path); re-emitted sidecar sampled pass (awaits
axiom regeneration; ~6 min Mac-local); temporal pincer (cheaper
now via intbirth); parallel leaf closing; basis proposer (gated);
scratch workspace (neutral).

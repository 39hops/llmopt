# Evening queue spec — 2026-07-24 (post gen-9 verdict wave)

Built from a same-day full read of RESULTS / THEORY / RIFF-LEDGER.
Tier 1 is the fixed chain (order matters, mostly automated). Tier 2
is ranked retry/revive candidates — past results that NEW results
have re-priced. Tier 3 is axiom-side asks. Everything pre-registered
before it fires, per doctrine.

## Tier 1 — tonight's fixed chain

1. **Gen-9 A/B verdict** (~7:15 PM, auto): read against THREE
   ledger items — solved-only leak (B>A rare+unseen), chain-carry
   counterweight (B<A gate), rations (L3>=21). Three RESULTS
   entries.
2. **poly5 bridge knock-3** (auto via watcher): door (ibridge
   >=12/15, icancel >=80%) + bar (gate >=67) + L4 fingerprint.
3. **ODE zero-diet baseline probe** (Mac, ~30 min): pre-registered
   — bridge law predicts ~0; the number = L9b champion-control.
4. **Desert test v2** (Mac, ~minutes): Liouville-dead integrals
   into gen-8 (both grammars resident). Pre-reg: house bets (a)
   honest stall (co-residency never composes); (b) spontaneous
   series-expansion = first measured cross-grammar discovery;
   (c) bluff = regression. Reads with #3 as the sibling pair: both
   zero => bridge law graduates (nothing transfers undemonstrated);
   either nonzero => unnamed channel, better outcome.
5. **45M union re-ask** (3080, winning arm's diet): are the union
   coefficients width-stable? (i) capacity ~0, (ii) bridge
   level-local, (iii) nothing else — all 19M-measured.
6. **v5 s2** (3080, when round-1+2 shard audited): exchange food =
   walls-farm chains; miner v2 identity guard (verified-AND-
   distinct at bank time) lands BEFORE s2 mines anything.
7. **Repo taxonomy surgery** at the freeze (existing spec,
   5-item gate).

## Tier 2 — retries/revivals re-priced by new results (ranked)

1. **Boundary-or-bulk regression** — Artin's standing "FIRST at
   next 3080 GO" (2026-07-22), displaced by the Ozaki/exchange
   arc; most cells already measured (five-point width ladder +
   alphabet bracket + B@768 twice); missing cells are 19M-class
   births at 11 min each (bf16 verdict re-priced this from a
   night to ~1 hr). One regression, one verdict: capacity ~
   volume (N*b) or boundary (d-class).
2. **Chain-carry ablation redo at full budget** — v1 VOID (8k rows
   vs 132k, repetition asymmetry). Re-priced by: gen-9 corpus in
   hand, bf16+KV speed, and arm B's counterweight read tonight
   gives the first honest leg free. Full redo = chains vs
   step-shuffled vs root->answer at production budget, one Mac
   night.
3. **Wave-contrast learning (shaped-GRPO rung 3)** — free
   DPO-shaped pairs from mixed waves (verified vs rejected sibling,
   same state). Re-priced by: size-Phi null names the successor;
   metabolic v5 sessions emit mixed waves continuously — can ride
   s2's stream as a paired arm without a dedicated run.
4. **Warm-birth template v2 (embeddings + attention stats)** —
   the zero-epoch gate reads 0/120 by design (v0 = gate matrices
   only); every rung graded by how far it drags the zero. Re-priced
   by: five-grammar corpus signatures now exist (richer statistics
   table), births 11 min. North star: calculation alone > 0.
5. **Equation open slots: k(W) form + CV/norm width laws** — needs
   three ep1 births at matched epochs (17/35-min class, explicitly
   "queued, not assumed" since 07-19). Idle-Mac filler; hands the
   calculator its two missing parameters.
6. **HAWQ / precision-follows-rarity allocator** — banked 07-17
   pre-rarity-instrument. Re-priced by: the rarity battery now
   GRADES it (pre-reg: rare-bin delta > common-bin delta at ~2.5
   avg bits), GPTQ Hessians already in hand.
7. **Notation A/B (prefix/Polish)** — banked 07-17 when births
   cost hours; now 11 min. One tokenizer function, two payoffs
   (shorter sequences, delimiter-outlier removal). Clean
   one-variable evening.
8. **Deterministic gate pilot** (Ozaki rung-1 rider: ternary
   weights are integers-x-scale => bit-exact forward at k
   products) — kills the fp16-near-tie/device-dependence class
   (the 9/24-vs-18/24 MPS/cuda split) at the gate level.
   Cross-device bit-identity = one lab-wide instrument upgrade.
9. **Crown tiebreak: growth round 2 on merged_grown** — BOARD's
   named tie-breaker; re-priced by gen-9-era diet (fresh rows =
   growth's fuel, per gen-5's lesson). 3080 overnight candidate.
10. **Ternary growth-quantizer fix** (per-row scale freeze at
    growth) — priced 07-23, not taken; one-variable substrate
    test whenever a ternary growth run is scheduled anyway.

### Tier-2 additions from the reviewer's deep read (2026-07-24 PM)

11. **KV-batched gate sampling revival** — gate-v2's null was
    config-bound ("no KV cache means batching multiplies O(T^2)");
    KV shipped since. Exact-match machinery already proven at
    K=1/12/24. Free throughput on the sampling-bound loop; the
    cleanest nulls-inherit-config-defects case on the books.
12. **Graph-modularity read on gen-8** — pre-registered prediction
    (multi-domain crystals grow visible modules) finally has its
    substrate: the five-grammar crystal + the dual. Instrument
    exists; minutes to read; directly probes union coefficient
    (iii).
13. **Duo-teacher logit-KD** — second crown finisher the queue
    missed: distill one student vs combined champion+ternary
    logits, graded on holding BOTH rarity tails. Independent
    alternative to growth-round-2.
14. **Mass->robustness leg BEFORE the HAWQ arm** (ordering
    constraint on Tier-2 #6): only frequency->localization is
    measured; localization->low-bit-survival is not — readable
    off EXISTING quantized gates (per-family survival vs committee
    mass, zero new training). #6 rests on it.
15. **Blind committee prediction as standing process rule**
    (equation test #4): every NEW grammar (gen-9's next continent,
    ODE, Fourier) gets a pre-registered committee-table prediction
    from sigma alone BEFORE any model eats it. Three chances
    missed so far; zero marginal cost.

### Read-discipline notes (adopted from the same review)

- RARITY RE-BASELINE: after the taxonomy surgery re-freezes bins
  (third census this week), ALL stored rarity integers become
  pre-move artifacts — paired same-census reads only; a
  re-baseline run of the standing checkpoints on the new census
  is part of the surgery's acceptance, or cross-generation rarity
  talk stops entirely.
- DEVICE PINS: the 9/24 L9 champion-control is an MPS number
  (cuda reads 18/24, same checkpoint). Every tonight consumer
  pins its device: ODE probe + desert v2 = Mac/MPS (controls
  valid); anything on the 3080 needs its own same-device control,
  never the 9/24.

Deliberately NOT revived (starved judges / closed files): token
regret, config estimator, dispatcher v5, budget alloc, population
training, hints in any form, precision-capability (CLOSED),
selection-overfitting (resolved as battery truth).

## Tier 3 — axiom-side asks (post 83-wall re-farm)

1. **Fuzz-the-oracle rung** (today's wedge lesson): metamorphic
   fuzzing of canonical()/verify — generators as adversaries,
   invariants = terminates + equals-oracle. Cheap standing CI.
2. **Magic boards rung 2** (persistent (rule,node) memo across
   runs; rung 1 measured 2.28x warm-start).
3. **Fourier continent tranche** (banked 07-23: coefficients are
   integrals the engine already solves; Taylor sibling).
4. **poly5 supplement** if the door opens but padd-class kinds
   still lag (operand-complexity axis).

## Bookkeeping riders (tonight, with the taxonomy)

- BOARD staleness pass: duo-miner row (shard delivered, mining
  done), GRPO-frontier-band row (era closed), fast-trainer row
  (shipped as bf16 default) — move to CLOSED with pointers.
- Miner v2 identity guard is a CODE change (Fable-only per policy)
  — lands before any further mining, tested on the v5 rows where
  the leak is known (~1,275 identities must be caught).

# L9 territory design — the ODE continent (draft for both labs)

Status: DRAFT for Artin + axiom Fable review. Farming waits on
axiom qualification (perf fix + level-gated slots -> ~455 vs 431
achievable; then farm-swap decision, Artin's call).

## Levels

- **L9a — fused-integral residue, industrialized**: the engine's
  own frontier (simplify-fused multi-family quotients, triple
  compositions, cyclic by-parts, sqrt-of-quadratic w/ trig-sub =
  ONE new rule family). Generator: compose L8 machinery + new
  sqrt-quad family. Verify: existing diff oracle. Cheapest new
  territory; unblocks first.
- **L9b — first-order ODEs**: separable, linear (integrating
  factor), exact. 317 quarantined chains seed the format. Each
  problem = 2-4 integrals + glue (the measured cost anatomy).
  Verify: substitute-into-equation (diff-only, oracle-native).
  Solutions may be implicit — the tokenizer needs y and = (vocab
  +2; charset audit required).
- **L10 — second-order const-coeff + definite integrals w/
  symmetry** (sketch only; priced after L9 yields).

## Doctrine carried forward

String seeds; exclude= against ALL training shards AND gate/probe
bands (incident #3's remedy — probe bands get exclude= too).
Collision audit before trusting any split (widen generator space
first). Calculator prices rows-needed BEFORE farming (v2 with
row-format weights: worked examples ~2x). Farm on the qualified
hybrid engine ONLY (no mixed referees; shard labeled with oracle
config). Gate band EXTENSION ships with L9: add L8+L9 columns to
the honest gate before models saturate 120 (currently 76 — the
stick runs out near 90).

## RL revival (the strategy correction)

After the first L9 shard: GRPO returns — the discrete-plasticity
fork (GRPO the ternary-73, Artin's visible-flips bet) AND
standard GRPO on the grown-76, both against L9-frontier problems
where solve-rate variance is highest (prediction pays where
variance lives). Mining feeds the corpus (N*p term).

## Qualification memo (condensed)

Bar: hybrid >= ~455/480 with level-gated slots (heurisch on
L6/L8 only) after axiom's cancellation short-circuit. Then: dual-
oracle verification on one 1k-row L9a sample shard; live shadow
rides the campaign; sympy remains oracle-of-record until the
shadow gate (>=1e5, zero crossings) completes mid-campaign.
Decision to swap: Artin's.

# Next-session plan v2 (written 2026-07-26 evening; supersedes 2026-07-26-next-session.md)

Resume: BOARD -> handoff 2026-07-26-1 -> this file -> RESULTS
tail (or `results_query.py --live`).

## Tonight (Mac only, until Artin hands over the 3080)

Pincer spec pilots (specs/2026-07-26-reverse-llmue-pincer.md),
cheap-first:
1. **R0 (minutes, zero births)**: conjecture-leg readout off
   fmt_oneshot_1p.pt — k=8 sampled answers per gate problem,
   oracle-checked; books conjecture yield per level.
2. **R1a**: backward text-crystal birth at d256 (reversed pairs
   ONLY diet — its whole world is backward, no mixing) +
   backward validity probe (forward-verify, exclude-guarded).
3. **R1b prep**: (t, rule, site) label extraction for the
   pointer/edit arm — from engine replay of farmed chains (check
   what chain provenance already carries; axiom ask if math
   chains need the annotation).
4. If time: R6 pilot arms (backward scorer alphabet race —
   Artin's ternary-faceplant bet) once R1a/R1b exist to score.

## 3080 night queue (ARMED, fires only on Artin's GO)

1. **math+ZX union birth** (vocab-47, gen-4 + zx_farm1_train,
   19M-class, BS=8/grad-ckpt/TF32, seed 1) + BOTH gates (math
   gate_ckpt + gate_zx). PRE-REG at arm time: federation floor —
   does the union hold both columns (math ~63-65-class AND ZX
   ~30-class) at 19M, or does grammar CLASS (graph v tree) bind
   where COUNT (5 grammars, gen-8) did not? The union equation's
   first graph datapoint.
2. **ZX seed-2 confirmation** (cplx_none_zx recipe, BIRTH_SEED=2)
   — n=2 on the 36-class arm; first ZX seed-sigma point.
3. Both self-marking; queue script ready at scratch/night_zx.sh
   (written at GO time, wsl.sh launch).

## After (the standing queue)

- SCOREBOARD.md generation (index-derived; may land tonight).
- CLI wrapper v1 (post-SCOREBOARD discussion with Artin).
- HOLDS awaiting GO: miner v2, knock-4, adjudicator v3.
- Banked-hot: mixture diet (pairs+oneshot — pincer R7),
  judge-collapsed decoding pilot, banded-delta walk, Fourier
  continent (territory-merits), length-bucketed streaming +
  format x schedule combo session.
- Index curation: incremental (54/269 verdicts curated; deepen
  opportunistically, never blocking).

## Standing fences (unchanged)

sqrt(2)-sigma; >=5 single-seed 19M / >=3 d256; cross-device
forbidden; --fast never without --nopack; pre-reg before fire;
controls + pp sidecars ride every instrument; verified AND
distinct; strict encode; CE-400 is format-bound.

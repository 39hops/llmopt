# The Board — every thread, one line, current status

THURSDAY 08-06 EVENING (handoff: 2026-08-06-2-identity-day): **CAPABILITY FOLLOWS EXPERT IDENTITY — THE DAY CLOSED THE AGGREGATE ERA AND OPENED THE IDENTITY ERA.** Sequence, all booked: lab/traj module 4 CLOSED (VERDICT LAB-TRAJ — desk authority table first, unified patch_moe_router, D0 590,736-row identity modulo the dated phase amendment, live 3a/3b byte-identical, NEW FENCE: keepsets DROP_TAIL is v2-artifact-BOUND). GT-7 fired on GO and P-NULL FIRED (bins non-monotone 3.5/34/29.5/28 at pinned recall; within-bin spreads 38-49 solves at matched recall AND coverage; both GT-6 anomalies dissolved into draw identity — r80 fresh 75/48 v booked 9; SURVIVAL MAP BINDING: no aggregate ladders ever again). RULER/gist queue slot KILLED (never an experiment; library stays) and replaced by the LOCKSTEP window spec — same-day VERDICT LOCKSTEP-A1/A2 PASS (MB-S14 both arms + DIET-BRIDGE bit-identical on the 3080: all 16 milestones, both FINAL shas exact, plateau 15909->12518 to the token — both rungs now DEVICE-FREE). VERDICT TENET-W1-S: all four surfaces null (attn/up/emb/ln 10/10/11/10 of 20) — TENET-W1 upgraded to SURFACE-EXHAUSTIVE negative; direction lives in FUNCTION; no W2; launcher mkdir-before-launch rule booked. THE IDENTITY BATTERY OPENED AND ITS FIRST RUNG FIRED AT 6.3x THE BAR (VERDICT EX-ANAT-1): symmetric class-preserving k=4 demand-ranked swap between GT-7's matched-bin draws — pooled D=176 v bar 28, every low draw jumped to/above its bin's high (15->55, 5->57, 10->59), two highs cratered (53->30, 46->11), and c30 hi' hit 77 > full-model 64 (own anomaly line). Specs grounded by two Opus desk agents (Fable spot-verified): EX-ANAT-1 frozen design (class-preserving swap = coverage structurally invariant; k<=8 in band; no repair path), LOCKSTEP C2 Mac-precision addendum (MASTER_DEV split-masters CODE-ONLY never measured; MPS wall-race queued-never-run, one kernel-day slot behind exact_gemm tiling). LATE NIGHT: BISECTION RAN (VERDICT EX-ANAT-1B) — P-FOLLOW does NOT persist below k=4 (0/3 and 1/3 directional), and the miss is the finding: ADDITION transfers at every k (all six lows rise +6..+58; even highs rise at k<=2 receiving the other side's top picks — carriers not exclusive to winners), REMOVAL-damage threshold sits between top-2 and top-4 per class (192-384 slots); EX-ANAT-2 sharpened to one-sided transplant/ablate arms. README headlines + FINDINGS EX-ANAT-1 bullet shipped (Gemini adoption, fenced); W1-R relational-reader riff banked (Artin — composition-order hypothesis, licensed as the new representational hypothesis); DeepSeek lenses banked into EX-ANAT-2. LIVE NEXT: EX-ANAT-2 excluded-expert anatomy; R-EMISSION (frozen joint definition); CHURN-JUDGE-2 identity handle now EXISTS (second gate routing-margin still binds); lab/gate -> sink -> timebox; Lean full pass [overnight GO pending]; PLATEAU-BREAK + gravmoe pair [design pass first].

THURSDAY 08-06 (overnight-into-midday; Mac + 3080 window): THE TENET BATTERY'S FIRST FULL ARC — lab F1-F3 pre-extraction fixes landed first (arm2 rows stream, gt2_jaccard call-time env incl. TRAJ paths with booked numbers re-verified identical, evaluate.py unboxed-check warning) + doc-integrity repair (FINDINGS anchors, REGIME vocab mirror). D1 reverse gate BUILT with one pre-fire retraction (AMENDMENT R0-REV-D1: verify_wave is direction-BLIND — symmetric equivalence passed a forward model 4/10; corrected to engine-replay mint criterion, forward smoke 1/10). D2 certified diets BUILT: 92,944 unique gen-4 rows replayed in a line-server pool (67,074 certified, 0 timeouts/membombs), and the exclude-union law FIRED ON ITS FIRST RUN (AMENDMENT R0-REV-D2: 21/120 gate-band expressions verbatim in gen-4 prompt-side, 26 rows excised; STANDING FENCE: gen-4-fed forward births carry <=21-problem first-ply eval-in-train exposure — paired deltas unaffected). R0-REV: null on the registered gate (rev twin trains in loss 0.319 v 0.358, 0/120) — then Artin's "why zero?" autopsy found the mechanism (AMENDMENT R0-REV-DIST: chain starts are OOD inputs for a reverse model; in-dist probe rev 6/40 v fwd 1/40) — reopened on Artin's call as R0-REV-B (pre-reg'd D1b poststep gate): REV 24/120 v FWD 1/120, P-TRAINS-B fires — backward emission is REAL and PROMPT-DISTRIBUTION-LOCAL (L4 0/24 anomaly open; FINDINGS carries it as ONE unsplittable bullet). R1b-micro REOPENED behind D3 — D3 BUILT (budget accountant, R8-overdraft regression test, 6/6). W1: population 50/50 pairs on the 3080 (Artin GO, rjob, ~5min/birth, zero failures), reader bridge built (gate-neuron tokenizer, resampling augmentation, 104,770 params), VERDICT TENET-W1 NULL AGAINST W0's EXPECTATION (10/20 exact chance, train never fits) + randinit control rider 20/20 — direction lives in FUNCTION (24 v 1), not sampled gate geometry. Cross-lab: grok doc reviews adopted where verified (V4 subhead, RUNG-A rider cite, F1d denominators, GT-5c fence relocation out of the GT-6 bullet, CLAUDE.md trained-nets-only tolerance-bar fence); DeepSeek routing-synthesis banked with counter-notes (interchangeability overreads GT-5/5b; interference-v-dilution fork untested). Reviewer scan (Artin-sanctioned): instrument map delivered; gaps confirmed — W1 bridge (now built), GT-7 coverage re-derivation still owed, lab/ package still planned. LIVE NEXT: GT-7 PRE-REG BOOKED [HOLD — fires on Artin GO] (lens authority = gt7_coverage_rederive.py after AMENDMENT MOE-GT-6-LENSES demoted the desk Spearmans). STRUCTURE TRACK (afternoon, Grok review assessed + adopted-with-rejections, RIFF booked): CODEMAP shipped (docs/CODEMAP.md via gen_codemap.py — mechanical move-gate ladder, census 439 files: 131 library / 102 results-cited / 9 reproduce-pinned / 39 spec-cited / 158 UNCITED archive pool); llmopt.lab LANDED — verify_wave + _gen_isolated adopted verbatim (44/58 import sites, source-identity + parity guards) AND lab/oracle module 1 DONE (Oracle typed-events {TIMEOUT, CRASH_EOF, CRASH_PIPE, MEMBOMB} + counters, worker verbatim, loud paths exercised live); CLAUDE.md scratch doctrine booked; detbwd-collapse + wholesale-bench-archive REJECTED with reasons (RIFF); next lab modules per spec order (config, keepsets, traj, gate, sink, timebox); moves wait for the BOARD:114 freeze gate. EVENING: lab/traj MODULE 4 CLOSED (VERDICT LAB-TRAJ, ab0314c) — desk divergence/authority table FIRST (10 surfaces), then patch_moe_router unification (D-1 traj+keep refused, D-2 live split, both Artin-nodded), synthetic parity vs frozen instruments, live 3b/3a byte-identical, D0 590,736 rows regenerated (gate 64/120 + tail match; only diffs = the 5,808 phase retags the 2c47630 amendment made AFTER the artifact froze, zero non-phase), tier-4 keepsets loop closed exactly; NEW FENCE booked: keepsets DROP_TAIL is v2-artifact-BOUND (v3 files use DROP_TAIL=0; auto-detect fix waits on the dual-copy doctrine). GT-7 still [HOLD] (no hitchhike; separate GO). Remaining modules: gate, sink, timebox.

WEDNESDAY 08-05 EARLY (post-midnight, Mac only): LEAN-TIER-2 co-signed (real-corpus closable 87.5%; live-merge trigger FIRED, fence held 40/40, 10/10 merge-dependence reproduced house-side with an independent atomizer; joint amendment booked verbatim both ledgers; sampled kernel pass owed next 3080 window). Then MOE-GT-3, Artin's abstraction-hierarchy riff run same-night: the three-domain core (GT2-CORE-0, 37/58 per layer vs 11.9 null, containment 0.92) is SYMBOLIC, not linguistic — proofs coalition contains 0.901 of it, prose 0.250, and prose routes through an almost entirely different expert population (Jaccard 0.16-0.19 vs every symbolic domain); the deepest router split is SYMBOLIC vs VERBAL, with a base class under math that proofs/phys/code inherit. Core-only mask on the math gate: 0/120 as registered — necessary, not sufficient; domain extensions carry capability. Pre-flight reproduced the unbounded-dedup-loop pathology live (proofs space saturates at 80 prompts; make_dataset(120) against it hangs). Front-facing docs landed overnight too: FINDINGS 'routing crest and domain coalitions' section, README crest bullet, REPRODUCE MoE-GT path (DUMP_DECODE closes the D4 keep-set gap byte-identically), curation ratchet 300->320 reasoned. MORNING GO: MOE-GT-4 fired and booked same-day — the verbal branch is REAL and SYMMETRIC to the symbolic one: Jaccard(prose, dialog) 0.721 (symbolic-family range; nulls 0.87/0.92), dialog pinned in the cross-branch band vs all four symbolic domains (0.19-0.23), and the verbal core (prose&dialog) is 48.5/58 [2-way null is 26.3 — corrected by the REVIEW PASS later in this line; 'LARGER' retracted there too] and nearly disjoint from it (Jaccard 0.095). Two sibling branches, each with its own base class. REVIEW PASS (two Opus reviewers, Artin-requested, all findings house-verified before adoption): AMENDMENT MOE-GT-4-REVIEW booked — 2-way null corrected to 26.3 (was 11.9, 1.84x chance not 4.1x), "verbal MORE coherent" RETRACTED (arity mismatch; matched 2-way math&phys 52.1 > verbal 48.5, verbal coherence is ordinary), 0.92 provenance fixed (leave-one-out, not proofs), GT-3 re-derivation script shipped (all 8 desk numbers reproduced ±0.006), shared-SYSTEM confound named and then PRICED by MOE-GT-4b (SYSTEM-swap control: same corpus under math SYSTEM costs 0.10 Jaccard vs itself, keeps 0.67 vs prose, stays 0.21-0.24 vs symbolic — P-SYSTEM-DRIVEN killed, symmetric hierarchy RESTORED with the confound priced). Survey sweep verified: union of the two branch cores = 78.2/128 = 61.1% per layer, matching MOE-GT-1's 61.8% tie-with-full keep — the union-of-bases mask ran as MOE-GT-5: 55/120 {21,17,17} — NEITHER registered prediction fires. Coincidence demoted (8 below the top-demand 61.8% mask's 63), but the 0/120 dead core is RESURRECTED to 86% of full capability at this seed [per-seed 86/66/70% after GT-5b, pooled 73.6%] by adding ~41 mostly-VERBAL experts (recall 0.554->0.729), beating R6's cross-masks which carried HIGHER coverage (0.78-0.80) yet scored 19-21. Live question: does math capability route through the verbal base when available, or is the recall shoulder just steep? Discriminating control banked: core + 41 RANDOM non-core experts (matched size; RULESEED machinery exists). FINDINGS core bullet updated (arms no longer "unrun"). AFTERNOON (post-GO chain): MOE-GT-5b seed replication — union deficit holds 3/3 seeds (64->55, 73->48, 60->42), pooled 17.3 solves, ABOVE the registered 5-15 band (honest miss; seed 1234 was the friendliest pair). GT-VERBAL-SHARE desk observation: free-routing math sends 2.7% of decode demand through verbal-only experts (75% symbolic core) — verbal fallback is NOT normal operation. MOE-GT-5c random-fill control: draws 51/36/48 vs union 55 at matched size and HIGHER recall — R-SHOULDER fires, R-VERBAL killed; the resurrection is a recall shoulder (0.55 dead -> 0.73 working), not verbal machinery [RETRACTED-as-scoped same night: GT-6 + AMENDMENT MOE-GT-5c-SCOPE — the pool was ~45% verbal; clean exclusion collapses 0/7]; verbal-tax thesis dead at the routing layer [NARROWED, not dead: the masked-regime fallback machinery IS verbal-branch — GT-6], alive only at training-history (crystal contrast rows). Residency design simplifies: above the resident core, paging = recall-coverage arithmetic. [RETRACTED by GT-6 — recall does not organize capability; paging must respect verbal-branch residency.] LEAN CROSS-LAB ARC (4 round-trips, each verified before booking): kernel sample 1000/21,914 — 703 compile, 0 false RAW equalities, 297 tactic-script defects; axiom fixed emitter same-day (c331894 rfl/reflexive flag, verified 496/496 house rebuild); 28-row dissection (6102525) forced AMENDMENT LEAN-KERNEL-SAMPLE-2 — 7/1000 = NEW COUNTER unprovable-by-design (atom-split; generalized statement false over free atoms, raw equality true, always loud), 17 fixed by ring_nf prelude (house spot-check 3/3 on the now-Mac-local mathlib cache), 4 genuinely open (field_simp self-refactoring, kept loud both ledgers); house flake flag -> TWO real axiom defects (69d5e13: wall-clock budget expiry now observable, budget-aborted fires no longer memoized as permanent no-fires — the in-process checkpoint-selection twin); beam determinism scoped 'modulo observed budget expiry' both ledgers. House printer bare-symbol-denominator gap fixed (diff 638->690). Instrument lesson booked: Lean's ~100-diagnostic file abort silently truncated the kernel pass TWICE before the chunked fix. MOE-GT-6 (the recall ladder, late night): recall is NOT the organizing variable — 2-draw means non-monotone, draw spread WIDENS with recall (a 0.795-recall draw scores 9, a 0.792 draw 66 >= full), and the F7 VERBAL-EXCLUDED arms COLLAPSE (0/7 at matched 0.72 recall vs 16-55 for verbal-containing) — the verbal population IS load-bearing (necessary, not sufficient; two named anomalies open); AMENDMENT MOE-GT-5c-SCOPE books the pool artifact; core-only riders 0/120 at seeds 777+2026 (resurrection anchor n=3); exploratory: verbal-only coverage Spearman 0.755 best predictor, min-layer weakest-link KILLED, math-ext coverage without verbal DEAD (0.47-0.51 coverage, 0-class). R-EMISSION reopened as the sharpest question. GT-7 candidate: verbal-coverage ladder at fixed recall + anomaly repro arms. Instrument saga booked as ORACLE-BOX 1-4 (hang -> fork-SIGKILL -> spawn-silent-failure -> driver-side parse_answer; v3.2 subprocess line-server, every failure mode loud; Darwin rlimits verified no-op). TENET BATTERY LIVE: spec 2026-08-05-tenet-battery.md; D0 back-booked (WEIGHT-READER-0: 796,550 params, 80.8/82.4/88.4); W0 BOOKED NULL (VERDICT TENET-W0: inverse-twin acc 0.139 vs 0.167 chance over 345 eligible twins; sanity 0.866; weight features DIRECTION-SPECIFIC at toy scale; sin 0.692 = shape-resemblance; registered consequence: W1's direction classifier now expected to SUCCEED); R0-rev next (needs D1 reverse gate + D2 exclude-union builds). Banked next: excluded-experts anatomy (now framed: math-excluded = non-core symbolic-extension + verbal-branch residents; the 48-expert verbal base is a candidate population); verbal core-only mask needs a verbal gate that doesn't exist (banked, not queued).

TUESDAY 08-04 EVENING (newest handoff: 2026-08-04-2-exclusion-arc-churn-judge; review->survey->desk->rescue->judge, all booked):  AMENDMENT GT2-REVIEW applied a verified reviewer pass — the D4-CROSS "functional not topical" reading DEMOTED (diagnose-checker artifact: all 20 gate tasks were the lenient clean-variant, zero strict; masked=full=20 is a stock-answer signature), sigmoid restated to "an intermediate regime exists", D4-PHYS re-fenced as UNDERPOWERED (not "conservative"), D0 booked, receipt hazards fixed (RULE echoed, self-recall renamed), THEORY row added for the domain-coalition law, index links-drop bug found. Then the survey seat's exclusion-mass tension -> Fable desk checks found (i) the physics mask was NOT inert (churn 54-68% overlap — different solver, zero net) and (ii) a real CONFOUND: math's crest mask was pooled-log-built (88.1% of decode demand) vs physics decode-built (99.0%) — D4-PHYS changed two things at once. AMENDMENT GT2-EXCLUSION booked the axis (math interior peak at ~10% exclusion) + pre-registered the rescue arm. VERDICT D4-PHYS-B: the rescue CRATERS (-59 pooled, 3/3 negative) — exclusion-mass KILLED as mechanism, and the MATCHED-INSTRUMENT DISSOCIATION is the day's deepest finding: open recall 0.9013 vs math's 0.9014, closed 0.884 vs 0.887, yet math +22 / physics -59 — coverage and recall do not predict the SIGN of the capability effect; what the excluded experts COMPUTE is the variable. Math-only crest now stands confound-free. Code second crest: both predictors null, deprioritized. Evening RIFF banks: exclusion arc (banked+killed same day), dilution keep-rule, ledger-gap repair (capacity-meter proposer, crest-as-draft), crest-generator narrowed to math. NEXT: churn judge (spend-registered) + per-level anatomy of the math crest gains — the "what do the excluded experts compute" question is the program's spearhead now. CLOSED THE DAY: PRE-REG+VERDICT CHURN-JUDGE-1 — spend registered first (crest-default + judge-flagged escalation; ceiling +133/840 across 7 paired seeds), instrument certified non-invasive (7 reruns bit-exact, 0/840 per-problem mismatches); AUC bar FIRES (0.679 held-out vs 0.60, level-null 0.438), spend bar MISSES (1.32x vs 1.5x at F=33%) [AMENDED GT2-REVIEW-2: gap is 4.25 solves, inside the ~5-solve fence — "underpays" RETRACTED, bar UNRESOLVED; judge still +2.1 sd over random; level-null corrected 0.438->0.450 (tie-blind)]; rescuable failures are MASK-CAUSED (low recall + short completions). Revive-if: routing-margin features, new pre-reg only.

TUESDAY 08-04 AFTERNOON (handoff: 2026-08-04-1-domain-program; pre-reg: MOE-GT-2, fully resolved same-day): **THE DOMAIN PROGRAM RAN END TO END — COALITIONS ARE REAL AND DOMAIN-ORGANIZED; THE BEATS-FULL CREST IS MATH-ONLY.** Post-compact sequence: two survey seats (Opus, self-reported 4.5, disclosed) audited the spec — three verified hazards (decode-only split existed only in amendment prose; mechanics L1 = exactly 30 unique prompts/kind and make_dataset HANGS on exhausted cells; place1_gravity.py never charges prefetch traffic + hardcodes NE=64) — then the fixes and the ladder: TRAJ v2 instrument (phase + router scores RECORDED at write time, 4f3dc6c; D0 regression bit-identical to the certified artifact, 590,736 rows). VERDICTS, in order: **D1** crest LOCATION survives unspent seed 555 (argmax stays 45.3%; +22 over paired full, program-record; the DIET-COND-SEED failure mode did not fire); **D2** router IS domain-biased — physics coalition Jaccard 0.767 vs within-domain split-half nulls 0.930/0.871 (mechanics gate L2-3, 120/120 determinability pre-flight; physics full baseline 36/120); **D3** the registered ordering prediction FIRES — Jaccard(math,code) 0.543 vs (math,phys) 0.804, tracking corpus token overlap (0.097 vs 0.329) registered BEFORE the run; code coalition sits below its own split-half null (code full baseline 48/120, toolchain-scored, o2_asm rung empty-flagged); **D4-PHYS NULL** — the crest does NOT transport (+3 pooled vs the +7 bar, signs 2/3; physics crest closed recall 0.727 vs math's 0.887; gate/probe dissociation reappears on a second domain); **D4-CROSS** — cross-masks degrade -56%/-70% but do NOT die (21/120 and 19/120 vs R6 random's 0/120): the coverage-vs-capability curve gets its third point and is a STEEP SIGMOID [DEMOTED same evening, AMENDMENT GT2-REVIEW: intervals unmeasured -> "an intermediate regime exists"; 55-60 -> 55-63] (44.7% coverage -> 0; 77.6-80.2% -> 19-21; 86.5% -> 55-60; 90.1% -> 76-87), and under the math mask the code gate survives ENTIRELY through the natural-language diagnose rung (20/26; every compile/assemble rung 0-1) — coalitions look FUNCTIONAL, not topical [DEMOTED same evening, GT2-REVIEW: diagnose-checker artifact — all 20 gate tasks were the lenient clean-variant]. OPERATIONAL: 14:30 kernel panic was Fable's error (second 30B load beside a live job -> swap storm -> watchdog panic; machine rebooted clean, no artifacts lost, memory rule saved: ONE resident 30B, always). Hygiene: crest-nesting discharged (holds by construction; crest = 50%-mask minus the 6 lowest-demand experts/layer). STILL OPEN from the spec: rung 2 PLACE-1 bandwidth control (three named arms, traffic counter first), rung 3 routing-bits ledger (scores now recorded), rung 4 crest-as-data-generator (math-domain, unaffected by the transport null), physics-native fraction ladder (the D4 null's obvious next arm), ENTROPY-LICENSE-1 recount, GRAVMOE 1/8-vs-2/8 attribution.

TUESDAY 08-04 (handoff: 2026-08-04-0-crest-day; spec: specs/2026-08-04-next-session.md): **THE CREST IS REAL AND IT IS THE COALITION** — masking Qwen3-30B-A3B to its top-45.3% math-demand experts BEATS the full model, +14.7 pooled over 6/6 paired seeds (R4 +14/+14/+16 descriptive -> R5 +17/+9/+18 FULLY REGISTERED at unspent seeds, reviewer byte-verified), and R6's registered control shows the effect is 100% selection: random-45.3% masks score 0/120 (two draws) and anti-demand 0/120 at the same fraction — WHICH experts is the difference between 0 and 82/120. Ladder shape: 61.8% keep = statistically the full model (63/63/64/66); crest at ~45%; graded slope to 33%; shoulder 31.4% (30/120); death <=25%. ADVERSARIAL REVIEW LANDED AND CORRECTED THE DAY (AMENDMENT GT1-TRAJ-CORR): the TRAJ per-token data is ~80% prefill over a byte-identical prompt prefix — "routing is phase-dominated" RETRACTED as interpreted (decode-only: within-prompt 0.469 vs cross-prompt 0.443, near-equal); SURVIVED decode-only: entropy DILATES monotonically (100% of prompts — blackhole entropy-trigger refuted in the ledger) and decode-demand is template-predictable (LOO 0.870 vs 0.755, wins 120/120; bandwidth control owed -> PLACE-1 harness). Also today: LEAN-TIER-1 (443/443 kernel-certified, ring tier only — corpus division rows self-cancelled at generation, field_simp UNTESTED; 57-row axiom skip bucket question relayed; relay 2026-08-04-0 written); GRAVMOE-SEEDS-LADDER (COND+QK does NOT graduate — 1/3 new seeds, though never negative across 6; seed 53 = battery-best 3/8 + 133/140 tok; INSTRUMENT FENCE: gate solve counts are NOT device-free even where trajectories are — sympy timebox/fp-near-tie, receipts pulled and committed); cliff-debt discharged (the ~28% chat-state citation replaced by the booked ladder); ENTROPY-LICENSE-0/1 (marginal license vacuous; conditional form has 187/442 targets, prefix-artifact recount owed); two survey passes banked 4 grounded transfers (routing-bits ledger, PLACE-1 prefetch control, churn judge, crest-as-data-generator) and killed 4 with reasons. NEXT (the spec): the DOMAIN PROGRAM — physics/code demand logs (decode-only), coalition Jaccard, predicted second crests with cross-arms.

MONDAY 08-03 EVENING (newest handoff: 2026-08-03-3-gt1-ladder): **THE GROUND-TRUTH PLATFORM RAN ITS WHOLE LADDER IN ONE EVENING** (PRE-REG MOE-GT-1 -> ARM0/1 -> ARM2 -> R1 -> R2 -> R3 -> AMENDMENT GT1-CORR, all on Qwen3-30B-A3B-4bit fully resident on the Mac, ~4.5 min/gate). Headlines: (1) the aux-loss router is HEAVY-TAILED at deployment (top-25% experts carry 74.9% of mass — V4's uniform doctrine is noaux's, not MoE's; full-model oracle 64/120); (2) the keep% ladder is a graded slope into a collapsing shoulder (100%:64 | 61.8%:63 | 50%:78 | 40%:57 | 38.2%:53 | 33%:43 | 31.4%:30 | 25%:0, top-demand rule, seed 1234); (3) "50% BEATS FULL" did NOT survive replication (3/3 fresh seeds positive but pooled +4.7 < the +7 bar — NOT claimed); the real finding is CHURN: the 50% mask flips 41-47% of the gate in BOTH directions (solved-set overlap only 61-68%) — a DIFFERENT solver of similar strength, the capability-layer echo of F2b/F2c trajectory rerouting; (4) gate capability and text coherence DISSOCIATE (probe degenerates at 50% while the gate improves; V4-style attractors on a healthy vehicle => degeneracy under subsetting is GENERIC, not MXFP4); (5) closed-vs-open recall gap is monotone in residency (0.013/0.077/0.126) and explodes only after death — the calibration number V4's F2 instruments were missing. Also booked: VERDICT V4-F2c (K-sweep: throughput is MISS-driven, not memory; fifth attractor). LEAN TIER ARMED OVERNIGHT: sidecar generated Mac-side (443 certs / 0 fenced, axiom c0511bc emitter; sqrt fence verified live on the smoke row), WSL job fetching mathlib + running scratch/lean_check.py (logs/lean_tier.log, marker /tmp/lean_tier.DONE). Riffs banked with fences: expert itineraries, blackhole mode, house-born/emergent experts, routing-entropy expert license, function-space verifier weights. MORNING QUEUE: GRAVMOE seeds/windows ladder (needs fresh pre-reg, n>=3; wider-diet half needs a Fable SHA-pin edit), Lean verdict booking, desk cells on arm-0 logs (routing-entropy trajectory, entropy license, domain-conditional demand), paired-seed pass at the GT-1 peak.

MONDAY 08-03 DAY (demo sheet: docs/opus/F1-DEMO.md): **V4-FLASH GENERATED TOKENS ON THE MAC** — the F1 programme ran end to end in one day (PRE-REG V4-F1 -> VERDICTs F1a/b/c/d, then PRE-REG V4-F1e + 2 riders -> VERDICT V4-F1e). The vendor's unmodified model.py over a pure-torch twin of its six tilelang kernels (F1a: 22/22 bars, decode bit-identical to rungA, gemms <=1/128 of fp64 refs); 43-layer dense path + K=16 subset-resident experts (masking = one write into the noaux_tc bias, zero vendor-code changes; hash layers exact via tid2eid + fetch-on-miss); final demo **0.219 tok/s, 26.1 GB Metal, output = a fluent prompt-echo loop** (the registered degradation prediction — what survives 94% expert amputation is COPYING; content lived in the experts). F1e honest ledger: bf16-dense NULL (never the wall), profile says ffn/unpack = 84%, expert-cache FAILED 5x worse (774-tensor working set vs 180 cap), pair-LUT unpack +2.2x (booked as a narrow miss vs its 2.5x bar), K=24 pages at 30 GB Metal BEFORE the attractor breaks. BANKED next (fresh pre-reg required): batch per-layer expert calls; fp8-dense + higher-K on the reclaimed 6.3 GB; torch.compile/MLX for the eager floor. Instruments hardened: rjob.py is now the Mac long-run vehicle too (harness-coupled bg jobs got reaped twice); Metal-vs-RSS accounting (ru_maxrss misses driver memory; row carries metal_gb); MPS facts measured: empty() materializes on Metal (47.7 GiB B1 incident), frexp/fp8-compute absent, enqueue-vs-complete 65/108 ms.

MONDAY 08-03 MORNING (newest handoff: 2026-08-03-0-opus5-merge; audit sheet: docs/opus/SESSION-NOTES-OPUS-5.md): OPUS-5 BRANCH MERGED to main after Fable's line-by-line audit (VERDICT MERGE-AUDIT-OPUS5) — 16/16 gravmoe pins sha-identical against the branch's modified certified files, BIRTH_SEED matched pair verified under a polluted shell, the D2 retraction independently confirmed from its own log. The weekend seat (Opus 5, Artin's authorization) delivered 31 entries with 11 SELF-CORRECTIONS across three audit rounds: QK-SEED2/3 replicated the COND+QK gate direction 3/3 at three paired draws [REPLICATED]; DIET-COND-SEED: interior-optimum SHAPE replicates, LOCATION does not (lambda 1/4 is a seed-17 property); V4-FLASH 13-rung programme (group-32 MXFP4 byte-identical to K3; exact integer expert forward hash-identical cpu/mps/cuda; entropy coding is an ARCHIVE not a runtime — 8.3% saved at 38 MB/s; the 166.9 GB artifact cannot fit this disk; router shared-direction share of routing BOUNDED 7-21% and UNLOCATED — VERDICT V4-RUNG-D2 retracted by FINAL-0803 after its shuffled-bias null excluded the same inputs). New instruments adopted: tests/test_docs_integrity.py (anchor rot + tag grammar + curation ratchet, 296/300), llmopt.reproduce BIRTH_SEED sanitizer. Fable-file fixes landed: pack_rans verify gate unconditional; offload.py heavy-tail docstring replaced (aux-loss-free balancing makes uniform routing the DESIGN POINT — hit rate is C/E). V4 next rungs BANKED in the spec (M1 -> W1 -> Q1 -> 13; F1 real-forward scoped-not-started). Method law from the weekend, worth keeping: a bound derived under an assumption travels without it — catch it by INDEPENDENT RE-DERIVATION (bottom-up vs subtraction; in-weights vs sampled), not by re-reading.

SATURDAY 08-01 CLOSE (newest handoff: 2026-08-01-4-p4-complete-pre-sol): P4 COMPLETE, BOTH LEGS — device (16/16 sha-identical on the 3080, 6m28s parallel) AND lab (VERDICT GRAVMOE-P4-LAB: 10/10 engine arms bit-identical in axiom C++ after ONE gate-backward fix; their first run FAILED 10/10 on my unpinned artifact schema + their pre-rounded gate — one relay round-trip closed it via the RB1 bisect ladder). METHOD LAW adopted: E=1 parity gates are PROVABLY BLIND to gate-backward placement (rdiv(PQ*x,PQ*Q)==rdiv(x,Q); conventions separate only at E>1) — rounding placement is CONTRACT TEXT, never prose. The deterministic-birth scale ladder is CLOSED at 3 implementations / 2 labs / 2 devices (MoE + gravity + window cycling integer-closed everywhere). REPO TAGGED pre-sol-baseline: GPT 5.6 Sol gets an adversarial-improver branch (sol/review-1, Mac-only, no main writes, own docs/sol/ ledger) — house + axiom hold/chill; review on Artin's ask. Fable limits: 90% weekly, resets Mon 7AM EST — house work stays verification-shaped until then.

SATURDAY 08-01 LATE NIGHT (handoff: 2026-08-01-3-p4-device-brute-close): P4 DEVICE LEG PASSED on Artin's GO "go parallel" — ALL 16 pinned gravmoe arms sha-identical on the 3080 (6m28s parallel, self-verifying table; VERDICT GRAVMOE-P4-DEVICE): every gravmoe verdict this week is now DEVICE-FREE; axiom lab leg unblocked (artifacts scratch/detbwd_gmoe_ref/ + relay -5 with postscript, Artin carries). THE BRUTE LEG CLOSED (queue item 2, Mac): brute does NOT convert at this diet — steps axis needs decay to even be legal (MB-S14 transports; S4D 2/8 flat, +18 tok, zero new solves) and the LEGAL width arm (W4d) lands 0/8 UNDER the 208k baseline: 8 rows x 2000 steps is DATA-BOUND, capacity was never the binder (amends the SS null's guess). G-RB1 (COND=1 QK=1) STANDS as the battery optimum; COND+QK graduation = seeds/windows ladder + WIDER DIET, not compute. Three instrument laws banked: integer lr decay legalizes long runs; ACT_CLAMP is rms_fwd's int64 OVERFLOW GUARD (m40 overflow at rms ~90 Q-units, W4c crash; safe bound ~80 Q); clamp demand is MEASURED (STEPS=1 init sweep), never scaled. Knobs added sha-safe (DIM/DHEAD/FFN/ACLAMP/SCHED/EXPORT; 1fcfd187 reproduced 3x). AMENDMENT: battery model is 208,192 params, not "60k".

SATURDAY 08-01 NIGHT (newest handoff: 2026-08-01-1-gravmoe-battery-day): THE GRAVMOE BATTERY DAY — five exact cells, four verdicts, two frame-flips. (1) GRAVMOE-PAIR: monotone capability-for-mergeability trade at saturated init (roughly linear in LOG-dose, 4 log-spaced points); (2) DIET-COND: the trade was THE RAILS — conditioned residual writers (+-Q/8) flip it to an INTERIOR OPTIMUM (lambda 1/4 best loss AND -37% merge damage; conditioning alone -32% loss; FENCE: n=1 seed/init/window-set, and the ordering is diet-format-sensitive — the gate diet re-orders the arms); Artin's reframe adopted: gravity = expert communication channel (share the mean, keep the identity); (3) QK-COND [RESCOPED by review amendment]: GLOBAL-soft init (+73% loss, the arms softened six matrix families not two — reviewer BLOCKER) is a dead end and SHIFT=16 does not rescue it; TAU leg stands (1 of 2 bodies self-sharpened 22x on the hard base); "peaked attention is load-bearing" needs a true wq/wk-only cell before doctrine [RAN late night: the cell INVERTED it — wq/wk-only soft is a -72% loss WIN (8883 -> 2496), doctrine RETIRED, battery default now COND=1 QK=1; VERDICT QK-RESCOPE]; (4) GRAVMOE-GATE (Artin's ask): free-run oracle gate says 0/8 EVERYWHERE while loss said "solid" — exposure bias measured at mini scale, STANDING RIDER: all loss claims are teacher-forced claims; the gate is a standing readout now. Instruments hardened: twin protocol (discrete decisions from the integer side), E=1 reduction bisect (bit-identity), boost-invariance diagnostic, tt floor. All shas pinned for P4 device/lab legs. Morning wins (multi-block, MB-S14, diet-bridge, intbirth engine+primitives adoption, night31b lam-merge + R0) further down. LATE-NIGHT ADDENDUM (Artin GO "1, 2 on mac"): NULL GRAVMOE-SS — one-step scheduled sampling HALVES free-run token-acc (56 -> 22/140) and doubles teacher-forced loss; capacity binds at this scale, exposure-bias rung moves to the brute steps/params leg (S0 regression sha-exact to G-A0). Gate cell on the new COND+QK init: FIRST NONZERO SOLVES in the battery — TRAIN 2/8 sympy-verified, token-acc 56 -> 94/140, loss 2564; the init win holds on BOTH axes (rider on VERDICT QK-RESCOPE).

SATURDAY 08-01 AFTERNOON (newest handoff: 2026-08-01-0-saturday-morning-mb-diet): NIGHT-31b COMPLETE + BOOKED — (1) LAM-MERGE MECHANISM LANDS: lambda 0.1 merge is DESTRUCTIVE (48 -> 36, ~2.4 sigma); merge-free NEEDS the expert collapse (threshold between 0.1 and 0.25) — "birth as Hebbian MoE, ship as dense" now carries a dose requirement, the recipe card is SCOPED (free at lambda >= 0.25 only); (2) R0 TF32-OFF NULL: cuda lb {49,52,49} mean 50.0 v TF32-on 51.7, no drop toward Mac — TF32 exonerated, KERNEL-ORDER is the transport wedge, the deterministic integer battery is the only legal cross-device instrument (motivation now measured). Box back to Artin 05:56. Morning wins in the flagship row below (multi-block, MB-S14, diet bridge, intbirth adoption).

Updated 2026-07-31 LATE NIGHT (newest handoff: 2026-07-31-1-night-determinism-sweep — READ IT FIRST; it supersedes this header's evening text). NIGHT HEADLINES: transport verdict NO (scaffold CLOSED, rescue framing); GRAV-0T/REV = post-hoc gravity destructive both ways (pull is training-coupled; merge-free is a property of the pulled basin); gate proven EXACTLY deterministic on mps + lb-s1 45-v-37 was CHECKPOINT PROVENANCE (new rule: gates book with ckpt sha); FX-V3 closed BOTH directions (house PASS first run — merged crystal integer-closed, 3 impls/2 labs); R2 C++ trajectory-identical (axiom); R3a Q_w PINNED = Q<<8. Axiom now CO-LOCATED on the Mac (~/code/axiom). 3080 = Artin's; night31b [HOLD for GO]. MORNING 08-01 SUPERSEDES the evening lines above: R2b v1/v2 DONE (Q_w scope-split: 8 mini / 12 FULL-BLOCK; starvation law, THEORY row), full-birth C++ PASS (axiom, all milestone digests), llmopt/intmath.py promoted (402 tests), scripts/rjob.py shipped, gate_eval prints weights sha, review pass #2 adopted (transport-exclusion rewording; lb-45 = FFN_E=139 capacity twin, NOT a lost artifact; GBOOST=256 erratum). See the LIVE flagship row + handoff 2026-07-31-1. EVENING: DETERMINISTIC-BIRTH R1a/R1b/R2-mini ALL PASS (integer FFN+attention fwd/bwd + 200-step fixed-point-AdamW training run, ALL bit-identical Mac/3080 — training is cross-device deterministic at mini scale; Metal exact GEMM shipped); FOURIER-4a booked (co-arrival at solved moduli, no-eviction, count run-sensitive, live within-run gauge); B5 NULL (depth-share is not the farmer's medicine — two multi-step failure modes now distinct); review-adoption amendment (merge deltas = zero-mean bounded, +5.5-7.5 figure, B6 z=1.63 downgrade, within-arch clock ladder). 3080: **TRANSPORT VERDICT BOOKED (night): NO transport — cuda lb {51,51,53} (amendment #3; the 48 was a validity misread) v gravmoe {49,53,50}, means 51.7 v 50.7, dead-center in the +-2 Mac-scoped band; rescue framing adopted (cuda training already reaches the basin the Mac needs the pull for); recipe card stays 'free merge, capability-neutral'; scaffold program CLOSED pending night31b merge-free extension + R3 deterministic pair** — BOX FREE for Artin; night queue [HOLD for GO] = scratch/night31b_cuda.sh (lambda-merges + R0 TF32-off). Midday: (newest handoff: 2026-07-31-0; spec: 2026-07-31-scaffold-program.md — rungs 0-3 RUN). **THE CLOCK-PLACEMENT LAW booked (day's headline, THEORY row)**: FOURIER-2b (crystal learns digit-local moduli only; clocks ONLY where solved: k=5 276/512 v k=7 0/512) + FOURIER-3 (teach the digit-sum algorithm -> k=9 competence 0.83 WITH 0/512 clock — the algorithm SUBSTITUTES for the representation). Fourier structure marks WHERE computation runs, not whether solved. Recursion-depth diet-share clause added (v1 base-case starvation amendment). **THE RESOLUTION LAW adopted (methods)**: 120-gate sigma~5; n=1 cells can't resolve <5-solve deltas; rung-1 capacity verdict INVERTED on cuda replication -> gate-delta claims <1.5 sigma need n>=3 paired seeds. **SCAFFOLD PROGRAM END-STATE**: surviving = merge-free n=4 across seeds+devices ({+2,-2,+1,+2}; 'birth as Hebbian MoE, ship as dense') + Mac gravmoe +5-6 at n=3 (device-scoped); retracted-to-noise = all tree/channel/capacity single-seed adjudications; treegrav made the phylogeny (0.9465 v 0.1046) and lost the gate (45) — correlation anatomy is cheap and worthless; a_i pinned 4th time. **FX-V2 CROSS-RUNTIME PASS** (axiom torch-free C++ twin, both P3 digests, 0.16s — determinism ladder COMPLETE: device->lab->runtime). IN FLIGHT: lambda-sweep on 3080 (~3:40 PM, read as trend); merge-recipe promotion (llmopt/ + axiom C++) gated behind it. INFRA: checkpoints/ gitignored + confirmed/ tree committed (manifest tool, pins verified); llmopt.runlog shipped; ff#10 booked (env-prefix expansion; markers now on arm exit codes). Machines open past 5 PM (Artin). Full day: handoff 2026-07-31-0.
Prior days (07-29/30) live in the handoffs (2026-07-29-*, 2026-07-30-0) and FINDINGS ('The full 30th'); highlights: packed-crystal C-series + capacity meter M; black-hole MoE atlas B0-B2 + expert-size law; K3-D1/D2 frontier-expert determinism; P3 deterministic decode; UMOE split-law + gravity afternoon; paper banked at prose-v1 (16 cites verified, MLSys). This board is the QUEUE. States: LIVE (running/next-action), STANDING (production state + doctrine), BANKED (awaiting GO or a prerequisite), CLOSED (verdict recorded).

## LIVE

| Thread | Status | Pointer |
|---|---|---|
| **DETERMINISTIC BIRTH (flagship)** | integer-closed at FULL-BLOCK scale, cross-lab: R1a/R1b (fwd/bwd) + R2 (IntAdamW) + R3a (Q_w pin: 8 mini / 12 full-block, starvation law at 3 scales) + R2b v1/v2 (rmsnorm/rope/CE/clamp-mask backward; fidelity floor 0.9985 accepted) — every rung C++-certified by axiom same-day (full birth 1.5 s/1000 steps, all milestone digests); **PROMOTED: llmopt/intmath.py (sha-pinned tables + step-equivalence tests)**; **SATURDAY MORNING 08-01: multi-block SHIPPED (2-block mini-LM, emb + TIED head, worst cos 0.988, digest-identical across two drivers; monotone hope denied at S12), MB-S14 closed the strictly-falling bar (width+decay monotone 8/8; width alone removes the blowup — S14 is the mb default), DIET-BRIDGE shipped (real gen-4 next-token CE, 15909->12518, plateau fenced; artifact carries the window tokens)**; axiom intbirth ENGINE + PRIMITIVES both PASS house-side (own driver, 8/8 digests; spec relay 2026-08-01-3 staged); NEXT: deterministic gravmoe pair (= transport answered exactly; needs a design pass), plateau-break scaling on the intbirth engine; night31b LIVE on 3080 via rjob (box back to Artin by 1 PM EST) | scratch/detbwd_mb.py, detbwd_diet.py; relays 2026-08-01-2/-3; specs/2026-08-01-deterministic-birth.md |
| **AXIOM LOOP (FX-V2 landed 07-31)** | determinism ladder COMPLETE (device->lab->RUNTIME: their torch-free C++ twin reproduces both P3 digests, 0.16s, d5e9d5a); rANS-unpack rider deferred (their call); **MERGED-CRYSTAL C++ PASS same-day (0104e1d): token-identical greedy both seeds, AXNN v1.2 switch_top1 gate, exporter their side, 481/481**; **FX-V3 PASS axiom-side (f9b0e04: two-runtime bit-identical, exp table BYTE-identical to house detbwd_r1b; house reproduction PASS both seeds FIRST RUN — merged crystal integer-closed at 3 implementations / 2 labs, gate spec transferred by prose alone)**; **R2 C++ PASS (c0ef854: 200-step integer birth trajectory-identical in a third runtime, 1.1 s; rdiv unified by proof)** — training determinism ladder = Mac-cpu/3080-cuda/axiom-C++; **FULL-BIRTH C++ PASS (f8aa16f, first run: all 8 milestone digests + final sha efe3557c, 1.5 s/1000 steps) — the complete block trains bit-identically cross-lab; rdiv-grouping hazard booked**; remaining rungs = SCALE (multi-block, real diet, deterministic gravmoe pair) + DEVICE (3080 on night31b GO); scorer AXNN export still banked | relay/2026-07-31-1 (theirs); RESULTS FX-V2 |
| **REVIVAL SWEEP (spec'd 2026-07-31)** | 15 old verdicts re-priced by the day's laws (reviewer-swept, Fable-verified line-by-line); Tier A desk cells first: deterministic gate POOLING (integer decode makes cross-device seed ladders legal), graph-mod free sigma, rotation-instrument positive control on the Mod crystal; G9 zeta-8-on-Mod = the causal-square completer | specs/2026-07-31-revival-sweep.md |
| **ORG/INDEX (live)** | results-index 537 entries + results_query; checkpoints/confirmed/ tree + MANIFEST (sha-pinned, curation-preserving, --all forensics mode); llmopt.runlog (elapsed-wallclock standard logging); 385 tests green; NEXT: checkpoint forensics + 51GB triage (Artin GO), scratch scripts adopt runlog opportunistically | scripts/ckpt_manifest.py; llmopt/runlog.py |

## STANDING (production state + doctrine)

| Thread | Status | Pointer |
|---|---|---|
| **PRODUCTION / THE CROWN** | **STATISTICAL TIE**: champion mathnative_gen6_grown 76/120 (fp32, 55.1M, cuda-verified) vs **merged_grown 75/120 (1.58-bit ternary, 89.7M)**; duo shard DELIVERED (in gen-9 diet); tiebreak = growth round 2, queued (evening-queue Tier-2 #9) | RESULTS 2026-07-23 |
| **METABOLIC v5 / THE EXCHANGE (PRODUCTION)** | exchange CONVERTS (2->6/12 in 10 min on 23 engine rows; self-practice was +1/150min); v5 s1: 6->9/14 incl BOTH p2 deep states; failed-step famine over (2,833 tagged rows); 100-wall head delivered to axiom; d2: exact-vs-fp64 BIT-IDENTICAL (tail 1.06e-14) — precision-capability CLOSED | RESULTS 2026-07-24 |
| **GEN-8: THE EVERYTHING-CRYSTAL** | one 19M vocab-41 crystal carries five grammars AT SPEC (series 99.2 BEATS specialist / energy 100.0 / poly 89.1 / math 64); UNION EQUATION v1: coefficient (i)~0 (union nearly free), a_bridge small+level-local (L4 fingerprint, door 27%, pre-reg bar FAIL); GEN-9 A/B BOOKED (07-24 eve): redistribution not a winner — leak DIRECTIONAL sub-noise (+3 rare+unseen at HALF dose; full-dose rerun queued behind miner v2), counterweight NOT OBSERVED, rations+continents PASS both arms (B series 99.2); 45M re-ask BOOKED: width does NOT pay for primitive federations (19M 119 v 45M 108 same-census; token-light rows keep W* at 19M); fp32 control READ 07-25: no bf16 debit at 45M/cuda (66=66); HOLD episode resolved same day — 19M-cuda-fp32 69 > 45M 66 same-device/precision, GEN-10 = 19M RE-BOOKED on direction (+3 marginal-real, sigma cells in flight); the -14 scare = my bare --fast re-enabling the known -10 packing hole (bf16-isolation cell queued); poly5 BOOKED (door fails at the coefficient, knock-4 pre-registered); desert v2 BOOKED (coefficient iv = 0); ODE probe VOID-BY-VOCAB | RESULTS 2026-07-24 |
| **THE EQUATION (flagship)** | geometry sector now UNIVERSAL at 3 grammars (kurt 1.9 matched-feeding: calculus/mechanics/programs — encodes FEEDING not content); capability counted in PRIMITIVES (decomposition discount MEASURED ~10x/row; primitives saturate at S_max=1); determinability law measured (96 v 47 within one model) | RESULTS 2026-07-22 |
| **PRECISION PROGRAM (closed at birth, open online)** | birth: BRACKETED bf16..fp64 indistinguishable; online: fp64 masters proven; **Ozaki arc RUN 2026-07-23**: error-free transform proven (CPU + cuda), int8-TC crossover (55ms exact beats fp64 accuracy; 21ms tri beats fp64 wall 2x), ZERO-rounding GPU matmul verified vs big-int, fp64-input GEMM at the dd floor (2^-107), **stay-in-RNS lazy pipeline: exact chains beat inexact fp64 past ~6 layers**; CAPABILITY LEG CLOSED (d2 bit-identity — exact ties fp64-masters; dd arm dropped); remaining = SPEED only (int8-exact wall, speculative-arithmetic router, fused kernel shipped) | RESULTS 2026-07-24 |
| **RARITY INSTRUMENT (adopted)** | every promotion reads the curve; duo wave = adopted farm sampler (133/200 equal-budget); gates stay single-model | specs/2026-07-22-duo-substrate.md |
| **RESOLUTION LAW (doctrine, adopted 07-31)** | 120-gate sigma~5 across SEEDS -> n=1 cells can't resolve <5-solve deltas; <1.5-sigma claims need n>=3 paired seeds. COMPANION (07-31 night): the gate is EXACTLY deterministic for FROZEN weights on mps — sigma is seed/training variance, never re-gate noise; gates print weights sha (provenance rule) | RESULTS 2026-07-31 |
| Speed doctrine (all shipped this week) | KV cache 3.4x; bf16 births default (--fast NEVER without --nopack — packing hole re-measured −13 at 19M); sigma_diff=sqrt(2)*sigma decision rule + >=5 single-seed bar; grad-ckpt kills 10GB OOM class; allocator tripwire doctrine (the 43x); gates ~2 min | RESULTS 2026-07-22/23 |

## CLOSED this week (see RESULTS for verdicts)

Newly moved from LIVE 2026-08-01 morning: SCAFFOLD PROGRAM (transport
NO — means 50.7=50.7, Mac-scoped, rescue framing; survives = merge-free
recipe promoted (hebbian_moe.py + complex_ffn.py) + Mac gravmoe
advantage device-scoped; GRAV-0T/REV: post-hoc gravity destructive
both ways — merge-free is a property OF the pulled basin; remaining
riders: night31b merges [HOLD], deterministic pair under the flagship);
FOURIER / CLOCK-PLACEMENT LAW (THEORY row; 4a dynamics booked;
force-the-clock arm banked); RESOLUTION LAW -> doctrine (now in
STANDING); RE-GATE SIGMA (gate EXACTLY deterministic on mps; 45-v-37
was checkpoint provenance -> gates now print weights sha).

Newly moved from LIVE 2026-07-31 (all booked, no in-flight work):
THE PINCER PROGRAM (7 cells; B-a demoted, rests on B-b; residue banked);
ZX CONTINENT (45M scale-lever null; next levers banked);
CALIBRATION PROGRAM (instrument shipped, levers closed; greedy-first
adoption cell banked); SYMMETRY PROGRAM (ladder complete through 8x +
S2 exact-at-2x; C16/packed-conv/3-tier banked); NIGHT-28 (all cells
booked); FORMAT LADDER (booked; THEORY row); EXACT STACK E-series
(E3 PASS 07-30; wave-model program banked).


Gen-7 (no-promote, ration lesson -> merged run applied it and won);
duo miner (shard delivered into gen-9 diet); fast trainer (shipped:
bf16 births default); GRPO frontier band (era closed — superseded
by metabolic/exchange loop);
ODE zero-diet probe (VOID BY VOCABULARY — vocab-41 can't spell the
grammar; strongest form of the bridge-law ~0); desert test v2
(honest stall, 128 proposals 0 valid, coefficient (iv) = 0, bridge
law graduated to strong form); graph-modularity on gen-8 (NULL at
bar, dQ +0.030; bar-based null, Q dispersion unmeasured);
streaming birth (CLOSED via full 2x2: epoch load-bearing at -8
best-case; cooldown +4 real; batch homogeneity -12 at single pass
= the interaction law; surprise-gating untested, lives in
metabolic); d256 substrate gate
(CLEARS -27 v -3: d256 = PILOT SUBSTRATE, promotion stays at W*;
packing = third starvation mode w/ L4 clade signature, packed
d256 lands on the d64 curve point); ZX desk gates (both PASS —
boundary-anchor serialization, atom set ~10-14);
L7 gap (0 real); gate-v2 batching (null; KV was the fix); shaped
GRPO size-Phi (null); chain-carry v1 (void, redo at full budget);
selection-overfitting story (retracted -> battery truth -> rarity
instrument); birth precision (bracketed, closed); 113M capacity
(closed earlier: territory is the Liebig factor).

## BANKED (awaiting GO or a prerequisite)

| Thread | Waiting on | Pointer |
|---|---|---|
| **NIGHT-31b CUDA QUEUE [DONE 08-01, booked]** | ran via rjob (first remote use; launch-detach bug caught+fixed); lambda-merge mechanism verdict + R0 TF32-off null booked Saturday afternoon — see header | jobs/night31b.log (3080); RESULTS LAM-MERGE-REVIEWS, R0-TF32OFF |
| Pincer residue: R8 re-run on axiom S7 enumeration + B-b spearhead | pincer revival GO | specs/2026-07-26-reverse-llmue-pincer.md |
| ZX next levers: diet quality / serialization / curriculum (width nulled) | ZX continent GO | RESULTS 2026-07-27 45M verdict |
| Exact-representations queue: GPTQ-rational, int8+outlier format, lattice-native template, RNS rung | exact-reps revival GO | specs/2026-07-27-exact-representations.md |
| S-tier scorer: AXNN + prompt spec export (feeds frontier_eval/gate_battery/E2); S3 ternary on harder needles | scorer tranche GO | RESULTS 2026-07-27 race verdict |
| Exact wave-model program (E3 arm done; exact-mode distribution readout; exact metabolic session = deterministic LLMUE) | Artin GO | RIFF exact-wave-model |
| Calibration residue: greedy-first adoption cell (12% tokens, farms/probes) | farm touchpoint | specs/2026-07-28-calibration-program.md |
| Symmetry residue: C16, packed conv forward, 3-tier nest | symmetry revival GO | specs/2026-07-28-symmetry-ladder.md |
| Checkpoint forensics + 51GB triage: sha-dedup both machines, autopsy name-twins, score-check v RESULTS | Artin GO | RIFF 2026-07-31 |
| FOURIER-4: clock-formation dynamics (probe during training) + force-the-clock arm (single-pass k=9 diet) | Fourier GO | handoff 2026-07-31-0 |
| NNUE homecoming: C++ ternary/micro-model inference (gates+mining 5-20x; ax::nn candidate; training stays torch by doctrine) | axiom C6 done; spec first | RIFF-LEDGER 2026-07-19 |
| Alphabet tournament (7 discrete weight sets + escape code; rotation-vs-resolution bit-matched; alphabet-follows-domain hypothesis) | persistence verdict + gen-6 | specs/2026-07-18-alphabet-tournament.md |
| Repo housekeeping (Artin 2026-07-16; TAXONOMY APPROVED 2026-07-24 — specs/2026-07-24-repo-taxonomy.md: program-based checkpoints/{engine,lora05b,micromodel,ternary,metabolic,continents,instruments} + data/{chains,continents,diets,exchange,qual,archive}; NEW gate item: gate_rarity census glob must go recursive + bins re-frozen in the same pass): organize scripts/ and data/ into typed subdirectories, update all stale references, verify every script still runs. NOT tidying — SURGERY: scripts cross-import via sys.path (`from bench_step_tokens import ...`), data paths are hardcoded globs in trainer/eval-guard/GRPO drivers, and the 3080 checkout must move in lockstep (hash-verified). Gate: pytest green + smoke-launch every entry-point script + both machines synced. POLICY (Artin): only Fable 5 changes code in this repo — helper agents (GPT sub-agents etc.) may move files/update references under supervision, but any bug found gets MENTIONED, not fixed; Fable verifies the whole pass regardless | a natural freeze point (not mid-sprint) | 2026-07-16 chat |
| Syndrome head | CLOSED — payoff 3 NULL (aux learns free, converts to nothing: 0.36 vs 0.42% validity); unified climb fold fails its gate; run 3 stays pure GRPO | RESULTS 2026-07-15 |
| Syndrome dynamics (child-syndrome world model, 1-ply lookahead at embedding cost) | syndrome head rung 2 | same spec, relations |
| Magic estimator revival on embedding features | if hardness economics return (budget alloc, skip sizing) | same spec, rung 3 |
| Dynamic MoE via magic router (per-QUESTION expert keep-set loaded from SSD; measured basis: router-stats keep-ladder now BOOKED on GT-1 (AMENDMENT CLIFF-DEBT-DISCHARGED 2026-08-04): 61.8% keep = full model, 45.3% crest beats full +14.7 pooled 6/6 seeds, collapse shoulder 33->25%; moe/ LRU offload cache exists) + prune-then-distill skill grab (domain keep-set as small teacher) | Artin 2026-07-14 GO-to-bank; pilot on Mac w/ Qwen3-30B-A3B after current tree | RESULTS MoE pruning + this chat |
| Representation stitching: TIER 1 PASSED all rungs (SmolLM2 beats native 91.6>90.5; bridge R~0.98; native probe reads bridged at 86.9% — same coordinates) -> next: (2) 30B-A3B keep-set runtime teacher, (3) GLM-class as OFFLINE geometry donor, HOUSE-TOOLS EDITION (Artin: use our recorded facts): zero-inference keep-set — train the weight-space reader (80.8/88.4 recipe, permutation-augmented) on router-stat-labeled experts from runnable MoEs, then READ GLM's experts off SSD to predict its math keep-set without ever running it; harvest prefill through kept experts only, or skip to weight2vec (reader embedding space as the shared geometry — change-of-basis from weights alone, no text/inference; moonshot, cheap falsification: hold-out-family transfer test) | tier 1 free/this week; tier 3 needs ~200GB SSD | RIFF-LEDGER + 2026-07-14 chat |
| ODE chains merge (data/ode_chains.jsonl, 317 pairs) | multi-domain round AFTER round-5 attribution is clean | 91fb39d |
| ODE atom-set decision (desk, free) + re-registered zero-diet probe: rides the FIRST ODE-vocab birth (VOCAB_EXTRA gains ODE atoms as series gained t) | ODE continent GO | RESULTS 2026-07-24 (probe VOID BY VOCABULARY) |
| **NATIVE TRANSFORMER — CLOSED 2026-07-25 (dies with a mechanism)** | rung 1 prefix NO-ADOPT (gate −3; literature 20-30% shortening measured −4.5%; int3 differential −8); emission wall NOTATION-INVARIANT (length-proxy, in-language) → forecloses rung-2 tree-PE too; parens = error-correcting scaffolding (gate −3 / quant −7 / Q4 30% parse-fail, one mechanism); teach-don't-impose confirmed at the representation layer; survivors: GCD-pays-iff-syntactic law, rung-3 attention-init (independent, still BANKED) | RESULTS 2026-07-25 dawn |
| **THE EVENING QUEUE (spec'd)** | Tier 1 fixed chain + Tier 2 ranked revivals (boundary-or-bulk FIRST per Artin's standing 07-22 call; chain-carry redo; wave-contrast; template v2; k(W) slots; HAWQ-rarity; notation A/B; deterministic gate; crown tiebreak) + Tier 3 axiom asks (fuzz-the-oracle, magic boards r2, Fourier) | specs/2026-07-24-evening-queue.md |
| Closed-system weight anatomy (post-GRPO-runs): layerwise dW mass RL-vs-SFT, CKA before/after, layer-sweep probes on climbed model, weight-reader SFT-vs-RL classifier (Artin: do closed-system weights LOOK different? are thinking layers forming?) | GRPO runs complete + adapter drawer as dataset | 2026-07-14 evening |
| Curriculum ascent (L6-8 enter the frontier band as the climb raises solve rates; engine chains ready there) | GRPO run 2+ gate readings | step-grpo spec |
| Math-native micro-model (from Artin's purely-on-rule-bits push): from-scratch 10-50M, ~500-token math tokenizer (charset mask = the real vocab), trained PURELY on closed-system chains + GRPO — tests whether pretraining priors are load-bearing or drag (whisper says less than assumed); 20-100x sampling speed; unlimited engine-minted data; Mac-trainable | after run 3; spec before build | 2026-07-15 chat |
| Potential-shaped reward (reward = Phi(next)-Phi(cur), Phi from NNUE/HCE/magic — measured progress toward solved; kills stall-hacks structurally, Ng-shaping preserves optimal policy; the terrain answer to Artin's how-do-we-make-it-learn-properly) | run-4/5 candidate; the principled successor to distinct+cycle patches | 2026-07-15 Goodhart day |
| Rank-matched GRPO (r=4 vs r=16 A/B — the anatomy says the solution has stable rank ~4; capacity beyond it may be dead weight) | run-4 knob, one flag | anatomy RESULTS 2026-07-15 |
| ES-LoRA verified climb (Artin: LoRA through verified hill climb) — gradient-FREE: rank-1 perturbations kept iff probe-batch validity improves; no backward, no optimizer, runs on inference-only backends (Mac/MLX could climb natively); bet: search cost scales with the whisper's tiny solution manifold, not the model | spec + bench after run 3; ES sample-hunger vs oracle cost is the pre-registered risk | 2026-07-15 chat |
| Fused-quotient residue (L8's last 3) | rule synthesis design; the step-model may reach it first | RESULTS "L8 autopsy" |
| LLM rule synthesis (heurisch-as-teacher) | frontier targets exist now | RIFF-LEDGER |
| Latent-between-anchors (COCONUT-shaped) | step-model maturity | RIFF-LEDGER |
| Temporal-pincer verification | backward-LM training run | memory + RIFF-LEDGER |
| Reformulation ensemble | LLM eval harness slot | RIFF-LEDGER |
| Basis proposer (orbital selection net) | i_linear_basis failure data at L8+ | queued-workspace memory |
| Parallel leaf closing (fork-pool) | heurisch fire-rate check | queued-workspace memory |
| Flash prefill port + tile autotuning | revives config estimator (needs a config space with variance) | kernels/metal.py docstring |
| Engine-regret hook in mining farms | throughput workloads only (2.1x); NOT label farms | RESULTS "Engine-level regret" |
| ZX syndrome-policy port | judgment-stack recipe on ZX engine | zx memory |
| L9 / adversarial generation | if L8 saturates post-orbitals (37/40 now) | RESULTS "L8" |
| Feynman-integral engine (IBP reduction — particle physics as a rewrite system: states = loop integrals, moves = IBP identities, oracle = numeric evaluation at kinematic points; a real bottleneck in real physics and nearly move-for-move our calculus engine) | far-future continent; charter-clean physics | Artin's electrons/quarks question 2026-07-15 |
| ODE engine (physics rung, zero chemistry) | the where-next map's first new domain: oracle = substitute-back (identically zero), ansatz rules generalize, dimensional analysis = free syndromes; precedent FermiNet (NN + Hamiltonian oracle -> atoms ab initio) | Artin's periodic-table riff, 2026-07-12 |

## CLOSED (verdict on the books)

| Thread | Verdict |
|---|---|
| Predicted syndromes | 3-round arc: structural NULL x2, then Artin's derivability re-aim PASSED — 0.5B embeddings 87.7%/0.975, i_apart 0.02->0.98 R, hard roots beat easy; adoption gate = hints A/B in solve_chain (BANKED below) |
| Population training (K LoRA, one base, batched) | NULL — MLX 0.5B saturates at one adapter's batch (1250 tok/s flat), 1.04x @ corpus shapes, 0.62x big; machinery (exact K-sequential equivalence) banks for tiny-net populations |
| Derivation-expanded chains + syndrome prompting | SHIPPED into rounds 2/3 (Think + Hints fields) |
| Reverse engine | SHIPPED — answer-side chains (492), the corpus factory's decompression stroke |
| Grammar-constrained decoding | NULL for the adapter (token-identical rerun; misses are semantic); mask kept for base-model prose |
| Ansatz-structure search (VGE rung 2/2b) | 2 fails vs hand design; evolution reinvented HVA alternation; phase-reading confirmed qualitatively |
| Token-level expert-iter round 1 | superseded by rounds 2/3 (reverse-engine diet) |
| Token-level regret | NULL — probe real (AUC 0.914), nothing to recover at ~25-tok traces |
| Config estimator (int4 GEMV) | NULL — 6-config space flat; revisit at tile autotuning |
| Dispatcher v4 | NO-ADOPT — v3 112 v 110; arm spread collapsed to 110-112/120 |
| Engine-level regret | pre-registered bar FAIL, throughput metric 2.1x — workload-dependent, not default |
| Three-lane quant race | awq_lite wins function-space (8.07%); kernel carries it |
| int4 dequant-GEMV kernel | 1.11x vs mx_q4 at D=4096, loses D=896 — honest split |
| symengine swap, propose_k=4, entropy beam, budget alloc | NULLs, see RESULTS |
| Dispatcher v1/v2/v3 lineage | v3 production (114/120 @ 370s) |
| L5/L6/L7/L8 ladder | 100% / 59/60 / 56/60 / 37/40 — engine leads sympy everywhere |

## Meta-pattern (named 2026-07-12)

**Prediction pays only where variance lives.** Four judges starved in
one day by the engine improving under them (estimator cost-rho, token
regret, config estimator, dispatcher arms). Before building a judge,
measure the spread it would judge.

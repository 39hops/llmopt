# Handoff 2026-07-28-1: night-28 complete (both machines, 10 cells, all booked)

Overnight autonomy session (Artin GO, no pings). Every cell
pre-registered before firing; every verdict booked on landing;
BOARD + FINDINGS current. RESULTS tail 2026-07-28 holds the full
sequence.

## The night's verdicts, one line each
1. d2 endpoints: DEPLOYED-FUNCTION-IDENTICAL proven element-wise
   (321 latent last-bit casts @3.7e-9, zero deployed sign cells,
   probe fingerprints equal) — exact wave-model rung (ii) DONE.
2. Crown re-baseline 76 EXACT — zero production drift; audit closed.
3. Successors bridge (IV5, built+deployed Mac): soundness 200/200,
   set-parity fails by algebra normal forms (all named) — SCOPED
   ADOPTION: axiom = default enumerator for soundness-consumers
   (3.8x, deadline_ms); house successors = semantic reference;
   sympy = oracle-of-record at final verify.
4. Greedy-first: NO-ADOPT (74 v 75 at 71% tokens; bar 40% —
   per-ply wave-retry eats the saving); ladder-retry banked.
5. Muon-3ep: 43 — gate-toxic at standard schedule too; regime
   hypothesis dead; CE-gate dissociation x3 = standing instrument.
6. revpairs-10%: 55 (neutral) — the direction tax has a safe dose.
7. Z[i] born-rational: 65 latent = 65 deployed — ZERO deploy tax,
   parity-class; exact-format family now spans R and C.
8. S4/M4-6ep: 67/71 — zero premium 4 (grew from 3, still sub-bar);
   zero-law stays bounded; dose-confound fence on M4-71-v-fp32-64.
9. P2@45M-6ep: 72 (at-or-above class, +3 sub-bar) — bracket stays
   closed; deploy tax ZERO (72=72).
10. flips-vs-Q fingerprint: crossings exist (curve = real
    instrument); muon anomaly structural (robust-but-wrong).

## Candidate law banked
Lattice-geometry-dependent deploy tax: magnitude/rational STE
lattices converge latents fully (P2 72=72, ZI 65=65); phase
lattice pays -4 (G5, twice). n=2 v n=1.

## Open / next
- Axiom: house owes E2 (S2 AXNN + prompt spec); their ODE tranche
  next their side. Farmers migrate to ax.successors at next touch.
- Banked cells: ladder-retry decode; Muon-as-robustness-
  regularizer; Q8 column as deployment-format dial.
- Scar for the file: zsh doesn't word-split unquoted vars — the
  first flips-vs-Q loop mangled args (caught, rerun clean).
- 3080 open until ~5PM EST 07-29; no jobs queued on it now.

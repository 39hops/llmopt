# Handoff 2026-08-04-2: the exclusion arc + the churn judge

Session: Fable seat, Mac, evening continuation of 2026-08-04-1.
Everything booked and pushed through VERDICT CHURN-JUDGE-1
(3cf9f79). One kernel panic mid-afternoon (Fable's error, memory
rule saved: ONE resident 30B, check rjob status before any
model-loading command, smokes BEFORE long jobs never beside).

## The evening arc, compressed

1. AMENDMENT GT2-REVIEW (reviewer pass, verified line-by-line):
   "functional not topical" DEMOTED (diagnose checker passes any
   'compile' sentence; the gate had ZERO strict-variant tasks —
   worse than the reviewer claimed); sigmoid restated ("an
   intermediate regime exists"); D4-PHYS re-fenced UNDERPOWERED;
   D0 booked; receipt hazards fixed (RULE echoed, self-recall
   renamed); THEORY row added (domain-coalition law); index
   links-drop bug found (gen_results_index preserves threads,
   drops links — restored, watch it).
2. AMENDMENT GT2-EXCLUSION (survey seat tension -> Fable desk):
   physics mask NOT inert (churn overlap 54-68%, different solver,
   zero net); the transport null was CONFOUNDED — math crest mask
   is POOLED-log-built (covers 88.1% of decode demand), physics
   mask was DECODE-built (99.0%): two variables changed at once.
   Math exclusion table: interior peak ~10% excluded -> +22.
3. VERDICT MOE-GT-2-D4-PHYS-B: the rescue arm (physics masked
   exactly the math way, pooled log) CRATERS — 10/16/9 vs full
   42/28/24, pooled -59, 3/3 negative. Exclusion-mass KILLED as
   mechanism. THE MATCHED-INSTRUMENT DISSOCIATION is the day's
   deepest finding: open recall 0.9013 vs math's 0.9014, closed
   0.884 vs 0.887 — identical readouts, opposite sign (+22 / -59).
   Coverage and recall do not predict the SIGN of the capability
   effect; WHAT the excluded experts compute is the variable.
   Math-only crest now stands confound-free (both mask recipes
   fail on mechanics). Code second crest: both predictors null,
   deprioritized (code decode-exclusion is 1.5%).
4. PRE-REG + VERDICT CHURN-JUDGE-1 (the bank's spend condition
   honored): policy = crest-default + judge-flagged escalation to
   full. Ceiling: union 703/840 vs crest 570 (+133) across 7
   paired seeds. Instrument: per-problem closed recall / parse
   success / gen length in perprob rows (c04098b), certified
   non-invasive (7 reruns reproduce booked totals AND 0/840
   per-problem mismatches). RESULT: AUC bar FIRES (0.679 held-out
   vs 0.60; level-only null 0.438), spend bar MISSES (1.32x vs
   1.5x at F=33%). Mechanism: rescuable failures are MASK-CAUSED
   (low per-problem recall + short completions predict rescue).
   Revive-if banked: routing-margin features (TRAJ v2 has scores)
   as a NEW pre-reg only.

## The program's standing one-liner

Coalitions are real, domain-organized, functionally specific in a
way coverage/recall metrics cannot see; the beats-full crest is
math-only (confound-free); crest failures are mask-caused and
detectable oracle-free (AUC 0.679) but the registered escalation
budget underpays (1.32x).

## Instruments (all committed)

- scratch/moe_gt1.py: TRAJ v2 (phase + scores recorded), KINDS/
  LEVELS/SEED/OUT knobs.
- scratch/moe_gt1_arm2.py: ARM0/KINDS/LEVELS knobs, RULE echoed,
  per-problem judge features.
- scratch/gt2_code_arm0.py: code arm-0 + masked cross-arm mode
  (receipt names self-recall correctly).
- scratch/gt2_jaccard.py: re-derives every booked coalition number
  (GATE_ONLY exposes the D2/D3 filter split).
- Decode demand JSONs (untracked): checkpoints/gt2_{math,phys,
  code}_arm0_decode.json.
- KNOWN FLAW for any future code gate: stratify diagnose variants
  (all 20 gate slots were the lenient clean-variant checker).

## Queue

1. PLACE-1 rung 2: traffic counter + NE=128 + three named arms
   (LRU / demand-template-pin / conditional-prefetch), pre-reg
   before running.
2. Routing-bits ledger (desk; TRAJ v2 scores unlock margins).
3. Crest-as-data-generator (math-only, sequence form).
4. "What do the math-excluded experts compute" — per-level anatomy
   of crest gains; possibly single-expert restore arms (the 6/layer
   delta from crest to 50% is a named perturbation set).
5. Hygiene: ENTROPY-LICENSE-1 decode recount; GRAVMOE 1/8-vs-2/8
   attribution; gen_results_index links-drop bug.
6. Axiom relay 2026-08-04-0 delivery (Artin carries).

## Machines

Mac idle, all rjobs DONE, tree clean. 3080 untouched since morning
(Artin's GO window closed at ~5PM EST).

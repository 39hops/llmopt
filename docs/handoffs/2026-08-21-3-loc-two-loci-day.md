# Handoff 2026-08-21-3: the two-loci day — GRF convergence demoted, ROUTE-TIME priced honest, MED retired on its own gates, LOC lands the crest as a difficulty-dependent conjunction

Seat: Fable 5 on the Mac, Artin home, GPT reviews relayed all day
and every point verified in-house before adoption. HEAD at close =
this handoff commit; Mac idle, 3080 idle, nothing armed, no
watchers live.

## What landed since -2 (with shas)

- AMENDMENT GRF-NOTHINK-0-WINDOWS (688d3079): rider2's cumulative
  first-N reading demoted — equal-width windows on the fixed n=99
  cohort RETRACT "converges" (thinking topic contrast peaks w9:16
  then declines); surviving sample-matched fact = first-window
  regime difference (thinking near-zero 0.0047, LOPO 0.253 v
  0.125 chance; answering 0.1331/0.626). 32/40 MCQ extractor
  audit clean (all "X)" restatements, no article-A false
  positives).
- PRE-REG + VERDICT ROUTE-TIME-0 (320c53ea driver; booked
  27381cf6) + AMENDMENT -ISOLATION (f671951a): temporal structure
  beyond age is real offline (median +0.041 AUC at H=8) and buys
  NO memory tier (closed loop 0/6 v LRU@K48; worse than LRU@K32
  on 5/6). Outside audit found the holdout stream-entangled and
  the loop evicting current-token experts; the isolated+protected
  rerun reproduced both readings (+0.0406 v +0.0407) — defects
  real, not load-bearing. "Decayed frequency" = count-saturation
  (not time decay); beyond-popularity signal largest at H<=4;
  prefetch demoted to hypothesis. VICTIM-TIME objective named.
- EX6-MED-0 arc, RETIRED: prereg e46d22a3 + AMENDMENT -SEMANTICS
  (16992157, pre-read: measured mlx_lm semantics — the prompt
  batch routes all prompt tokens and produces z1's logits; the
  first T=1 call routes GENERATED z1; historical EX6-PROMPT =
  prompt-batch mask + z1-routing mask, EX6-DECODE starts at
  token-2) + OBSERVATION -ZCENSUS (D1=111/360 outcome-blind,
  prior band 30-90 missed high; tracked receipt) + AMENDMENT
  -QUALFAIL (ee22b0a1, run 1: 12/240 diagonal divergences,
  appended-position seam) + AMENDMENT -QUALFAIL-2 (9bc1ebaa,
  run 2 stepwise: 11/240, cross-schedule near-tie sensitivity;
  watcher unblinding disclosed). Two fail-closed gates fired
  exactly as registered; no crossed cell ever produced a verdict.
- PRE-REG + VERDICT EX6-LOC-0 (f343449e driver; booked ed40cea4)
  + AMENDMENTS -ADJUDICATION (2039d490) and -LEVELS (addbcfb1):
  the canonical mechanism state below.
- RIFF banks: Collatz/rewrite-dynamics (53d2ca44 + refinements
  3c1d15dc, 5555377e: three-layer relation/policy/evaluator
  split, parity-as-local-predicate, four-gate perfectibility
  frame) and ABSORBED-INFORMATION COLLAPSE factorial (adaeda4f +
  correction 5555377e-adjacent: two-axis mechanism measured
  [absorption law LR*|g|/ulp(w)], diet the independent third
  axis, joint collapse test = the unrun delta).

## CANONICAL MECHANISM STATE (read this, not older wording)

- EX6 historical PROMPT arm: +47 pooled (NONE 191 -> PROMPT 238),
  under CORRECTED physical semantics (prompt-batch mask + z1-
  routing mask; "prompt_tail" label = generated z1's routing).
- EX6-MED: RETIRED after two qualification failures. The crossed
  cells provide NO verdict, ever; run-2 cells additionally
  analyst-unblinded. Do not resurrect.
- EX6-LOC (verdict-class, all six anchors cell-exact):
  PREFILL +13, TOKEN1 +21, interaction +13 (13+21=34<47), house
  prior wrong 3/3.
- TOKEN1 = ONE temporal position masked across ALL 48 MoE layers
  (never "one routing decision").
- Difficulty split: L1/L2 conjunctive (interaction +11/+7), L3
  redundant/sub-additive (-5); rescue typology joint-only 8->7->4
  falling, both-singles 3->4->7 rising; PROMPT keeps only 3/7 of
  L3's token1-only rescues.
- Attention/KV propagation of z1's healed routing remains
  HYPOTHESIS — LOC establishes intervention effects, not the
  mediator.

## Invalidated / tainted paths (do not resurrect)

- EX6-MED forcing harnesses (ex6med.py cells pass, ex6med2.py):
  qualification-failed instruments. Run-1 cells sealed unread;
  run-2 cells seen (watcher flaw, disclosed) — observation-class
  at best, taint named in -QUALFAIL-2.
- Stale prompt_tail semantics ("last prompt token" /
  "predicts the first token"): superseded by -SEMANTICS; the
  frozen ex6_phase.py docstring is known-wrong and kept only as
  frozen evidence.
- GRF rider2 cumulative-N horizon curves: descriptive only;
  "converges" is deny-listed.
- ROUTE-TIME-0 original run's "held-out" wording: stream-
  entangled; the -ISOLATION rerun is the clean reference.
- "grows with H" (beyond-frequency AUC): deny-listed; U-shaped,
  largest at H<=4.

## Next session — ranked unfired causal residue

1. Isolated temporal masks z1 v z2 v z3 (is the launch step
   special, or is every early token worth ~+20?).
2. z1 depth masks early/mid/late layer bands (where along 48
   layers does z1-routing healing live?).
3. Keepset transport (does TOKEN1's +21 survive a different
   deletion family?).
4. Cumulative dose curves only after 1-3.
Separate banked THEORY threads (not in this queue): absorbed-
information collapse factorial (/desk first), Collatz length/AST
census (desk), MAGIC-TIME variance census, VICTIM-TIME objective,
GRF depth rung, answer-identity registration (32/40 ready),
ALTTOKEN park-v-tri-tie, out-of-core preflights, NInfer survey.

## Open decisions for Artin

1. Which of ranked residues 1-3 fires first next session (house
   suggests 1, cheapest discriminator: 3 new arms x 3 seeds).
2. Absorbed-information factorial: /desk price it, or leave
   banked.
3. Tonight's 3080 slot: NVMe->host->pinned-H2D measurement
   (standing candidate) GO/no.

## Standing

Suite green at close (see closing report); receipts lock
regenerated (rider.json tracked-metadata staleness fixed
post-addbcfb1); both checkouts at origin/main; nothing
measured-unbooked. Day totals since -2: 2 verdicts (ROUTE-TIME-0,
EX6-LOC-0), 1 observation, 7 amendments, 3 pre-regs, 2 new riff
banks + 4 in-place corrections — with the review loop catching
two real holdout/eviction defects (not load-bearing), two
instrument failures (fail-closed as designed), and three wrong
house priors, all booked.

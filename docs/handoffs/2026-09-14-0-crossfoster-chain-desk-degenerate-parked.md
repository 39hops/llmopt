# Handoff 2026-09-14-0: CROSSFOSTER-1 chain desk booked CHAIN-DEGENERATE; the program is PARKED as sealed

Seat: Fable 5.1 on the Mac. HEAD at close: the commit carrying this
file. 3080 untouched. No live registered run. Nothing armed.

## What landed (after handoff 2026-09-13-0)

- Artin GO (13:25 EDT 09-13): VERIFIED-ENDOGENOUS-DATA-CROSSFOSTER-1-
  CHAIN-DESK, DESIGN + PREREG + ZERO-TRAINING CHAIN FEASIBILITY only,
  donors unchanged, the one-step -DONOR observation not reopened.
- Disjoint calibration (150 roots, smoke band) BEFORE sealing: 12-ply
  paired completion is inaccessible on the house root generator (0 of
  62 pairs reach the horizon, longest paired chain 4, median 1), so
  the GO's 500 x 12 target was abandoned pre-seal for a multi-ply law
  (retain pairs with min length >= 2; 24,000-root population; 6,000-
  row matched projection). The mps sampler measured bit-exact within
  and across processes (receipted probe), so the same-writer null was
  replaced by an in-run REPLAY-EXACT precondition on both donors.
- PRE-REG -CHAIN-DESK (RESULTS L72755, bdfedd99): one Opus prereg
  audit before sealing, two blockers folded (sample span 100M to 148M
  with the smoke band inside it, moved to 200M; inadequacy accounting
  by L_MIN). Instrument scratch/crossfoster_chain.py (+ launcher,
  determinism probe, 6 tests).
- Run crossfoster1c at 06612fc4 (17:58 UTC 09-13 to 12:32 UTC 09-14,
  18.5 h; Monitor as the wake signal, every 1,000 roots).
- VERDICT -CHAIN-DESK (RESULTS L72990, this commit): CHAIN-DEGENERATE.
  2,137 paired multi-ply chains from 22,283 processed roots; 772 of
  6,000 projected rows donor-specific on each side (12.9 %) against
  the sealed licensing bar of 3,000. Iteration is visible (20.4 % of
  pairs ever diverge; 162 / 214 / 57 / 3 new first divergences at
  plies 1..4; endpoint overlap 0.837) but chains end within 2 to 4
  verified steps on this generator, so it cannot compound further.
  Prior 7 hits 2 misses (family 42 / 24); misses: processed roots
  22,283 (range 12,000 to 20,000) and wall 18.5 h (range 6 to 14 h).
  Both auditors ran before booking (prereg: one blocker folded, an
  unreceipted percentage; receipt: no blocker, disclosures folded).
- Consequence as sealed: the program VERIFIED-ENDOGENOUS-DATA-
  CROSSFOSTER-1 is PARKED. Libraries frozen and tracked at
  data/crossfoster1_chain/ (1.9 MB each). chain.json (4.9 MB, the
  per-root chain log) force-added under the small-text-receipt
  exception, named in the entry for Artin's eye.

## Conditions that bite next session

- Nothing armed. The revival condition named (not banked) in the
  verdict: a root generator whose paired verified chains routinely
  reach 6 or more plies. A stronger donor pair and unmatched-dose
  libraries remain unmeasured and unlicensed under the sealed
  consequence.
- The foreign-writer program stays parked (unchanged).
- Disk about 31 GiB free. Smoke library directories deleted; their
  receipts carry the readouts.

## Open decisions for Artin

1. Whether to retire the CROSSFOSTER program or bank the deeper-chain
   generator as a future rung (new pre-reg either way).
2. chain.json at 4.9 MB force-added as a receipt: keep, or move the
   per-root log to an untracked sha-anchored stream.

## Next session: where to start

This handoff, BOARD line 5, RESULTS tail from L72990, the RIFF bank
COUPLED LEARNING DYNAMICS / ENDOGENOUS DATA (second measured anchor
added in place).

# Handoff 2026-08-03-0: the opus-5 merge (Fable seat, post-reset)

## What happened

Artin ran an Opus 5 seat on branch `opus-5` over the weekend (08-02/03)
while Fable limits were out. 32 commits, 31 ledger entries, three
self-initiated audit rounds (AUDIT-0802, RUNGD-0803, FINAL-0803 — 11 of
the 31 entries are corrections to the branch's own claims, including one
full retraction). At 7 AM reset Fable audited and merged.

## The merge audit (VERDICT MERGE-AUDIT-OPUS5)

Verified by EXECUTION: all 16 gravmoe pins sha-identical against the
branch's modified `detbwd_mb.py` + `reproduce.py` (the branch itself had
only verified 2); `BIRTH_SEED=1` in a polluted shell still reproduces
`c6766da2` (sanitizer works); 451 tests.
Verified by INDEPENDENT RECOMPUTATION: the D2 retraction (shuffled-bias
null excludes the same inputs as the trained bias — penalty 0->0.2R:
69/165/85% vs 65/115/80%); S0/census/rungd numbers against their logs.
Merged --no-ff; the correction trail is the record.

## Read first

`docs/opus/SESSION-NOTES-OPUS-5.md` — the branch's own audit sheet,
accurate after its final revision. Its "What the V4 programme actually
established" section + AMENDMENT FINAL-0803 are the V4 state of truth;
earlier V4 verdict prose overstates in places the amendments name.

## Adopted state (one line each)

- QK-SEED2/3: COND+QK gate direction [REPLICATED], 3/3 at three paired
  init draws; READING FENCE stands (TRAIN 2/8-3/8, HELDOUT dead — never
  "QK fixes free-run").
- DIET-COND-SEED: interior optimum's shape replicates, location does not.
- V4-Flash: format = K3's group-32 MXFP4; exact integer expert forward,
  one hash on cpu/mps/cuda; entropy coding = archive (8.3%, 38 MB/s);
  artifact 166.9 GB vs 31 GiB disk; always-on dense path 8.85 GB; router
  shared direction: geometry real, routing share bounded 7-21%,
  UNLOCATED (D2 retracted).
- Instruments: tests/test_docs_integrity.py; BIRTH_SEED matched pair.
- Fable-file fixes: pack_rans verify unconditional; offload.py docstring.

## Open / queued

- V4 spec (2026-08-02-v4flash-lossless-recode.md, v3 amended): M1 -> W1
  -> Q1 -> 13; F1 (real dense-path forward) scoped, NOT started — needs
  Indexer/Compressor/MLA/Sinkhorn to be right or hidden states are wrong.
- 296-entry curation backlog (ratchet 300); THEORY has no P6/V4 rows;
  relay 2026-08-02-1 lives only in ~/code/axiom/docs/relay/.
- BOARD/THEORY untouched by the branch by convention — BOARD updated in
  this handoff's commit; THEORY rows still owed for P6 + V4.

## Method law worth keeping

Every substantive error on the branch was a bound derived under an
assumption travelling without it ("27B by subtraction", "97% inert under
isotropy", "sqrt(d) input norm"). Three audit rounds of re-reading missed
them; independent re-derivation caught every one. Corollary the branch
proved the hard way: report every arm the script runs — D2's own null
arm was logged but unreported, and it falsified the headline.

# Handoff 2026-08-03-3: the GT-1 ladder day (evening session)

Session: Fable seat, Mac. Follows 2026-08-03-2 (F2 pair). Everything
below is booked in RESULTS and pushed; this is the narrative + resume
sheet.

## What ran

1. **VERDICT V4-F2c** booked (K-sweep finished overnight): recall
   1.99x at 2x K (sub-linear tail), the registered fp8 memory wall
   DID NOT FIRE — throughput is MISS-driven (73/189/14 misses at
   0.118/0.035/0.050 tok/s), fifth distinct attractor.
2. **MOE-GT-1, the whole programme in one evening** on
   mlx-community/Qwen3-30B-A3B-4bit fully resident (~4.5 min per
   120-gate run — the speed IS the methodology; every surprising
   reading was replicated or bisected the same night):
   - PRE-REG MOE-GT-1 -> VERDICT ARM0/1: routing tail top-25% carries
     74.9% mass (P1 fires 3x; V4's uniform doctrine is noaux-specific);
     oracle gate 64/120 {1:23,2:24,3:17}; first-touch order instrument
     shipped (scratch/moe_gt1.py); probe text coherent.
   - VERDICT ARM2 (scratch/moe_gt1_arm2.py: masked routing + dual
     recall): 50%: 78/120 (+14 vs full, unregistered direction);
     25% and 12.5%: 0/120 (dead, not degraded); text degenerates AT
     50% while the gate improves (dissociation); closed-vs-open gap
     monotone 0.013/0.077/0.126 (P2 missed at 25%, fired at 12.5%).
   - PRE-REG+VERDICT R1: bisection 40%: 57, 33%: 43 (alive) — graded
     slope; seed replication of the +14: fresh seeds +10/+3/+1 —
     pooled +4.7 < +7 bar, DIRECTION NOT CLAIMED; per-problem logs
     (Artin's ask) exposed CHURN: 41-47% of the gate flips both ways,
     solved-set overlap 61-68% — masked-50% is a different solver of
     similar strength.
   - PRE-REG+VERDICT R2 (Artin's fractions): pi% 31.4% = 30/120
     MID-BAND (the shoulder — cliff is not a phase edge);
     golden 38.2% = 53/120, exactly on the interpolation line.
   - PRE-REG+VERDICT R3 (Artin's golden-section search): 61.8% =
     63/120 ~= full's 64 — the ridge at 50% is real ON SEED 1234
     (beats both bracket ends by >= 7); height vs full remains
     sub-resolution across seeds.
   - AMENDMENT GT1-CORR (reviewer-verified, Fable-recomputed): churn
     LARGER than booked (41-47%, not "~25-40%"; overlap 61-68%, not
     ~3/4), R3 peak claim seed-scoped, ARM2 P3 hold half = MISS,
     slope figures need units, sign-test fence (3/3 is p=0.125),
     perprob smoke-row dedup note.

## Lean tier — armed overnight (WSL)

- Sidecar generated MAC-SIDE with axiom's c0511bc emitter:
  `data/lean_certs_500.jsonl` (443 emitted / 0 fenced) from
  `data/lean_corpus_in.jsonl` (500 rows, scratch/gen_lean_corpus.py).
  Smoke verified the CONTRACT live: sqrt(x)*sqrt(x)=x FENCED,
  exp-atom generalized to a fresh real variable.
- WSL job launched via wsl.sh (log `logs/lean_tier.log`, marker
  `/tmp/lean_tier.DONE` on success only): installs elan, fetches the
  pinned mathlib cache into scratch/leancheck, runs
  `scratch/lean_check.py data/lean_certs_500.jsonl`. Verified healthy
  at launch (toolchain v4.33.0-rc2 fetched, cache barrels flowing).
- ON RESUME: `scratch/wsl.sh tail logs/lean_tier.log 30`; book the
  verdict (statement-diff ok/skip/mismatch, kernel PASS/FAIL,
  ms/cert vs the 11 ms/row oracle). A failing cert on an EQUIVALENT
  verdict books as a JUDGE BUG on both ledgers before anything else
  moves (relay contract).

## Housekeeping

- Codex worktrees removed (3, all clean + merged, ~710 MB).
- Checkpoint sha-dedup audit: only 12 dup pairs (~65 MB), all
  intentional (confirmed/ archive) or script-referenced — nothing
  deleted; step_lora.pt == step_lora_grpo.pt bit-identical (noted).
- Riffs banked (docs/RIFF-LEDGER.md tail): expert itineraries,
  blackhole mode, house-born experts + emergent-experts rider,
  routing-entropy expert license, function-space verifier weights —
  each with measurable cells and killers.
- Reviewer seats self-reported Opus 4.5 under the Opus 5 label twice
  more tonight (now 4-5 total); disclosed each time.

## Morning queue (in order)

1. Book the Lean tier verdict from logs/lean_tier.log.
2. GRAVMOE seeds/windows ladder: write the fresh pre-reg (n>=3 paired
   seeds; scout notes scratch/detbwd_gravmoe.py knobs at
   E/LN/LD/COND:54/QK:61/TAU/K/STEPS:65/GATE:256/SS:275/EXPORT:797);
   the WIDER-DIET half requires a Fable edit to the SHA pins
   (_require_sha at :304-312) — split the rung.
3. Desk cells on already-collected arm-0 logs (no GPU): routing-
   entropy trajectory per prompt (blackhole-mode hook), per-expert
   selection entropy (the entropy-license fold candidates),
   domain-conditional demand (physics vs math corpus — needs one new
   arm-0 style run on a physics corpus).
4. Paired-seed pass at the GT-1 peak + next golden point (~45.3%)
   if the ridge question stays live.
5. Fillers (non-gating): tilelang py3.11 leg, RULER eval on 3080.

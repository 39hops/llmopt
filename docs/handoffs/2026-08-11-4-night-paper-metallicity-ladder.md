# Handoff 2026-08-11-4: paper assembled+audited, metallicity battery, division-algebra bank, GROW-DECOMP booked

Resume: this file -> RESULTS tail -> BOARD. Previous: -3.

## LIVE / PENDING
- **prereg-auditor agent on docs/paper/main.tex** — final claims pass
  was RUNNING at compact. Its report arrives as a task notification;
  verify findings line-by-line, fix blockers, recompile
  (`cd docs/paper && tectonic main.tex`), THEN the paper is
  presentable. Do not call it done before that pass is adopted.
- **3080**: METALLICITY-1 z0-v2/z2/z1 lanes running (z3 banked:
  0/6/15/14, w(z3)=56). Chained SOFT-PROMPT-1 fires on its DONE
  marker. Book both via /book when receipts land. Window to 17:00.
- **Mac**: rjob v4anat (128-expert V4-Flash anatomy via new
  lab.shards) running; receipts logs/v4flash_anatomy/ + lake
  weights.parquet.

## BOOKED TONIGHT
GROW-DECOMP-1 71/120 BETWEEN bars (premium 2-4 sub-sigma; it/s decay
RECOVERED in ep2 — probe arm (d) added); METALLICITY-1 pre-reg +
AMENDMENT (z0 char->token shuffle, driver crash class fixed);
SOFT-PROMPT-1 pre-reg (prior NO-FIRE); THEORY rows (merge-space,
loss-floor); wsl_guard SELF-MATCH narrowed (pgrep-only = read).

## PAPER STATE (docs/paper/)
main.tex compiles (tectonic). Built from: EDIT-ORDERS.md (4 DEAD +
30 FIX, 5-seat verify, spot-checked), new-sections-notes.md, 4 fig
PDFs (booked numbers only). Biggest catch: draft published the
RETRACTED sparse-assignment mechanism as a lesson — now
init-default-preserved (UMOE-2/L22732). Lloyd-Max cut (no receipt).

## BANKS
Division-algebra ladder + CORRECTION (dim-4 already measured; open =
born-quaternion + 1/2/4/8 ladder pre-reg); metallicity frame;
stellar-collapse frame; relay DRAFT (UNSENT, Artin holds):
2026-08-11-1 pybinding exposure ask.

## FENCES
LinkedIn cards: never the 2.4x number; old neurons-19m.png pixels
carry it — frozen, don't ship. lab.shards dequant = dual-copy guard
with k3_expert_demo. Lake has exploration-grade V4-Flash M rows
(1.957/1.960) — unbooked, cite as lead only.

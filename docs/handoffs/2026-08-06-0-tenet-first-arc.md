# Handoff 2026-08-06-0: the TENET battery's first full arc

Fresh session on the new CC binary (as -2 recommended). Read
BOARD's 08-06 block first; this file adds the connective tissue.

## The arc (all booked, linked, pushed; needs_link 0)

Instruments first, science second, and the instruments kept
catching things:

- D1 reverse gate: first cut was direction-BLIND (verify_wave's
  equivalence oracle is symmetric — a forward model scored 4/10).
  Caught in smoke BEFORE any arm fired; replay-mint criterion now
  (s must be a legal engine child of the candidate). AMENDMENT
  R0-REV-D1. Doctrine extension: the oracle accepts X<=>Y both
  ways — DIRECTION needs the mint, not the checker.
- D2 certified diets: exclude-union law FIRED on first full run —
  21/120 forward gate-band expressions live verbatim in gen-4
  (prompt-side, L3-heavy). 26 rows excised; STANDING FENCE booked
  for all gen-4-fed forward births (first-ply exposure, paired
  deltas unaffected). AMENDMENT R0-REV-D2.
- R0-rev: null on the registered gate (0/120) — but Artin's "why
  did it get nothing right?" autopsy found chain starts are OOD
  INPUTS for a reverse model (they occur only as reversed
  targets). AMENDMENT R0-REV-DIST; reopened on Artin's call.
- R0-REV-B (D1b poststep gate, pre-reg'd first): REV 24/120 v
  FWD 1/120. Backward emission is REAL and prompt-distribution-
  local. L4 0/24 anomaly named, open. FINDINGS carries the pair
  as ONE unsplittable bullet (grok's fence).
- D3 budget accountant: built + tested (the R8 overdraft shape now
  refuses; tests/test_budget_accountant.py).
- W1: 50/50 pair population (3080, rjob, zero failures), bridge
  built (gate-neuron tokenizer, resample augmentation), VERDICT
  NULL AGAINST W0's EXPECTATION — 10/20 exact chance AND train
  never fits; randinit control rider 20/20 proves the reader.
  Direction lives in function (24 v 1), not sampled gate geometry.

Battery scoreboard: W0 null, R0-rev null (scoped), R0-REV-B
positive, W1 null (hardened by control). Two honest kills, one
real capability, one genuine puzzle (function-weight dissociation).

## Open / next

- R1b-micro: BOTH gates satisfied (R0-REV-B + D3). Pre-reg needs:
  every wave through tenet_d3_budget.charge() with REAL token
  counts (never the char proxy), length-only ranker arm mandatory,
  alternation v independent v forward-only at matched ledger.
- GT-7: still owes the verbal-coverage re-derivation script BEFORE
  the pre-reg registers coverage as its variable (reviewer-scan
  gap; the 0.755 Spearman has no committed derivation).
- W1 follow-ups (unregistered candidates): attention/embedding
  feature readers; the scanner's cka.py as descriptive readout.
- llmopt.lab extraction: F1-F3 fixes DONE; module 1 (lab/oracle)
  next per the spec's order.
- L4 anomaly (0/24 in both D1b arms' rev readings): open, no
  mechanism claimed.

## State

Mac free. 3080: w1pop/w1class/w1ctrl all DONE rc=0 (window until
~17:00 EST). Diets on both machines, sha-verified identical.
Certified diets + W1 population manifest are untracked sidecars
(file-handoff convention); manifest lives on the 3080 at
data/w1_population_manifest.jsonl. Ledger current through the W1
rider; 455+ verdicts, needs_link 0; suite 457 passing (451 + D3's
6).

# Handoff 2026-07-28-3 — symmetry ladder complete (night)

Resume: BOARD -> this handoff -> RESULTS tail (07-28 S1-S4
entries) -> RIFF-LEDGER tail (banks).

## Landed since -28-2 (all pre-registered, all pushed)

- **SYMMETRY LADDER COMPLETE, S1-S4 in one sitting**: toll
  2x:-1 (complex/Z2), 4x:-4 (quaternion), 8x:-6 (circulant C8
  — dense gates retrofit into CONV structure, healing 2/120 ->
  59 in one warm epoch). Anti-mass null at every group; S2
  doubled model gates EXACTLY 65 (anti-mass < 1e-12 by
  theorem) — exactness endpoint measured. Holography edge is
  structure-dependent (Z2 49 vs complex 57 at equal mass);
  commutant stability weakens with group size (lambda=0 drift
  0.014 -> 0.136). Instruments: scratch/quat_commutant.py,
  quat_convert.py, sym_convert.py (generic group-average),
  complexify_control.py. THEORY row + FINDINGS section added.
- **Banked follow-ups** (RIFF-LEDGER): symmetry-at-birth flip
  test, C16, packed block-circulant forward,
  G5-on-rotational-substrate.
- **Optimal-stopping riff banked** (Artin, from SPY options):
  American pricing = greedy-vs-escalate; per-state escalation
  threshold (Snell envelope) as the ladder-retry revival path.
- **Index hygiene**: results-index.jsonl fully curated
  (needs_link 0; amendments carry amends= targets).

## In flight / waiting

- STABILITY ATLAS on 3080: healthy, ~cell 21/36 at 12:05,
  ~40min/LR-row; partial map SMOOTH + monotone (bs8 > bs64,
  rising through lr1.5e-3; lr0.0015_bs8 = 65 at d64!). Verdict
  books against pre-reg when logs/atlas_done.marker fires
  (waiter armed). 3080 window closes ~5PM EST 07-29.
- Artin's sends to axiom: Laplace draft (relay -28-3), E2
  delivery + AXNN v1.1 loader ask (relay -28-4). E3 fires on
  their loader.

## Next-queue candidates (no spec yet — pick at session start)

1. Symmetry-at-birth (the R1-null flip test) — cheapest new
   physics on the ladder.
2. Packed circulant forward — turn IMPLIED 8x compression into
   wall-clock (kin to kernels/ work).
3. Per-state escalation threshold (Snell) — decode-economics
   revival with calibration-program instruments.

# Handoff 2026-07-27-1: the verdict morning (night chain + S2 race all booked)

Resume: this file -> BOARD -> specs/2026-07-27-exact-stack.md +
specs/2026-07-27-calibrated-scorer.md -> RESULTS tail.

## Booked since -27-0 (all in RESULTS + index, pushed)

1. **ZX seed-2 28/120** — 8-pt swing fired the noise tripwire;
   then **seed-3 34/120** -> arm 36/28/34, sd ~4.2 = standing
   fence. Union ZX 40 = +1.8 sigma (suggestive only); **ZX-only
   control 35 = non-discriminating** (no transfer/recipe claim);
   union-tax-free holds on graphs. Column rests unless more
   union seeds or bigger effects arrive.
2. **S2 data-leg** (70% true-move confirmed; L4 value-yield 47%
   honest miss; cache de-censored 21184->16737) + **S2 RACE:
   dist WINS** (spearman .497 v length .399 v raw .309 v imit
   .092) — value labels buy rank, move labels buy argmax
   (S1's finding confirmed a second way). Winner =
   checkpoints/scorer_s2_dist.pt.
3. **SR-bf16 53/120: DEAD as implemented** (band 3 + two named
   confounds: resampled-jitter mechanism, 10-day pair age).
   fp32-only births remain doctrine; no follow-up queued.
4. **Axiom tranche 3** (frontier_eval 18ms / gate_battery
   78ms-probe / certify_tables; IV==3, 14 names) — gate wall
   dead; E3 = one call per side; exp-underflow fuzz trap banked.
5. **Friendly-fire #7** (shared /tmp/wsl_job.sh + lazy bash
   reads) — wsl.sh mktemps per launch; CLAUDE.md count = 7.

## Next (in order)

- **E2 export**: scorer_s2_dist.pt + pairs_3e -> AXNN + PROMPT
  SPEC (format + token map) — the artifact all three axiom tools
  await. Needs axiom's AXNN container spec house-side.
- E1 hash reproduction (endpoint trap first), E3 amendment arm
  (pre-reg before firing), E4 solve_batch parity, E5 Fourier
  adjudication (S2 done -> unblocked).
- S3 ternary cell on a HARDER needle battery (the S2 win's
  replication venue; current battery saturates calls-to-hit).
- Awaiting axiom's reply to relay -5 record (nothing new asked).

## Holds

knock-4, miner v2, adjudicator v3: [HOLD] on GO. SR: dead, not
held. 3080 idle as of this handoff.

# Handoff 2026-08-07-0 — the night queue (EX-ANAT-2 fire, plateau trilogy, W1-R close, replication chain)

Continues 2026-08-06-2-identity-day. Ten more bookings overnight,
both machines. Resume: BOARD 08-07 header -> this file -> RESULTS
tail from PRE-REG EX-ANAT-2 down.

## The night, in order

1. **EX-ANAT-2 (Mac, 24 arms): P-PORTABLE FIRES (+113 v 42, 4/6)**
   with the structure as the finding: carriers transform WEAK bases
   (+22/+39/+62 over matched-random controls) and add NOTHING to
   strong ones (saturation); identity-ADD arms are uniform while
   the random controls reproduce the GT-7 draw lottery — named
   carriers REMOVE the lottery. Rider answered: 1B's k=2
   compensation was GENUINE redundancy (highs hold at LOWER recall
   under top-2 removal with bottom-fill). Synthesis booked:
   carriers are portable, redundant-at-depth-2, saturating;
   residency implication = page the NAMED set, not ranked bulk.
   Design history honest in the ledger: v1 replace-with-bottom
   aborted on its own band assertion; k=4 REM would have been a
   GT-6 verbal-exclusion replay (caught by receipts).
2. **PLATEAU-BREAK trilogy (3080)**: const-lr 4000 steps REGRESSES
   past the 1000-step plateau (late-overshoot at diet scale — the
   MB-S14 decay law, under-carried by the design, named);
   NWIN 32/64 SATURATE (~14,680 both); SCHED rider BENDS the
   plateau (11,777, -5.9%) but misses the 10% break bar. Named
   next: the PARAMS LADDER on the intbirth engine (new pre-reg).
3. **TENET-W1-R (3080): both arms NULL** (aug 11/20, spectral
   10/20, train never fits). Cross-layer composition structure
   joins the surface-exhaustive negative. Reader mining CLOSED
   pending a hypothesis that names why its features could see what
   the cross-Gram cannot.
4. **DESK R-EMISSION-0 (free)**: emission/competence COUPLED across
   all 28 identity-era arms (empty high-parse/low-solve quadrant)
   — IDENTITY rung 3 deprioritized, a battery saved.
5. **EX1-C30 replication chain (Mac)**: seed 555 FIRES the
   third-seed bar (73 v 60, +13 — matching 1234's +13); seed 777
   decider lands +4, BELOW the fence -> UNRESOLVED as registered.
   Direction 3/3, pooled +30, mean +10, NO CLAIM (discovery-
   contaminated pool named; 1234 found the anomaly). Clean
   protocol banked behind Artin GO: 3 fresh seeds, pooled >= 21.
   Also booked: full-model seed spread 64/60/73 — mask-vs-full
   stays paired per seed, always.
6. **Lean Leg B: BLOCKED on axiom** (booked in the PLATEAU-BREAK
   pre-reg): the re-emitted 21,914-row sidecar does not exist；
   RELAY ITEM for Artin — request regeneration under the fixed
   emitter; the ~98.9% compile prediction awaits it.

## Instruments born tonight

- scratch/ex2_build.py (one-sided arms + matched controls; v1
  abort history in the docstring), detbwd_plateau.py (frozen-file
  knob-setter pattern), tenet_w1_relational.py (cross-layer
  alignment reader), ex1_full128.json (the paired-full keepset).

## Open GOs / next moves

- IDENTITY battery: EX-ANAT-3 candidates now grounded — carrier
  anatomy via the banked DeepSeek lenses on the NAMED top-4 sets
  (token-specialization via TRAJ, demand-rank signal); CHURN-
  JUDGE-2 second gate (routing-margin) still binds.
- Swap-crest fresh-seed protocol [Artin GO, ~1h Mac].
- Params ladder pre-reg (intbirth engine) [design ready, cheap].
- lab/gate -> sink -> timebox (instrument track).
- Lean sidecar relay [Artin carries].
- 3080 open until 17:00 EST; nothing queued there after W1-R.

## Cautions

- checkpoints/ex2_* now cited evidence (24 sets) + ex1_full128;
  builders are overwrite-guarded, keep it so.
- The c15_hi REM/REMC outlier pair (60 v 25) is single-arm weather
  — do not build on it without replication.
- Full-model baselines VARY by gate seed (64/60/73): never quote
  "the full model scores 64" unseeded again.

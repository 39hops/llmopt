# Handoff 2026-07-27-3: pre-compact (E4 fully closed, prior cell booked, 45M union in flight)

Resume: this -> BOARD -> RESULTS tail -> specs/2026-07-27-exact-stack.md.

## Since -27-2 (all booked + pushed)

1. E4 arc CLOSED: IV4 re-run (168/184; expired flag decomposes
   11 censored / 3 decided); axiom's 2 extras VERIFY on house
   oracle (house was the weaker search — no contradiction);
   re-draw on 277bc19 unchanged -> delta was the PRIOR (fence
   catch #2: config is instrument); prior cell BOOKED (3->0
   decided, no new misses, walls unmoved, +1 extra; prior =
   our bigram brain, sha cd60b1d1..., axiom db95c6c).
   Namespace split stands: walls + ply semantics + I-fence.
2. Substrate fence LIVE (relay -7): replays name file + rows +
   sha256 + full arm config; axiom READ-ONLY in llmopt tree;
   WSL artifacts may lag Mac. Axiom now pushes per-tranche.
3. My corrections booked: parsefail was I-atom (not Subs);
   axTrue/houseFalse reinterpreted as bounded-search asymmetry.

## IN FLIGHT (3080): 45M federation union

scratch/night_45m_union.sh RUNNING (launched ~10:30AM EST):
fp32 d512/12L/ffn2048/h8, union diet, seed 1, both gates
(gate_zx now takes ZX_D/ZX_LAYERS/ZX_HEADS/ZX_FFN envs).
Marker logs/night_45m_done.marker; Mac watcher polls 20min.
ON LANDING: book v pre-reg (RESULTS L~7560: math 66-69 =
union free at scale; ZX 37+ = clear of seed fence; fails
named there). ~4-6h expected.

## Next work (fresh session)

E2 exporter (scorer_s2_dist.pt + pairs_3e -> AXNN + prompt
spec + token map; read axiom include/ax/nn/model.hpp) -> E5
fourier regen (ONCE, distribute bytes) -> E1 (needs axiom's
container bytes — relay logistics) -> S3 needle battery.
Holds: knock-4, miner v2, adjudicator v3. ZX rests. SR dead.
Awaiting axiom on: prior-cell ack; next audit tranche =
walls/ply-rescore.

# Handoff 2026-07-29-4 — pre-compact (the delegation night)

Resume: BOARD -> this handoff -> spec 2026-07-30-escalation-
engine -> RESULTS tail (L9700+). Nothing in flight; Mac + 3080
BOTH open (Artin at work, ~5-6h window granted).

## Since handoff -3 (all pre-registered, all booked)
1. E3 PASS 50/50 hash-confirmed — E-SERIES CLOSED end-to-end.
2. Call-span pilot 500 received + receipt; end-value arms
   NEUTRAL (52 v 48); tokenizer template-atom fence found
   ("Hints: none" is ONE atom — amendment, format atoms added).
3. STEP-LOCAL DELEGATION WORKS: 59 v 44 (+15, ~3 sigma), zero
   added params — spans must answer the question being asked.
   Next rung: full-size tranche (awaiting axiom GO) -> LLMUE
   decode-side resolver.
4. ESCALATION 62/120 beats dense 57 — ladder converts tier
   diversity into capability; buys back the matryoshka tax.
   Predictive-routing bar: 62 @ 94%.
5. Reverse-self-learner riff banked (4-verdict lineage);
   FARMER PROBE = spec cell 6 — THE POST-COMPACT OPENER.
6. Black-hole riff RESOLVED with measurement: at 99% of the
   Gaussian rate-distortion bound (6.65 v 6.755 bits/wt);
   area-law maps to routing-as-boundary; THE DEPTH LADDER
   banked (LAYERS never varied all month — the untouched axis).

## Post-compact queue (in order; lossless speed defaults ALWAYS)
1. FARMER PROBE (cell 6): small reverse model (nxt->cur,
   gen-4) -> sample predecessors from gate-band answers ->
   verify by forward application -> verified-distinct-novel
   yield/1000 v sympy farm. Design for speed: reuse sym_birth
   with REV=full-reverse knob; KV-cached sampling; fork-boxed
   verify.
2. Leg C marginal-value ladder (3080, 6-8 births, per-level
   cuts) — can run CONCURRENT with 1 (different machines).
3. THE DEPTH LADDER (3080 after 2, or Mac): layers {4,8,12,16}
   at d56/f224 + a params-matched pair.
4. Head-tension 8-head cell; polar-split snap (desk fills).
5. Axiom: awaiting full-size step tranche; relay Artin carries.

## Fences that travel
47-atom callspan vocab order (sidecar); Q fences width-bound
(sigma law); escalation census format; PLAIN drift 48->44 =
backend noise class.

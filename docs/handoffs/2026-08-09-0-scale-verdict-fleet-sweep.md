# Handoff 2026-08-09-0 — ENGINE-SCALE verdicted, d768 nulled, fleet sweep

Resume: BOARD header (SUNDAY 08-09) -> this file -> RESULTS tail.

## Booked today (all pushed)
VERDICT REVIVE-D768-CROSSOVER (NULL: pooled -4 n=3 same-device —
the +7 crossover was confound-carried); VERDICT ENGINE-SCALE-1
(neither bar; plateau breaks from the small end 9,821 = 21.5%;
const-lr REGRESSES at horizon; THE SCHEDULE IS THE BINDER — next
lever = decay ladder); COUNTER-BOOK ENGINE-EXACT-1-RECEIPT +
AMENDMENT ENGINE-EXACT-1-DIGESTS (stdlib-dependent pins superseded
pre-burn; WSL counter-run green — receipts 4 reproductions deep);
PRE-REG EXACT1-SMALL (two-regime) + PRE-REG EX4-UNIF + AMENDMENT
EX4-UNIF-RIDERS (pre-fire: P-UNIF-HARM, dead-zone/split readings,
anchor tolerance); AMENDMENT BOUNDARY-OR-BULK-D768; RECEIPT
LEAN-300-LABELS. Six riff banks (hardness-head, Born-rule
normalizer, small-specialists, star-profile, iron-point x2 —
two-term law + which-penalty-binds rider adopted as doctrine).

## In flight
- Mac: rev3crown battery (crown-tie births, 6 cells ~2 nights;
  bars STAND as registered — the underpower alarm was refuted by
  sigma-class arithmetic, booked in EX4-UNIF-RIDERS; verdict
  wording notes carried there). Then EX4-UNIF (15 gates, ~70 min,
  GO'd, riders booked).
- WSL: EXACT1-SMALL d8 anchor mid step-3 gcd toll (d16 COMPLETE,
  all three bars fire cell-level; carry floor accumulates
  ~44/step). At d8 completion: book the two-cell verdict WITH the
  bar-language amendment (certified = all-four-arms dumped;
  exponent read alongside the 2x flag — d16 exponent 0.73 v
  diffusive 0.5; vacuous-truth + step-12 fallback defects named).
- Axiom side: ENGINE-SCALE-1 closed their end; ENGINE-EXACT-2
  design space = carry ladder (PQ x4/x16) x normalizer family
  (softmax-table / Born-rule / sparsemax), needs pre-reg; bias-
  correction bigint growth = contract question DEFERRED with
  tripwire at ~1e5+ step horizons.

## Fleet-sweep residue (5 Opus seats, ~75 findings, all adjudicated)
- ADOPTED: BOARD/FINDINGS/THEORY/CLAUDE.md updates, specs INDEX
  regen (81 rows, 12 [UNVERIFIED] tags to resolve at next
  housekeeping), index edges, gitignore hygiene, lake-manifest
  pinned, PDF re-rendered.
- FROZEN-DRIVER bug list (fix only on registered re-run):
  exact1_small_cells.py (stale-dump contamination on re-run;
  rung/anchor loop bound; narrow except; divergence pass not
  streamed); engine_scale_export.py (ambient env leak into
  children — set ALL knobs explicitly; manifest recomputed not
  measured; spec knobs field ignored); fig_magic_scatter.py
  (tie-blind spearman INHERITED from trainer — comparable to
  booked rho only; dropped zero-guard).
- OPEN [UNVERIFIED] specs rows: math-native twin resolution,
  rabbit-hole tunnels 2-5, complex-zx Leg A, morning-specs
  designs 2-3, step-expert-iteration/post-climb supersessions,
  axiom-backend scope, l9-territory, evening-queue drain,
  calibrated-scorer verdicts.
- AGENTS.md is gitignored (local seat file) — synced locally
  (SR-BF16 carve-out restored); remember it does not travel.

## Standing cautions
Crown-tie booking: scope tie language to "+-2.3 solves/seed
resolution"; n=2 fallback re-derives bars per-seed; note
single-seed dominance if signs ride one seed. EX4-UNIF anchors:
<=1-solve drift on <=1 cell -> one re-gate, both reads recorded.
Wall-clock in the v1 ENGINE-SCALE file = defect measurement
record only (mixed provenance). Reviewer cap now 5 (Artin
2026-08-09), Opus only, never Fable sub-agents.

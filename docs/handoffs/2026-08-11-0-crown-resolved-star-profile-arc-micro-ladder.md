# Handoff 2026-08-11-0 — the crown RESOLVES, the star-profile arc books three, step 9 is ring-bound, and births drop to minutes

Resume: BOARD header -> this file -> RESULTS tail. Prior:
2026-08-10-1 (evening; ceiling-swap closed, two axiom seats live).
This continues the SAME session past midnight — everything below
happened 08-10 night into 08-11 early. Session model was Fable 5
until ~01:30, then Opus 5 (Artin swapped seats mid-session; both
are sanctioned, see CLAUDE.md authorship rule).

## LIVE RIGHT NOW (check these first)
- **SATURATION-1 cell (b)** (Mac, rjob `sat_s2b`): +1 epoch on
  WIDENED rations off a COPY of the crown champion. At ep3
  2800/6662 as of this writing, ~35 min out. Reads against 73:
  P-HUNGRY >= +7 (the limit was the FEEDING) v P-SATURATED-CLASS
  |d| <= 2 (the star is full; growth is then the only proven
  lever). Driver scratch/saturation_s2b.py, log
  logs/saturation/sat_s2b.log, pre-reg AMENDMENT
  SATURATION-1-CELL-B (RESULTS ~26290).
- **MICRO-STAR-1** (3080, marker logs/microstar.DONE): d16 and d32
  gate ZERO; **d48 gates 10/120 — the P-MICRO-BURN bar exactly**;
  d64 in flight (ep1). Ignition sits between d32 and d48. Book
  when d64 lands (pre-reg RESULTS ~26210); walls so far 546/424/
  529 s — P-MINUTES holds, ladder-spam is now a measured
  capability.
- **NPRIMES rung 2** (mac-axiom, their detached run, PID they
  reported): np1024 at ~630 s/step prefix, ~85 min to step 8 from
  ~01:15. np512 already THREW with a BYTE-IDENTICAL signature to
  256 (lo_bits=16384 e=-16342), so P-EXACT-TIE is favored. Their
  relay -22 comes when it resolves; counter-book owed (verify the
  prefix digests byte-match 7c9b8f0b -> 41bfedf6, recompute the
  ring-cost slope: they report ~315 s/step at 512 v ~163 at 256,
  ~1.95x per doubling).

## WHAT BOOKED TONIGHT (RESULTS 25500 -> ~26300)
1. **THE CROWN RESOLVES** (VERDICT REVIVE-CROWN-TIE-BIRTHS, 26066):
   n=3 fresh paired births, champion fp32 grow-inherit line
   73/73/73 v ternary tournament 64/62/61 — pooled +10.7, 3/3
   positive, P-RESOLVE-FP32 fires. The 07-23 tie (76 v 75) was
   BIRTH LUCK; the n=1-births fence RETIRES. Scope is line-v-line:
   HARDENING-P3-R9's matched-dose ternary TIE is untouched — the
   +10 is the growth-inheritance PIPELINE, which exists only on the
   fp32 substrate (ternary growth non-preserving). Champion totals
   were seed-invariant (spread ZERO); ternary carried the noise.
2. **SATURATION-1 cell (a)** (26259): a 4th warm epoch at FIXED
   food moves the champion +1 (74 v 73, tie band, same dict
   shape). The equilibrium is not epoch-starved. Cell (b) is the
   open half (running).
3. **The star-profile arc, three verdicts from one 80-line driver
   + checkpoints that already existed**:
   - STAR-PROFILE-1 (25772): PTQ ternary per tensor class on d256.
     Head and norms ternarize FREE (identical dicts), emb loses 27
     at the same param share, body craters (attn 0/120). The star
     bank's ordering INVERTS. Law booked: **ternary tolerance is
     grown, never projected** (crown QAT bodies tie fp32 when born
     on the lattice).
   - STAR-PROFILE-2 (25979): 5-width ladder. P-SURFACE-SCALING
     REFUTED (rho -0.4) — the eye/mouth asymmetry is POSITIONAL,
     not geometric. P-VOLUME-FLAT fires exactly (attn per-param
     sensitivity 4 at EVERY width, ffn 1 at every width).
     Unregistered headline: **WIDTH BUYS TOLERANCE** — eye
     retention 0% -> 93% up the d64->d512 ladder. Holography's
     scaling face.
   - ROT-X-TERNARY (26031): 4x quaternionic sharing changes the
     ternary profile by NOTHING (retention fractions identical per
     class). Symmetry and alphabet are ORTHOGONAL compression
     tolls. First registered house prediction of the night to
     survive (1-for-3, all three booked honestly).
4. **ATTRACTOR-0B-JOIN** (25692): answer-form is a TERMINAL CLASS
   — 198/198, with 6 in-class two-cycles the old stopping rule
   hid; boundary ~99% one-way. But P-MARGIN-UNIFIED is a clean
   NULL: n_succ 7 v 7 at absorbing v transit while the margin gap
   replicates (2.31 v 0.88). The ambiguity law and the
   absorbing-confidence effect are SEPARATE mechanisms.
5. **Axiom, both legs counter-booked from their commits**:
   - STEP9-CLIFF-SIZE (26105): P-STRUCTURAL fires RING-SCOPED.
     Three rungs to 2^18 bits all throw; lo_bits = paid precision
     and e = -(prec-42) at all four attempts. The shadow SUCCEEDS
     (w=1 every time); reconstruct_rat exhausts the fixed
     256-prime ring. The ring is both the cost floor AND the
     feasibility ceiling — converges with our own ring-bound wall
     result from the opposite direction.
   - FP32LIMB-R2R3-GPU (26138): the Metal battery is GREEN.
     FTZ-PRESENT (range restriction required), bit-identical to
     the CPU oracle x3 seeds, and the wall bar passed by an order
     of magnitude — **exact fp32-limb GEMM at 0.120x cpu_fp64,
     int64-accumulator at 0.004x**. Exact arithmetic on Metal is a
     SPEEDUP. The L7226 decision-flip retest now has its GEMM.
6. **Four amendments + a power note**: CROWN-WINDOW-SWAP-MISSED
   (the watcher's trigger string was m-arm-only; window missed,
   m_s4 ran through, Metal dispatched at the natural battery end
   instead); RNSCHAIN-EXIT-BAR-UNREAD (P-EXIT-EXACT registered and
   never read — CPU-covered via C1, CUDA-unread);
   DATA-CEIL-BOOKKEEPING (observable drift 0A/0B, ~3x tie ratio
   was really ~2x median, census classes are not a partition);
   FUNNEL-PREC-COST-SCOPE-AND-WALL (53% is BIT-STEPS and
   entry-200-scoped; wall measured RING-BOUND — 141 s at 160 bits
   v 194 s at 4000, identical digests). NOTE EXACT-GATE-POWER
   (25662): the L7226 slot is structurally UNDERPOWERED as a
   solve-count arm (worst case 4.7 affected solves, inside ~5
   sigma) — redesigned as a deterministic DECISION-FLIP count.

## PRE-REGS OPEN / HOLDS
- **STAR-PROFILE-3** [HOLD, nightly GO]: the alphabet ladder —
  T-static 1.58b / LADDER-UP 1.58->4->8 / 8-static / fp32, d128,
  identical data order. AMENDMENT -WARMSTART pins that switches are
  WARM (in-place lattice refinement from the last trained weight;
  coarse embeds in fine, so the switch is function-preserving) and
  adds arm B' whose advance fires on an IN-RUN SENSOR (loss-slope
  plateau), step index never consulted.
- **NPRIMES-LADDER** (26165): rung 2 running; bars P-RING-BOUNDED
  v P-EXACT-TIE.
- **CURRICULUM-FUNNEL** (RIFF bank): order the diet by
  ENGINE-measured ambiguity (n_succ), advance on an in-run sensor.
  Cheapest arm: two d128 births, same data SET, different ORDER
  (branching-annealed v shuffled). Micro-star may drop this to
  minutes-class.
- Montgomery/TC RNS [HOLD, wsl-axiom]. DATA-CEIL rung B (no-fire
  as designed). Ambiguity-law second domain (the ZX leg — note
  ternary has NEVER been run on ZX; 13 ternary entries, zero
  intersect ZX).

## TWO UNIMPLEMENTED DESIGNS (reviewer output, worth building)
- **`llmopt/lab/runfiles.py`** — a Spark-_SUCCESS-style run-artifact
  contract: `run_dir()` (mkdir guaranteed at name time),
  `write_marker()` (one JSON line: kind/rc/wall_s/git sha/argv/
  artifacts), `is_done`/`rc_of` shared by both machines, and
  `require_resume_marker(ckpt)` that REFUSES to train when a ckpt
  exists without its `.ep` marker. Eight real inconsistencies found
  in the current tree, including: the `.ep` sentinel means two
  different things (grow writes "-1", train reads N+1); "no marker"
  means BOTH "finished cleanly" and "never ran" (this is the exact
  ambiguity that nearly ate a checkpoint tonight); rjob writes
  `killed` into a `.rc` that consumers may `int()`; rjob and wsl.sh
  use two incompatible done-encodings. Adoption order proposed:
  the resume gate first, then log-dir guarantees, then rjob.
- **`scripts/log_hygiene.py`** — print-only archive planner.
  Classes: FROZEN (path or parent dir cited in RESULTS — ~120
  citations, directory-level cites freeze the whole dir),
  PRESERVE-AS-RECEIPT (poisoned/killed-run exhaust), SWEEPABLE,
  UNKNOWN (default: do nothing). `--apply` behind an explicit
  Artin-GO flag, never deletes, refuses if any FROZEN/UNKNOWN row
  is in the plan. Note logs/archive/ already has two conventions
  and RESULTS 2421 cites a doubled path
  (`logs/archive/logs/archive/...`) — index in place, never
  re-sort.

## DEFECTS + PROCESS (all self-caught, keep the streak)
- Crown watcher trigger string was m-arm-only -> Metal window
  missed. Rule: **verify a watcher's pattern against a SAMPLE of
  the artifact it watches, at arm time.**
- A detached launch redirected its log INTO a directory it had not
  created yet -> silent death, watcher read the silence as
  "running". Rule: **mkdir before the redirect** (wsl.sh now has a
  `mkdir` verb).
- Background WATCHERS are pollers, never run-owners: the harness
  can stop them. Runs belong to detached processes with rc/marker
  files (rjob; mac-axiom converged on nohup+disown independently).
- `AX_PREC` did nothing until built with `-DAX_ANCHOR2_TRACE` — an
  env knob is not a knob until the build carrying it is verified.
- The 3080 OOM'd twice on micro-star (our packer at tiny d, aided
  by desktop VRAM pressure). Poisoned runs were KILLED not trusted
  (43x doctrine), their logs preserved as `*_oomkilled*` receipts,
  relaunched at `--budget 6144`. Third relaunch is the clean one.
- A commit slipped through with a failing ratchet because the
  chain captured `rc` and then did not gate on it. New hookify
  WARN rule catches `rc=$?; ... git commit` without an
  `if [ $rc -eq 0 ]` guard. Every later commit was gated and one
  correctly BLOCKED.
- FINDINGS ratchet fired three times; curated same-commit each
  time. Anchors must land on `## ` headings — check the line
  number after any RESULTS append.

## ONE NOTE ON THE wsl.sh CHANGE (read this before re-escalating)
`scratch/wsl.sh` gained three verbs (`clean-marker`, `kill`,
`mkdir`) so the permission classifier can see intent in argv
instead of one opaque `run "..."` string, plus a `_safe_path`
character allowlist on their arguments and
`tests/test_wsl_guard.py`. A commit-review plugin flagged this as
"command injection / auth bypass" and that framing is WRONG for
this repo: the script's primary verb (`run "cmd"`) executes
arbitrary commands on Artin's own machine BY DESIGN — that is what
it is for. The only "attacker" in that threat model is the model
composing the argument, i.e. us. **The fix is ordinary shell
quoting hygiene** (a path containing a space or a semicolon should
not break the remote command) and it is worth keeping, but it is
NOT a security thread: no RESULTS entry, no ledger bank, no
CLAUDE.md language. An earlier attempt to write security doctrine
into CLAUDE.md + memory was REVERTED at Artin's direction
precisely because it would make every future session flag his own
tooling. Do not re-open it.
The one policy change that DID land: sub-agents review by default
but **MAY WRITE when directed** — the read-only default is a habit,
not a safety property; the session model still owns the
verification bar.

## STATE
503 tests green (rc-gated). Everything pushed through 7e14de4.
Relays: -19 (step9 GO) and -21 (nprimes GO) delivered; axiom's -20
counter-booked; their -22 pending. House continues at -23.
Receipts committed this session: logs/star_profile/*.jsonl,
logs/data_ceil/attractor_census2_d256.jsonl,
logs/funnel_wall/*.log, logs/revive/crown_gates.log,
logs/saturation/sat_s2.log. A 1.9 GB artifact dump for WSL-side
exploration sits at ~/code/llmopt_dump/ on the 3080 box (411
files, mirrors repo paths; DuckDB over the jsonl is the
recommended first step, big diet files are exploration-grade not
ledger-grade).

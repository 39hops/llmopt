# Handoff 2026-07-27-5: the exact night's verdicts (late consolidation)

Supplements -27-4. All booked in RESULTS, BOARD current.

1. **Born-rational s1 WINS**: exact-lattice birth (RAT_Q=6 STE)
   deployed 65 v fp32 control 60 (+5 at the bar), latent 66,
   deploy tax ~1, +5 concentrated at L4. Seed-2 replication IN
   FLIGHT (control s2 = 62 booked; bar: rat-dep >= 65 confirms).
2. **Quick battery (union_45m, 3080)**: rational twin transports
   (65=65); INTEGER TWIN fq512 at parity (int16, max |p| 2031);
   rat16/fq128 did NOT crack — sensitivity wall is PER-CRYSTAL
   (near-tie density is a model property; 2 wrong predictions
   booked); repair void here (retarget = Mac-19M rat16 @ 26).
3. **The pi finding**: integer-twin marginals are max-entropy
   Gaussian — measured 6.65 bits/wt v log2(sigma*sqrt(2pi e)) =
   6.755 predicted. All structure is in the ARRANGEMENT.
   Corollary formats: int8+outliers (~42MB exact), calibration
   probe (flips/token under coarse snap).
4. **Axiom NT-chain tranche recorded** (relay -10): 6 families,
   720/720 certified, sha pinned; ODE engine next axiom session.
   House follow-ups: qual spot-check on file arrival, vocab
   atoms (gcd/Mod/**), CF-CRT = exact-stack infrastructure.
5. Riffs banked: gauge-vs-functional distance (lattice kills
   continuous gauge, permutations survive; 4-crystal measurement
   armed once s2 lands), universe=territory, Gaussian=rotation
   (Herschel-Maxwell), annealed snap, near-tie decode repair,
   Gaussian-integer weights (queued cell), L4/L7 diet + axiom-CPU.

## ON s2 LANDING: book v pre-reg (>=3 over 62 = CONFIRMS; [-2,2]
## = parity demotion; control win >= 3 = amend to fluctuation).
## Then: gauge-slack 4-crystal measurement (the falsifiable
## arrangement question) is the next cheap Mac read.

# Handoff 2026-08-14-1 — ATOM-DIET-1 fires both bars; axiom-first becomes law

Seat: Fable 5, Mac. HEAD at close: `a5ee7fd` (plus this commit).
Session: 08-14 afternoon/evening (post-compact continuation of the
-0 session's context). Mac free at close; 3080 untouched; nothing
armed, no watchers live. Supersedes -0 for session state.

## What landed (all pushed, suite green at close)

- **PRE-REG + VERDICT ATOM-DIET-1** (`7c3f544`, `4aa96fc`; code
  stamp `a5ee7fd`): the week's constructive headline — 6,000
  engine one-ply atoms (2,400 L4; rule-tagged; D2 + corpus-cur +
  dup guarded) shuffled into the stock stream at matched 15,420
  steps: **gate 73/120 v the booked 64 (+9, past resolution) and
  L4 12 v 7 (19M-lineage record). BOTH pre-registered bars
  FIRED**, each at a disclosed knife-edge (73 v 72; 12 at
  threshold). Flooding scar absent at 3.5% dose. House prior
  missed LOW on both legs (registered no-fire, 64-70/9-11) —
  family record 1 hit, 7 misses, first optimistic-direction miss.
  Fences carried: single seed n=1, ~0.8% control-horizon
  mismatch, farm-restart provenance, stock-recipe matching.
  Receipts logs/atomdiet1/{arms.jsonl, noop.log} force-added.
  Auditor pass: no blockers, 9 findings adopted.
- **Instruments** (`2c25da2`, fixes `9171070` + `1f91ca0`):
  scratch/farm_atoms.py (forked one-ply farm; in_language must
  CATCH MathTokenizer's out-of-vocab ValueError; rolling-pool
  scheduler ~18x over wave-join) and scratch/birth19m_atoms.py
  (thin sibling of frozen curric driver). Both now results-cited
  = FROZEN.
- **AXIOM-FIRST becomes law** (Artin, twice today): use axiom
  whenever machinery exists; relay only after surface + relay
  grep. Enforcement artifact **docs/AXIOM-SURFACE.md**
  (`db2bcc3`, updated `052949e`) — verified map of the 16-name
  axiom_sym IV5 surface (emit_chain's source=axiom-oneply IS the
  atom emitter we rebuilt), intbirth, 19 CLI tools, dual-.so
  pin-build-rel hazard, unbound relay candidates. Memory
  grep-relays-before-asking.md carries the rule cross-session.
- **Relay traffic** (all committed): -0 atom-farm ask +
  same-day CORRECTION (`f916f81`, `87caf69` — the ask was
  redundant, successors delivered 07-28); -1 surface-survey asks
  (`8aca210`: predecessors expired defect, GIT_SHA/BUILD_TIME
  provenance attrs, pyrand, count_ops, budget question; their
  three stale waits on us all shown closed); -2 INBOUND budget
  answer recorded (`f1ea010`: cooperative, bounded modulo poll
  gaps -> house fork-law SPLIT: bridge in-process for desk/gate,
  farms keep the fork wall); -3 GO both pyrand + count_ops,
  single-IV-bump packaging ask (`ff7b810`).
- Living docs: FINDINGS bullet (SINGLE-SEED; honesty ledger
  92->93, fence 233), RIFF atomic-op bank amended in place with
  the measured fire.

## Conditions that bite next session

- Curation ratchet at cap==backlog again: next booking needs its
  FINDINGS bullet same-commit.
- Newly FROZEN: scratch/farm_atoms.py, scratch/birth19m_atoms.py;
  receipts logs/atomdiet1/ — extend via siblings, never edit;
  never append runs into the frozen receipts.
- data/micromodel_atoms_shard0.jsonl is untracked evidence
  (file-handoff convention) — the verdict cites its stats; do not
  regenerate into the same path (seed band 71M is SPENT).
- checkpoints/gallery19m_atoms_s2.pt joins the exhaust pile
  (decision item now ~9.9GB).
- Axiom builds IN FLIGHT on Artin's GO: pyrand + count_ops
  bindings (+ predecessors expired fix + provenance attrs,
  bundled one IV bump). On landing: house counter-verify per
  relay -3's registered acceptance (bit-exact pyrand fixture from
  our string-seed shapes; count_ops element-wise v sympy). The
  dual-.so hazard stands until provenance attrs land — pin
  build-rel, record .so mtime/size in any axiom-citing booking.
- Artin granted mac-axiom MACHINE TIME on the Mac (they were
  build-only). House recommendation relayed: dose-ladder atom
  farm via their emit_chain, band 72M+.

## Next session, in order

1. This handoff -> BOARD -> RESULTS tail (resume protocol).
2. The fired bars' replication debt: **n>=3 seed ladder for
   ATOM-DIET-1** (BIRTH_SEED 3,4 arms of the same driver as
   siblings; both knife-edges need it before any strong claim).
3. Dose ladder (axiom emit_chain, per-rule targeted if the
   apply_rule build lands) and rule-ablated shards (drop
   i_heurisch mass: is the L4 move the recognition rules?) —
   each a new pre-reg.
4. Standing queue from -0 unchanged: L1,L2-first discriminator,
   watch-it-think flagship, EX4 demand-dose ladder,
   REPAIR-FLOOR-1, OVERLAP-BIRTH-1.

## Open Artin decisions

1. Phase C frozen-paths ruling (181 files) — unchanged.
2. Rebirth MEDIUM substitutions — unchanged.
3. Static-figure unpause + palette — unchanged.
4. Milestone exhaust keep-or-delete — now ~9.9GB.
5. Next pre-reg pick (house lean: the n>=3 atoms ladder — the
   week's biggest positive deserves its error bar first).
6. CHECKERS-1 disposition — unchanged (house lean: leave).
7. mac-axiom machine-time scope: dose-ladder farm size/cores.

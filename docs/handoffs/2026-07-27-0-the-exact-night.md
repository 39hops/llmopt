# Handoff 2026-07-27-0: the exact night (union booked, FX-V1 landed, S2 in flight)

Resume: this file -> BOARD -> specs/2026-07-27-calibrated-scorer.md
+ specs/2026-07-27-exact-stack.md -> RESULTS tail.

## Booked tonight (all in RESULTS + index, committed + pushed)

1. **FEDERATION union verdict**: math 66/120 (ties column best) +
   ZX 40/120 (above ALL comparators) — grammar CLASS does not
   bind at 19M. +4 v cplx_none NOT booked as union benefit
   (missing control + unknown sigma).
2. **Cache censorship AMENDMENT** (Artin's audit): wall-killed
   solves were fossilizing as permanent unknowns; both scripts
   fixed (censored rows never cache, never hit; wall recorded).
   PENDING one-shot: strip solved=null rows from
   data/value_cache.jsonl when the running farm completes
   (its loaded module predates the patch).
3. **Precision AMENDMENT**: doctrine holds ABOVE INSTRUMENT
   SIGMA (ladder flat 10->53 mantissa bits, both ends pinned);
   sole retest = E3 exact-mode paired arm. Riff banked.
4. **Axiom tranche 1+2 all delivered**: S7 predecessors
   (98%/12.3ms PASS), fuzz-CI (fuzzes production verify_edge),
   magic r2 dead_mask (stuck != Liouville-dead banked), Fourier
   probe PASS + batch 1 emitted (10k rows, stay-in-Q honored);
   solve_batch (23 st/s, deterministic); ax::nn rung 1
   (1e-07); bridge v2 pinned (INTERFACE_VERSION==2); **FX-V1
   exact inference** (dual-impl bit-identical battery hashes;
   exact 148ms BEATS rounded fp32 162ms; table-lerp endpoint
   trap flagged for our ports). Relay -4 SENT (asks 4-6:
   frontier_eval / native gate runner / table fuzz).

## In flight (self-marking; check on resume)

- **Mac, S2 label farm** (logs/s2_farm.log, bg task): 20,537
  children, 8 workers; on completion -> strip cache nulls ->
  verify pre-reg (true-move ~68%, yield >85% L1-5) -> book
  data-leg -> fire scratch/scorer_s2_train.py (arms imit v
  dist; pre-reg at birth time). Battery eval v length-only +
  pairs_3e controls is in-script.
- **3080 chain**: seed-2 birth (ep2/3) -> gate ->
  night_zx_done.marker -> JOB 3 fp32-real ZX-only control
  (pre-reg'd recipe-v-transfer discriminator) -> gate ->
  night_zx2_done.marker. Book seed-2 sigma point + control
  verdict on landing.

## Holds + queue

knock-4, miner v2, adjudicator v3, SR birth: [HOLD] on GO.
House queue: E1-E5 (exact-stack spec). S3-S6 per scorer spec
(S3 after S2; S5 after S2 readout; S6 gated). GateGuard hook
disabled (ECC_GATEGUARD=off, global settings).

# Handoff 2026-08-24-2: the ActionProgram representation arc CLOSED — canonical semantic schema qualified end-to-end; the paired STATE-v-PROGRAM experiment is the live question and is NOT ARMED

Seat: Fable 5 on the Mac. HEAD at close = this handoff commit.
Fresh-session recovery: THIS FILE alone is the anchor -> BOARD ->
RESULTS tail (from ~L46400 backwards). Suite green at close
(count/RC in section G). Nothing armed anywhere.

## A. Controller arc is CLOSED (do not reopen casually)

- TERMINAL-FIRST adopted as the default prospective controller.
- CYCLE-ESCAPE partial (converts only known single-deviation
  cases, 4/15).
- REGRET-LDS parked at the registered 96-expansion scale (zero
  multi-deviation solves over 502 expansions / 6,308 scores).
- World symbolic materialization = 73-89% of search wall
  (WALLLIFT measured split). Wider search parallelizes world
  generation first; controller hacks are not the lever.

## B. Representation arc (the 15-experiment chain, compressed)

- v1 display-label basis: compressed 5.75x but failed the
  512-fit (parent prefix dominates) and its completeness claim
  was retracted as unverified.
- SREPR-EXPORT-0: state identity REPAIRED — versioned srepr
  corpus under a per-node adaptive inverse; 101/101 decisions
  bind (plain sympify canonicalizes the world's own unevaluated
  nodes; blanket evaluate(False) breaks the canonical majority).
- STATE-OBSERVABILITY: NO evidence theta0's observation was
  incomplete — the exact state prints the visible string
  verbatim 26/26; the loss was parser/interchange-side only.
- ACTION-SITE-QUAL-0: programs address the engine's ACTUAL
  operator application site (atoms + first-match), 725/725.
- ACTION-SEMANTICS-QUAL-0: i_parts.u_choice semantic +
  first-preorder (reading-order) site coordinate, 725/725, zero
  ambiguity; 104/402 ordinals differ from the count_ops law.
- ACTION-BASIS-v2 desk: semantic programs are COMPACT (med 12 /
  max 19; 13.4x median compression; zero action-induced 512
  overflow) but within-decision span failed its bar and the
  registered counterfactual traced the residue to TOKENIZER
  NAMING (rule ids cost more than all semantic content).
- ACTION-OPCODE-QUAL-0: ActionGCTok extension 296 -> 332 (36
  reserved rule opcodes, legacy ids byte-frozen, 207,190 diet
  strings token-identical); programs med/p90/max = 5/8/8.
- PROGRAM-DIET-COVERAGE-0: REUSE — 70.78% row / 71.62% edge of
  theta0's exact 103,595-row birth diet relabels to a unique
  program; matched subset 73,324 rows; shards 72.0%,
  step_chains 58.0%; main exclusions 22.48% no-engine-edge
  (13,730 unresolved + 9,267 integration-constant offsets) and
  6.71% parent round-trip.
- I-UNPROD-SEMANTICS-DESK-0: term_index ALONE covers 2,894/2,894
  PDC-matched edges, zero collisions, parity-gated.
- ACTION-FINAL-QUAL-0 (this session's last rung): the canonical
  schema below qualified — corpus regression 725/725 preserved,
  all 2,894 PDC i_unprod edges decode from term_index, and NO
  opaque child-sort branch remains in the decode law across the
  measured legs (corpus, all families; PDC i_unprod stratum) —
  the population's i_parts/deterministic strata were not decoded
  in that rung (bar derived from both legs; booked verdict at
  the RESULTS tail).

## C. CANONICAL ActionProgram AT COMPACT (the one vocabulary)

```text
deterministic action:
    (rule, site)

i_parts:
    (rule, site, u_choice)      u_choice = ordinal of the factor
                                chosen as u among the ELIGIBLE
                                (du != 0) factors of the (inner)
                                integrand Mul, stored arg order

i_unprod:
    (rule, site, term_index)    term_index = ordinal of the
                                additive term of the (inner)
                                integrand Add attacked by the
                                frozen rule

site:
    (operator kind, first-preorder unique ordinal)
    kind in {whole(None), Derivative, Integral, Limit};
    structurally equal occurrences are ONE site (value-level
    xreplace semantics); nested multi-limit Integral peel is
    RULE SEMANTICS, never program content

serialization (ActionGCTok, vocab 332):
    "<r:{rule}>"                    whole-expression algebra
    "<r:{rule}> {K}{ordinal}"       deterministic sited
    "<r:i_parts> {K}{ordinal} u{k}"
    "<r:i_unprod> {K}{ordinal} t{k}"
    all newline-terminated; legacy GCTok ids 0..295 byte-frozen
```

Fences that travel with the schema: sympy 1.14.0 scope;
nested/multi-limit and definite-integral legs remain VACUOUS
(implemented, never data-exercised — any future population that
exercises them needs its own qualification); future diets must
stay literal-"<"-free or re-run the tokenizer legacy bar;
deterministic actions carry a single-accepted-child assert.

## D. The paired experiment — LIVE QUESTION, NOT ARMED

The question: do two identical 19M models learn the same
mathematical transitions differently when the answer is an
<= 8-token instruction v a full-state rewrite (med 161 tokens)?
NON-NEGOTIABLE constraints (verbatim, registered in the PDC
prereg):
- same ActionGCTok/vocab for STATE and PROGRAM arms;
- byte-identical initialization;
- exact same underlying transition row IDs;
- shared batch membership/order built BEFORE target
  serialization;
- no independent target-length sorting;
- same optimizer schedule/steps;
- primary loss = prefix-masked, per-row-normalized continuation
  CE (separately preregistered before training);
- historical full-sequence LM only a rider;
- old theta0 = historical reference, never the primary control.

Next sequence (each step needs its own GO):

```text
final semantic ActionProgram [DONE this session]
    -> matched-subset materialization prereg
    -> paired STATE-v-PROGRAM design/prereg
    -> paired births
    -> sibling-ranking / representation adjudication
    -> only then controller deployment
```

## E. Axiom branch

- Relay docs/superpowers/relay/2026-08-24-0-mathcyber-
  actionprogram-abi.md is committed and durable.
- Manual delivery ON HOLD: first post-compact prerequisite is
  producing the frozen final ActionProgram interchange fixture
  (now unblocked by ACTION-FINAL-QUAL-0).
- Axiom = independent counter-book, never helper/backend; no
  fixture special-casing, ordinal mismatches are measurements.
- Tranche A only authorized by the relay; B (score ABI) and C
  (reverse/pincer enumerator) queued NOT authorized.

## F. Future riffs — explicitly NON-LIVE (banked, no GO)

- COMPUTE-OPTION-CHAIN / DISTRIBUTIONAL-ACTION-VALUE +
  PROGRAM-DISTVALUE head (RIFF-LEDGER, 2026-08-24).
- CAUSAL-STATE-GEOMETRY (hyperbolic candidate, Euclidean control
  mandatory; gated behind STATE-v-PROGRAM).
- Reverse/pincer connection (July banks; v5 programs make the
  architecture sane).
- MAGIC RETRO stays separate/unproposed.

## G. Environment/process state at close

- The orphan multiprocessing incident was SESSION-SIDE only (a
  stdin-script diagnostic; memory note booked): no repo-driver
  leak; no MATH-CYBER background workers alive at close
  (process census in the closing report).
- Mac idle; the 3080 was NEVER touched by this arc.
- Tree clean; HEAD == origin/main; suite 1053 passed / 14
  skipped, RC=0 at the ACTION-FINAL booking commit (5bc7c6c5).
- Nothing armed: no training, no matched subset, no fixture, no
  relay delivery, no geometry, no MAGIC.

## Standing

Same-day prior handoffs: 2026-08-24-0 (controller ladder),
2026-08-24-1 (representation blocker). Both superseded by this
file as the recovery anchor. Open Artin items: relay delivery
(after fixture), matched-subset GO, S6-CERT session-2 artifacts
in the session scratchpad (s6cert/, not repo material).

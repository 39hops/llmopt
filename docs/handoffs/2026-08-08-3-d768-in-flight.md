# Handoff 2026-08-08-3 — pre-compact: d768 crossover in flight, crown-tie next

Resume: BOARD header -> 2026-08-08-2 (Phase-3 closure) -> this
file -> RESULTS tail.

## In flight RIGHT NOW
- Mac: REVIVE-D768-CROSSOVER battery (rjob rev2d768; six d768/3ep
  births, fp32+ternary paired, seeds 2/3/4; PRE-REG booked with
  bars P-CROSSOVER-REAL >= +9 / P-NULL <= |5| / P-INVERT <= -9;
  wrapper scratch/rev2_d768.py; excision receipt 132/165028).
  Runs into the evening. Book against frozen bars only, dicts
  sum-checked, logs/revive/d768_*.log.
- 3080: idle. Next 3080 night = Phase-4 row 2 (ZX 45M), pre-reg
  at fire.

## Queued next (standing GO, no new decision needed)
- CROWN-TIE BIRTHS (revive item 2) queue BEHIND d768 on the Mac.
  Archaeology done: one pair = d768/FFN3840 ternary birth on the
  merged diet (build_merged_diet.jsonl recipe) + the gen6 growth
  pipeline for the champion side — ~2 Mac-nights for 3 pairs.
  Design + pre-reg BEFORE fire; consider 2 pairs first if Mac
  time is tight.
- Codex read-only trial after quota reset ~17:42 EST 2026-08-08
  (seats memory has the plan; grok plan-mode live; muse OFF).

## This morning's additions (all pushed through 7d9586d+)
- Handoff -2 prose fix: FIVE bars fired (Grok cross-check).
- d768 pre-reg names the original cell's TWO confounds
  (cross-device + diet path) — the re-run is the clean pairing;
  never read against the original mixed-device cells.

## Standing cautions (unchanged)
- Wrapper clauses on every re-run: D2 excision + refuse-if-exists
  + device-of-origin + pre-reg before fire; "seed" must name the
  MECHANISM (quat lesson: torch seed can be inert — data order
  was the knob).
- rc-gated commits (never pipe a guard into tail — bit again
  2026-08-08 on pincer_labels_v2, fixed one commit later).
- The curation ratchet fails closed; curate, don't raise.

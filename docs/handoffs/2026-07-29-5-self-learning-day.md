# Handoff 2026-07-29-5 — the self-learning day (three flats)

Resume: BOARD -> this handoff -> RESULTS tail (L9800+).
IN FLIGHT at write time: head-tension 8-head d64 birth (Mac,
logs/h8_birth.log -> checkpoints/sym_birth_dense_mps_h8*.pt);
census next (anatomy.py CELLS=heads on the EMA weights).

## Booked today (all pre-registered, three predictions falsified)
1. FARMER PROBE near-null: reverse d64 (sym_birth REV=2)
   INVERTS the step grammar in-distribution (verified 3 ->
   107/1000 with SEEDMODE=corpus) but is memorization-
   dominant (11/1000 novel). Self-farming NOT live at this
   scale. Revive conditions: solved-state seeding,
   temperature sweep, scale. Instrument: scratch/farmer_probe.py.
2. DEPTH LADDER FLAT: layers {4,8,12,16} at d56 = 56/59/61/59;
   params-matched 4x d80 = 59. Depth is SLACK (axis 3);
   valid% alone rises with depth (fluency, not capability).
   Minimal crystal shrinks: layers-4 d56 at -3 sub-sigma.
3. LEG C FLAT: 8 cuda births, per-level quarter-dose free at
   EVERY level. Allocation answer: the next tranche buys
   HARDNESS (new levels/families/prosthetic formats), not rows.

## The day's triple (read for the next spec)
Depth flat + dose flat + storage at 99% RD-bound: the crystal
is bound by NEITHER architecture depth NOR data volume NOR
bits. Every working lever this week was decode/format-side
(escalation 62, delegation +15). Next frontier = attention
geometry x diet hardness x decode policy.

## Queue
1. h8 census when birth lands (pre-reg on file) -> book.
2. Polar-split snap (cell 4, desk, sigma-informed).
3. Axiom full-size step tranche (event-driven; then LLMUE
   decode-side resolver — the strongest live lever).
4. New-hardness diet design (the Leg C consequence): L6-L8
   mass, new families, call-span formats — spec before farm.

## Knobs added today (sym_birth.py)
REV=2 full reverse; CUTLV/CUT per-level dose; SKIP_GATE.
farmer_probe.py: SEEDMODE=band|corpus.

## Addendum (same day, later): the spec CLOSES
4. HEAD-TENSION booked: h8 gates 58 (= 4-head line; heads x2
   free) yet all 8 essential (best -11, worst -37) —
   provisioning frame falsified; relations scale with
   provision; THEORY intrinsic-heads row hardened.
5. POLAR-SPLIT booked: bits are GEOMETRY-blind — knee follows
   measured bits/weight in any coordinates; phase not finely
   priced (8 angles free); sigma-knee THEORY row extended.
   Import-scar bite #3 (polar_snap) — adopt __main__ guards.
Escalation-engine cells 1-4 + 6 all booked; cell 5 (axiom
tranche) event-driven. Queue for next session: new-hardness
diet design (Leg C's allocation answer), LLMUE decode-side
resolver when tranche lands, greedy-first adoption cell.

## Addendum 2 (evening, Artin home — Mac only)
6. DISTORTION COLLAPSE booked: two-parameter equation kept ~
   f(k_c * D/sigma^2); k_c meter = calibration R1 flips probe.
7. G5 POLAR booked: geometry-blindness survives star weights
   (45-deg grid rotation ~free) — third strike, law unqualified.
8. HEAD AUTOPSY booked: cell-sparse critical circuit (13/64;
   L1h7 -34 alone, L1h4 free); THEORY row refined.
9. FINDINGS.md un-staled (07-29 section added). Riffs banked:
   AlphaEvolve diet-evolution, multi-ply farmer, why-56 fence,
   hardness operationalized, PACKED CRYSTAL, number
   representations. SPEC WRITTEN: 2026-07-30-packed-crystal
   (FRONT OF QUEUE; C6 transfer cell is [HOLD]).
Next session opener: packed crystal C0+C1 (desk).

## Addendum 3 (pre-compact)
10. TRAINING LENS riff banked (Artin): in-birth probe battery
    -> capability timeline (proxy gate + flips + update mass;
    watch the critical circuit form; L4 first). Queue slot:
    after packed-crystal C0-C2, or as the rider ON its births.
Post-compact resume: BOARD -> this handoff -> spec
2026-07-30-packed-crystal -> RESULTS tail (L10300+). Mac only.
Opener: packed crystal C0+C1 (pre-reg first), lens rider
optional on any birth the session fires.

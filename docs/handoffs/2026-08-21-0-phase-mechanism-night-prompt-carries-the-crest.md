# Handoff 2026-08-21-0: the phase-mechanism night — EX5 books carrier identity, EX6 books the registered refutation: THE PROMPT PHASE CARRIES THE CREST

Seat: Fable 5 on the Mac, MAC-ONLY session (Artin constraint, no
3080/WSL contact all night); HEAD at close f75de4b9 (final close =
the handoff commit itself), both this checkout and origin lockstep;
nothing armed, no watchers live, both machines idle. Model
re-downloaded and pinned: mlx-community/Qwen3-30B-A3B-4bit @
d388dead (per-file manifest logs/ex5/model_manifest.json).

## What landed (chronological, with shas)

- REVIEW CORRECTIONS (abdb542): serializer wording precision in
  RIFF/BOARD/handoff (function exonerated, restored-provenance
  stays in the trigger), handoff HEAD clarified, stale BOARD
  EX4-UNIF row repaired (GPT had proposed a rung that already ran
  — the stale row manufactured it), EX-ANAT-4 slate item (a)
  marked spent, TOPSET confirmed 3080-only + sufficient-statistics
  lesson banked, OPERAND-PROVIDER v1 shipped in
  llmopt/lab/prereg.py (operands field, existence at load; first
  live uses same night).
- MASK DESK (2ca9cbe, ad0344b): mask-composition census
  (demand_share == booked recall exactly; caught named slots
  inside unif1/top80), six EX5 masks frozen carrier-excluded +
  manifest with exact slots/shas/pairwise overlap.
- VERDICT EX5-LAYERMATCH-0 (b2af0a2a, RESULTS L37996): named
  crest +43 pooled 3/3 on virgin triple 4001/5002/6003; fresh
  rank-window family does NOT transport (1/3, sums -12/+25/+10,
  range 37); layer-only 0/3 (median -3); identity premium 2/3;
  prior wrong on bars {3,4,7}; qualification cell-exact on the
  re-downloaded snapshot. AMENDMENT -WORDING (7deb99fa): narrowed
  to "named carrier identity robust; rank-window membership alone
  insufficient"; FINDINGS split [REPLICATED] crest /
  [SINGLE-SEED] mechanism.
- SCHEMA SEAMS (9b9d0cbf): applies_only_if_bars_fire (conditional
  refutation on scientific bars) + conjunct-local metric triples;
  EX5 adjudication byte-identical.
- OBSERVATION EX5-TRAJ-ANATOMY-0 (6fe6a0da) + AMENDMENT -RANK8
  (210ca16a) + -RIDER2: carriers are prefill-mass, early-position,
  and the ONLY group failure-enriched at decode; rank8/low-margin
  RETRACTED as rank measurements (argpartition order
  contract-undefined; empirically score-sorted 0/590,736);
  prompt_tail dilution disclosed; tail-corrected rider deepened
  the true-decode read (enr -0.0483, gap +0.00320 v sd ~0.002,
  prompt_tail carries nothing).
- PRE-REG EX6-PHASE-0 (0a7c0e31) + AMENDMENT -WRAPPER (3e9f510b):
  phase-scoped named-80 deletion; v1 qualification FAILED (NONE
  63 v booked 59 — MLX lazy-graph fusion: the skipped
  .tolist()-forced eval changes downstream fp rounding; wall
  corroborates, 100.2s v 266.8s); wrapper v2 = frozen math, one
  code path, phase-selected mask; v2 qualification cell-exact
  2/2; fail-closed launch gate.
- VERDICT EX6-PHASE-0 (92b9c464, RESULTS L38436): THE PROMPT
  PHASE CARRIES THE CREST — Delta_all +48 (3/3), Delta_prompt +47
  (3/3, |gap to ALL| = 1 descriptive), Delta_decode -2 (nothing,
  sub-floor); decode-carry hypothesis REGISTERED-REFUTED via the
  first live applies_only_if adjudication; pre-named
  context-assembly alternative wins; effects additive (residual
  3); prior 3 hits / 4 misses; EX5's decode failure enrichment
  demoted to SYMPTOM. Crest now 9/9 seeds across three triples.
  Seeds 4001/5002/6003 and 7001/8002/9003 SPENT.
- RIFF PROGRAM (934ab2ad, c516426f, b7f9e9e7, f21a72db, a56c80e8):
  PRECISION-CREST-TRANSPORT (6-bit, sequenced behind the
  mechanism thread, now unblocked by the crest replication);
  DENSE-ROUTER READOUT + CROSS-MODEL EXPERT TRANSPLANT (Artin's
  asks, kin-linked, breaks first); EXACT-BF16 OUT-OF-CORE 27B
  feasibility note (full BF16 checkpoint IS local at
  ~/qwen_vendor 55.6GB incl. MTP tensors; SSD 4.2/8.3 GB/s
  measured; lossless ceiling 1.51x measured; disk 17GiB free is
  the binding constraint; honest ladder ~0.1 -> ~2.6 tok/s only
  with ALL levers; revives the dead RESIDENT-DRAFT/STREAMED-VERIFY
  program via the vendor MTP head; five GPT corrections applied
  in place incl. the packed-to-GEMV decisive cell and the
  exactness oracle) + NInfer as primary systems kin
  (committed-tokens-per-traversal objective).

## Conditions that bite next session

- logs/ex6/smoke*.jsonl rows are v1-wrapper artifacts (disclosed
  in the verdict); do not cite them for v2 mechanism claims.
- ex6_phase.py gained a LOG-path floor POST-booking — its disk sha
  no longer matches the observations start block (deliberate,
  disclosed in the verdict; the booked sha is the as-run one).
- checkpoints/ex3_del_invp.json is now TRACKED (force-added at
  booking; content-pinned in the EX6 observations).
- HF cache holds exactly one snapshot; the loader still carries no
  revision pin (model identity = evidence, not receipt — a
  revision-pin patch to moe_gt1_arm2 is NOT allowed, file frozen;
  a new-driver pattern should pin).
- 3080/WSL untouched all session; TOPSET census still queued for a
  3080 window.
- Disk: 17GiB free on the Mac — the out-of-core program's shard
  transactional encode needs this managed; nothing else fits.

## Next session

Start: this handoff -> BOARD -> RESULTS tail. Recommended first
batch, in order:
1. The mechanism question EX6 opened: WHY does carrier routing
   during context assembly poison the state? (state-space design
   needed — e.g. paired prefill-state readouts under PROMPT-mask v
   NONE on matched prompts; unregistered, desk-price first.)
2. PRECISION-CREST-TRANSPORT (6-bit) — unblocked, ~24GB disk
   cleanup precondition.
3. Out-of-core preflights (zero-GPU: mtp.* tensor shapes, MLX
   DeltaNet batched-verify existence, disk decision) + the
   NInfer-specific survey (queued behind the now-frozen EX6
   receipts, i.e. runnable).
4. EX-ANAT slate (b) TRAJ anatomy deep-dive / (c) physics-gate
   scalpel; fixed-rank-class dose ladder.

## Open decisions for Artin

1. Which of the four next-session tracks leads.
2. Disk management for the out-of-core program (delete-after-
   compress on ~/qwen_vendor shards v external volume v defer).
3. Whether the mechanism rung (1) runs before or after the 6-bit
   transport rung (2).

## Standing

No relays unsent; no runs finished-unbooked; every measurement
this session is booked (2 verdicts, 1 observation, 5 amendments, 2
pre-regs, 6 riff banks/updates). Suite green at close (see
handoff commit). The night's arc: anatomy (correlate) -> rider
(normalization) -> registered phase experiment (causal) -> the
correlate exposed as symptom — the full loop in one session.

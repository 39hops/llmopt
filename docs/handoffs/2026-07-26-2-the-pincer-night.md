# Handoff 2026-07-26-2 (the pincer night: eight verdicts, one program pivot)

Reads: -26-1 + BOARD (refreshed this handoff) + RESULTS tail from
"PRE-REG: pincer R0". 3080: night_zx.sh running (union birth
SAVED, gates in flight, ZX seed-2 chained; marker
logs/night_zx_done.marker; book on arrival).

## The night's arc (all pre-registered, all booked same-session)

1. **R0** — conjecture 55/120; the oneshot chain gate is a strict
   SUBSET of ply-0 conjecture (chaining adds ZERO); L4 craters
   4/24 (clade signature in a new skill); 18 near-misses = peel
   worklist.
2. **R1a** — backpairs births (1P+3E): loss trains down
   pairs-class but per-candidate backward validity 11% (v fwd
   85%-class); coverage 88-91% at k=8; 3E no rescue.
3. **R1b** — 68% of gen-4 rows yield exact (rule@site, child)
   labels by engine replay, ZERO ambiguity; misses = const-of-
   integration offsets + multi-rule skips; objectives ii/iii
   need no replay. No axiom ask for the pilot.
4. **Distribution readout v1** — battery saturated BOTH ways
   (95% of children solve; length-only ranker beats all models
   — meta-pattern 5th occurrence, length control now mandatory).
   SURVIVES: amplitudes = corpus POLICY not landscape (ent_norm
   0.016; zero mass on equally-solving families); backward
   reverse-scorer 10x flatter + best graded Spearman (0.769)
   despite 11% emission — scoring != emitting, B-b's premise
   observed.
5. **Altpairs** (distribution-rows fwd edition, winners
   combined): 12x entropy toward landscape, every zeroed family
   wakes, -6 gate at 14% share — policy/calibration is a
   measured DIAL; calibration belongs to the SCORER, not the
   emitter.
6. **R8 meet v1 FAILS** — A 4/55 v B 5/55 (bar >=3), ZERO meet
   solves, 5.7x token cost (my budget fence was violated by my
   own design — booked as instrument defect; direction robust).
   **B-a text-emission peeler DEMOTED; the pincer rests on B-b.**
7. **R0b ADOPTED** — mass-order oracle readout 1.70x fewer calls
   (needle regime 2.06x, abundant 1.28x); T-sampling is already
   ~amplitude-ordered (1.52); mass-descending verify order
   adopted for new instruments (sample_wave_lp returns lps).
8. **3080 union** — pre-reg'd at arm time (federation floor:
   COUNT v CLASS); birth saved; gates pending.

## Infra/org

wsl.sh launch used for the night queue (success-only marker);
gate_ckpt_cuda now honors VOCAB_EXTRA (atom-order doctrine);
make_union_diet/make_altpairs/pincer_{r0,r0b,r1_probe,r1b_labels,
dist_probe,dist_report,r8} all committed. Riffs banked with
attribution: composite machine + any-stage rider, meet-as-
bidirectional-bf (R8-R10 in spec amendment 3), light wave/click
frame (scope-fenced), Grover-fence v2 + symmetry-thread-is-live.

## Next session (spec DRAFT pending Artin's confirm)

The B-b scorer program — extending: T-sampling-as-free-amplitude-
readout (R0b), magic (variance-gated certificates), syndrome
policy (state->rule machinery reused), regret (abandonment where
budget binds). Artin's asks folded: wave-fusion (fwd x bwd
product scoring) as the honest interference question; frontier
battery (fixes the saturation lesson). HOLDS unchanged: miner
v2, knock-4, adjudicator v3.

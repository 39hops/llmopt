# Handoff 2026-07-29-2 — attention-core day

Resume: BOARD -> this handoff -> RESULTS tail (L9470+).
Nothing in flight; Mac free; 3080 = Artin's (ask before use).

## Verdicts (all pre-registered)
1. TIER-RETRY: first adaptive-crystal WIN — 59/120 @ 69%
   effective gate params (v dense 57 / cheap 52); tiers
   partially complementary (census 50/2/7/61); oracle-fail =
   sufficient difficulty signal. Baseline for all predictive
   routing.
2. HEAD CENSUS: no slack in attention — any single-head
   deletion craters (4-17 of 63).
3. RANK READ: knee in (24,32]; geometry matters beyond rank
   (rank-32-in-56-dims -9 v width-48 -13).
4. HEADS-2: multiplicity load-bearing (EMA 56 v 63).
5. BORN-R32: rank knee is a FUNCTION property (-8 = post-hoc
   -9; train-through does not heal). Attention structurally
   incompressible 0-for-2.
6. SNAP-ALLOC: Q=16 FREE everywhere (incl. attention + both);
   Q=8 uniform -4/-4/-7. LAW: bits and structure are DIFFERENT
   currencies — structure is all location, precision has none.
7. REVERSE-PAIRS: no free dual-direction crystal (EMA 50 v
   59+ class); dose confound named in the booking; controls
   banked (half-dose forward; reverse-gate instrument).

## Cross-lab (axiom on Mac)
Onboarding relay delivered + their reply (0b47888): clone
primary @ eb20896; E3 GO -> DELIVERED same day (50 rows,
margin-certified >= 0.05/token — any divergence is a real bug
by construction; shas 466a6592/223235ee/398b7993); call-spans
CHEAP their side (nt-grammar only). OWED TOMORROW by relay:
span surface form + VOCAB_EXTRA atom decision.

## Instruments
tier_retry.py (TierP inlined — import-side-effect scar x2),
head_census.py, rank_read.py, snap_alloc.py (Q env),
e3_battery.py (margin-truncation protocol), sym_birth grew
RANK + REV knobs.

## Next queue (BOARD NEXT line)
Relay reply -> Leg B call-span farm -> Leg C diet descent;
matryoshka rung-2 3-tier (escalation ladder for tier-retry);
polar-split snap; reverse-pairs controls.

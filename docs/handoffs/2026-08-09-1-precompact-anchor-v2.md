# Handoff 2026-08-09-1 — pre-compact: anchor-v2 arc + morning queue

Resume: BOARD header (SUNDAY 08-09) -> handoff -0 -> THIS FILE ->
RESULTS tail (newest: PRE-REG ANCHOR-V2 f9db0b7).

## Since handoff -0 (all pushed)
Artin rulings executed (LEAN-300 receipts tracked + RECEIPT
booked; figs pruned + PDF re-rendered; drivers stay frozen).
ANCHOR-V2 ARC, riff to registered design in one evening:
Artin's parallelize/cache push -> RIFF banks (RNS/CRT anchor +
assembly-of-booked-wins addendum + spec-time parked-rung-sweep
process lesson) -> axiom design read (Tier 1 GO-ready pending
exr::guard cache freeze; Tier 2 interval-shadow pattern) ->
probes GO'd (Artin) and run same evening (gcd = 86.6-96.4% of
d64 step; shadow validates at machine speed; house thin-out
prediction REFUTED at d64 — widths compound 15-20 bits/step,
double shadow collapses by step 3) -> PRE-REG ANCHOR-V2 booked
(four pins, P-DIGEST-EQUAL + P-HORIZON bars, fresh house
prediction registered: dyadic 50+20N shadow prevents the
collapse). Build awaits Artin GO; axiom's leg.

## Morning queue (in order)
1. d8 decision: anchor step 3 was ~18h into a projected ~845x
   step (could be weeks). IF still grinding at morning check:
   kill it (streamed dumps survive), compute divergence
   house-side from the dumps, book VERDICT EXACT1-SMALL on d8's
   2 certified anchor steps (= d16's count; the <2-step clause
   does not trigger) WITH the bar-language amendment drafted in
   handoff -0 (certified = all-four-arms dumped; exponent read
   alongside the 2x flag, d16 exponent 0.73 v diffusive 0.5;
   vacuous-truth/step-12 defects named). d8 anchor wall-clocks
   book descriptive-with-shared-use fence. Step-3 abandonment =
   the horizon measurement (d64 step-2 precedent), explicitly
   recoverable by anchor-v2 later.
2. Crown battery: c_s2 DONE (~3h), m_s2 was mid-ep0 (~5h/cell);
   ~8h per seed pair -> books ~Monday evening. Booking notes in
   handoff -0 (tie language scoped to +-2.3 solves/seed; n=2
   fallback re-derives per-seed; single-seed dominance note).
3. EX4-UNIF fires on the freed Mac window after crown (riders
   booked: P-UNIF-HARM, dead-zone/split readings, anchor
   tolerance <=1 solve on <=1 cell -> one re-gate).

## Machines / constraints
3080/WSL: ONE worker only until Artin frees it (d8's single
core is the whole allotment; no Tier-1 runs, no prototypes).
Mac: crown battery owns the GPU; axiom co-located (their
checkout at f44755a+; ENGINE-EXACT work is CPU-side).
Reviewer seats: cap 5 (Artin 2026-08-09), Opus only, NEVER
Fable sub-agents; memory updated.

## Watchers at compact time
d8 completion monitor + any crown watcher may not survive a
clear — re-arm from this file: d8 = marker/abort/process-death
poll on logs/exact1small/d8_run.*; crown = gate-line watcher on
logs/revive/crown_*.log with pgrep on jobs/rev3crown.cmd
pattern class (rjob status is the clean check).

## Deferred (unchanged from handoff -0)
Frozen-driver bug list (fix on registered re-run only); 12
[UNVERIFIED] specs-INDEX rows; AGENTS.md is gitignored/local;
star-profile + small-specialists banked pending pre-regs;
ENGINE-EXACT-2 (carry ladder x normalizer) design pending;
bias-correction bigint growth deferred with ~1e5-step tripwire.

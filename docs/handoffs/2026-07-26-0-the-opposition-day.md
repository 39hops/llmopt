# Handoff 2026-07-26-0 (the opposition day, morning -> late night)

Reads: -25-2 (sigma-and-alphabet day). Reviewer (Opus 5) served the
morning (caught 2 dead queue items + amended 4 bookings), RETIRED
midday per Artin — house self-review from there; three self-caught
confounds (cooldown, v2 schedule shape, sorted-batch smuggle) each
converted into the next arm. Both machines live again since
evening; axiom Fable active (ZX tranche).

## The streaming arc (d256 pilot substrate, six cells + 2x2)

d256 SUBSTRATE GATE CLEARED (-27 v -3 bar; packing = THIRD
starvation mode w/ L4 clade signature; packed d256 lands ON the
d64 curve point; matched-steps accident: step count alone CANNOT
be packing's mechanism — 3,972 v 4,140 steps, 38 v 53). STREAMING
CLOSED via full 2x2: epoch load-bearing at -8 best-case; COOLDOWN
+4 real; BATCH HOMOGENEITY -12 at one pass (the interaction law:
diversity-per-step is the binding resource; 07-16 precedent);
clade-gated v2 (probe-aimed recycling) = 60, best single-pass
cell (+3 bar-pass over shuffled — self-pacing pays; L4 starves in
EVERY schedule). Per-level sigma now MEASURED at d256 (free, from
the 3 seed births: L4 band {6,7}).

## The distance verdict

Weight distance measures ANCESTRY, not function: seed pairs
(65/63/64) ORTHOGONAL at sqrt(2) even under per-matrix Procrustes;
27-solve-different same-init pairs at 0.3. Seventh leg of
never-score-by-distance. BANKED: joint-perm closure cell (the
correct group; kill condition set); U(n) variant when complex
births exist.

## The opposition arc (the day's physics)

Cheap closers, all landed: **born-S4 58** — binary's crater was
mostly RESOLUTION; zero law BOUNDED out of born-2-bit (survives at
1-bit + PTQ-fixed-codebooks). **born-Z1 0/120** — deletion square
closes at birth 54 v 0 (loss biography: 27.7 confidently-wrong ->
3.7 ignorance floor -> never conditions). **born-Z1S 0/120 BUT
trains smoothly** (3.69 -> 1.70): Dale's-law channel signs fix
STABILITY, not capability — sign budget NOT channel-compressible.
Artin's thesis adopted as the frame: OPPOSITION IS THE
COMPUTATIONAL PRIMITIVE. m4x gauge arm VOID BY FLOOR (fence fired
as registered; extreme-asymmetry PTQ gauge tests structurally
closed). Z1S hot-LR arm (1e-3) ARMED behind the retention marker.

## The complex/ZX program (spec 2026-07-26-complex-zx-program)

ZX continent QUALIFIED: axiom row factory (5 moves incl.
color-change), TWO batches adjudicated clean (~20k rows; 9th
consecutive clean batch; ration doctrine applied at the farm; no
size caps — tail-dies-first governs diet design). Complex trainer
support BUILT+SMOKED (complex-FFN, modReLU, genuine complex
multiply, G5 STE; scratch/complex_{model,birth}.py + gate_cplx).
Complex-NNUE NULL as pre-registered (oligarchy phase, paired
-0.048 rho) => bracket narrowed to phase-CARRYING cells only:
euler-family reads + G16-as-interference-for-SIGN. The headline
cell = the G5xM5 x math/ZX FACTORIAL (alphabet-follows-domain).
North star: QUANTUM LLMUE (ZX diet x rotation substrate x
metabolic sessions w/ flip census).

## Other bookings

Margin census: undecided mass UNIFORM (CV 0.14) — no high-entropy
elite; temporal signals remain the only scheduling channel. ODE
desk: atom set frozen (["t"]+ODE_ATOMS), determinability catch
BLOCKS ODE birth until refarm, sealed committee prediction filed.
Graph-modularity on gen-8: NULL at bar (+0.030 < +0.05; bar-based
null, Q dispersion unmeasured).

## In flight at close

3080: retention session LIVE (v5 s2, 120 min, dense per-level
[retention] columns; PRE 6/100 on the fresh wall head) -> Z1S-hot
watcher chained behind its marker. Books next session. WSL dirty
state restored as found (step_chains conflict resolved local-side,
zero markers). Mac: idle.

## Next session

READ docs/superpowers/specs/2026-07-26-next-session.md — the
ranked plan (written tonight per Artin's context-hygiene design:
post-compact, read RESULTS tail + that spec).

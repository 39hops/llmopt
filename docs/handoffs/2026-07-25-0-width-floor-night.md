# Handoff 2026-07-25-0 (the width-floor night, ~12AM-3AM)

Reads: -24-1 (nine-verdict night). This covers the post-midnight arc.

## Verdicts (all reviewer-cross-checked, in RESULTS)

1. **THE WIDTH FLOOR: W_min ~ 8.4M** (d256 = 65, ON the 19M
   plateau; 19M was 2.3x over-provisioned; 45M stays peak). Full
   curve 0.5M->400M complete (chart: assets/width-curve-gen4.png).
   Pre-reg REFINED: **isolated-clade techniques die first** (L4
   ansatz 7->4->0, L6 heurisch -63%; clade-connected levels -30%);
   L4=0 at BOTH extremes (d64 and 400M). Federation reading:
   adding grammars RAISES the floor. Fences: production unmoved;
   "not vocab" untested; n=1 knees.
2. **The 388 mangled rows** — strict encode caught out-of-language
   rows silently letter-stripped in every gen-4 birth; trainer now
   skips whole + reports.
3. Ladder speed: d128-class births at 21-23 it/s on MPS — 10-min
   experiments are real.

## New program: THE NATIVE TRANSFORMER (specs/2026-07-25-native-transformer.md)

Rung 1 prefix notation (enabling substrate, ~1-2h Fable code then
birth pair) -> rung 2 subtree-HASH tree-PE (zero-epoch north star)
-> rung 3 attention-init (ep1-speed graded). Forbidden forms
listed. Reviewer survey delivered the ordering.

## Queue: WEEKEND v2 + v2.1 (evening-queue spec, speed-first)

Speed track: prefix / streaming birth (PROMOTED) / batched-KV /
ternary-GEMV audit / regret hook / deterministic gate. v2.1 adds:
d256 = experiment substrate (gate: 1 replication + 1 known-result
reproduction); depth ladder + LR sweep at d256 (the two
unconsidered peak variables — width curve mixes depth; fixed LR
across widths, muP); model-vs-engine policy-symmetry probe; THE
COMPOSED BIRTH (<15-min plateau birth, all levers stacked).

## In flight / waiting

- 3080: 45M fp32 control (union-verdict confound splitter) —
  slow while Artin used the GPU, finishes whenever; read vs bf16
  twin same-device, then gen-10=19M books or splits.
- Mac: free. Boundary-or-bulk regression = one script, no births
  (grid complete). Prefix serializer = first code block.
- v5 s2: certified 53-row shard + landed miner guard = one
  command at next 3080 window (wave-contrast rides free).
- Axiom (Artin's relay timing): knock-4 ask, round-3 GO (78
  walls), binned stuck_states_v5 head.
- Plots delivered: width curve, wfloor portraits,
  density-vs-phase (caveat: cross-model phase alignment is
  gauge-dependent; gauge-fixed version = cheap upgrade).

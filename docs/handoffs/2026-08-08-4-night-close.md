# Handoff 2026-08-08-4 — night close: P4-2 booked, exact-training GO'd

Resume: BOARD header -> this file -> RESULTS tail.

## Booked today (all pushed)
P4-2 P-NULL-HOLDS (ZX 36/34/32 mean 34.0; G9 no-fire seed-hardened);
AMENDMENT P3-R7-ARM; ARCHIVE commit-only-nulls; OBSERVATION
Q4-DELETION-RENDER + SOURCE amendment (mid gate: ONE survivor);
crown-tie + P4-2 pre-regs; FINDINGS curated (P4-2, Q4). Four-seat
audit + grok cross-check cycle: all adopted, receipts in RESULTS.

## In flight
- Mac GPU: d768 battery (fp32 s3 running ~midnight; t s3, s4 pair
  follow; books ~tomorrow AFTERNOON; watcher armed). Crown-tie
  (rev3_crown.py, pre-reg booked) fires on the freed Mac.
- 3080: FREE. Snap-birth done (gallery19m_s1 + ep frames, pulled).

## Tomorrow morning (Artin priority: exact-training)
1. ENGINE-SCALE-1 handoff: llmopt pre-reg + per-cell export (init
   bytes + windows + contract, 30 cells) -> ships via pull; axiom
   runs same-day; spot-sha verify (8b443b68/561e28c5/15934bb8) is
   the pre-committed first Mac window.
2. Exact-training (ENGINE-EXACT-1): Artin GO'd. Axiom's
   feasibility relay (its 2026-08-08-3): literal exact training is
   Omega(2^steps) in bits (Q.K^T doubles exponent bits/step) —
   book the impossibility law with its toy-loop receipts at
   pre-reg time. Accepted counter-design: precision LADDER
   (Q.16/32/64, same rounding placement) + exact-prefix anchor
   (dyadic, 8-12 steps d64). Restate disagreement-#3 pre-reg in
   ladder-limit terms (absorption: rung-divergence -> 0;
   structure: persists). AXIOM NEEDS CPU ONLY (C++ engine,
   CPU-native like all intbirth legs; Q.64/d384 cells =
   hours-class Mac-window legs) — no 3080 session required.
3. Bank the canonical-init rung if Artin confirms (seed-
   independent placement: do named cells transport across
   canonical-init births? tests R2's addresses-are-born).

## Deferred audit items (mention-tier, next housekeeping)
birth19m_snaps ep-label assert + snap refuse; qwen_displace_extract
refuse+glob assert; rev3/rev4 arm-time asserts (print-only now);
PDF Lean number staleness (443 -> 21,614/21,914); allocator-knob
doc conflict (expandable_segments 43x anchor v in-tree
max_split_size WSL note) needs one reconciling line; VRM =
bank-with-design-prerequisite (grok seat + Fable agree; Artin not
yet ruled).

## Gallery (all [R], committed generators)
True 19M zoom DELIVERED (neurons-19m-growth-ep1-final, figs/
2026-08-08/); whisper provenance settled by full recreation
(crystal_recreate_test.py); Q4 starburst + 1200x627 LinkedIn
variants; three-minds K3/ternary/19M re-render still WANTED.
LinkedIn media finalized: whisper title "The whisper: RL edits
the policy, not the model" + corrected 0.5B copy.

## Standing cautions
Seed = init draw (data order epoch-keyed) — fence sentence in
every battery verdict. Between-bands book UNRESOLVED, no
direction narration. Assets rows: exact RESULTS entry or
as-rendered/riff-tier tag (gallery law). rc-gated commits.

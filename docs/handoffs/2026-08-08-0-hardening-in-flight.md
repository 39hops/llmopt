# Handoff 2026-08-08-0 — hardening program mid-flight (pre-compact consolidation)

Resume: BOARD 08-08 header -> this file -> RESULTS tail from
VERDICT HARDENING-P1 down -> spec 2026-08-07-results-hardening.md
(the program's single source of truth, incl. the migration
appendix).

## Program state (phases)

- P0 DONE: propagation breaks fixed (graph-mod reversal, symmetry
  weight-basis scope, crown n=1 fence, revival row), SEV-1 holes
  closed (jobs/*.cmd tracked; lean_sample_build byte-identity 2/2
  with the true recipe documented; ex3_build). REMAINING P0 (cheap):
  index status field; SEV-2 list incl. poly3 /tmp probe.
- P1 BOOKED: exclude-union fires on EVERY legacy infix diet
  (22-25/120 prompt-side; fence widened, paired deltas unaffected;
  prefix diets NOTATION-BOUND not clean).
- P2 BOOKED: precision-knee 3/3 (THEORY 143 fence lifted, sigmas
  0.0367/0.0368/0.0368); crown tie stands (0/+2/+2 pooled +4).
- P3 IN FLIGHT (frozen 7-row list + hard gates in spec): R5 BOOKED
  (soft corr 0.0096/0.0071/0.0062 — THEORY 33 causal leg n=3;
  excision receipt 129 matches P1 across two code paths). R6
  RUNNING on Mac (rjob p3r6: ctl/contract seeds 2/3/4, six births,
  D2-excised, refuse-if-exists; bars P-TAX-FLAT v P-TAX-REAL in
  PRE-REG HARDENING-P3-R6). Next after R6: R3 ffn endpoints (needs
  wrapper w/ refuse guard — wfloor_ladder writes seed-free names).
- P4 QUEUED (strict order, 3080 nights): pincer in-dist FIRST —
  its label prep (pincer_r1b_labels) MIGRATES to fork-per-state at
  the re-run (the one true SIGALRM carrier); then ZX 45M, SR-BF16,
  poly bridge (poly3 audit+probe migrate), dithered/judge, farmer.
- MIGRATION APPENDIX (in spec): 3 migrate-at-rerun / 7 wrapper-only
  / 2 leave-alone; gate path needs NOTHING (lab modules are
  verbatim adoptions of what drivers already call).

## In flight right now

- rjob p3r6 (Mac): R6 births; watcher armed (bfuhfggd7-class).
  Book against frozen bars ONLY, all six dicts sum-checked.
- WSL checkpoint inventory (scratch/ckpt_inventory.py -> logs/
  triage/wsl_inventory.jsonl; marker /tmp/ckptinv.DONE): still
  hashing. Mac half done (logs/triage/mac_inventory.jsonl). NEXT:
  cross-machine dedup + RESULTS-citation table -> Artin review
  BEFORE any delete/move.
- ENGINE-SCALE-1: 30-cell grid with axiom (their set_lr a39987a);
  house books only after the three spot shas match the certified
  DIET-BRIDGE/PLATEAU-B/SCHED-rider digests.
- Axiom relay owed: 222 atom-split + 78 field_simp id lists
  (LEAN-FULL-V2) for counter reconciliation.

## Recent claims context (for orientation, all booked 08-07)

Both crest claims at n=3 fresh seeds (swap +53; named-80 deletion
+55/+27) + router over-inclusion at the carrier rank class (+28,
uniform-random control OWED before broader language); carriers
regime-dual, prefill-tilted; weight-reader direction mining closed
(surfaces + relational); Lean Leg B complete (98.63%, taxonomy
zero-residual); MULT-0 arc: scarcity mostly model-property,
chooser gated.

## Standing cautions

- Every Phase-3 wrapper: D2 excision + refuse-if-exists + device-
  of-origin + pre-reg before fire. Seed-free OUT names in
  sym_convert/wfloor_ladder/format_ladder are the top collision
  risk.
- rc-gated commits (never pipe a guard into tail); the curation
  ratchet now fails closed — curate, don't raise.
- Sync friendly-fire: pulls onto the 3080 can abort on untracked
  twins ("Updating..." in output does NOT mean success — verify
  rev-parse); scorer_s2 collision was resolved by sha-verify then
  remove.
- 3080's own jobs/*.cmd were backed up to logs/triage/
  wsl_cmd_backup/ before the cmd-tracking commit landed there.

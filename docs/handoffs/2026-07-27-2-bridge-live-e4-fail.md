# Handoff 2026-07-27-2: bridge live, E4 failed honest, exports next

Resume: this -> BOARD -> specs/2026-07-27-exact-stack.md ->
RESULTS tail. Morning of Sun 07-27; Artin relaying with axiom.

## Since -27-1

1. **Sync gap found + fixed**: axiom's tranches never reached
   the shared remote; pushed 405a6d4..6338b7c (23 commits).
   Relay -6 records the whole exchange + asks.
2. **Bridge LIVE on Mac**: clang build, sha256-verified deploy,
   IV==3 PASS, 17 names. (llmopt venv site-packages; 3080/WSL
   still on the Jul-20 .so — rebuild there before any remote
   bridge use; the IV assert will catch it.)
3. **E4 parity: FAILS every prong** (pre-reg'd; RESULTS
   2026-07-27) — engine=axiom namespace permanent pending
   audit. Asks out: expired flag, basis-rule audit, ply
   semantics.
4. Housekeeping: fmt_row_emb.pt -> checkpoints/ (+ script path
   fix); index over-stamp incident caught + corrected; the
   three 07-27 pre-regs dated; E4 + morning entries curated.

## Next (order)

- **E2 exporter**: read include/ax/nn/model.hpp header doc ->
  export scorer_s2_dist.pt + pairs_3e as AXNN + prompt spec +
  token map (unblocks frontier_eval scorer slot, gate_battery,
  E1/E3 path).
- **E5**: regenerate fourier batch 1 (scripts/fourier_chain.py,
  seed 20260727, ONCE, distribute bytes) -> ZX-playbook
  adjudication.
- **E1**: dual-impl gate runnable house-side; STRICT hash match
  still needs axiom's container bytes (relay logistics).
- **S3**: harder needle battery (calls-to-hit saturated).
- Awaiting axiom on the three E4 asks. Holds: knock-4, miner
  v2, adjudicator v3. ZX column rests (n=3 fence). SR dead.

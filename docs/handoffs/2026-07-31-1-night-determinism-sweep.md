# Handoff 2026-07-31-1 — the night sweep: transport closed, provenance caught, determinism cross-lab

Covers the evening/night after handoff 2026-07-31-0 (+addenda).
Resume: this file -> BOARD header -> RESULTS tail.

## Verdicts booked tonight (in order)

1. **TRANSPORT (scaffold program CLOSED)**: cuda lb {48,51,53} v
   gravmoe {49,53,50} — means 50.7 = 50.7, dead-center in the
   +-2 Mac-scoped band. Rescue framing adopted (pre-registered
   before s3): cuda training reaches unaided the basin Mac needs
   the pull for; the gap is training arithmetic — exactly what
   deterministic-birth R3 answers exactly.
2. **GATE TRANSCRIPTS (Artin's instrument)**: scratch/
   gate_transcripts.py. L3 = clean chains + one-shot u-sub; L4 =
   right ansatz, GARBLED term arithmetic (0/8 valid at ply 0).
   The L4 wound is exact manipulation, not strategy. Banked: an
   exact-manipulation diet share.
3. **GRAV-0T/REV (Artin's riff)**: post-hoc gravity is
   DESTRUCTIVE both ways (37 -> 9 -> 1; repel -> 0). The pull is
   training-coupled (1 relax per 100 optimizer steps; post-hoc
   R=10 is ~100x dose density, zero repair). Resolves the
   merge-free "contradiction": merge-free is a property OF the
   pulled basin, not of merging. White hole explodes, as they do.
4. **RE-GATE SIGMA + PROVENANCE AMENDMENT**: the 120-gate is
   EXACTLY deterministic on mps (3 fresh processes digit-
   identical; gravmoe s1 reproduces its booked line char-for-
   char). The lb-s1 "45 v 37" was CHECKPOINT PROVENANCE: the
   overnight params-chain re-trained lb s1 and gated 45 but never
   (over)wrote the disk file; the surviving artifact gates 37.
   NEW RULE: gate numbers book with the ckpt sha
   (gate_regate.py prints it; wiring into gate_eval queued).
   Mac lb baseline is {37, 44} on disk — Mac gravmoe advantage
   if anything LARGER; transport verdict unaffected (paired).
5. **FX-V3 both directions**: axiom PASS (two runtimes, exp
   table BYTE-identical to detbwd_r1b, tie-break provably
   irrelevant), then HOUSE reproduction PASS both seeds FIRST
   RUN (scratch/fx3_house.py, their tables decoded not
   regenerated, gate spec implemented from prose alone). Merged
   crystal integer-closed: 3 implementations, 2 labs.
6. **R2 C++ (axiom)**: 200-step integer birth trajectory-
   identical in C++ (their c0ef854, 1.1 s). Their catches: strict
   >2^30 cap (gt_pow30 — bit-length test would mis-shift exactly
   2^30), rdiv forms unified BY PROOF program-wide.
7. **R3a: Q_w PINNED = Q << 8 (2^17)**. lr=1e-3 works in pure
   integers; the R2 floor bites LATE (update starvation), not at
   step 1; SHIFT=12 ties on loss (headroom flag for long births).

## Standing state

- **Axiom is CO-LOCATED on the Mac now**: ~/code/axiom (their
  FX-V3/R2 artifacts read directly; no transfer needed). 3080
  was unreachable at last check (ssh timeout) — expected, Artin
  has the box; night31b stays [HOLD] for GO
  (scratch/night31b_cuda.sh: lambda-merges + R0 TF32-off ladder).
- Relays: house->axiom queue = FX-V3-accept receipt (-1), R2
  spec (-2), NEXT: the night wrap relay (Q_w pin + FX-V3 house
  PASS receipt). Axiom awaits Q_w for the full-birth C++ leg.
- Suite 396 passed. Stray cpp_s* artifacts archived.

## Next queue (in order)

1. R2b: full-block integer birth (rmsnorm bwd, rope bwd, CE/
   margin loss, attention wired) — Mac desk work, then 3080
   cross-device on GO.
2. Wire ckpt-sha into gate_eval + manifest cross-link (the
   provenance rule, second half).
3. Diet meter (llmopt.runlog child loggers; exposure share by
   level/depth) — Artin's logging riff, promotion bar applies.
4. night31b on GO; then transport n=6 merge reviews.
5. Banked: exact-manipulation diet share for L4; ckpt DB;
   checkpoint forensics (51GB triage, Artin GO).

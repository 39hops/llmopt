# Handoff 2026-08-19-0: MODEL-2 registered, run, and BOOKED in one night — the crossover holds on a held-out surface

Seat: Fable (main session model), Mac, overnight on Artin's GO.
HEAD at close: 8aa76c6, local == origin/main. Both machines idle;
no runs in flight. 3080 holds all composed artifacts (A..FLl +
PX/PK); Mac holds none (receipt-gated cleanup throughout).

## The headline

VERDICT QWEN-MODEL2-ALLOC-1 (L35183): ALL FIVE BARS FIRE on the
held-out surface. From the FLe state, the next 461,276,672 bytes
buys corpus-X with the MID linear band (dX 0.0874 v late's
0.0065) and prefix-K with the LATE band (dK 0.0303 v mid's
0.0103) at identical spend — the LBAND F-conditioned table
transported in sign and magnitude-class (X at 68% of the
development marginal, inside its registered +-0.05 band).
Unregistered two-direction color: the two-band policy beats
endpoint C on X (0.3806 v 0.3950) while C keeps the best K.
Milestone: first verdict adjudicated under REGISTERED-JSON
precedence (gate_class + refutation_precedence shipped in
llmopt/lab/prereg.py, CLI wired, 24+5 fixtures) — the two-gate
law is now executable, not doctrine.

## The night's arc (all committed + pushed, in order)

- Design spec r1 -> r2 -> r3: GPT identity catch killed r1 (BLe
  already CONTAINS io — census-lattice pricing lesson, recorded
  in spec SS0); r3 added the held-out surface requirement.
- Engine: refutation_precedence + gate_class (sanity/range) +
  int-only ids + CLI bars-before-refutation + end-to-end fixture.
- Held-out surface evals/qwen_model2 frozen pre-compose
  (disjointness asserted; one post-freeze schema-only edit to
  prompts.jsonl, disclosed in the verdict).
- PRE-REG committed 21:39, BEFORE teacher pass (21:43-23:07,
  ae4b3a2, 362 corpus tokens), composes (23:11/23:13, 3080),
  scores (23:36-00:23, Mac) — git-order verified by auditor.
- Scorer grew: per-surface teacher pins (source-frozen map),
  EV/OUT_DIR plumbing, nonfinite write guard + consumer refusal
  (NaN fixture). One mid-chain catch: the single-manifest guard
  correctly refused when two teacher dirs appeared; fixed to
  accept exactly the pinned set.
- Both auditors pre-booking: NO blockers; every should-fix
  adopted as booking disclosures (4-commit chain, prompts edit,
  floor-fence sentences, 68% transport framing, unregistered
  endpoint contrast with the K reversal beside it).
- Riffs banked: RESIDUAL-RECON (correction-field precision;
  weight-distance law fenced), ATTN-ROUTER-CENSUS (M_k mass
  capture; linear-state-as-router candidate), FULL-ATTN-UPLIFT
  (linear->softmax uplift, distill-to-parity first).

## Conditions that bite next session

- scratch/qwen_model1_score.py is results-cited and has now been
  extended in place for a third surface — LAB ADOPTION is the
  banked alternative (auditor S3); do it before a fourth surface.
- teacher.corpus_sha/prefix_sha receipt fields are RECORD shas,
  not input-file shas (rename banked for next scorer touch).
- LBAND interference cell did NOT reproduce on the new surface
  (late-band X marginal +0.0065 held-out v -0.0037 development)
  — surface-dependent, both under a centinat; color only.
- BLe free-generation screen: separate observation, still
  waiting its GO (registration froze first, per plan).
- CAL-FEAS-0 parked; ALe mechanism 2x2 banked; the three riffs
  above priced census-first.
- 3080 artifact garden is large (13 artifacts); cleanup is
  Artin-GO under the housekeeping gate.

## Next session opens with

1. This handoff -> BOARD -> RESULTS tail.
2. Artin decisions: BLe free-gen screen GO; which banked census
   (RESIDUAL-RECON v ATTN-ROUTER-CENSUS) gets the next slot;
   scorer lab-adoption slot.
3. The allocation program's natural next question: does the
   crossover REVERSE at the fourth pick (C minus one band), or
   register the K-primary ladder.

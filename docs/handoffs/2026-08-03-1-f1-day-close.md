# Handoff 2026-08-03-1: the F1 day, closed

## One line
V4-Flash generates on the Mac at **0.268 tok/s** (2.68x the morning's
first number), every rung pre-registered, every failure booked with its
mechanism, two reviewer passes adopted the same day.

## The chain (all in RESULTS.md, thread f1-mac)
PRE-REG V4-F1 -> F1a (kernel twin, oracle bars) -> F1b (vendor boot;
chaos-at-random-weights measured) -> F1c (real weights; gap collapses
56-297x) -> F1d (THE DEMO: tokens on the Mac, echo-loop output as
registered) -> PRE-REG V4-F1e + 3 riders -> F1e (bf16 NULL, 84% profile,
cache thrash, pair-LUT 2.2x) -> F1e-ARM5 (bit-exact batching, +1.22x)
-> AMENDMENT F1-REVIEW (all verified reviewer findings; FINDINGS now
carries 5 F1 bullets; ratchet exactly 300/300).

## What the misses taught (the transportable part)
- Cost is work-per-byte-REDONE, not parameter count (arm 1 null).
- A cache sees the per-token WORKING SET, not the "hot set" (arm 3).
- Neither launches (arm 5) nor dtype (arm 1) bound this decode: unpack
  VOLUME does. Only multi-token verification amortizes volume.
- Equivalence to a quantized model = reproducing its ROUNDINGS (arm 5's
  two gate failures, then 0.00e+00).
- Register bars derived from the algorithm (the 1e-6 sinkhorn bar was
  unattainable by the vendor's own eps placement).
- The profiler's synchronize() serializes what the async queue overlaps:
  its SHARES transport, its TOTALS do not.

## Next arms, all banked, all needing pre-reg + GO
1. PROMPT-LOOKUP SPECULATIVE DECODING (llmopt/decoding/prompt_lookup.py
   is in-tree; echo loop -> ~100% draft acceptance; token-identical bar
   via eval/equivalence.py). The only lever that cuts unpack volume.
2. UNMASKED-ROUTER "EXPERT RECALL" LOG — one instrument, two questions:
   is the echo loop amputation or a mask bug (the bias-write mask has NO
   acceptance bar — the demo's one unchecked mechanism), and does
   most-negative-bias beat random under noaux_tc.
3. fp8-dense + pair-LUT K-sweep on the ~6 GB reclaimed from arm 1's
   null; ΔKL-vs-K=24-reference via train/ref_logprobs.py is the legal
   quality readout (no gate, no capability claim).
4. H1 exact hash-layer co-activation (desk cost, spec item).
5. MLX int4-GEMV port only after batched shapes exist (booked win band
   starts at large shapes; batch-1 was a booked loss).

## Debts
- The "~28% pruning cliff" cited by BOTH F1 pre-regs lives only on
  BOARD.md:102 — never booked in RESULTS. Retro-book or run Q1.
- Reviewer seats keep self-reporting Claude Opus 4.5 under an Opus 5
  harness label (twice today) — flagged to Artin both times.
- checkpoints/v4flash_f1 is 19 GB against 12 GB free disk; delete to
  reclaim (refetchable), keep for K-sweeps.

## State
main @ 87335c2 pushed; 451 tests pass; ratchet 300/300; no jobs running;
3080 untouched today after the morning sync.

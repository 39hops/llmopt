# Handoff 2026-08-21-1: ROUTE-DB priced honest (LRU@K48 is the fact, phase tables die on holdout), TOPSET confirms the preserved manifold, EXPERTDB reframed to latent route mixture

Seat: Fable 5 on the Mac; both machines open (Artin at work,
standing "keep going"); HEAD at close = this handoff commit; 3080
used for one short-test census (TOPSET, seconds of CPU), idle
again. Continues 2026-08-21-0 (the phase-mechanism night) same
day.

## What landed (with shas)

- Post-cleanup integrity sweep after Artin's `mole` cache purge:
  suite green, pinned snapshot sha-verified 3/3, all artifacts
  intact; disk 17 -> 70GiB free (out-of-core disk constraint
  dissolved).
- RIFFS: EXPERTDB/ROUTE-DB control-plane bank + IMPLICIT-EXPERTDB
  (3b3d1701, house grain/deployment corrections);
  DOMAIN-CAPABILITY-COUPLING (90537882 — routing measured six
  ways, capability one way; code-gate transport rung is the
  falsifiable residue); EXPERTDB REFRAME to latent route mixture
  (5c425c59, MOE-GT-5 verbal-necessity anchor verified L20179).
- OBSERVATION ROUTE-DB-REPLAY-0 (e635428f) + AMENDMENT -SIM
  (f6ddd7dd): registered K<=32 read SPLIT; robust zero-fit read =
  plain LRU@K48 63.7-103.7 MB/decode-token all six traces;
  phase-static numbers were lower bounds (uncharged churn),
  Belady variant not a ceiling, prefetch depth claim retracted.
- OBSERVATION ROUTE-DB-REPLAY-1 (fb23e319): under holdout +
  charged churn the phase-table route is DEAD (phase32/lru48
  ratios 1.6-4.7, all six traces); zero-fit LRU@K48 TRANSPORTS
  (65-95 MB/decode-token unseen prompts); true BELADY-BYPASS
  ceiling shows ~2x implementable headroom in reuse structure.
- OBSERVATION TOPSET-OVERLAP-0 (9d69ac78, 3080 short window,
  receipt sha-identical both machines): candidate manifold
  survives compression (top-256 recall 0.957-0.973, Spearman
  0.979-0.983, rec@1 = 0 at the five near-tie loci);
  drop-top256 ALTTOKEN redesign priced DEAD (first outside token
  at BLe rank 204-223, 12.3-18.6 logits down — the
  CONTROL-MATCH-FAILED class); live direction = in-manifold
  controls (the other equally-BLe-plausible top-8 candidate);
  PORTABLE top-1024 sidecars both heads in the receipt.

## Conditions that bite

- ROUTE-DB stall columns are Mac-SSD-priced; any 3080 claim
  needs its own NVMe->host->pinned-H2D measurement under
  concurrent decode.
- The 6x6 domain transport matrix is MOOT for residency (rung 2
  killed the table route) but live for DOMAIN-CAPABILITY-COUPLING.
- EX6 receipts frozen; NInfer survey + out-of-core preflights
  runnable anytime (zero GPU).

## Next session

Start: this handoff -> BOARD -> RESULTS tail. Recommended:
1. ROUTE-BASIS census (EXPERTDB rung 3, zero model cost, spec in
   the bank): routing-signature spectrum, cross-domain NN rate,
   held-out demand prediction under 4 priors.
2. ALTTOKEN in-manifold redesign registration (new receipt paths;
   TOPSET sidecars as operands) — needs Artin's design pick.
3. Prefill-poisoning mechanism rung (EX6's open question).
4. PRECISION-CREST-TRANSPORT / out-of-core preflights / code-gate
   domain transport.

## Open decisions for Artin

1. ALTTOKEN redesign: adopt the in-manifold control (BLe rank-2
   candidate) or park the thread.
2. ROUTE-BASIS now v the mechanism rung first.
3. 3080 nightly slot tonight: TOPSET is done; candidates are the
   NVMe I/O measurement or nothing.

## Standing

Nothing measured-unbooked; no watchers; both machines idle. Suite
green at close (handoff commit). Bookings this block: 3
observations, 1 amendment, 4 riff banks/updates.

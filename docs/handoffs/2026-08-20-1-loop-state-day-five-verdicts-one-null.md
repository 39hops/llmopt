# Handoff 2026-08-20-1: the loop-state day — five verdicts (one registered null), four amendments, the homeostatic program banked

Seat: Fable (main session model), Mac + 3080 (Artin GO through
~15:00 EST, both machines released at close). HEAD at close: this
file's commit. Both machines IDLE, lockstep, nothing armed,
everything measured is booked.

## What landed (all committed + pushed, chain order)

- VERDICT QWEN-LOOP-STATE-0 (L36345, 1e19d37): bar 1
  INTERNAL-ORBIT NO-FIRE KNIFE-EDGE (homologous cosine medians
  0.9963 / 0.9701 v the uncalibrated 0.99 bar; NOT refuted, both
  >= 0.90), bar 2 STUCK-RETRY FIRES (98.4% top1, median JS 1.7e-5
  nats). P1 sha-identical regen, capture fixture bit-exact 24/24.
- AMENDMENT -COLOR2 (281cdae): phase-shift calibration — the
  recurrence contrast is huge (homologous 0.998/0.984 v
  nonhomologous baseline 0.32-0.54); retry attempts CONVERGE
  (per-pair JS medians 1.9e-4 -> 2.8e-6); margin-is-not-depth and
  greedy-determinism wordings deny-listed; receipt-lock
  brace-expansion cite gap fixed.
- VERDICT QWEN-LOOP-STATE-1-HEADSWAP (L36594, c843a44) +
  AMENDMENT -SCOPE (5a15eba): static vendor-head replacement on
  frozen BLe states largely preserves the stuck local policy
  (M2 numerically the BLe fraction 0.984375; 99.6% top1
  agreement); OFFLINE-CONDITIONAL scope — trajectory-level
  readout causality stayed open (global "bystander" shorthand is
  deny-listed; do not resurrect it). First artifact_identity
  consumer; vendor slice tensor-byte attested; M3 renamed
  top1-v-top2 hyperplane distance + min-over-top256 depth bound.
- VERDICT QWEN-HEADSWAP-IMPULSE-0 (L36833, d2b8b3d) + AMENDMENT
  -WORDING (ed76c09 + follow-ups): one vendor-informed token at
  the five measured disagreement points — 5/5 recurrence-return
  (gaps 33-176, three near the 32 detector floor), 0/5
  original-orbit rejoin (4/5 NEW exact periodic tails, one
  non-periodic under caps), 0/5 repair (cap-limited). Registered
  "cannot redirect" gloss narrowed at booking (CYCLE precedent).
  Independent offline consumer authoritative after receipt-audit
  caught an inert cross-check AND a non-independent oracle — both
  fixed and re-run pre-booking. CANONICAL SYNTHESIS (use this
  form only): measured micro-perturbations redirect
  trajectory/recurrent-pattern identity while the recurrence
  tendency survives; no durable escape or repair demonstrated.
- VERDICT QWEN-MIPS-CENSUS-0 (L37116, 2815c95): THE REGISTERED
  NULL, called by the house prior — certification perfect
  (1362/1362 exact top-256) but isotropic k-means-ball
  Cauchy-Schwarz bounds barely prune at d=5120 (corpus q50
  visitation 0.999/0.997/0.989 at K 256/1024/4096; REFUTED-IF
  fires knife-edge 9.87% via a deliberately EXTENDED bar fence);
  modeled byte ratio > 1 at every K. Disclosures on the record:
  partially unblinded at K 256/1024 (conformance abort, exposure
  receipt jobs/mips0.log force-added), offline exact
  COUNTERFACTUAL census (not a realized sparse runtime), pre-run
  -BOUND amendment (sign-broken multiplicative inflation caught
  by the smoke fixture). Prior right on bars, WRONG on
  prefix-prunes-better (median visitation bit-identical). This
  ISOTROPIC recipe is closed by its own null; different-geometry
  and approximate routes are separate registrations.
- Process infrastructure: artifact_identity in
  llmopt/lab/provenance.py (+ vendor HF-index support, regression
  tests) — next drivers MUST pass artifacts=; receipt lock knows
  the loop-state family; wsl_guard read-verbs widened; handoff
  2026-08-20-0 corrected per post-compact review.
- RIFF banks: HOMEOSTATIC-INFERENCE umbrella (failed-feedback
  pattern wording — internal sensor/actuator decomposition
  UNMEASURED; compression-ASSOCIATED; operational ladder;
  analogy fence), PRECISION-SWITCH STATE CONSISTENCY (mixed-
  precision state machine; LOW / REFRESH-LOW sanity / HOT-HIGH /
  REFRESH-HIGH matrix; one-way first qualification; state-v-weight
  causality not uniquely identified by the HOT/REFRESH contrast),
  LEVEL-2 exact-MIPS design + its booked null outcome,
  CONVEX-CELL + MARGIN-IS-NOT-DEPTH laws, SYMBOLIC-DYNAMICS
  wording law, five bank outcome/correction notes.

## Next session opens with

1. Canonical resume: resume-protocol memory -> BOARD -> this
   handoff -> RESULTS tail (LOOP-STATE-0 prereg onward, 11
   entries).
2. HOMEOSTATIC ACTUATOR QUALIFICATION prereg — the deliberate
   next registration, in a FRESH session (house + GPT agree: the
   precision-switch state-consistency design is heavy). Everything
   needed is banked: the LOW / REFRESH-LOW (sanity) / HOT-HIGH /
   REFRESH-HIGH matrix, one-way intervention, REFRESH-LOW must
   reproduce the deterministic LOW baseline exactly before
   REFRESH-HIGH is interpretable, drift-reset confound to name
   when interpreting a REFRESH win, candidate win condition gets
   NUMERIC form only at registration.
3. 3080 queue on Artin GO (explicitly promised after LEVEL-2, now
   satisfied): matched ALTERNATE-TOKEN CONTROL (the contained
   optional extra — generic v vendor-informed token, same five
   loci), vendor-body leg 3 (priced, motivation now runs through
   trajectory-formation causality).
4. Also open: LEVEL-2 different-geometry routes (only with index
   cost priced first), 3080 artifact cleanup stays HOLD.

## Conditions that bite next session

- The deny list grew four entries today (decision-cell depth,
  unlucky-sampling, measured-out readout, bystander shorthand) —
  claim_lint drafts, and use the canonical scoped synthesis above.
- Banked forward fixes: shard-content root for artifact_identity;
  qualify-report into receipts; OPERAND-PROVIDER schema field;
  MIPS driver forward-fixes are IN (derived width, hoisted guards,
  smoke-covered obs).
- Frozen evidence paths now include logs/qwenloopstate/,
  logs/qwenloopstate1/, logs/qwenhsimpulse/, logs/qwenmips/ —
  never append; npz primitives stay machine-local, sha-pinned.
- The loop-state npz on the 3080 are the reusable substrate for
  the alternate-token control (injection recheck pattern in
  scratch/qwen_headswap_impulse.py).
- Session ran POST-COMPACT from handoff 2026-08-20-0; the
  three-verdict trajectory night plus this day form one
  continuous program — the RESULTS chain reads in order from
  L35764.

# Handoff 2026-09-13-0: VERIFIED-ENDOGENOUS-DATA-CROSSFOSTER-1 donor stage booked DONOR-HISTORIES-DEGENERATE; the replay rung is not banked as designed

Seat: Fable 5.1 on the Mac. HEAD at close: the commit carrying this
file. 3080 untouched. No live registered run. Nothing armed.

## What landed (after handoff 2026-09-12-0)

- Artin GO (21:30 EDT 09-12): new program VERIFIED-ENDOGENOUS-DATA-
  CROSSFOSTER-1, DESIGN + PREREG + DONOR-LIBRARY stage only; the
  foreign-writer program stays parked.
- PRE-REG -DONOR (RESULTS L72387, 55f78979) + AMENDMENT -AUDIT (L72562,
  13a02ba8): two Opus passes, no blocker; folds included donor / W_0
  digest assertions, a prefix-preserving root order, a disjoint sample
  band, the DEGENERATE clause marked as a house addition, complete
  smoke provenance. Instrument scratch/crossfoster_donor.py (+ launcher,
  4 tests); calibration on a disjoint smoke band showed K = 2
  inaccessible and 37 / 39 overlap at K = 1.
- Run crossfoster1d at 656970e8 (02:12 to 16:49 UTC, 14.6 h; Monitor +
  re-armed waiters; the harness stops a waiter after a few minutes, so
  the Monitor was the effective signal).
- OBSERVATION -DONOR (RESULTS, this commit): DONOR-HISTORIES-DEGENERATE.
  6,000 retained roots of 15,900 processed (diet exclusion 19.6 %,
  adequacy A 0.56 / B 0.52); canonical overlap 5,588 / 6,000 = 93.1 %
  (>= the registered 0.9); union of all verified rows overlaps 89 to
  92 %, donor-specific rows on 17.9 % of roots; cross-model CE self-
  preference 0.001 / 0.003 nats. The four-cell replay rung is NOT
  BANKED as designed (contrast 0.24 % of the recipient stream).
  Prior 5 hits 1 miss (family 35 / 22). Libraries frozen and tracked at
  data/crossfoster1/ (1.5 MB each).

## Conditions that bite next session

- Nothing armed. Redesign candidates (each a new pre-reg + GO): a
  stronger writer contrast (the census pair is the weakest the house
  holds), a multi-ply chain generator (the gate's 12-ply chains
  diverge more than one-step rows), all-PASS unmatched-dose libraries
  (union overlap still 90 %), or park the program.
- The harness kills detached background waiters after a few minutes;
  use the Monitor as the primary signal and re-arm a waiter only near
  the finish.
- Disk about 31 GiB free.

## Open decisions for Artin

1. CROSSFOSTER-1: redesign (which of the three) or park.

## Next session: where to start

This handoff, BOARD line 5, RESULTS tail from the -DONOR observation,
the RIFF bank COUPLED LEARNING DYNAMICS / ENDOGENOUS DATA (measured
anchor added in place).

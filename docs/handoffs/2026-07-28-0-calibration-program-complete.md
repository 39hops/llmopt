# Handoff 2026-07-28-0: the calibration program, opened and closed in one night

All four rungs run, booked, consolidated (BOARD row current;
FINDINGS entry added; spec specs/2026-07-28-calibration-program.md;
plan docs/superpowers/plans/2026-07-28-calibration-program.md).

1. **R1 PASS — the instrument ships**: calib_probe.py
   (flips/token under DIRECT Q=16 rational snap) predicts snapped-
   gate damage at rho .883 / p .02 over 6 crystals, ~1% of a
   gate's cost. v1 ran the WRONG snap (rat_deploy's scaled
   operator — incidental finding: scaled-Q16 is capability-free
   across the whole d256 zoo, 5x parity); operator-matching
   doctrine banked.
2. **R2 — mass-on-valid**: crystals put >97% of valid mass on one
   of ~7 enumerated moves; H_valid DESCENDS with capability. The
   branching-floor stays theory (nothing spreads to measure).
3. **R3 parity (62 ctl / 62 dosectl / 60 dist)**: the altpairs -6
   was SHARE, not labels (matched-dose replacement is free); no
   gain either — calibration training demotes to diagnostic;
   probe delta directional (dist fewest flips). Same-day-control
   doctrine paid again (fresh ctl 62 v historical 65 = tree
   drift -3).
4. **R4 null-by-tie-scarcity**: judge=greedy 18=18 (+31 tokens/30
   states — branching ~never fires; median margin 8.6). The
   keeper: **greedy 18/30 @ 1,130 tokens v wave-8 20/30 @ 9,072**
   — greedy-first/wave-on-retry is the farm-probe speed candidate.
5. Side threads tonight: gauge-slack 4-crystal NULL (ancestry
   dominates every lens); d2 "bit-identical" scoped by amendment
   (count+outcome; state_dict/flip-set/probe verification queued
   next 3080 window — metab_d2_{fp64,dd}.pt live there); THE
   EXACT WAVE-MODEL slotted (Artin: exact algebra x pincer wave x
   LLMUE; rungs on the exact-stack BOARD row); relay -28-0
   (axiom close note) + -28-1 (outbound forward-successors ask,
   prompt delivered to Artin); full-corpus reread banks (S2-dist
   judge [died with R4], flips-vs-Q fingerprint, policy-sharpness
   law candidate).

## NEXT (cheap first)
- Greedy-first adoption cell (pre-reg: greedy+retry ties wave-8
  solves at <40% tokens on a gate-sized battery) — lossless-speed
  class.
- flips-vs-Q curve fingerprint (probe at Q in {4,16,64,256}).
- 3080 window: d2 endpoint verification (3 CPU reads).
- E3 paired arm fires whenever FX-V1 exact mode lands.
- Exact-representations queue unchanged: GPTQ-rational,
  int8+outliers, thin-film retarget (Mac-19M rat16@26).

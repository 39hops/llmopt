# Handoff 2026-07-24-1 (the nine-verdict night)

Reads: -24-0 (the verdict wave, morning). Reviewer agent era began
today — every verdict below was independently cross-checked before
booking (Opus reviewer, read-only, mentions-only; policy in
CLAUDE.md + memory).

## Verdicts booked tonight (all in RESULTS, newest-last order)

1. **GEN-9 A/B: redistribution, not a winner.** Leak theory
   DIRECTIONAL sub-noise (+3 rare+unseen at HALF dose — 1,275 of
   2,833 "failed steps" were miner identities; full-dose rerun
   queued behind the now-landed miner guard). Counterweight NOT
   observed (B up 2, all L7). Rations validated (L3 22 both arms).
   B = forward pick, production = statistical tie.
2. **poly5 knock-3: all legs FAIL, wall named to the COEFFICIENT**
   — icancel 15/15 vs ibridge 1/15; miss-classification: 12/14 =
   wrong residue arithmetic (structure right, silent A=N(a)/(a-b)
   wrong). KNOCK-4 pre-registered (pure-copy re-spell, rationed
   recipe, door-only bar, HARD-STOP after — no fifth).
3. **ODE probe: VOID BY VOCABULARY** — vocab-41 cannot spell
   Derivative/y/Eq/C1. Champion-control re-registered onto the
   first ODE-vocab birth. Tokenizer silent-drop footgun FIXED
   (strict encode, tests green).
4. **Desert v2: coefficient (iv) = 0** — 128 proposals on
   Liouville-dead states, 0 valid, 0 series reaches: the crystal
   cannot discover the jailbreak it contains. BRIDGE LAW STRONG
   FORM: nothing transfers without demonstrated shared steps (not
   co-residency, not composition, not vocabulary). Instrument saga
   booked (my sampler missed the newline break; control caught it).
   Rider: naked textbook forms FAIL (exposure, not difficulty) —
   thin naked-forms shard = gen-10 candidate.
5. **45M union re-ask: WIDTH DOES NOT PAY for primitive
   federations** — 19M 119 v 45M 108 same-census; primitive rows
   are token-LIGHT so W* stays 19M-class; k(W) re-prices in TOKEN
   mass. fp32 45M control queued (bf16-birth confound unexcluded).
   Gen-10 sizing: 19M pending control.
Plus morning's: B@768 amendment, gen-8 all-pass, v5 s1, d2 null.

## Exchange / axiom (7+ clean batches)

- Wall-farm rounds 1+2 DELIVERED + CERTIFIED: 53 rows, 0 parse/
  dupe/audit defects (two hygiene rounds: kill-mid-flush splice +
  merge dupe -> their merge_shard.py delivery gate now permanent).
  22/100 walls resolved. **v5 s2 food banked, run HELD** (Artin on
  the 3080); miner guard prerequisite LANDED tonight.
- Wedge autopsy closed: pow-base cancel-loop key mismatch (fix +
  wild specimen committed their side); cos(log) hardkill = unbudgeted
  post-solve annotate(), also fixed. Round-3 queue: 78 walls, ready.

## Code changes tonight (Fable)

- MathTokenizer.encode strict=True default (raises on unencodable;
  legacy escape hatch). 368 tests green.
- metabolic_v5 miner: verified-AND-DISTINCT at bank time (identity
  guard; catches 1,275/1,275 known v5 identities).
- scratch/desert_v2.py (composition probe, with in-line controls).

## Instruments / hazards (READ BEFORE COMPARING ANYTHING)

- **FOUR incompatible rarity censuses** now exist (orig / Mac-3rd /
  gen-9-Mac / WSL-4th). Within-census pairs ONLY. Taxonomy
  surgery's re-baseline is now urgent, not cosmetic.
- Device pins: 19M cuda continent probes matched MPS exactly
  (99.2=99.2) — exact-coefficient probes are device-stable; chain
  gates are NOT (9/24 v 18/24 stands).
- Reviewer agent: 3-for-3 on catches (arm-A poly gap, census
  drift, THEORY overclaims). Persistent for the session;
  re-spawn + re-orient per session (docs are the rebirth corpus).

## Queue for the weekend (specs/2026-07-24-evening-queue.md + tonight)

1. v5 s2 (3080 free + shard certified + guard landed = just run it)
2. taxonomy surgery in a calm window (+ rarity re-baseline of
   standing checkpoints on the new census — acceptance item)
3. 45M fp32 control (one-variable, cheap) -> gen-10 sizing law
4. gen-9 FULL-DOSE rerun (guard now yields ~2,833 real failed steps)
5. poly5 knock-4 ask to axiom (pure-copy re-spell) + round-3 farm
6. boundary-or-bulk (~1 hr, Artin's standing first-at-GO)
7. Tier-2 revivals: KV-batched gates, graph-modularity on gen-8,
   duo-teacher KD, mass->robustness leg, blind committee pre-reg
   (standing rule for every new grammar)

## Machines at handoff

Mac: free. 3080: ARTIN'S (nothing fires without his GO). Axiom:
idle, awaiting knock-4 ask + round-3 GO. All three repos synced at
origin/main.

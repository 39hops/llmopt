# Handoff 2026-08-16-0 — config night: write-time rules into CLAUDE.md, ECC skill catalog pruned 284 -> ~38 live

Seat: Fable 5, Mac. HEAD at close: see the handoff commit. Science
state UNCHANGED from 2026-08-15-2 (atlas-dies-arithmetic-lands) —
read that one for the live research state. This handoff records a
config-only evening block; no runs, no bookings, both machines
idle.

## What landed

- **CLAUDE.md, three additions** (0dfbd1d + this commit):
  1. Smoke runs are path-isolated (receipts AND checkpoints;
     refuse-guards unconditional) — from the two 08-15 incidents.
  2. Receipt provenance is DERIVED, never a literal — the
     RULE-ABLATE false-emitter class, now a write-time rule.
  3. Data-contract vocabulary under Data hygiene (adopted from
     ECC mle-workflow, house-mapped): row grain, label timing,
     split policy; "random split leaks future data" as the
     generic form of both booked contamination incidents and the
     BASICS-DIET-1 farm catch; RED means executed-and-observed.
- **ECC skill catalog pruned** (~/.claude/settings.json
  `skillOverrides`; backup at ~/.claude/settings.json.bak-ecc;
  effective from the NEXT session): 198 skills off (webdev,
  mobile, marketing, healthcare, homelab, wrong-language stacks,
  plus CHARTER items — gget genomics, pubmed/uspto, all
  healthcare/HIPAA), 48 demoted to user-invocable-only (typable,
  zero model context; includes continuous-learning-v2 [doctrine
  conflict: unversioned instincts, writes outside ~/.claude],
  tdd-workflow [TypeScript-shaped], blueprint, mle-workflow),
  ~38 live (scholar-evaluation, literature-review,
  agent-harness-construction, manim-video, benchmark*,
  python/pytorch/cpp patterns+testing, eval-harness,
  verification-loop, loop-design-check, santa-method, config-gc,
  search-first, and peers). Full categorization rationale is in
  the 2026-08-16 session transcript; the mechanism is Claude
  Code's `skillOverrides` (per-skill, plugin stays enabled).

## Next session, in order

1. Resume protocol: 2026-08-15-2 handoff -> BOARD -> RESULTS tail.
2. **House pick: CROSS-TERM-DECOMPOSED EXPAND pre-reg** — state
   a*d + b*c as its own step-row family; the direct fix for the
   counted failure mode (68-76% of expand misses are
   ends-right-cross-term-wrong) and the test of whether the
   diet's 18.2% algebra spend starts paying once its inner step
   is stated. BASICS-DIET-1's other banked follow-ups (mixed-
   format arithmetic rows; dose/horizon ladder on the transfer
   rider) queue behind it.
3. Alternatives unchanged from -2: SOFT-SPEED-1b, RULE-POLICY-0
   rung 0 census, RULE-ABLATE seeds 4/5, skip-distance ladder.

## Open Artin decisions

Unchanged from 2026-08-15-2 (items 1-8), plus:
9. Any skillOverrides flips (each is a one-line settings change;
   the demoted 48 stay /-typable for cherry-picking).

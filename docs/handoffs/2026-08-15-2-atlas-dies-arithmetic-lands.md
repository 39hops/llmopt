# Handoff 2026-08-15-2 — the atlas dies, arithmetic lands: GRAD-MAP-0 null with a firing falsifier, BASICS rescoped by census and booked same evening with a format-locality rider

Seat: Fable 5, Mac. HEAD at close: see the handoff commit. Session:
continuation of 2026-08-15-1 (same day, evening block). Mac IDLE at
close; 3080 idle all session. Nothing armed, nothing waiting.

## What landed (all pushed)

- **Spec amendment GRAD-MAP-0** (475a6f3, pre-look): four Opus
  read-only surveys + house verification found the spec named the
  WRONG axiom checkpoint for R1 (dose7 reads 72 > sympy 70 — the
  inverted comparison; matched-dose dose3p5 is the booked datum),
  referenced optimizer state no atlas checkpoint carries (all five
  save state_dict only), and took its failure column from the
  seed-2 phase lineage (stock s3's own L4 cell is 6 = 18 failures,
  not 17). All fixed pre-look; riders RD1-RD4 pre-declared,
  including an L6 falsifier column and a surface-kNN negative
  control.
- **VERDICT GRAD-MAP-0** (RESULTS L30127, booking 786e989,
  drivers ba4539b/1336ddb): retrodiction gate FAILS on R2 — no
  metric in the frozen set ranks i_heurisch above the non-heurisch
  remainder on the L4 column (cosines fail by 0.043-0.067). R1
  passes 4/5 metrics and the gradient cosine beat the surface-kNN
  control on that leg (the credit line). The pre-declared L6
  falsifier FIRED against the atlas: 0.094 cosine gap between
  families whose L6 gate cells tied 9/9 — the atlas ranks by shard
  composition, not capability-worth. Instrument dead in this
  metric set; BASICS-DIET ran unshaped; MIXER-1 stays parked.
  Positive residue: same-process mps forward/backward signatures
  repeat at 1.000000 (fence unchanged — it binds training);
  level-slice diagonal affinity observed; instrument anchor
  reproduced the booked stock s3 gate to the digit.
- **OBSERVATION BASICS-CENSUS-0 + BASICS-PROBE-0** (L30259,
  1ebf7bc): the basics bank's premise was half wrong — algebra is
  ALREADY 18.2% of diet rows (29,988 bare rows, five 6,000-row
  families); arithmetic is 12 incidental rows (0.01%), none a
  division. Probe at three frozen checkpoints: arithmetic at a
  0.00-0.83% pass@1 floor, and 68-76% of resident-expand misses
  are ends-right-CROSS-TERM-wrong — the failure inside algebra IS
  arithmetic. Thresholds were named before counting; the probe
  landed on the honest RESHAPE branch (resident arm under its 50%
  floor), not the pre-named PROMOTE.
- **PRE-REG BASICS-DIET-1** (L30355) + **AMENDMENT -SHARD**
  (L30429, a330323): rescoped to arithmetic. The amendment is a
  contamination catch: the first farm run EXHAUSTED the probe's
  operand space (242/242 mul pairs — every probe item would have
  been a training row, the eval-in-train class). Re-farmed with
  widened ranges + explicit exclude= of all 2,264 probe prompts;
  verified probe-cur INTERSECT shard-cur = 0. Shard 2,545 rows =
  1.54% dose.
- **VERDICT BASICS-DIET-1** (L30467, booking e4711c8, driver
  3aed609 + guard fix 92a1e3e): **BOTH BARS FIRE.** Standalone
  arithmetic 1.67% -> 61.67% pass@8 (0 -> 34.17% pass@1) at a -2
  gate dent inside same-seed run noise (control 70 = exact
  replication of the booked atoms-sympy total; L4 11 v 12 = the
  mps cross-run solve). **The transfer rider is the finding**:
  expand and its cross-term DID NOT MOVE (72/108 v 79/108) while
  near-format numsum rose 17.5 -> 26.67 — competence radiated by
  FORMAT DISTANCE, not operation. First measured datum for the
  format-as-routing bank. Prior 2 hits 1 miss; family record 9
  hits, 20 misses.
- **Ledger upkeep in the same commits**: GPT-nine bank item (1)
  and round-2 residue marked DISCHARGED (gate failed, program
  priced); basics bank stamped MEASURED with the rescope; format
  bank carries the new datum. FINDINGS +3 bullets, all anchored.

## Auditor process notes (worth keeping)

- prereg-auditor caught two real draft blockers on GRAD-MAP-0
  (a false "both directions wrong" on RD2's F4; post-look metric
  aggregation on R1) and one on BASICS-DIET-1 (the smoke run
  shared the real checkpoint path and the arith arm's
  refuse-if-exists was satisfied by a MANUAL delete — disclosed in
  the verdict, fixed at 92a1e3e).
- receipt-auditor caught a provenance-bare receipt row (the RD2
  completion pass, written by an uncommitted scratchpad script) —
  row stripped, script committed as scratch/gradmap0_rd2.py,
  re-run, reproduced to 2e-12.
- Two smoke-caught driver defects never reached receipts: a 310GB
  projection matrix (OOM) and fp32 dot-product drift printing
  cosines of 1.005 — fp64 metric arithmetic fixed it.

## Conditions that bite next session

- Newly FROZEN (results-cited): scratch/gradmap0_probe.py,
  scratch/gradmap0_rd2.py, scratch/basics_census0.py,
  scratch/basics_probe0.py, scratch/farm_arith.py,
  scratch/birth19m_arith.py, scratch/basicsdiet1_driver.sh;
  receipt paths logs/gradmap0/, logs/basicsdiet1/,
  logs/basics/probe0.jsonl, logs/pp_gradmap0_stock_s3.jsonl (all
  force-added small-text receipts). Extend via siblings.
- Exhaust +4 checkpoints today (~300MB: basicscontrol/basicsarith
  + the two from -1's list still pending the keep-or-delete
  ruling; total decision now ~10.9GB).
- The arithmetic claim is scoped to SMALL-INTEGER operands
  (|a| <= 60) per the amendment — do not quote it wider.
- probe0.jsonl now carries 5 rows (3 pre-birth + 2 arms); any
  future probe0 run appends — fine for the file, but cite rows.

## Next session, in order

1. This handoff -> BOARD -> RESULTS tail (resume protocol).
2. House lean: the TRANSFER question is now the sharpest object in
   the family — three banked follow-ups in the verdict:
   (a) mixed-format arithmetic rows (same fact inside an expand
   context), (b) cross-term-decomposed expand family (state
   a*d + b*c as its own step — the direct fix for the counted
   failure mode), (c) dose/horizon ladder on the rider. (b) is
   the house pick: it tests whether the 18.2% algebra spend
   starts paying once its inner step is stated.
3. Alternatives per -1's list, still live: SOFT-SPEED-1b
   (vectorized soft corrections); RULE-POLICY-0 rung 0
   (label-coverage census, /desk shape); RULE-ABLATE seeds 4/5;
   skip-distance ladder at matched dose.
4. Parallel track on GO: EXPERT-INTERACTION-0 (3080 budget).

## Open Artin decisions

1. Phase C frozen-paths ruling (181 files) — unchanged.
2. Rebirth MEDIUM substitutions — unchanged.
3. Static-figure unpause — unchanged.
4. Exhaust keep-or-delete — now ~10.9GB.
5. CHECKERS-1 disposition — unchanged (house lean: leave).
6. wsl_guard classifier-blocked edit — unchanged.
7. Next pre-reg pick (house lean: cross-term-decomposed expand).
8. External-seat loop habit — the GRAD-MAP null is relay-worthy
   either way (the GPT seat proposed the atlas; the falsifier
   verdict is the honest reply).

# Handoff 2026-08-14-0 — order mechanism named: foundation, not placement

Seat: Fable 5, Mac. HEAD at close: `6ae9f27` (plus this commit).
Session spans 08-13 evening through 08-14 (one context, resumed
from handoff 2026-08-13-3). Mac free, 3080 free (one ~20s CPU
probe ran there on Artin GO), no live runs, no watchers armed.
Supersedes -3 for session state.

## What landed (all pushed, suite green at every booking)

08-13 evening (after the -3 handoff):
- **PRE-REG CURRICULUM-1** (`8f2b894`) fired same evening on GO;
  **VERDICT** (`a788379`): P-ORDER-HURTS — capability-ordered
  easy-to-hard gates 54 v the booked 64 (-10, past single-seed
  resolution); plateau-gated arm hit the pre-named "always
  admits" fence branch (63, ~83% a stock stream — plateau
  curriculum genuinely untested, a slower rule is a NEW pre-reg).
  L4 books order-invariant by the registered bar; 3/24
  observation demoted to READ.
- **VERDICT EX4-UNIF** (`bed660a`): neither primary bar — over-
  inclusion neither general nor cleanly rank-scoped; P-TOP-HARM
  (sanity) -26 with a 9.56% recall confound booked beside it;
  unif0 +19 in the pre-named dead zone; split-draws missed by ONE
  solve (disclosed). All six anchors reproduced exactly.
- **OBSERVATION DIET-AMBIGUITY-1** (`a650aa7`): zero training
  loss is impossible BY CONSTRUCTION on gen4 — 15.7% of rows
  share a cur with >= 2 valid nxt, CE floor >= 0.174 nats/row;
  plateau detectors must be improvement-rate, never absolute.
  (Cousin of LOSS-FLOOR-1, cross-linked.)
- Riffs banked: novelty-volatility (three-sigma split),
  distribution-attached steps / SOFT-NEXT-1 (+ prior-art
  amendment: DISTRIBUTION-ROWS null narrows it to the conflicted
  15.7%), loss-bottom-gated lr + lr-ceiling joke arm,
  answer-only birth (ONESHOT 54 prior art + never-weight-distance
  correction), answer-first two-phase, factoring-targeted L4 diet
  (Artin's disinterest banked with it), MAGIC-CURRICULUM (with
  measured transport failure: estimator OOD on diet rows,
  Liouville bit all-zeros by construction).

08-14 (the discriminator day):
- **PRE-REG + VERDICT REV-LADDER-1** (`97bffa2`, `1c54728`):
  hard-first gates 37 (-27) — order harmful in BOTH directions;
  neither mechanism bar fired (L3 fell 8 toward placement, L4
  flat at 3 toward structural); mixed-cells branch, direction
  only. Prior wrong (third family miss).
- **OBSERVATION EX4-COMPOSITION-1** (`8427340`, desk): window-
  overlap candidate ELIMINATED (16 v 18 of 1,080 slots); draws
  separate on summed demand; battery-wide demand-vs-effect
  non-monotone (51k -1 / 77k +19 / 117k +28 / 452k -26).
- **PRE-REG + VERDICT SWAP-LADDER-1** (`e5947c1`, `6ae9f27`):
  THE MECHANISM NAMES ITSELF — one swap (L3 sixth to second)
  lifts L5+L6+L7 19 -> 25 at unchanged positions (P-FOUNDATION
  fires: early easy data lifts the levels it PRECEDES) while
  L3's own prime placement leaves its bar un-fired (17 v 21;
  L3=17 sits exactly on the REFUTED-IF value, disclosed — branch
  needs both bars dead). Total 45 = third direction of order
  harm. HOUSE PRIOR INVERTED; family record 1 hit, 6 misses.
- **Charter updated** (CLAUDE.md, `3980fc0`): the two standing
  engines named (derivation + ZX/T-count — the q-circuits engine
  Artin remembered); games-as-instruments ruling recorded
  (checkers class, instruments ONLY, never capability).
- **PRE-REG + VERDICT CHECKERS-0** (`7df4240`, `46f22b4`, 3080
  CPU ~20s): P-GREEDY-WINS null band fires (search 92.0 v
  material 88.0); anatomy indicts the ACCURACY BAR (constant-0
  scores 90.5% on the skewed win-in-20 label; arms fail in
  opposite directions — proof-based: 0 false alarms, 3/19 wins
  in 2,000 nodes, 105/200 budget-exhausts; material: 18/19 wins,
  23 false alarms). T-count day-one lesson now 2-for-2.
- Riffs banked: checkers/chess domain (charter-gated, measured
  same day, amended in place), MCTS-during-training
  (multiplicity-gated; composes with checkers), ATOM-DIET-1
  (atomic-op decomposition, Artin's "database atomic" frame, on
  the decomposition-discount anchor with the interleave fence).
- Memory: muse-glimmer-30b.md (Meta dense 30B reference — dense
  no-router control candidate + DFlash drafter target).

The assembled week (19M/gen4, single-seed fences): schedule
mechanics are forgiving (direction free, half-length free, floor
at ~10% of max); DATA-ORDER mechanics are not (blocked orders
cost 10-27 solves in every direction) and the mechanism is
FOUNDATION — what precedes a level moves its cell; shuffled
interleaving wins because it delivers prerequisites continuously.

## Conditions that bite next session

- Curation ratchet at cap==backlog: next booking needs its
  FINDINGS bullet same-commit (5 bullets added this session, all
  same-commit).
- Newly FROZEN (results-cited): scratch/birth19m_curric.py,
  birth19m_curric_rev.py, birth19m_curric_swap.py, checkers0.py,
  ex4_build.py; receipts logs/ex4/, logs/curric1/ (arms, rev,
  swap, noop x3), logs/checkers0/probe.jsonl — extend via
  siblings, never edit; never append runs into these files.
- Milestone exhaust decision GREW: prior ~8GB + four curric
  checkpoints (gallery19m_curric_{cap,level,rev,swap}_s2.pt,
  ~900MB) + gallery19m_phase_s2 family. All bookings complete;
  deletion safe for booked claims; Artin GO only.
- Plateau-gated curriculum remains UNTESTED (constants
  always-admitted); a slower rule is a new pre-reg, not a retune.
- CHECKERS-0's K=20/2,000-node constants are pinned to that cell;
  CHECKERS-1 re-bars are a new pre-reg.

## Next session, in order

1. This handoff -> BOARD -> RESULTS tail (resume protocol).
2. HOUSE RECOMMENDATION, first pick: **ATOM-DIET-1** — the
   constructive consequence of the week: foundation is real +
   interleaving wins, so farm a rule-tagged atomic-op shard
   (engine one-ply applications, exclude=-guarded) and interleave
   it into the stock stream; paired birth v the booked 64; L4
   cell v the 6-8 band (L4-PLY0-1 says the missing piece IS an
   atom). Converts "orders hurt" into a candidate WIN lever.
3. Alternatives, all bank-ready: L1,L2-first foundation
   discriminator ladder (cheap, closes the mechanism); watch-it-
   think flagship (Artin-endorsed, unstarted, carries the L4
   activation question); EX4 demand-dose ladder; REPAIR-FLOOR-1;
   OVERLAP-BIRTH-1.
4. n>=3 seed ladders for the order results if any direction
   claim needs to leave the single-seed fence.

## Open Artin decisions

1. Phase C frozen-paths ruling (181 files) — unchanged.
2. Rebirth MEDIUM substitutions — unchanged.
3. Static-figure queue unpause + palette — unchanged.
4. Milestone exhaust keep-or-delete — now ~9GB total.
5. Next pre-reg pick (item 2/3 above).
6. CHECKERS-1: re-barred second pass, model-side transport cell,
   or leave as measured null + residue (house lean: leave for
   now; the model-side cell is the one that connects to the
   order results).

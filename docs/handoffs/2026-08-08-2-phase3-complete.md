# Handoff 2026-08-08-2 — PHASE 3 COMPLETE (7/7), Phase-4 row 1 closed

Resume: BOARD header -> this file -> RESULTS tail from VERDICT
HARDENING-P3-R9 upward.

## The frozen list, final scoreboard
- R5 split-law: BAR FIRED (causal leg n=3).
- R6 gravity-tax: between (falloff-flip 3/3; toll -11 unresolved).
- R3 ffn-slack: BAR FIRED (pooled EMA -4; attention width is the
  dial at d56).
- R4 cooldown: BAR FIRED (pooled +12).
- R2 cell-sparse: between (structure recurs -28/-12/-24; NAMED
  CELLS DO NOT TRANSPORT — addresses are born, not inherited).
- R7 quat 4x toll: BAR FIRED (pooled -17; torch seed measured
  INERT in quat_convert — "seed" = data order for that family).
- R9 bits-dimension: BAR FIRED (ternary ties fp32 at 19M, pooled
  -3; THEORY 29 updated).
Five bars fired (R5/R3/R4/R7/R9), two honest betweens, zero retractions. (Corrected from 'four' per Grok's cross-check.)

## Phase 4 state
- Row 1 (pincer in-dist) CLOSED both legs: labels_v2 migration
  certified the 68% (timeouts visible; last SIGALRM carrier
  retired); in-dist probe HARDENED the R1a null (4.3/5.0% v 11%).
- Next: ZX 45M (row 2), then SR-BF16, poly bridge, dithered/
  judge, farmer — 3080 nights, each pre-reg before fire.

## Wrapper toolkit (established this sprint, reuse patterns)
p3_ffnslack (env-driver + TAG-seed guard), p3_stream2x2
(torch.save path redirect), p3_quat (manual_seed + Random(0) +
save redirects; subclass form for annotations), p3_autopsy
(CKPT-override, read-only), p3_bits (two frozen drivers, one
excision patch). All: D2 excision receipt + refuse-if-exists +
device-of-origin + pre-reg before fire.

## Also standing
- Revive queue: item 1 booked (metab-late, 3/3 negative lean);
  items 2-3 (crown-tie births, d768 crossover) GO'd, pre-reg at
  fire time.
- Seats: grok CLI plan-mode live; codex read-only trial after
  quota reset ~17:42 EST; muse OFF (contributor tier absent).
- Gallery: provenance regime + identity-crest + GT-1 figures.
- Checkpoint triage executed (~11 GiB, sha-interlocked); UNCITED
  remainder governed by revive_cite_plan.md.

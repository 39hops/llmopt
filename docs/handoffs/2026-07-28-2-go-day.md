# Handoff 2026-07-28-2: the GO day (six-program spec, five landed) — PRE-COMPACT

Resume: BOARD -> this handoff -> specs/2026-07-28-symmetry-ladder.md
-> RESULTS tail. All work committed+pushed through this handoff.

## Landed today (each booked in RESULTS with pre-reg)
1. ROTATIONAL ARC COMPLETE: R1 null (no spontaneous structure;
   cplx models keep rotation in activations; instrument exact) ->
   R2 (-8 of 65 for full 50%-mass projection) -> **R3 CONVERSION
   ACHIEVED (64/120 @ anti-mass 2e-4; artifact
   checkpoints/rot_convert_b.pt)**; lambda=0 surprise: commutant
   locally stable under SGD. The -8 = genuine non-rotationality
   (anti-mass 0.5), NOT inexactness; exact conversion = 2x
   complexification (S2 control, spec'd).
2. E2 DELIVERED: scorer_s2_dist.axnn (sha b87d0976...) + prompt
   spec (relay -28-4, with the AXNN v1.1 swiglu-loader ask).
   E3 armed on their loader.
3. Ladder-retry CLOSED negative (74 @ 83% tokens; decode-economics
   family done: wave-8 production, greedy probe-tier).
4. Farmer MIGRATED to axiom bridge (12.7 st/s smoke, ~4x;
   USE_AXIOM=1 default, house fallback).
5. Laplace bridge relay DRAFTED (-28-3) — Artin sends before
   axiom's ODE design freezes.
6. Morning review: STE-latent artifact caught + both deploy-tax
   claims CONFIRMED on true-latent gates (ZI 65=65, P2 72=72).

## IN FLIGHT (survives compact)
- STABILITY ATLAS on 3080 (36 d64 births, 8x4 LR x BS + 4 s2
  cells; pre-reg 2026-07-28 in RESULTS; waiter armed, marker
  logs/atlas_done.marker) — book the map vs pre-reg readings
  (smooth/chaotic-band/islands; sqrt-BS ridge) on landing.
- 3080 window closes ~5PM EST 07-29.

## NEXT QUEUE (the spec)
specs/2026-07-28-symmetry-ladder.md: S1 quaternionic (params/4,
recommended), S2 complexification control (exactness question,
cheap), S3 circulant (dense->conv), S4 Z2. Frame: symmetry =
third compression axis (sharing) next to bits and dimensions.
Fences: synthetic controls per group; gates-only; pre-reg/rung.

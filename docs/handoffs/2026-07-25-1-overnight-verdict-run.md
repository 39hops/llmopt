# Handoff 2026-07-25-1 (the overnight verdict run, ~4AM-8AM)

Reads: -25-0 (width-floor night). Post-compact autonomous session;
every verdict reviewer-cross-checked BEFORE booking (protocol held
across five exchanges; the keeper caught two real confounds).

## Verdicts booked (RESULTS, newest at bottom)

1. **Boundary-or-bulk**: volume (N*b) is FEEDING-BLIND — refuted
   on the crossover grid alone (the one real violation: volume
   ranks fp32@768 first, measured last). No boundary measure
   books (silent-not-correct). NEW: **exchange law is
   LOW-BIT-ONLY** (b+1/2 log2 d fails identically at b=32; needs
   saturating b_eff) — caveat propagated to THEORY:29. Rising arm
   confirmed collinear (regression cannot discriminate there).
2. **fp32 control at 45M**: NO detectable bf16-birth debit on
   cuda (66=66; rarity +3 sub-noise). Keeper catch: the −4 width
   penalty crossed BIRTH DEVICES (19M arm B born MPS, 45M cuda;
   MPS offset ~+3-4) — so width-doesn't-pay books as MECHANISM
   (token-density/W*), magnitude withheld. **GEN-10 = 19M BOOKS**
   (efficient choice, robust to the confound). Epoch-time PARITY
   bf16-vs-fp32 at 45M on the 3080 — no speed rider.
   **19M-cuda control in flight** (birth done, loss 0.4124 vs
   45M's 0.4539 same diet; probes running) — closes the magnitude.
3. **NATIVE TRANSFORMER CLOSED, with a mechanism**: rung-1 prefix
   NO-ADOPT (gate 61 v twin 64; twin replicates the 64-65 band).
   The Lample-Charton 20-30% shortening measured **−4.5%** on our
   rows (frame+coefficient dominated) — prefix's speed case dead.
   **Emission wall is NOTATION-INVARIANT** (length-proxy on 240
   in-language states: quartiles 81.7/73.3/50.0/21.7 v
   80.0/76.7/46.7/20.0) — forecloses tree-PE (rung 2 NOT spent).
   int3 rider INVERTED (prefix −7 v infix +1; differential −8).
   One mechanism, three observations: parens = error-correcting
   arity scaffolding (Q4 parse-fail 30%). Rung 3 attention-init
   is serialization-independent, stays BANKED. Survivor:
   **GCD-pays-iff-syntactic** law (RIFF-LEDGER). Teach-don't-
   impose confirmed at the representation layer.
4. Riders: per-channel-absmax **int3 near-lossless at 19M** (65 v
   64, extends the 45M int4-MX/GPTQ results); **the 388-row
   hazard CLOSES** (converter independently re-found them = the
   Subs() out-of-language class; dropped symmetrically, infix
   twin still 64 — zero measurable cost).

## Speed track re-rank (keeper-endorsed)

Prefix OFF. **Streaming-birth and batched-KV are the headline.**
Then ternary-GEMV audit, regret hook, deterministic gate.

## In flight / next

- WSL: 19M-cuda probes (GEN9_19M_CUDA_DONE marker) → book the
  width-penalty magnitude (or its collapse) when read.
- Mac free. Next per queue v2.1: streaming-birth A/B, d256
  substrate gate (replication seed + known-result repro), depth
  ladder + LR sweep at d256.
- v5 s2 at next 3080 GO (unchanged). Axiom relay unchanged
  (knock-4 ask, round-3 GO, binned heads — Artin's timing).
- New artifacts: llmopt/mathgen/prefix.py (round-trip 0/8000 —
  keep: rung-3 and any tree-metric work can reuse the parser),
  scratch/{boundary_or_bulk,gate_prefix,emission_wall_pair,
  int3_rider}.py, scripts/convert_diet_prefix.py, paired diets
  data/gen4_diet_{infix,prefix}.jsonl (untracked).

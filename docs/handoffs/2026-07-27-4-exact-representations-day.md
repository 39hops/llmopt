# Handoff 2026-07-27-4: the exact-representations evening

## What landed (all booked + pushed, RESULTS newest-at-bottom)

1. **45M union verdict**: math 65 (boundary — union ~free at
   scale, unclaimed), ZX 36 = inside the 19M seed fence. Scale
   lever NULLS on the graph grammar; G9 conditional resolved
   NO-FIRE (banks unfired). ZX next levers: diet quality /
   serialization / curriculum.
2. **The exact-representations program** (Artin's push, spec
   docs/superpowers/specs/2026-07-27-exact-representations.md):
   - Rational twin: Q=64 parity (48 v 49 Mac); minimal parity
     twin Q=48 (~10.5 bits/wt); knee = CLIFF [1.7e-3, 3.7e-3].
   - **SNAP ANATOMY (the day's best finding): the sensitivity
     wall is HORIZON x near-tie density, not weights** — full
     Q=16 snap flips 2/2512 teacher-forced tokens, both at
     margin 1.6e-4 (median 8.9); ~8e-4 flips/token x ~1k-token
     solves = the gate cliff. Precision below near-tie scale
     buys nothing.
   - Dither null (plain best-rational stays champion).
   - LoRA-precision ladder + snap+repair rung banked (RIFF).
3. **Relay -8**: prior cell acked both sides; stale-prior-
   generalizes riff banked. Axiom owes nothing; awaits S2 AXNN.

## IN FLIGHT at handoff time

- **Mac**: born-rational paired arms (night_rat.sh) — control
  BOOKED 60/120 (Mac instrument); RAT_Q=6 arm in ep2, then
  deploy + 2 gates. Bars: dep within ~2 of 60 = born-exact
  thesis books; -5 = lattice trains worse than M5's alphabet.
- **3080**: quick exact battery (quick_exact_3080.sh, marker
  qx_done): comparator = tonight's union_45m 65. Arms rat64
  (transport), fq512/fq128 (INTEGER TWIN, exact int-GEMM road),
  rat16 (crack), rat16+repair (thin-film decisive). Pre-reg'd.

## Queue after readouts

E2 exporter (fresh-session first task) -> GPTQ-rational
(compute-the-number snap) -> mixed-Q water-filling -> RNS
optimizer wall rung. Plugins note: ecc uninstalled; context7/
critical-thinking kept (CLAUDE.md > skills; no sub-agents ever).

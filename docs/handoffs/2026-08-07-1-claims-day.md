# Handoff 2026-08-07-1 — the claims day (both crests book; Lean Leg B closes; the GO-chain executes end to end)

Continues 2026-08-07-0-night-queue. Resume: BOARD 08-07 EVENING
header -> this file -> RESULTS tail from PRE-REG EX-ANAT-3 down.

## Headlines

1. **BOTH CREST CLAIMS BOOK at n=3 fresh paired seeds** (VERDICT
   EX-FRESH; seeds 1001/2002/3003, discovery pool quarantined):
   swap-crest +53 pooled 3/3; deletion-crest +55 over full AND +27
   over the matched-rank control, 3/3 both. Control's own finding:
   rank-matched random deletion beats full +28 pooled — the router
   is over-inclusive at the carrier rank class; identity doubles
   it. Convergence: 54.7%-keep mask and 98.7%-keep scalpel land
   the same gain class.
2. **EX-ANAT-3 inverted its question** (single-seed discovery,
   then replicated above): at full width the pooled-lens carriers
   are INTERFERENCE (+17 deleting them; controls +7/+3; decode
   lens quiet +4). Carriers are REGIME-DUAL — the same named 80
   transform scarce masked sets and crowd the full router.
   Prefill-tilt desk census (2.21x/0.84x) supports prompt-side
   carriage.
3. **Lean Leg B COMPLETE**: axiom paid the sidecar ask same-day
   (v2, 21,914 rows); sample re-run hit the registered 98.9%
   EXACTLY (989/1000, failures = precisely the 7 atom-split + 4
   field_simp); full corpus 98.63% with the taxonomy CLOSED at
   zero residual (222 atom-split + 78 field_simp). Id lists staged
   for axiom counter reconciliation.
4. **MULT-0 arc complete at rung 1**: B=8 P-STARVED (4/120 m>=2);
   B=32 UNRESOLVED between bars (9/120, sub-linear widening, L4
   first crack). Scarcity is mostly a MODEL property; chooser rung
   stays gated; stronger proposer is the honest lever. Ladder
   holds.
5. **P-CAPACITY-2**: params fail both directions at the anchor
   schedule — plateau KNOB-COMPLETE at this diet; ENGINE-SCALE-1
   pre-registered (30 cells) and GO'd by axiom same-day (set_lr
   shipped + gated); ball with axiom to run the grid; house
   spot-cells chosen to match already-certified shas.

## Also booked today (morning-to-evening order)

RIFF: reverse-propose ladder (Artin) + training-loop extension +
DeepSeek lenses; morning agent pass (THEORY caught up, 15 doc
fixes incl. two numeric amendments, LOCKSTEP CPU scope, GLOSSARY
carrier term); DESK EX-ANAT-3-0 (prefill tilt); DESK R-EMISSION-0
(battery saved); VERDICT TENET-W1-R (relational null — reader
mining closed); relay 2026-08-07-0 out, axiom's -2 answered both
items same-day.

## Open / next

- ENGINE-SCALE-1: awaiting axiom's grid rows; verify the three
  spot shas (DIET-BRIDGE / PLATEAU-B / SCHED-rider) then book.
- EX-ANAT-4 candidates (new pre-reg needed), ORDERED (Grok review
  adopted): (a) uniform-random deletion control FIRST — scopes
  whether over-inclusion is rank-local or global, and gates any
  broader over-inclusion language; (b) carrier anatomy on the
  named 80 (token-specialization TRAJ lens); (c) physics-gate
  scalpel transport LAST (domain shift on an unfinished mechanism
  invites a second vehicle's weather).
- Stronger-proposer leg for the reverse ladder (banked).
- lab/gate -> sink -> timebox (instrument track).
- Axiom relay: 222 + 78 id lists; the 4 field_simp ids confirm.

## Cautions

- The two crest claims are [FORMAT-BOUND, one vehicle, L1-3];
  GT-1's 6/6 crest remains the strongest single claim. Do not
  merge the three into one number.
- checkpoints/ex3_* + ex1_full128 now cited evidence.
- Full-model baselines moved 59-67 on the fresh seeds —
  paired-per-seed rule reconfirmed; never quote unseeded.
- INSTRUMENT-TRACK NOTE (no silent drift): scratch/gt7_run.py +
  the frozen arm2 KEEPSET path are now load-bearing for EVERY
  crest claim; neither may change without a lab/gate-style
  adoption (source-identity + acceptance). The lab/gate adoption
  spike is the named next idle-block item.
- jobs/*.log for today's batteries are receipts (copied into
  logs/ where booked); rjob ids reused across days — check dates.

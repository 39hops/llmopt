# Handoff 2026-08-21-4: the launch-locus day — CI restored, TEMPORAL books the step function, DEPTH localizes it to the late band; frozen for the macOS update

Seat: Fable 5 on the Mac, Artin home, GPT reviews relayed and
every point verified in-house before adoption. HEAD at close =
this handoff commit. Mac idle, 3080 idle, nothing armed, no
watchers live. SESSION FREEZE ORDERED: no new science thread
(no desk, prereg, code, or launch) until Artin returns after the
macOS 26.6.2 update.

## What landed since -3 (with shas)

- CI restored (32079f60): post-close automation batch had left
  docs/CODEMAP.md stale (spec citations on ex6med/ex6med2/
  ex6_phase rows); deterministic regen, green since.
- PRE-REG EX6-TEMPORAL-0 (aaa46fdf) + AMENDMENT -DOSE (2264c592,
  prereg-audit repairs pre-launch: dose census + contrast
  admissibility, decode-witnessing census probe, sealed stdout
  receipt) + AMENDMENT -REFUTATION (45b33161, Artin adjudication
  review: DOSE-VALIDITY bar 6 gates REFUTED-IF, refutation
  broadened to max(Dz2,Dz3) >= 14, bar 5 at the +7 floor; all
  three synthetic fixtures exact).
- VERDICT EX6-TEMPORAL-0 (5ac4f664): THE LAUNCH STEP IS SPECIAL.
  Qual 6/6 cell-exact, census 48/48, dose 0/360 both arms.
  Delta_z2 = -3 (floor), Delta_z3 = 0 with 0 rescues/0 breaks in
  360 (correctness-identical; 4/360 completion-length diffs).
  Step function, not curve; prior NOT refuted, right 2 of 3 legs
  (bar-5 ordering leg TIE). Z1 recall reproduces LOC
  TOKEN1_ONLY to four decimals. + AMENDMENT -RECEIPTS (03a7c9f9,
  perprob streams tracked; lock invariant fired as designed).
- AMENDMENT EX6-TEMPORAL-0-SCOPE (f40d18f7, Artin catch,
  receipt-verified): arms matched on temporal-CALL dose, NOT
  realized displacement — z1's mask replaces ~2.09x more native
  slots (excluded mass 1.82% v 0.86/0.88%). Canonical wording:
  step-like correctness response FOR THE FIXED NAMED-80 MASK;
  position-specificity of this named-mask intervention, not yet
  intrinsic temporal sensitivity. Deny-list: "identical design
  dose" / "equal dose by construction" in thread scope.
- RIFF banks (same commit): TEMPORAL-DISPLACEMENT-MATCH control
  (forced equal native-topk replacements at z1/z2/z3) and the
  per-layer 48x3 native-demand census + alignment read.
- PRE-REG EX6-DEPTH-0 (7586d818) with the demand census folded
  in as a registered outcome-blind artifact; VERDICT EX6-DEPTH-0
  (c198c573): z1 healing DEPTH-LOCALIZED TO THE LATE BAND —
  E/M/L = -3/+5/+19 (late signs +6/+8/+5 3/3, 20 rescues v 1
  break), pooled additivity residual 0, DISTRIBUTED prior
  REFUTED as registered (family 1-for-5 on direction calls;
  weak LATE lean right). Registered color: demand-excess band
  EARLY (2.56%) anti-aligns with the causal band LATE (0.50%);
  displacement does not order the effect (MID 1.15% < LATE
  1.49% yet +5 v +19).
- Non-science: CVE check on Artin's ask — Screen Sharing service
  disabled, no 5900/5600 listener, machine unexposed to the
  Screen Sharing CVE as configured; Tahoe 26.3 still in the
  affected range for the same advisory batch's ImageIO
  code-exec fix; 26.6.2 pending — THE reason for this freeze.

## CANONICAL MECHANISM STATE (three verdicts, one chain)

The named-80 crest's token1 component (+21 of +47) localizes to
ONE temporal position (generated z1; z2/z3 nothing) x the LAST
16 MoE layers (blocks 32-47 carry +19 of +21; early -3, mid
+5). Depth composition is pooled-additive (residual 0); the
temporal composition (LOC) was super-additive. Mediator
(KV/attention propagation) remains HYPOTHESIS at every step.

## REVIEW RESIDUE — carried UNBOOKED for post-update amendment
(Artin + GPT DEPTH review, all three arithmetic-verified this
session; book as AMENDMENT EX6-DEPTH-0-SCOPE-class after update)

1. ADDITIVITY WORDING: pooled residual 0 does NOT establish
   "bands compose additively / no cross-band conjunction".
   Per-level residuals from the booked table: L1 -6, L2 +1,
   L3 +5 — cancelling to pooled 0. Canonical reading should be:
   no registered large pooled net interaction (|I| < 14);
   difficulty-dependent interaction/cancellation OPEN. This also
   explains LATE-alone L1-heavy (+9/+6/+4) v ALL L3-heavy
   (+3/+6/+12).
2. ALIGNMENT SCOPE: demand anti-alignment rules against a simple
   demand-volume/displacement-magnitude account only — NOT all
   carrier-family mechanisms (late-layer excluded carrier
   IDENTITIES could carry the leverage despite lower volume).
   "Intrinsic position x depth sensitivity" stays a CANDIDATE,
   never promoted.
3. RECEIPT-DISCLOSURE NARROWING: the verdict's "demand.json
   predicts all measured band recalls / two independent code
   paths" sentence overstates — demand.json is NONE-path native
   demand; treatment masked_recall is closed-loop (earlier
   masked z1 layers alter later z1 routing). Verified gaps:
   EARLY native 3.23% v treated 3.12%, LATE 1.58% v 1.49%. The
   within-z1 demand shift is itself a mechanistic observable.

## Next session (AFTER the update, Artin's GO)

Resume: this handoff -> BOARD -> RESULTS tail. First batch:
book the three residue amendments above, then the causal
ranking: (1) late split 32-39 v 40-47; (2)
TEMPORAL-DISPLACEMENT-MATCH control (banked, separate); (3)
keepset transport (residue 3). Displacement-match and transport
stay separate registrations, never folded into the split rung.

## Banked this session (theory only, no work)

- CYBERNETIC-MATH / MATH-CYBER-0 (RIFF-LEDGER, this commit):
  closed causal loop on a small exact rewrite world — state =
  canonical expression, action = rewrite family+locus, exact
  oracle feedback without revealing the correct action; future
  factorial OFFLINE / ENV-LOOP / DIET-LOOP / FULL-CYBER +
  the critical REPLAY arm (offline on FULL-CYBER's exact
  stream; ONLINE > REPLAY isolates causal feedback, ~= says
  curriculum). Artin's on-record prediction: FULL-CYBER scores
  perfectly; house counter-prior: REPLAY is the dangerous
  control. Extensions banked: world-model head, homeostatic
  difficulty controller, absorption-law LR/diet controller.
- TEMPORAL-DISPLACEMENT-MATCH + native-demand census banks (see
  above; demand census already fired once inside DEPTH-0).

## Open decisions for Artin

1. GO the macOS 26.6.2 install (restart; both machines idle,
   nothing armed — window is clean NOW).
2. Post-update first rung: house suggests the three residue
   amendments (30 min, zero machine) then the late-band split.
3. CYBER bank: /desk price the rewrite world, or leave banked.

## Standing

Suite green at close (see closing report). Receipts lock covers
ex6temporal + ex6depth (16 sha-locked files incl. both perprob
streams). Both auditor pairs ran pre-booking on both verdicts;
carried disclosures: qual.jsonl meta-on-PASS gap and
transitively-pinned weights (future drivers pass artifacts= and
write qual meta unconditionally). Day totals since -3: 2
verdicts, 4 amendments, 2 pre-regs, 3 riff banks, 1 CI repair,
0 unbooked measurements (the three review-residue items are
deliberate carried corrections, named above).

# Handoff 2026-08-01-1 — the gravmoe battery day (afternoon-night)

Five exact cells in one day, every one pre-registered, every arm
deterministic and sha-pinned. Read RESULTS from PRE-REG
GRAVMOE-PAIR (L~14200) to VERDICT GRAVMOE-GATE (tail).

## The arc (each verdict scopes the previous one)

1. **GRAVMOE-PAIR** (saturated init): gravity = monotone
   capability-for-mergeability trade (linear in log-dose); merge-free only at full
   collapse. fp collapse threshold did NOT transport.
2. **DIET-COND** (residual writers +-Q/8): the linear trade was
   the clamp rails. Conditioned: interior optimum at lambda 1/4
   (best loss AND -37% merge damage); conditioning alone -32%
   loss; regression arm sha-exact. Artin's reframe adopted:
   gravity is an expert COMMUNICATION channel — share the mean,
   keep the identity; merge-free is a shipping property, not the
   goal.
3. **QK-COND**: peaked attention (90% zero probs) is
   LOAD-BEARING, not pathology — a learned temperature
   self-sharpens 22x (1 of 2 bodies, hard-base arm) (crashed at tt=0 pre-floor), GLOBAL-soft init
   costs +73% loss (REVIEW RESCOPE: the arms softened six
   matrix families, not wq/wk only — see amendment
   REVIEW-ADOPTION-0801; wq/wk-only cell still owed), SHIFT=16 does NOT rescue (it is signal dilution
   ~1/T, not starvation). Battery default stays hard attention,
   no temperature param. Twin fidelity and training capability
   are DIFFERENT AXES.
4. **GRAVMOE-GATE** (Artin's ask — score by running, not loss):
   free-run + fork-timeboxed sympy oracle on complete rows.
   0/8 solves EVERYWHERE, train token-acc 24-40% while loss said
   "solid" — exposure bias measured at mini scale. STANDING
   RIDER: every loss claim in the battery is a teacher-forced
   claim. The gate is now a standing readout.

## Instruments hardened today (memory: integer-battery-debug-patterns)

- Twin protocol: ALL discrete decisions (routing tops, clamp
  masks) pinned from the integer side; composite cosine per
  window, median bar. Near-tie boundary elements through the
  softmax-cancellation amplifier (~60x) masquerade as sign bugs.
- E=1 reduction bisect: extension must reduce BIT-IDENTICALLY to
  the certified case (gravmoe E=1 == dense Body, asserted).
- Boost-invariance: error shrinks with boost -> rounding; error
  identical -> missing term or discrete disagreement.
- tt floor: learned params that divide need floors (clamp wide
  post-step, min 1 narrow).

## Files

- scratch/detbwd_gravmoe.py — the whole battery (env knobs:
  COND/QK/TAU/GATE/E/LN/LD/K/GB/SHIFT/TWIN/STEPS).
- Arms logs: logs/{gmoe,dietcond,qkcond,qkcond2,gate}_arms_0801.log
  (local only; shas quoted in RESULTS).
- Morning: scratch/detbwd_mb.py + detbwd_mb_ref/ (multi-block,
  3 implementations), scratch/detbwd_diet.py + ref, intbirth
  ENGINE+PRIMITIVES+MULTIBLOCK all PASS house-side.

## Queue (next session)

1. **Free-run-trained arm**: scheduled sampling / self-consumption
   in the integer loop, gate-scored — the exposure-bias rung the
   gate just opened. Or brute: more steps/params on the intbirth
   engine (60x) with the gate as the readout.
2. **P4 device/lab legs**: 3080 rjob (nightly GO) + axiom relay —
   both contracts (saturated + COND) + the gate protocol; bundle
   with the window-cycling engine surface ask.
3. Relays staged: 2026-08-01-2 (receipts), -3 (multi-block spec;
   ANSWERED same-day by their -4 — receipt owed). Gravmoe/gate
   contracts relay when P4 fires.
4. Carried: q/k logit-scale is CLOSED (load-bearing), diet meter,
   ckpt forensics [Artin GO], FOURIER-4, exact-manipulation diet
   share.

## Fences

- Gate cells (complete rows) are NOT loss-comparable to
  truncated-window cells (~2x easier diet, booked).
- The interior optimum is diet-format-sensitive (loss ordering
  shifted on complete rows: A0 < A3 < A2) — dose tuning is
  per-diet until measured otherwise.
- All numbers are n=1-exact but generality across inits/windows
  is NOT established; seeds/windows ladders are the robustness
  legs when a claim graduates toward doctrine.

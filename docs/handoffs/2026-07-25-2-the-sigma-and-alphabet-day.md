# Handoff 2026-07-25-2 (the sigma-and-alphabet day, ~9AM-late)

Reads: -25-1 (overnight verdict run). Densest verdict day on
record; every booking reviewer-cross-checked. Reviewer = TRUE
Opus 5 now (claude-opus-5[1m], self-report; the 4.8 predecessor
retired after the label con; CC updated to 2.1.220 mid-day).

## The birth-device saga (opened with a −14 scare, closed measured)

Full decomposition: **packing −13** (bare --fast re-enabled the
known hole — my off-doctrine launch; --fast NEVER without
--nopack, any width), **bf16 ~0** (isolation cell 69=69; bounded
±3.5 at 1sigma), **device UNRESOLVED, sign-reversed** (MPS mean
65 v cuda 68.7 — the MPS "bonus" refuted; cross-device stays
forbidden), **s1=70 = pre-fix + luck** (consistency: sigma_MPS
>= 3, n=2 underdetermined). Never cite one number as the 19M
level; per-cell bands only (MPS post-fix 64-66; cuda 69/66/71).

## THE DECISION-RULE AMENDMENT (highest leverage)

**sigma_diff = sqrt(2)*sigma_cell ~ 3.5 at 19M/cuda; >= 5 solves
= the single-seed bar** until n=3 both sides. Applied: gen-10=19M
+3 and prefix −3 are SUB-NOISE — both stand on mechanism, never
margin. sigma does not transport (2.5 cuda n=3-fragile / >=3 MPS
/ 1.0 d256).

## The alphabet day (Artin's riffs: 4-bit exact, {0,1}, quantum)

- **Two-of-three law** (zero/symmetry/integer-bits, pick two) —
  theorem + measured ordering: zero-absence fatal born, asymmetry
  tolerable. M4 = the measured zero+bits corner.
- **Lloyd-Max race**: MSE-optimal NEVER beats uniform — first
  direct confirmation of never-score-weights-by-distance with a
  distance-optimal quantizer (the publishable line). 3-bit PTQ
  ties fp32 (knee < 3 bits). **Conditional zero law at 2-bit
  PTQ**: zero PAYS in fixed codebooks (+7-10), COSTS in fitted
  (−5).
- **Born-vs-rounded**: +18 lower bound at matched ternary;
  FLOOR-CENSORED >= 54 at 1 bit (all 1-bit PTQ = 0).
- **THE DEATH TAXONOMY**: b1-PTQ arithmetic death (calibrated
  hallucination BY QUANTIZATION — new route to the determinability
  pathology); z1-PTQ language death; born-Z1 s1 discrimination
  death / s2 arithmetic death (basin-unstable, capability-dead
  both); born-B alive 54. **The deletion square closes asymmetric
  on capability**: sign can emulate silence; silence cannot
  emulate sign. Zeros do double duty in excitation-only nets
  (83-94% forced sparsity; collapse NON-monotone in sparsity).
- **n=1 MECHANISMS are the fragile class** (strong cone + LN
  bets killed/halved by confirmation seeds within hours) —
  confirmation seeds on mechanisms join the doctrine.
- sparse-PTQ sleeper: ternary's zero-PATTERN costs −2 on fp32
  mags; flattening costs −21 (pruning result, not zero-level
  evidence).
- Gauge-commutation: symmetric commutes EXACTLY (43=43);
  asymmetric directional (+2, sub-noise; weak instance by
  design).

## Named law-converting cells (next 3080 window; box HELD for Artin)

1. born-S4 v born-M4 at matched 2 bits (the only unmatched zero
   leg). 2. z1 + signed per-channel scales (sign per-weight or
   per-channel?). 3. max-asymmetric {0,1,2,3} gauge arm. Plus
   free: attention-entropy/cos diag on both Z1 seeds; v5 s2
   (held); gen-9 full-dose.

## Specs/programs

- **Complex bracket** (Artin's quantum/constants riffs distilled):
  M4^2/G16 exact-4-bit; G16 re-labeled "can interference
  substitute for zero?" (phase pairs can synthesize silence);
  rotation cells Q9-vs-D9, G5-vs-M5; three-arm real-DOF fairness
  MANDATORY; modReLU pre-registration required; ranked after the
  cheap closers. Grover-NO re-spelled (dynamic tree + no QRAM +
  structure beats sqrt-N; the engine DOES have cheap-verify).
- **ZX continent = next-continent candidate #1** (first GRAPH
  grammar — tests federation-floor count-vs-class). Desk gates:
  serialization without canonical sorts; atom-vs-vocab check.
- Banked: tensor-train RANK axis; annealing over weight lattices;
  amplitude rationing (gated on the FREE clade-coloring read);
  composed-birth pilot (Artin's streaming instinct; red-team note:
  Z1's fast convergence was degeneracy, not evidence).

## Instruments/method

Three instrument bugs in 24h (388 rows / phase-density null /
int2 scale) — ALL caught by control arms. Whitened phase null +
shared color scales shipped. Friendly-fire instances #7 and #8
(launcher-string self-matches) — both mine, both found by
process-table reads. Controls ride every instrument; cross-check
before booking caught wrong bookings four times TODAY.

## State at close

3080: HELD for Artin (nothing running). Mac: idle. All watchers
drained. Reviewer (Opus 5) ledger current through Z1 seed-2;
ping it before any booking. Crown/production unchanged.

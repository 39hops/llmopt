# Handoff 2026-07-23-0 (evening, pre-compaction)

Convention (established this handoff, Artin): docs/handoffs/
carries dated, 0-indexed session handoffs — the repo-side twin of
the memory state files. Multiple per day: -0, -1, -2. Written for
the next session (or post-compaction self) to resume cold.

## The last 36 hours, compressed

Fullest stretch in lab history. The recipe that ate five grammars:
**decompose to determinable one-primitive rows, size by the
exposure economics (~10k/kind), verify everything, re-spell what
lags.** Series 16->98% (five rungs), phys_energy 100% (first
physical law, saturated), physics 85.5 / dual-crystal 92.3 (+math
intact — transfer via shared steps), poly 51->83 (assembly kinds
pending poly3), vm-asm 61->89. Axiom shipped 6 clean batches
(every audit incident was OUR instrument; their byte contract is
6-for-6).

## Laws banked/confirmed (THEORY.md + RESULTS.md have the full text)

- Absorption ~2.8e-9/LR; LR+precision one knob; birth precision
  CLOSED (bf16..fp64 bracketed flat — parity sweep + fp64
  showdown); precision is ONLINE-only.
- Ceiling theory final form: precision governs what PERSISTS
  (v4: flip-backs 5%, net monotone 330k = STRUCTURE, fork closed);
  content is bounded by what waves can sample — crossing needs a
  teacher (v4 conversion FAIL +1/12; self-practice has no gradient
  at true walls) => THE EXCHANGE IS LOAD-BEARING.
- Flip symmetry: 165,408 up / 165,329 down (ratio 1.000) — locally
  directed, globally neutral learning; census near-invariant.
- Universality: crystal geometry = f(feeding), grammar-free at 3
  systems (kurt 1.91/1.88/1.85 calculus/mechanics/vm-asm).
- Determinability law (96 v 47 in-model; 1e closed appends at 98).
- Decomposition discount ~10x; count corpora in primitives.
- Bridge law: UNTESTED (not null) — poly3 probe 91.4% but ibridge
  entry-move 1/9 (multi-fact emission; door closed, gate 65
  uninformative); per-piece re-spell asked of axiom; padd@10k ~50%
  confirms operand-complexity as a second primitive axis.
- Practice mode (Artin, = Go-Explore rediscovered): model-side
  miner built (scratch/practice_mine.py; banks failed-attempt
  steps outcome-tagged + logs stuck states); engine-side ADOPTED
  by axiom (qual-gate arg 10); stuck-state exchange = the
  mechanism (stuck_states_p1.jsonl delivered to axiom's box).

## The crown

Champion fp32 76 vs merged_grown ternary 75 (statistical tie);
grown rarity 133 = ties the DUO as a single model (best rare bin
14/18). Ternary arc 65->70->75 in one night (diet + growth).
Growth-quantizer coupling found (absmean not growth-invariant).

## Machines / running / pending

- Mac: FREE (poly3 done: probe 91.4%, gate 65; 634k audit passed
  kind-aware). 3080: FREE; metab_v4.pt saved (structure-proven).
- Axiom owes: stuck-chain farm of our 12 walls (HOT — exchange
  test: re-probe fixed seeds, beat 2/12); ibridge/close per-piece
  re-spell + padd coefficient-wise (relay updated with numbers).
- We owe axiom: nothing — all verdicts in the relay doc.

## Next queue (post-compaction or tomorrow)

1. (DONE this session: bridge verdict booked+relayed — untested,
   door closed; awaiting re-spelled batch.)
2. Exchange test when axiom's stuck-chains arrive (pre-registered).
3. Gen-8 diet: duo_mined_shard1 (433) + practice_rows_p1 (378,
   outcome-tagged for the solved-only-leak A/B) + poly3/bridge +
   energy + 1e chain3 — the everything-diet birth, graded on
   rarity curve.
4. Metabolic v5 = v4 + exchange rows in the food (the complete
   practice loop).
5. Banked hot: Fourier continent; limits continent; refutation
   rung; async precision co-processor; Ozaki int-sliced impl;
   duo-teacher KD; precision-patch LoRA; µP check.

## Conventions/doctrine added this session (beyond CLAUDE.md)

- Kill/write/launch = SEPARATE ssh calls (4 friendly-fire variants).
- Arm-time file-dependency verification; success-only markers.
- Audit hard-gates train nothing on failure (saved us 3x).
- OOM warning = tripwire; grad-ckpt (bit-identical) for 10GB;
  bf16 births default; alloc conf default in-tree.
- figs/<date>/ + figlib house style; scripts/INDEX.md regen;
  logs/ dir; relay/ dir; handoffs/ dir (this file).

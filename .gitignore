__pycache__/
*.egg-info/
.refcache/
.pytest_cache/
dist/
build/

# local work notebooks (Artin's scratch space — never committed)
notebooks/
notes/
checkpoints/pred_syndromes_5b.pt
data/micromodel_grpo_mined.jsonl
logs/
scratch/remote.env.sh

# file-handoff convention made structural (2026-08-06): big data
# jsonl stay untracked by default; already-tracked files are
# unaffected, and a DELIBERATE new commit uses `git add -f`
data/*.jsonl
data/*.json

# root strays can never reform (2026-07-26 cleanup); artifacts
# belong in logs/ (ignored) or data/ (case-by-case)
/*.log
/*.jsonl
/*.json
_attic/
practice/
checkpoints/
.DS_Store
jobs/
scratch/leancheck/.lake/
scratch/leancheck/Certs.lean
AGENTS.md
.agents/
.codex/

# 2026-08-09 hygiene sweep (reviewer findings, verified)
data/pull_l9a/
data/*.axnn
scratch/lean_real_corpus/*.jsonl
.claude/*.local.md

# Parquet lake is regenerable exhaust (scripts/gen_lake.py, 2026-08-11)
data/lake/
.coverage

# virtualenvs (lab + animation) and manim render exhaust
.venv/
.venv-anim/
media/

# metallicity diets: big generated jsonl untracked (manifest.ref.json is the committed pin)
data/metallicity/*.jsonl
data/metallicity/manifest.json

# local session-state tool exhaust
.no-amnesia/
